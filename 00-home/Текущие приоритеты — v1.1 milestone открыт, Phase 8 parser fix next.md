---
tags: [priorities, v1-1-active, parser-fix, phase-8]
date: 2026-05-13
status: v1-1-planning
current_phase: 8
next_command: /gsd-discuss-phase 8
---

# Текущие приоритеты — v1.1 milestone открыт, Phase 8 parser fix next

**v1.1 milestone открыт** 2026-05-13 через полный `/gsd-new-milestone` workflow. 4 фазы (8-11), 24 reqs mapped 1:1, ROADMAP approved.

## Phase 8 next — Parser Bug Fixes

**Command:** `/gsd-discuss-phase 8` (recommended) или `/gsd-plan-phase 8` (skip discussion)

**Reqs:** PARSE-FIX-01..05 (5 items)

**Scope:**
- selectolax 0.3 → 0.4 upgrade (Lexbor backend `:lexbor-contains("ОБЪЁМ" i)`)
- Goldapple parser: volume через structured PDP block (Bug #1)
- Goldapple parser: brand/name separation через `<meta itemprop="name">` микроразметку (Bug #2)
- Viled parser: volume через `props.pageProps.attributes[].name == "Размер"` JSON (Bug #3)
- Null-rate sanity gate `parser_drift_null_volume_rate` (Bug #1 regression prevention)
- Smoke-probe URL rotation (`gates.py:36` SMOKE_URLS + 1 per shape variant)

**Mandatory sub-spike (before any code):**
- 30-PDP shape-sampling в `.planning/spikes/v1.1-brand-name-shapes/`
- Anti-overfitting: STEREOTYPE PDP screenshot — single shape. Реальный goldapple catalog содержит TOM FORD (multi-word ALL-CAPS), Dolce & Gabbana (ampersand), 19-69 (numeric brand), НАТУРА СИБИРИКА (Cyrillic ALL-CAPS) и др.
- Bonus: 15-min Wave-0 probe против live viled beauty PDP — verify `props.pageProps.attributes` JSON path (clothing fixture есть; beauty не верифицирован)

**Verification gate:**
1. Live dry-run yields `goldapple_comparable_count > 0` (was 0 in run #13)
2. `goldapple_volume_norm` non-null rate ≥90% на не-volumeless категориях
3. Invariant canary `brand.lower() not in name.lower()` holds
4. ~818 tests green (803 + ~15 new)
5. Null-rate sanity gate actively fails synthetic regression run

## Phase 9 — Live-HTML Harness

**Reqs:** TEST-HARNESS-01..06 (6 items, B4/B5 = P2 cheap-bundle)

**Scope:**
- syrupy 4.7 dev-dep + `HTMLSnapshotExtension(SingleFileSnapshotExtension)`
- `tests/fixtures/<retailer>/_live-YYYY-MM-DD-<slug>.html` (retailer-grouped slicing preserved)
- `tests/live/test_parser_drift.py` под `@pytest.mark.live`
- Pydantic `RawProduct` validation на `SqliteSnapshotWriter` boundary
- P2 bundle если быстро: brand-coverage quota + `capture-fixtures` CLI subcommand

**Locks Phase 8 retroactively** — fixtures captured во время Phase 8 ARE evidence что fix работает.

## Phase 10 — Audit Paperwork Carryover

**Reqs:** AUDIT-DEBT-01..05 (5 items) — **parallel-safe с Phase 8/9**

**Scope:**
- SECURITY.md для Phase 2 / 4 / 6 через `/gsd-secure-phase`
- VALIDATION.md для Phase 4 через `/gsd-validate-phase`
- Audit verdict flip `tech_debt` → `clean` в `milestones/v1.0-MILESTONE-AUDIT.md`

## Phase 11 — Operator Deploy на Yandex Cloud kz1

**Reqs:** DEPLOY-01..08 (8 items) — **LAST, calendar-bound**

**Locked decision:** Yandex Cloud kz1 (NOT Hetzner EU) per пользовательский выбор. KZ-IP для anti-bot.

**Critical pitfalls wired in:**
- DEPLOY-03: `load_dotenv(verbose=True)` в `src/ga_crawler/__main__.py` — fix D-705 recurrence из run #13
- DEPLOY-04: `sudo timedatectl set-timezone Asia/Almaty` — Yandex Cloud KZ default Moscow TZ
- DEPLOY-05: Pre-deploy Camoufox×Yandex compat smoke — Camoufox 0.4.11 на Yandex Cloud Ubuntu не верифицирован
- DEPLOY-06: Egress connectivity smoke — `curl -I` к api.telegram.org / hc-ping.com / goldapple.kz / viled.kz from KZ

**Verification gate:** First Sunday 2026-05-XX 23:00 Almaty cron tick → Telegram доставка xlsx с `match_count > 0` + HC `/start` + `/success` pings + 4 blocked UAT items flip to `pass`.

## Что НЕ в v1.1 (defer to v1.2 / v2.0)

- viled SSR pagination (page-1 limitation, 82 SKU)
- Docker image (Camoufox Firefox 135 vs Playwright image incompatibility)
- fuzzy matching
- KZ-legal / ToS review
- web dashboard
- Polish (хотя 803 → ~818 tests это не "polish" — это hardening)

## Open Questions для plan-phase (не блокирующие)

- Exact `:lexbor-contains()` literal whitespace/case для goldapple volume label "ОБЪЁМ / МЛ" — resolve через syrupy live capture в Phase 8 (15 min)
- Viled beauty PDP JSON path — resolve через Wave-0 probe (15 min)
- B4/B5 включать в Phase 9 если landit < 30 min, иначе defer to v1.2 — operator decision на plan-phase stage

## Related

- [[2026-05-13 — live-run #13 vskrыl 3 парсер-бага, v1.1 milestone открыт через /gsd-new-milestone]] (последняя сессия)
- [[Goldapple PDP renders volume в structured flexbox blok, не в microdata]] (decision)
- [[viled volume — в attributes Размер JSON-поле, не в name]] (decision)
- [[Yandex Cloud kz1 over Hetzner EU для KZ-IP anti-bot]] (decision)
- [[Forward-only no backfill runs 1-13]] (decision)
- [[`.env` не подхватывается Python при прямом uv run — D-705 recurrence]] (debugging)
- `.planning/ROADMAP.md` § v1.1 (Active) — 4 phases expanded
- `.planning/REQUIREMENTS.md` — traceability table 24/24
- `.planning/research/SUMMARY.md` — convergent synthesis
- `.planning/research/v1.1-PARSER-BUG-FINDINGS.md` — bug evidence + live PDP screenshot reference

---

**Bottom line:** v1.0 заархивирован code-clean (paperwork debt отдельная история). v1.1 phases 8-11 planned 1:1 с reqs. Phase 8 (parser fix) — крайний blocker для всего остального; Phase 11 calendar-bound на следующее воскресенье после landings 8-10. Ожидаемая длительность v1.1 — 1-2 недели зависит от скорости plan-execute циклов.
