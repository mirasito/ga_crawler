# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/

# uv
.uv-cache/

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Secrets (D-08: IPRoyal credentials)
.env
.env.local
.env.*.local

# Spike browser state (D-04: persistent context — не коммитим cookie)
.planning/spikes/01-goldapple/browser-state/
.planning/spikes/01-goldapple/.camoufox-state/
.planning/spikes/01-goldapple/.cache/

# DBs (Phase 2+ artifact, добавляем превентивно)
*.db
*.db-journal
*.db-wal
*.db-shm

# Phase 2 — backups directory: dir tracked (.gitkeep), .db files ignored (DATA-06)
backups/*.db
backups/*.db-journal
backups/*.db-wal
backups/*.db-shm
prices.db
prices.db-journal
prices.db-wal
prices.db-shm

# Logs
*.log
logs/

# Build artifacts
dist/
build/
*.egg-info/

# Phase 3 runtime artifacts (T-03-07-12: never commit live snapshot/sitemap data)
.planning/runs/
# Top-level runs/ written by scripts/uat3_live_run.py (repo_root=. driver)
/runs/
# UAT live-run driver log dir
/.uat-run/

# Editor/agent local state (Obsidian vault config + Claude Code scheduler lock)
.obsidian/
.claude/scheduled_tasks.lock
