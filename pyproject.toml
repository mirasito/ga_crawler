[project]
name = "ga-crawler"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "curl-cffi>=0.15,<0.16",
    "patchright>=1.55",
    "python-dotenv>=1.0,<2.0",
    "selectolax>=0.3,<0.4",
    "structlog>=25.0,<26.0",
]
