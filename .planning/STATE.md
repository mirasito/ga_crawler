---
gsd_state_version: 1.0
milestone: v1.0
milestone_name: milestone
status: Executing Phase 02 (Plan 02-05 complete; next: 02-06)
last_updated: "2026-05-07T17:40:00.000Z"
progress:
  total_phases: 7
  completed_phases: 2
  total_plans: 26
  completed_plans: 24
  percent: 92
---

# State: GA Crawler

**Last updated:** 2026-05-07
**Mode:** Phase 1 COMPLETE (2026-05-06). Phase 3 COMPLETE (operator-approved 2026-05-06). **Phase 2 EXECUTING** — Plan 02-05 (Wave 4 orchestrator keystone) complete 2026-05-07: 4 src files (`runner/gates.py` extended +100 LOC with D-203 retailer-agnostic helpers `auto_suggest_threshold` + `final_threshold_gate` + D-218 `parse_quality_gate` + 3 backward-compat shims `auto_suggest_m`/`final_m_gate`/`final_n_gate`; `runner/stats.py` extended +73 LOC with `VILED_STATS_KEYS` 9-tuple + `ViledStatsBuilder` mirror; `runners/viled_run.py` 328 LOC ships `run_viled_phase` 8-step pipeline catalog→fetch→parse→normalize→persist→parse-quality gate FIRST→sanity-N gate SECOND→single atomic patch_stats; `runners/main_run.py` 294 LOC ships `run_weekly` composition with DATA-05 try/except lifecycle + D-221 viled brand-list derivation from snapshots table mid-run + D-211 Norm06Writer.persist after both phases); `cli.py` D-212 cutover (DELETED 4 Stub classes + `goldapple-run` subcommand; ADDED `weekly-run` with 7 flags; KEPT `goldapple-smoke` verbatim). PARSE-05 + CRAWL-05 + NORM-06 (orchestration wiring) closed. 6 test files unblocked + 1 cascading rewrite (`test_storage_integration.py` against production storage now that stubs deleted) = 60 new GREEN tests. 321 → 381 passed (+60), 5 → 2 skipped. Phase 3 frozen modules untouched. 0 deviations from plan (1 in-test bugfix: my fake catalog used substring match `"men" in url` which falsely matched "women"; auto-fixed). 3 commits + summary. **PRIOR**: Plan 02-04 (Wave 3 viled parser + fetcher + enumerator) complete 2026-05-07: 6 src files (`parsers/types.py` StockState Literal + `parsers/viled_nextdata.py` 224 LOC __NEXT_DATA__-only parse_pdp + `parsers/dispatcher.py` ParseDispatcher routing viled/goldapple + `enumeration/viled_catalog.py` 267 LOC fetch_catalog_urls + `fetchers/viled.py` 215 LOC ViledFetcher with curl_cffi sync + tenacity 3-retry + per-SKU isolation + 2s pacing + `config.py` 72 LOC ViledConfig.from_pyproject) close PARSE-01..04 + PARSE-06 + CRAWL-01,03,04,06. WAVE0-PROBE A1+A2+A4+A10 paths all consumed; live A4 follow-up probe (10 URL conventions tested) confirmed SSR ignores all `?page=N` variants — runtime guard breaks early on stuck pageNumber, v1 limitation = page-1 only (120 SKUs across both catalogs, sufficient for D-201 sanity_gate_n=100). 2 deviations: Rule 1 (price/realPrice semantics inverted in plan source code; corrected to PROBE-A2 reading: price=current, realPrice=was, with live discounted fixture as regression-canary), Rule 3 (catalog pagination URL convention is unknown — implemented graceful page-1 fallback). 247 → 321 passing tests, 13 → 5 skipped. Phase 3 frozen modules untouched. 2 commits + summary. PARSE-05 + CRAWL-05 deferred to Plan 02-05. **PRIOR**: Plan 02-03 (Wave 2 normalizers) complete 2026-05-07: `src/ga_crawler/alias/{__init__.py,yaml_loader.py}` + `src/ga_crawler/normalizers/{__init__.py,brand.py,name.py,volume.py,facade.py}` ship 382 LOC (4+81 + 4+48+30+172+43) implementing NORM-01..05 closure: `YamlBrandAlias` reads YAML once at __init__ (D-207), exposes `lookup(brand_norm)→list[str]` (BrandAliasProtocol) + concrete-only `canonical_for(slug)→str|None` reverse helper (NOT in Protocol per Open Q1); `normalizers/brand.py` IMPORTS `_normalize_punct` from `enumeration/slug.py` (REUSE confirmed by inspection test — no duplication); `normalizers/name.py` NFKD+lowercase+strip-punct+collapse-whitespace; `normalizers/volume.py` 24-entry UNIT_TABLE (мл/ml/милилитр/миллилитр/г/гр/грамм/л/литр/шт/штук/унц/унция/унций/кг + ml/milliliter/g/gram/oz/ounce/fl/kg/l/liter/pcs/pc) + 3-layer grammar (`Set of N × M unit` → `N [optional unit] × M unit` → keyword-only multipack returns None / single-volume) + Volume frozen dataclass (Decimal amount, str unit, int count) + `detect_multipack` INDEPENDENT of `parse_volume` (Open Q4: flag persists when per-unit volume unparseable like `набор пробников` or `10 шт`); `normalizers/facade.py` Normalizer composes brand/name/volume + satisfies NormalizerProtocol via @runtime_checkable (verified inline). 30 GREEN tests across 4 unblocked files (test_yaml_brand_alias 7 + test_brand_normalizer 6 + test_name_normalizer 6 + test_volume_normalizer 11 — all 18 volume-corpus.yaml + all 11 brand-corpus.yaml cases pass). 217 → 247 passing, 13 skipped, 0 failing. enumeration/slug.py + interfaces.py UNCHANGED (0-line diff). 4 commits (b875b0b RED + be94a4d GREEN-task1 + 42d7df6 RED + 7c36316 GREEN-task2). 0 deviations. Next: `/gsd-execute-phase 02 04` (Wave 3 viled parser + fetcher + enumeration).

## Project Reference

**Core value:** Команда viled.kz один раз в неделю получает детализированный, сопоставленный по позициям отчёт о ценах конкурента (goldapple.kz) и может корректировать собственное ценообразование, видеть ассортиментные разрывы и отслеживать чужие промо-акции.

**Current focus:** Phase 02 — project-skeleton-viled-crawl-storage

## Current Position

Phase: 02 (project-skeleton-viled-crawl-storage) — EXECUTING
Plan: 6 of 6 (next up: 02-06 Wave 5 closeout — brand-aliases.yaml seed + backup script + final E2E)
| Field | Value |
|-------|-------|
| Phase | 02 — project-skeleton-viled-crawl-storage (Wave 0 + Wave 1 + Wave 2 + Wave 3 + Wave 4 keystone complete; next: Wave 5 closeout Plan 02-06) |
| Plan | 02-01 ✓ (Wave 0 bootstrap) + 02-02 ✓ (Wave 1 storage layer) + 02-03 ✓ (Wave 2 normalizers NORM-01..05) + 02-04 ✓ (Wave 3 viled parser + fetcher + enumerator + ViledConfig) + 02-05 ✓ (Wave 4 orchestrator keystone — D-203 gates refactor + ViledStatsBuilder + run_viled_phase + run_weekly + cli.py D-212 cutover) — next: `/gsd-execute-phase 02 06` (Wave 5 closeout: brand-aliases.yaml seed + backup script + final live-fetch E2E) |
| Phase 3 status | Phase 3 — Goldapple Crawl (Waves 0-7 complete; verifier re-run pending for Truth 4 closure). Frozen during Phase 2 work; storage layer in 02-02 leaves Phase 3 modules untouched. |
| Status | Phase 3 plan 03-08 (Wave 7 gap_closure: NORM-06 brand-intersect bucket fix) executed 2026-05-06: `enumeration/goldapple_sitemap.py` adds `BRAND_TOKEN_MAX_DEPTH=3` + `index_by_brand_token(slug_map, known_brand_tokens) -> dict[brand_token, list[url]]` Path A longest-prefix-in-whitelist algorithm (each URL goes to exactly ONE bucket — the longest whitelisted prefix; orphan slugs dropped); `enumeration/slug.intersect_brand_pool` and `runner/stats.compute_norm06_forward` accept `brand_bucket` (param renamed from `sitemap_slugs`); `runners/goldapple_run.py` Step 3.5 builds `known_brand_tokens` whitelist via `slug_fy_bilingual` over every alias of every viled brand, then `brand_bucket = index_by_brand_token(slug_map, known_brand_tokens)` before passing to `compute_norm06_forward`; structural D-305 / Pitfall 3 invariant (only viled-known tokens become bucket keys, longest-match wins, no cross-contamination between brand-extension families like tom_ford/tom_ford_beauty unless operator-disambiguated). 192/192 non-live tests green (181 baseline + 7 brand_token_index + 3 intersect new + 1 E2E). 4 commits (1 RED + 1 GREEN Task 1 + 1 Task 2 + 1 Task 3). 0 deviations. CRAWL-02 BLOCKER closed.</status_truncated_for_archive>
| OLD_STATUS_KEPT_BELOW | Phase 3 plan 03-06 (Wave 5 orchestrator + CLI) executed 2026-05-06: `runners/goldapple_run.py` ships `run_goldapple_phase(run_id, viled_brands, repo_root, brand_alias, normalizer, snapshot_writer, run_writer, *, headless=True, M=1000, smoke_urls=None, fetcher_factory=None, sitemap_fetcher=None) -> PhaseResult` async orchestrator implementing all 14 numbered architecture-diagram steps (sitemap fetch → persist → week-over-week diff → brand intersect via compute_norm06_forward → Camoufox boot → smoke_probe → run_loop → parse_pdp + detect_state disambiguation between stale-sku and parse-fail → Phase 2 normalizer protocols → SnapshotWriter.append → Camoufox teardown via async-with __aexit__ Pitfall-7 always-cleanup → final_m_gate → auto_suggest_m via _gather_prior_counts + run_writer.get_stats history scan → atomic patch_stats Pitfall-6 single-call merge), `PhaseResult` dataclass (status/goldapple_count/reason/stats_delta/unmatched_viled_brands/new_goldapple_slugs); `cli.py` ships argparse CLI with `goldapple-smoke --run-id N [--headless false]` (live D-312 smoke probe + diagnostics JSON output) + `goldapple-run --run-id N --viled-brands a,b,c [--repo-root .] [--sanity-gate-m 1000] [--headless false]` (full Phase 3 with stub Phase 2 storage) + 4 stub Phase 2 implementations (StubBrandAlias.lookup → [brand_norm], StubNormalizer.brand/name/volume lowercase+strip / None, StubSnapshotWriter.append → append-only JSONL DATA-03, StubRunWriter.patch_stats/get_stats/fail → JSON dict-merge mirroring SQLite json_patch); `__main__.py` enables `python -m ga_crawler ...`. **16 new integration tests** (test_run_e2e_with_phase2_mocks.py 6 scenarios with FakeFetcher+mocks: happy-path/smoke-fail-aborts/final-gate-fail-run-to-completion/NORM-06-forward-counts/atomic-stats-merge-once/auto-suggest-when-history; test_norm06_diff_integration.py 4 scenarios: first-run-no-diff/second-run-finds-new/third-run-latest-predecessor/removals-ignored; test_storage_integration.py 6 scenarios: jsonl-append/append-only-no-overwrite/patch-merges/fail-records-reason/get-stats-empty/cli-help-emits-subcommands). **179/179 tests green** (Wave 0+1+2+3+4+5 = 163 prior + 16 new). 0 deviations from plan — all `<action>` blocks shipped on first verify pass. |
| Progress | `[██████░░░░░░░░░░░░░░] 1/7 phases` (Phase 1 complete: 9/12 plans; Phase 3: 6/7 plans = 86%) |
| Branch strategy | none (single-trunk) |
| Resume file | `.planning/phases/02-project-skeleton-viled-crawl-storage/` (run `/gsd-execute-phase 02 03` for Wave 2 normalizers; Phase 3 verifier re-run still queued) |

## Performance Metrics

| Metric | Value |
|--------|-------|
| Phases planned | 7 |
| Phases completed | 1 |
| v1 requirements mapped | 48/48 |
| v1 requirements completed | 4/48 (RECON-01..04 — all four Phase 1 requirements closed; CRAWL-02 infrastructure complete via Phase 3 Wave 5 orchestrator wiring Wave 1 brand-pool + Wave 3 fetcher + Wave 4 gates end-to-end; final CRAWL-02 closure deferred to Wave 6 manual live checkpoint where success criteria 4 (1-hour live run) and 5 (NORM-06 review queue populated by real run) are empirically verified) |
| Plans created | 19 (Phase 1 = 12, Phase 3 = 7) |
| Plans completed | 15 (Phase 1: `01-01`, `01-02`, `01-04`, `01-05`, `01-06`, `01-07`, `01-08`, `01-11`, `01-12`; Phase 3: `03-01`, `03-02`, `03-03`, `03-04`, `03-05`, `03-06`) |
| Plans skipped | 3 (`01-03` IPRoyal, `01-09` multi-geo proxy, `01-10` Tier 3 escalation — explicit SKIP per Camoufox-fingerprint-solves-gate verdict) |
| Spawned agents (this session) | roadmapper, gsd-planner, gsd-plan-checker, gsd-executor (inline) |
| Checkpoints | 1 (Phase 1 sign-off) |
| Phase 02 P01 | 25 min | 3 tasks | 33 files |
| Phase 02 P02 | 25 min | 2 tasks | 3 created + 7 modified |
| Phase 02 P03 | 12 min | 2 tasks | 7 created + 4 modified |
| Phase 02 P04 | ~70 min | 2 tasks | 6 created + 8 modified + 1 doc addendum |
| Phase 02 P05 | ~16 min | 3 tasks | 4 created + 5 modified + 1 cascading test rewrite |

### Plan Execution Metrics

| Plan | Duration | Tasks | Files | Date |
|------|----------|-------|-------|------|
| 01-01 (spike skeleton) | ~3 min | 3/3 | 7 created | 2026-05-05 |
| 01-02 (uv init + spike deps) | ~5 min | 3/3 | 4 created | 2026-05-05 |
| 01-04 (robots/ToS audit) | ~38 min | 2/2 | 9 created, 1 modified | 2026-05-05 |
| 01-05 (sitemap + page-volume) | ~52 min | 3/3 (Task 1 substituted) | 9 created, 1 modified | 2026-05-05 |
| 01-07 (viled curl_cffi feasibility) | ~25 min | 3/3 (Task 1 substituted) | 7 created, 2 modified | 2026-05-05 |
| 01-06 (JSON-endpoint hunt) | ~28 min | 3/3 (Task 1 substituted) | 5 created, 1 modified | 2026-05-06 |
| 01-08 (Camoufox 100-fetch) | ~45 min (incl. plan rewrite + smoke debug) | 4/4 (D-14 revised mid-spike) | 7 created, 4 modified | 2026-05-06 |
| 01-11 (MEMO finalize) | ~10 min | 3/3 | 0 created, 1 modified | 2026-05-06 |
| 01-12 (Obsidian + skill + STATE) | ~5 min | 4/4 | 3 created (Obsidian note, project skill, this STATE update), 1 modified (this STATE.md) | 2026-05-06 |
| 03-01 (Wave 0 bootstrap) | ~9 min | 3/3 | 13 created, 2 modified | 2026-05-06 |
| 03-02 (Wave 1 enumeration) | ~6 min | 2/2 | 8 created, 0 modified | 2026-05-06 |
| 03-03 (Wave 2 microdata parser) | ~12 min | 2/2 | 5 created, 0 modified | 2026-05-06 |
| 03-04 (Wave 3 Camoufox fetcher) | ~5 min | 2/2 | 6 created, 0 modified | 2026-05-06 |
| 03-05 (Wave 4 gates + stats) | ~6 min | 2/2 | 7 created, 0 modified | 2026-05-06 |
| 03-06 (Wave 5 orchestrator + CLI) | ~7 min | 2/2 | 7 created, 0 modified | 2026-05-06 |
| 03-08 (Wave 7 gap_closure: brand-token bucket) | ~10 min | 3/3 | 1 created, 6 modified | 2026-05-06 |
| 02-01 (Wave 0 viled bootstrap) | ~25 min | 3/3 | 33 created, 2 modified | 2026-05-07 |
| 02-02 (Wave 1 storage layer) | ~25 min | 2/2 | 3 created, 7 modified | 2026-05-07 |
| 02-03 (Wave 2 normalizers NORM-01..05) | ~12 min | 2/2 | 7 created, 4 modified | 2026-05-07 |
| 02-04 (Wave 3 viled parser + fetcher + enumerator + ViledConfig) | ~70 min | 2/2 | 6 created, 8 test files modified + 1 doc addendum | 2026-05-07 |
| 02-05 (Wave 4 orchestrator keystone — gates refactor + ViledStatsBuilder + viled_run + main_run + cli D-212 cutover) | ~16 min | 3/3 | 4 created (viled_run.py + main_run.py + 2 unit tests), 5 modified (gates.py + stats.py + cli.py + 2 unit-test stubs unblocked), 3 rewrites (2 integration test stubs unblocked + test_storage_integration.py D-212 cascade) | 2026-05-07 |

## Accumulated Context

### Key Decisions

| Decision | Source | Constraint Imposed |
|----------|--------|--------------------|
| Strict-key matching only on v1 (`brand_norm + name_norm + volume_norm`) | PROJECT.md | Forces brand-alias YAML as v1 deliverable in Phase 2; defers fuzzy matching to v2 |
| Append-only snapshot history keyed by `run_id` | research/ARCHITECTURE.md | No in-place updates; Phase 2 schema is final from week 1 |
| `was_price` and stock-state enum captured in v1 schema | research/SUMMARY.md | Avoids retroactive backfill; prevents v1.x re-crawl |
| Match-rate as a tracked KPI from week 1 | research/SUMMARY.md | Phase 4 must log/store match-rate to establish historical baseline |
| Reporter is independent of delivery (file on disk first, Telegram wraps it) | research/ARCHITECTURE.md | Phase 5 produces archive without Telegram; Phase 6 is a thin wrapper |
| Two Telegram chats (ops vs business) | research/PITFALLS.md | Pre-send sanity-gate (Phase 4 + Phase 6) prevents broken reports reaching pricing team |
| Goldapple anti-bot tier decided empirically before any production code | research/SUMMARY.md | Phase 1 is throwaway spike; Phase 3 stack waits on its decision memo |
| SQLite + WAL on v1; Postgres only if SQLite hits limits | research/STACK.md | Single-writer batch fit; defers infra complexity |
| System cron with `CRON_TZ=Asia/Almaty`; no APScheduler/Celery/Prefect | research/STACK.md | Phase 7 minimum; one weekly job |
| Backend-only — no UI / dashboard / API | PROJECT.md (Out of Scope) | All phases `UI hint: no` |
| viled.kz committed rate-limit = 2s sequential | plan 01-04 (RECON-04) | Phase 2 viled crawler config constant; courtesy-only (no Crawl-delay, no anti-scraping clauses in Privacy Policy) |
| goldapple.kz committed rate-limit = 3-5s random uniform, concurrency=1 | plan 01-04 + D-04 + Pitfall 13 | Phase 3 goldapple crawler config; starting point for 01-08 experiment, validated/adjusted there |
| Stealth UA strategy for goldapple (NOT honest UA) | plan 01-04 — robots.txt blocks SemrushBot/MJ12bot/BLEXBot/DotBot | Phase 3 fetch layer uses curl_cffi/Patchright realistic-browser impersonation; no `ViledPriceMonitor/1.0`-style self-identification |
| goldapple anti-bot is GLOBAL (every HTML route gated, not just product pages) | plan 01-04 empirical — 11 ToS-slug candidates all return identical 18 912-byte JS-challenge shell | Strengthens D-01 (start at Tier 2 / Patchright); vanilla Playwright will likely fail too; goldapple ToS text deferred to post-01-08 warm Patchright re-fetch |
| `/rest/` Magento API is robots-Disallowed on goldapple | plan 01-04 (robots.txt §Rest API block) | plan 01-06 JSON-endpoint hunt must avoid `/rest/`; focus on `__NEXT_DATA__`/JSON-LD/non-`/rest/` ajax routes |
| goldapple sitemap.xml is **plain-deliverable** via curl_cffi (sitemapindex with 3 sub-sitemaps, 112,317 URLs total, 100,779 product-numeric URLs, 1,461 brand slugs) | plan 01-05 empirical | Phase 3 architecture splits: enumeration via curl_cffi (Tier 0) + product render via Patchright (Tier 2). No need to render brand-listing pages. Reduces total Patchright fetches by ~60% |
| Catalog-wide ~69 products/brand average (from sitemap) is Phase 3 anchor for proxy budget; sample facet count ≠ SKU count (caveat) | plan 01-05 | Phase 3 estimate: ~50 brands × 69 products = ~3,450 fetches/week; ~600 MB bandwidth, ~$2.10 IPRoyal Tier 3 proxy, ~4.4h duration at 3-5s rate-limit |
| viled.kz is luxury fashion + niche perfumery (NOT mass-market beauty); brand selection for 01-08 = Jo Malone London, Tom Ford, Creed, Frederic Malle, Givenchy | plan 01-05 — autonomous probe viled __NEXT_DATA__ | Phase 2 viled-list extraction pattern: Next.js __NEXT_DATA__ JSON-blob (confirmed for both privacy page in 01-04 and homepage in 01-05); Phase 3 product-fetch focus on luxury/niche brands |
| viled fully Tier 0 confirmed: 15/15 HTTP 200 via curl_cffi impersonate=chrome at 2s pause, avg 485ms; no Patchright, no proxy needed for Phase 2 | plan 01-07 empirical | Phase 2 viled fetch layer = curl_cffi only (consistent with research/STACK.md "Tier 0 — viled.kz" but now empirically backed) |
| Phase 2 PARSE-02 для viled = __NEXT_DATA__-first extraction (NOT JSON-LD-first as for goldapple) — viled has zero JSON-LD on product pages | plan 01-07 empirical (0/15 JSON-LD, 15/15 __NEXT_DATA__) | Phase 2 parser dispatch: per-retailer extraction front-end. viled = `props.pageProps.{item, attributes}`; goldapple D-14 = JSON-LD Product.offers.price (pending 01-08 confirmation) |
| Phase 2 viled enumeration = sitemap-only (42,294 product URLs across 9 sub-sitemaps under /item/<numeric_id>); no HTML pagination crawl needed | plan 01-07 empirical | Phase 2 CRAWL-01 simplified: sitemap-index → sub-sitemaps → product URLs. Incremental delta available via sitemap `<lastmod>` |
| viled was_price requirement (v1 schema) directly satisfiable from week 1 via realPrice field — no retroactive backfill | plan 01-07 empirical | STATE.md "was_price captured in v1 schema" decision now has data-supporting field path |
| viled currency mapping for Phase 2 NORMALIZE-01: "₸" (display unicode) → "KZT" (programmatic); single-currency site, hardcode | plan 01-07 empirical | Phase 2 normalizer constant |
| goldapple anti-bot vendor identified as **GroupIB / F.A.C.C.T.** (Russian-market rebrand of Singapore-based GroupIB), NOT Cloudflare/DataDome | plan 01-06 empirical (challenge HTML reveals `window.gib.init({cid:'w-goldapple', gafUrl:'ru.id.facct.ru/id.html'})`) | Material reorder of Phase 1 escalation tree: **Camoufox is now a primary 01-08 candidate** (different fingerprint surface, Firefox-based vs Chromium), not Tier-4 last resort. 2026 Patchright benchmarks target Cloudflare/DataDome/Akamai, not GroupIB — uncharted territory. |
| Patchright on KZ-laptop direct (D-06 baseline, no proxy) is empirically INSUFFICIENT to clear goldapple gate (0/7 in 01-06 with 20-25s wait per page) | plan 01-06 empirical | STATE.md gate "if ≥98/100 + challenge<10% — proxy not needed" decisively fails → **01-03 IPRoyal REQUIRES REVIVAL before 01-08**; ungate it. Phase 7 prod-IP-geo flag: EU-Hetzner likely WORSE than KZ-residential (GroupIB likely whitelists local TLD/IP-geo). |
| goldapple gate API contract: `POST /web/api/v1/settings` (24/24=403 for blocked sessions); telemetry sinks `/front/api/event*` (200) and `https://sp.goldapple.ru/front/api/apm/events` (POST 202) accept blind; `https://ru.id.facct.ru/id.html` iframe loaded for cross-origin device fingerprint | plan 01-06 empirical | 01-08 must poll `/web/api/v1/settings` in background to detect gate-clearance before starting product fetches. May need 5-15min warmup pattern (idle browse, scroll). |
| D-14 success-criterion verification deferred to 01-08 post-gate-clearance (challenge shell has no `__NEXT_DATA__`/JSON-LD; cannot confirm/deny presence on real product HTML without first passing the gate) | plan 01-06 — challenge shell is the only HTML obtained | Phase 3 parser implementation BLOCKED on 01-08 reaching real product HTML. `sample-payloads/goldapple-jsonld-sample.json` is `[]` with explicit D-14 ALERT pointer. |
| **Tier 2 = Camoufox direct, no proxy** for goldapple (99/100 D-13 PASS, 0% gate-shell rate, NOT FRAGILE per D-15). D-01 (Patchright start) superseded; D-08 (IPRoyal pre-register) cancelled. Plans 01-03, 01-09, 01-10 SKIPPED. | plan 01-08 empirical (signed-off MEMO 2026-05-06) — see `.planning/spikes/01-goldapple/MEMO.md` | Phase 3 production engine LOCKED. Phase 7 hosting recommendation = Hetzner CX22 EU + smoke gate; IPRoyal KZ residential as fallback if EU+Camoufox combination fails. |
| **D-14 revised:** goldapple uses inline microdata (`<meta itemprop="price">`), NOT JSON-LD Product schema. Goldapple's only JSON-LD block is `OfferShippingDetails` (shipping policy). Phase 3 parser uses microdata extraction; viled separately uses `__NEXT_DATA__` parser. | plan 01-08 smoke test 2026-05-06 | Phase 3 has TWO parser strategies, not symmetric. Parser dispatch by site. |
| **Camoufox Python 0.4.11 == upstream Firefox 135.0.1-beta.24** — verified empirically via `camoufox.pkgman.installed_verstr()`. Pin `camoufox[geoip]==0.4.11` is the PEP 440-valid semantic equivalent of plan's `0.4.11+camoufox.135.0.1-beta.24` (which is a local-version identifier not on PyPI). | plan 03-01 (Task 1) | Phase 3 fingerprint stability anchored on PyPI-publishable pin; manual upgrade workflow per D-313 unchanged. |
| **`[build-system]` hatchling required for src/-layout** — without `[build-system]` block, `uv sync` does not install the project package itself, breaking all `from ga_crawler.* import` statements downstream. Added in plan 03-01 Task 2 as Rule 3 deviation. | plan 03-01 (Task 2 verification) | All Wave 1+ plans depend on `ga_crawler.interfaces` import — bootstrap blocker now resolved. |
| **Cyrillic-presence regex must use explicit KZ-glyph alternation, NOT contiguous range** — `[ә-і]` raises `re.error: bad character range` because KZ glyphs span U+0456 to U+04E9 with non-adjacent codepoints. Use `[а-яёәғқңөұүһі]` (explicit list). Same fix applies to the slug-whitelist regex `_normalize_punct`. RESEARCH §Pattern 2 line 396 contains the source-of-truth bug. | plan 03-02 (Task 1, first pytest run) | All bilingual slug-fy implementations downstream must use the explicit-list form; copy the constant from slug.py rather than re-typing. |
| **Apostrophes must be stripped (NOT hyphenated) before non-alphanum→hyphen step** — RESEARCH §Pattern 2 line 435 mandates `L'Oréal Paris → loreal-paris`; verbatim algorithm `[^a-z0-9а-я]+ → -` produces `l-oreal-paris`. Strip `' ’ ʼ ʹ` first. | plan 03-02 (Task 1, parametrize case 6) | Phase 2 normalizer (NORM-02) and any future slugifier MUST handle apostrophes before hyphen-replacement; same rule for curly quotes. |
| **respx is httpx-based, NOT curl_cffi-compatible** — for tests that mock `curl_cffi.requests.get`, monkey-patch the wrapper function directly (e.g. `_fetch_xml`) instead of declaring respx routes. respx routes are silently bypassed by curl_cffi. | plan 03-02 (Task 2 test design) | All Tier 0 fetcher tests follow this pattern; documented in `test_sitemap_parser.py` docstring as canonical reference. |
| **selectolax `Node.css_first(...)` searches the WHOLE subtree, not direct children** — for sibling-priceType lookups in microdata, naive `parent.css_first('link[itemprop="priceType"]')` falsely matches priceType nested inside `[itemprop="priceSpecification"]` descendants. Iterate `parent.css(...)` and walk each candidate's ancestor chain back up to (but excluding) parent; reject candidates whose chain crosses an `itemprop='priceSpecification'` boundary. RESEARCH §Pattern 4 lines 568-572 contains the source-of-truth bug. | plan 03-03 (Task 2, first pytest run — 5/30 failed including `test_pricetype_filter_picks_top_level_not_strikethrough`) | All future microdata extractors that need scope-bounded selector queries must apply this pattern; Phase 4 matcher follow-on may face similar issue if it walks brand subtrees. |
| **Gold Card / 'при авторизации' walk-up MUST be bounded by `[itemprop="offers"]` ancestor** — otherwise `parent.text()` reaches `<body>` and includes label text from a SIBLING offer block, falsely poisoning the public-price classification. Encoded as: stop returning False when `cursor.itemprop == 'offers'`. | plan 03-03 (Task 2, after fix #1 — `test_pricetype_gold_card_section_excluded` still failing) | Any future text-context heuristic that walks ancestor chains MUST declare a stop boundary; ad-hoc walks are unreliable on real-world DOMs that nest multiple offers. |
| **`importlib.metadata.version(pkg)` is the canonical Python 3.8+ way to read installed-package versions** — NOT `getattr(pkg_module, "__version__", default)`. Some packages (camoufox 0.4.11) ship `__version__` as a SUBMODULE (`camoufox/__version__.py`), not a string attribute on the package, so `getattr(camoufox, "__version__", "unknown")` returns the submodule object. PEP 566 metadata reads the distribution's `Version:` field directly. | plan 03-05 (Task 1, `test_smoke_diagnostics_shape`) | All future runtime-version probes use `importlib.metadata.version(...)`; assume nothing about how the package's source code exposes its version internally. |
| **`int(0.7 × value)` is NOT the same as the "0.7×value" rational arithmetic** because 0.7 cannot be exactly represented in IEEE 754 binary float — `0.7 * 2700.0 == 1889.9999999999998`, which `int()` truncates to 1889 (not 1890). Plan-time hand-arithmetic for test expectations must be Python-evaluated, not human-computed. Acceptable drift for D-310 (operator-confirmed PR; sub-2000 suggestion rounds to whole hundreds anyway). | plan 03-05 (Task 1, `test_auto_suggest_uses_last_4_only`) | All future test cases involving non-integer multipliers must run the formula in Python before locking the expected integer; alternatively, document the IEEE 754 truncation behavior in the test docstring (current pattern in test_auto_suggest_int_truncates). |
| **D-305 / Pitfall 3 is enforced STRUCTURALLY via longest-prefix-in-whitelist bucket index (Path A), NOT interpretively** — `intersect_brand_pool` does pure exact-key `dict.get()` against a precomputed `brand_bucket` whose keys are themselves filtered through `known_brand_tokens` whitelist at indexing time. `index_by_brand_token(slug_map, known_brand_tokens)` tests depth-3..depth-1 prefixes (longest first), assigns each URL to exactly one bucket (longest-match wins, then `break`), drops orphan slugs. Cross-contamination between brand-extension families (e.g. `tom-ford` / `tom-ford-beauty`) is impossible by construction — operator opt-in to disambiguation by adding both to `viled_brands`. `BRAND_TOKEN_MAX_DEPTH=3` covers single/two/three-word brand names; beyond depth 3 false-positive risk grows. | plan 03-08 (Wave 7 gap_closure, closes 03-VERIFICATION Truth 1 BLOCKER) | All future operator-driven slug-prefix-matching schemes (e.g. category-bucket indexing in Phase 4 matcher, fuzzy v2 alias-expansion in MATCH-V2-01) MUST follow this pattern: (1) precompute a whitelist from operator-known tokens, (2) bound depth, (3) longest-match wins, (4) orphan rows dropped. Source-level D-305 guards via `inspect.getsource` are cross-platform (no shell-grep false-positives); use these in any future verifier check. |
| **`patch_stats` MUST use single-call SQL `UPDATE runs SET stats = json_patch(stats, :delta)` — NEVER read-modify-write** — Pitfall 6 atomic-merge invariant. Phase 2 viled.* writes and Phase 3 goldapple.* writes can land in the same row across the run; any read-modify-write loop loses keys to last-writer-wins. SQLite's `json_patch()` is RFC-7396 MergePatch atomic at the SQL execution layer. Caveat (Pitfall 4): RFC-7396 treats null as DELETE, so `patch_stats` rejects None values upfront with a loud `ValueError("Pitfall 4: …")` — the silent failure mode (key disappears) is converted to an early traceable bug. | plan 02-02 (Task 2, `test_patch_stats_atomic_merge_pitfall_6` + `test_patch_stats_rejects_none_pitfall_4`) | All future RunWriter-shaped accumulators (Phase 4 matcher stats, Phase 5 reporter rollups) follow the same single-SQL-call pattern. ORM read-modify-write is a Rule 1 bug. Sentinels for "key absent" are -1, "", or [] — never None. |
| **`SqliteRunWriter.create()` and `.finalize()` are concrete-only methods, NOT added to `RunWriterProtocol`** — Open Q1 (Phase 2 RESEARCH) drift-avoidance. Adding them to the Protocol breaks Phase 3's existing stub contract (3-method Protocol: patch_stats / get_stats / fail). Plan 05 orchestrator calls `create`/`finalize` on the concrete `SqliteRunWriter` instance directly, not via the Protocol surface. `interfaces.py` stays untouched (verified by 0-line `git diff`). | plan 02-02 (Task 2, `git diff src/ga_crawler/interfaces.py` empty) | Phase 4+ orchestrators that need lifecycle methods beyond the 3-method Protocol declare them on the concrete class only. Adding to the Protocol is a breaking change requiring a versioning sweep across all stub callers. |
| **`v_current_snapshots` SQL VIEW returns 0 rows when no successful run exists** — `MAX(run_id) WHERE status='success'` returns NULL when the predicate matches no row; `WHERE run_id = NULL` is always false in SQL three-valued logic. Verified via `test_view_empty_when_no_success_runs`. | plan 02-02 (Task 1, D-221 verification) | Phase 3 (Plan 05) brand-pool derivation reads `SELECT DISTINCT brand_norm FROM v_current_snapshots WHERE retailer='viled'` and MUST handle empty result gracefully (degenerate first run with no prior week is the dominant initial-bootstrap case). |
| **viled price/realPrice semantics: Reading A — `price` = current/sale, `realPrice` = was/MSRP** — Empirically verified against canonical fixture (price==realPrice==187700) and discounted Frederic Malle fixture (price=356745 < realPrice=419700, customer-facing price is the lower one). The plan source code originally inverted this; auto-corrected as Rule 1 deviation. WAVE0-PROBE.md §A2 + STATE.md plan 01-07 ("realPrice satisfies was_price requirement") are the source-of-truth. | plan 02-04 (Task 1, `test_discounted_fixture_real_corpus` regression-canary) | All future viled parser changes MUST keep this reading; the live discounted fixture pins `current=356745, was=419700` as a permanent assertion. Any inverted reading will fail the test. |
| **viled stock-state derivation: `item.count` (int) + `item.purchaseType` (str enum 'ONLINE'/'PREORDER'), NOT `attributes[0].in_stock`** — viled has no boolean stock field anywhere in `pageProps`. Mapping: count > 0 + purchaseType==ONLINE → IN_STOCK; count > 0 + purchaseType==PREORDER → UNAVAILABLE; count == 0 → OUT_OF_STOCK; count missing/non-int → UNKNOWN. PREORDER spelling is provisional (no PREORDER PDP found in 6 sampled); first weekly run will confirm. | plan 02-04 (Task 1, `_map_stock_state(item)`) per WAVE0-PROBE A1 REVISED | All viled stock-state checks read `item.count` directly. If a PREORDER fixture surfaces in production with a different spelling, update both `_map_stock_state` and the WAVE0-PROBE A1 entry simultaneously. |
| **viled catalog pagination: SSR ignores `?page=N` (and 9 other URL conventions) — runtime guard breaks early on stuck pageNumber, v1 limit is page-1 only (120 SKUs total across both catalogs)** — Live probe 2026-05-07 tested ?page=N, ?pageNumber=N, ?p=N, ?offset=N, ?from=N, /page/N, /N, _next/data/{buildId}/...json with each query param, and 4 /api/* candidates. Every successful response returned `pageNumber=1` with the same `content[0].id`. The catalog presumably uses client-side XHR pagination requiring CSRF or other request-signing not exposed via simple GET. v1 effective output (60 men + 60 women) is above D-201 sanity_gate_n=100; resolution deferred to Phase 3/7 ops follow-up. | plan 02-04 (Task 2, addendum to 02-WAVE0-PROBE.md, Rule 3 deviation) | Plan 02-05 orchestrator can rely on `fetch_catalog_urls()` returning ~60 URLs per catalog. Phase 3/7 ops backlog: reverse-engineer the XHR pagination via real browser session capture, OR pivot to per-`groupId` filter walks for full 7,697-SKU coverage. |
| **curl_cffi exception types live at `curl_cffi.requests.exceptions`, NOT `.errors`** — `.errors` only exports `CookieConflict, CurlError, RequestsError, SessionClosed`. The full Timeout/ConnectTimeout/ReadTimeout/ConnectionError/HTTPError/RequestException stack lives in `.exceptions`. Tenacity retry-set MUST import from `.exceptions`. `test_a10_import_path_correct` negatively-asserts `.errors` lacks Timeout to catch any future curl_cffi reorganization. | plan 02-04 (Task 2, fetchers/viled.py imports) per WAVE0-PROBE A10 REVISED | All future curl_cffi-using modules import exception types from `curl_cffi.requests.exceptions`. The negative-assertion test in `test_viled_retry_policy.py` is the cross-version drift detector. |
| **viled currency hardcoded to "KZT" unconditionally** — `attributes[0].currency` raw value is logged via `viled_unexpected_currency` warning if it is not "₸"/"KZT", but the snapshot field always carries "KZT". STATE.md plan 01-07 lock; viled is KZ-only retailer. | plan 02-04 (Task 1, `parsers/viled_nextdata.py::parse_pdp`) | All viled snapshots in the storage layer carry currency=KZT; the comparator/reporter does not need a multi-currency code-path for viled. |
| **D-203 retailer-agnostic gates: refactor with backward-compat shims, NOT rip-and-replace** — `auto_suggest_threshold(history, factor, min_runs)` and `final_threshold_gate(count, threshold)` are the canonical helpers in `runner/gates.py`; `auto_suggest_m`, `final_m_gate`, `final_n_gate` are kept as 1-line shims forwarding to the new helpers. Phase 3 callers (`runners/goldapple_run.py` imports `auto_suggest_m`, `final_m_gate`) stay green — `git diff src/ga_crawler/runners/goldapple_run.py` empty after refactor. | plan 02-05 (Task 1 — D-203 refactor) | All future "generalize a Phase-N specific helper" refactors follow this pattern: add the new generic helper, leave the old name as a 1-line shim with explicit docstring "Phase N shim", verify zero diff in callers. Phase 3 frozen-modules invariant preserved. |
| **Sequential gates D-218: parse_quality FIRST, sanity_gate_n SECOND — both can fail same run; audit-trail invariant holds in either failure path** — `viled_run.py` runs `parse_quality_gate(null_rate)` BEFORE `final_threshold_gate(count, N)`. If parse-quality fails (>5% null required-field rate), the run fails with `parse_quality_below_threshold` reason BEFORE sanity-N is checked. If parse-quality passes but count < N, the run fails with `sanity_gate_n_failed`. In EITHER failure case, the snapshot rows already persisted to the DB remain (DATA-03 immutable); failure to gate does NOT roll back. The integration test `test_sanity_n_gate_fails` asserts both run.status='failed' AND `len(snapshots) == 5` simultaneously. | plan 02-05 (Task 2, `tests/integration/test_viled_run_e2e_with_real_storage.py`) | Phase 4 (matcher) follow-on may want similar gate sequencing — apply this pattern: persist first, gate after, single atomic stats merge regardless of gate outcome. |
| **`v_current_snapshots` is unusable mid-run because the run row's status is still 'running'** — main_run.py's D-221 brand-pool derivation reads `SELECT DISTINCT brand_norm FROM snapshots WHERE retailer='viled' AND run_id=:rid` directly (NOT v_current_snapshots) because the VIEW filter is `WHERE run_id = (SELECT MAX(run_id) FROM runs WHERE status='success')` — and the current run's status is still 'running' between viled and goldapple phases. Querying the snapshots table directly with an explicit run_id is functionally equivalent and unblocks the brand-intersect step. | plan 02-05 (Task 2, `_derive_viled_brands_from_snapshots` in main_run.py) | All future cross-phase data-passing inside a single run uses direct table queries; the VIEW is only for post-finalize reporting/inspection (Phase 5 reporter, Phase 6 delivery). |
| **DATA-05 lifecycle: try/except in main_run.py + idempotent run_writer.fail catches double-call gracefully** — `run_weekly` wraps the body in try/except; any uncaught Exception triggers `run_writer.fail(run_id, traceback)`. If a phase already called `fail()` (e.g. viled phase failed sanity-N gate and called fail before returning), the outer except's second `fail()` call is idempotent (`SqliteRunWriter.fail` UPDATEs unconditionally; second UPDATE is a no-op-ish status='failed'→'failed'). Verified by `test_data05_uncaught_exception_finalizes` patching `SqliteSnapshotWriter.append` to raise. | plan 02-05 (Task 2, `run_weekly` exception handler) | All future composition orchestrators (Phase 4 matcher, Phase 5 reporter, Phase 6 delivery) follow this pattern: wrap body in try/except, call fail() with stack-trace on exception, guarantee `runs.status` is never stuck at 'running' on any code path. |
| **D-212 stub deletion has exactly one cascading test surface: tests/integration/test_storage_integration.py** — Phase 3's parallel-development stubs (`StubBrandAlias`, `StubNormalizer`, `StubSnapshotWriter`, `StubRunWriter` in `cli.py`) were imported by `test_storage_integration.py` only. The fix is a clean rewrite: exercise the production `SqliteSnapshotWriter` / `SqliteRunWriter` instead of stubs. Mock fixtures in `tests/conftest.py` (`mock_brand_alias`, `mock_normalizer`, `mock_snapshot_writer`, `mock_run_writer`) take over the testability role for unit + mocked-integration tests. | plan 02-05 (Task 3 cascading fix per D-212) | All future "delete-and-rewrite" cleanups verify cascade fan-out by running `grep -r "<deleted symbol>" tests/` BEFORE deletion; expected: 0 hits or every hit is a planned test rewrite. The cascading rewrite must preserve test narrative (append-only, atomic merge, fail-record) at the new abstraction. |

### Active Todos

- Run `/gsd-execute-phase 02 06` — Wave 5 closeout: brand-aliases.yaml seed (≥50 viled brands D-206) + bin/backup.sh (online sqlite3 .backup with 4-rotate D-219) + tests/integration/test_backup_script.py (DATA-06) + final live-fetch E2E (`python -m ga_crawler weekly-run --viled-only --sanity-gate-n=10` against real viled.kz)
- [Phase 2 closeout cascade] Operator backlog: reverse-engineer viled XHR pagination beyond page 1 (Phase 3/7 ops backlog item — current page-1-only limit yields ~120 SKUs, sufficient for D-201 N=100 but blocks full 7,697-SKU corpus)
- [Phase 3/7 ops backlog] viled catalog pagination beyond page 1 — reverse-engineer the XHR pagination call from a real browser session OR pivot to a per-`groupId` filter walk to reach the full 7,697-SKU corpus. Current v1 limit: 120 SKUs (60 men + 60 women, page 1 of each catalog). Closes when Phase 7 ops budget allows.
- [Phase 3 backlog] Operator live re-run for Phase 3 Truth 4 closure — `goldapple-run --run-id 43 --viled-brands givenchy,jo_malone_london --sanity-gate-m 10` (small subset confirming 1-hour live run pattern)
- Run `/gsd-plan-phase 3` — Phase 3 context gathered, ready for planning. Picks up `.claude/skills/spike-01-goldapple/SKILL.md` + `.planning/phases/03-goldapple-crawl/03-CONTEXT.md` automatically
- Run `/gsd-discuss-phase 2` — Project Skeleton + viled Crawl + Storage (viled stack hot-data ready from 01-07; can run in parallel with Phase 3 plan/exec since Phase 3 contracts to Phase 2 modules)
- [Phase 7 backlog] Camoufox+EU smoke fetch before locking Hetzner hosting — if regression, revive D-08 (IPRoyal trial)
- [Phase 7 backlog] KZ-legal review (30 min с юристом) for ToS compliance — bundle = `tos-audit.md` + `viled-privacy.txt` + both `*-robots.txt` snapshots + GroupIB/F.A.C.C.T. vendor flag
- [Phase 3 ops backlog] Weekly Camoufox-vs-goldapple smoke ("does the gate still pass?") — covered by D-312 integrated smoke probe; threshold = gate-shell rate >5% per spike-skill

### Active Blockers

(none — Phase 3 Truth 1 BLOCKER closed by plan 03-08; Truth 4 UNCERTAIN remains for operator live re-run, NOT a blocker per verification report)

## Session Continuity

### What Was Just Done

- `/gsd-execute-phase 02 05` plan 02-05 (Wave 4 orchestrator keystone — gates refactor + ViledStatsBuilder + viled_run + main_run + cli D-212 cutover) 2026-05-07 — 3 tasks executed sequentially, **0 deviations from plan** (1 in-test bug auto-fixed inline: my fake catalog used substring `"men" in url` which falsely matched "women"; switched to `"/men/"` / `"/women/"` segment match):
  - **Task 1** (`ab8e44f`): refactored `src/ga_crawler/runner/gates.py` (139 → 239 LOC) — added D-203 retailer-agnostic helpers `auto_suggest_threshold(history, factor=0.7, min_runs=4)` + `final_threshold_gate(count, threshold)` + D-218 `parse_quality_gate(null_rate, threshold=0.05)` + 3 backward-compat shims (`auto_suggest_m`, `final_m_gate`, `final_n_gate`) forwarding to the new generic helpers; extended `src/ga_crawler/runner/stats.py` (146 → 219 LOC) with `VILED_STATS_KEYS` 9-tuple namespace + `ViledStatsBuilder` mirror of GoldappleStatsBuilder (Open Q7); 4 new unit test files (49 GREEN tests): test_auto_suggest_threshold (8 tests including factor/min_runs overrides + Phase 3 shim parity), test_sanity_n_gate (12 tests boundary), test_parse_quality_gate (7 tests boundary), test_viled_stats_builder (16 tests including disjoint-namespaces invariant). Phase 3 GoldappleStatsBuilder + GOLDAPPLE_STATS_KEYS UNCHANGED. 321 → 370 passed.
  - **Task 2** (`890f050`): created `src/ga_crawler/runners/viled_run.py` (328 LOC) shipping `run_viled_phase(*, run_id, config, brand_alias, normalizer, snapshot_writer, run_writer, dispatcher=None, fetcher=None, fetch_catalog=None) -> ViledPhaseResult` 8-step pipeline (catalog enum across both /men/ and /women/ catalogs → ViledFetcher.run_loop → ParseDispatcher.dispatch → Normalizer brand/name/volume + detect_multipack → SqliteSnapshotWriter.append per-batch → parse-quality gate D-218 FIRST → sanity-N gate D-201/CRAWL-05 SECOND → SINGLE atomic patch_stats Pitfall 6); created `src/ga_crawler/runners/main_run.py` (294 LOC) shipping `run_weekly(repo_root, *, db_path, headless, viled_only, goldapple_only, sanity_gate_n, sanity_gate_m, aliases_path, pyproject_path) -> MainRunResult` composition orchestrator with DATA-05 try/except lifecycle (uncaught Exception → idempotent run_writer.fail with stack-trace; best-effort Norm06 audit artifact even on crash) + D-221 `_derive_viled_brands_from_snapshots(engine, run_id)` reads `snapshots WHERE retailer='viled' AND run_id=:rid` directly (NOT v_current_snapshots — VIEW unusable mid-run because runs.status='running') + D-211 Norm06Writer.persist owned by Phase 2 (single call after both retailer phases complete). 9 new integration tests across 2 files (4 viled_run e2e: happy_path 10 SKUs across 2 catalogs / sanity_n_gate_fails with audit-trail invariant / atomic_stats_merge_pitfall_6 spy verifies single patch_stats call / per_sku_isolation 1 fetch failure mid-loop with 9 records returned; 5 main_run e2e: full_run_cycle_viled_only / viled_failure_blocks_goldapple with goldapple-not-called assertion / crash_finalizes_run / data05_uncaught_exception_finalizes patches snapshot_writer.append to raise / norm06_ledger_written_on_success). 370 → 379 passed; Phase 3 frozen modules untouched.
  - **Task 3** (`737b1b0`): rewrote `src/ga_crawler/cli.py` (218 → 165 LOC, -53 LOC after stub deletion) — DELETED 4 Stub classes (StubBrandAlias / StubNormalizer / StubSnapshotWriter / StubRunWriter), DELETED `goldapple-run` subcommand + `_cmd_run` handler, ADDED `weekly-run` subcommand with 7 flags (--repo-root, --db-path, --sanity-gate-n, --sanity-gate-m, --headless, --viled-only, --goldapple-only) backed by `runners/main_run.run_weekly`, KEPT `goldapple-smoke` subcommand verbatim. Cascading test fix: `tests/integration/test_storage_integration.py` rewritten to exercise production `SqliteSnapshotWriter` / `SqliteRunWriter` (replaces prior Stub-class tests; +3 new CLI help-output verification tests asserting goldapple-smoke listed, weekly-run listed, goldapple-run NOT listed). 379 → 381 passed.
  - **Test suite**: 321 → 381 passed (+60 GREEN); 5 → 2 skipped (1 carryover Wave 5 stub for Plan 02-06 + 1 deliberate skip in viled_run e2e where reaching "successful parse with null required fields" requires post-parse mutation that bypasses the orchestrator contract — gate semantics covered exhaustively in tests/unit/test_parse_quality_gate.py).
  - **Phase 3 frozen modules untouched**: `git diff src/ga_crawler/runners/goldapple_run.py src/ga_crawler/interfaces.py src/ga_crawler/parsers/goldapple_microdata.py src/ga_crawler/enumeration/goldapple_sitemap.py src/ga_crawler/fetchers/goldapple.py` empty.
  - **Pitfall 6 verified**: test_atomic_stats_merge_pitfall_6 wraps run_writer.patch_stats with a spy and asserts exactly 1 call on success path with all viled.* keys merged.
  - **DATA-05 verified**: test_data05_uncaught_exception_finalizes patches SqliteSnapshotWriter.append to raise RuntimeError; result.status='failed', run row finalized with finished_at non-null, fail_reason carries the exception type.
  - **CLI verified**: `python -m ga_crawler --help` shows {goldapple-smoke, weekly-run}; `python -m ga_crawler weekly-run --help` shows all 7 flags; `goldapple-run` is gone.
  - **D-212 cutover verified**: `grep -c "class Stub" src/ga_crawler/cli.py` = 0; the 2 remaining "goldapple-run" matches in cli.py are docstring references documenting the deletion (functional CLI doesn't expose the subcommand).
  - 3 commits, 4 source files created/modified (viled_run.py + main_run.py NEW + gates.py + stats.py + cli.py modified), 6 test files unblocked or rewritten. Self-check PASSED.
  - SUMMARY → `.planning/phases/02-project-skeleton-viled-crawl-storage/02-05-SUMMARY.md`. **Cascading constraints for Plan 02-06**: (1) seed `config/brand-aliases.yaml` with ≥50 production viled brands (D-206); (2) ship `bin/backup.sh` online sqlite3 .backup with 4-rotate per D-219; (3) ship `tests/integration/test_backup_script.py` for DATA-06; (4) run a final live-fetch E2E (`python -m ga_crawler weekly-run --viled-only --sanity-gate-n=10 --db-path=/tmp/e2e.db` against real viled.kz) and assert DB has rows + Norm06 ledger writes; (5) document in Phase 3/7 ops backlog: reverse-engineer viled XHR pagination (current page-1-only limit yields ~120 SKUs, sufficient for D-201 N=100 but blocks the full 7,697-SKU corpus).

- `/gsd-execute-phase 02 04` plan 02-04 (Wave 3 viled parser + fetcher + enumeration + ViledConfig) 2026-05-07 — 2 tasks executed sequentially, **2 deviations** (1 Rule 1 + 1 Rule 3, both auto-fixed):
  - **Task 1** (`f8132cd`): `src/ga_crawler/parsers/types.py` (StockState Literal enum, PARSE-06) + `src/ga_crawler/parsers/viled_nextdata.py` (224 LOC __NEXT_DATA__-only parse_pdp + ViledRawProduct frozen dataclass + `_extract_next_data` + `_map_stock_state`; PARSE-04 sanity range [100, 1_000_000] inclusive; currency hardcoded "KZT" with `viled_unexpected_currency` observability warning) + `src/ga_crawler/parsers/dispatcher.py` (ParseDispatcher concrete impl satisfying ParseDispatcherProtocol; `_registry = {viled, goldapple}`). 23 GREEN tests in test_viled_nextdata_parser.py + 8 in test_viled_stock_state.py + 7 in test_parse_dispatcher.py = +38 tests. **Rule 1 deviation**: plan source code had price/realPrice semantics inverted relative to live fixtures — corrected per WAVE0-PROBE A2 (price=current, realPrice=was, discount when `price < realPrice`); live discounted Frederic Malle fixture (price=356745, realPrice=419700) pinned in `test_discounted_fixture_real_corpus` as a permanent regression-canary.
  - **Task 2** (`e342dde`): `src/ga_crawler/config.py` (72 LOC ViledConfig.from_pyproject() via 3.12-stdlib tomllib; reads `[tool.ga_crawler.crawl.viled]` namespace; defaults match TOML so direct construction yields identical values) + `src/ga_crawler/enumeration/viled_catalog.py` (267 LOC fetch_catalog_urls — walks page 1 then attempts `?page=2..N` for `totalPages`, runtime-detects SSR-not-paginating server, breaks early gracefully) + `src/ga_crawler/fetchers/viled.py` (215 LOC ViledFetcher curl_cffi sync + tenacity retry stop_after_attempt(3) + wait_exponential_jitter(2, 30) + per-SKU isolation + 2 s pacing; exception types imported from `curl_cffi.requests.exceptions` per WAVE0-PROBE A10 REVISED). +36 GREEN tests across 5 test files (test_viled_catalog_paginate.py 11 + test_viled_fetcher_isolation.py 5 + test_viled_retry_policy.py 8 + test_viled_rate_limit.py 9 + test_viled_fetcher_mocked.py 4). **Rule 3 deviation**: catalog pagination URL convention is unknown — live probe of 10 URL variants (`?page=N`, `?pageNumber=N`, `?p=N`, `?offset=N`, `?from=N`, `/page/N`, `/N`, `_next/data/{buildId}/...json` ×3, `/api/*` ×4) confirmed every variant returns page 1 (`pageNumber=1` invariant + `content[0].id=408872` invariant). Implemented graceful page-1-only fallback; v1 effective output = 120 SKUs (60 men + 60 women) which still satisfies D-201 sanity_gate_n=100. Resolution deferred to Phase 3/7 ops. Documented in WAVE0-PROBE.md addendum.
  - **WAVE0-PROBE consumption**: A1 REVISED (item.count + item.purchaseType, no in_stock bool), A2 VERIFIED (Reading A: price=current, realPrice=was), A3 VERIFIED (Tier 0 only), A4 REVISED + LIMITATION-DOCUMENTED (pageProps.items.{...} keys + SSR-not-paginating runtime guard), A10 REVISED (curl_cffi.requests.exceptions). All paths consumed verbatim.
  - **Test suite**: 247 passed → 321 passed (+74 GREEN); 13 → 5 skipped (only Wave 4/5 stubs remain). Phase 3 frozen modules untouched: `git diff` empty against interfaces.py, parsers/goldapple_microdata.py, enumeration/goldapple_sitemap.py, fetchers/goldapple.py, runners/goldapple_run.py.
  - **Voluntary additions**: curl_cffi-native exception types (Timeout, ReadTimeout, ConnectionError, HTTPError, RequestException) added to tenacity retry-set alongside synthetic TransientFetchError (richer operator observability — curl_cffi's own raised types retry directly with original traceback); `test_a10_import_path_correct` negatively-asserts `.errors` lacks Timeout (cross-version drift detector); 4 extra negative-path parser tests; live discounted fixture regression-canary; `test_real_catalog_fixture_extracts_60_urls` integration test against the Wave 0 catalog fixture.
  - **Cascading constraints for Plan 02-05**: `runners/viled_run.py` orchestrator imports `from ga_crawler.config import ViledConfig`, `from ga_crawler.parsers.dispatcher import ParseDispatcher`, `from ga_crawler.parsers.viled_nextdata import ViledRawProduct`, `from ga_crawler.parsers.types import StockState`, `from ga_crawler.fetchers.viled import ViledFetcher, fetch_one_isolated, TransientFetchError`, `from ga_crawler.enumeration.viled_catalog import fetch_catalog_urls`. Concrete contracts: `ParseDispatcher().dispatch("viled", html, url)` returns 9-key dict or None; `ViledFetcher(run_id=N, pause_seconds=2.0).run_loop(urls, stats, sleep_fn=time.sleep)` is sync, N-1 sleeps for N URLs; `fetch_catalog_urls(base, pause_seconds=2.0)` returns deduplicated page-1 URLs (~60); `ViledConfig.from_pyproject("pyproject.toml")` returns frozen dataclass.
  - 2 commits, 6 src files created, 8 test files modified + 1 doc addendum. Self-check PASSED.
  - SUMMARY → `.planning/phases/02-project-skeleton-viled-crawl-storage/02-04-SUMMARY.md`. Next: `/gsd-execute-phase 02 05` (Wave 4 orchestrator + PARSE-05 + CRAWL-05).

- `/gsd-execute-phase 02 02` plan 02-02 (Wave 1 storage layer) 2026-05-07 — 2 TDD tasks executed sequentially, **0 deviations**:
  - **Task 1 RED** (`e331875`): `tests/unit/test_storage_models.py` (3 tests: 17 Snapshot cols + 6 Run cols + UNIQUE constraint) + `tests/unit/test_snapshot_writer.py` (5 tests incl. Phase 3 dict-shape Pitfall 7 + per-batch commit DATA-04) + `tests/integration/test_storage_wal.py` (3 tests for journal_mode=WAL + synchronous=NORMAL + foreign_keys=ON) + `tests/integration/test_v_current_snapshots.py` (2 tests for D-221 view). All skip markers removed; failing on `ModuleNotFoundError: ga_crawler.storage`.
  - **Task 1 GREEN** (`2c23f8d`): `src/ga_crawler/storage/__init__.py` package marker + `src/ga_crawler/storage/sqlite.py` (~190 LOC at this point) ships SQLModel `Run` table (run_id PK, started_at, finished_at, status, fail_reason, stats TEXT JSON), SQLModel `Snapshot` table with 18 columns + composite UNIQUE (run_id, retailer, sku_id) + 2 supporting indexes (ix_snapshot_retailer_brand_norm, ix_snapshot_run_retailer), `make_engine` factory with `event.listens_for(engine, "connect")` listener applying `PRAGMA journal_mode=WAL` + `synchronous=NORMAL` + `foreign_keys=ON` on every connect (DATA-04), `init_db` schema bootstrap via `SQLModel.metadata.create_all` + `CREATE VIEW IF NOT EXISTS v_current_snapshots AS SELECT * FROM snapshots WHERE run_id = (SELECT MAX(run_id) FROM runs WHERE status='success')` (D-221), `SqliteSnapshotWriter` INSERT-only with per-batch commit (default batch_size=100; payload filtered through `Snapshot.model_fields.keys()` to mitigate Pitfall 7 schema drift between Phase 3 dict-shape and Phase 2 richer dict). 13/13 Task 1 tests green.
  - **Task 2 RED** (`3fdadf0`): `tests/unit/test_run_writer.py` (7 tests: create/patch_stats_atomic_merge_pitfall_6/overrides/rejects_none_pitfall_4/get_stats_missing/fail_idempotent/finalize_only_running) + `tests/integration/test_run_writer_lifecycle.py` (1 full create→patch(viled+goldapple)→finalize cycle) + `tests/unit/test_norm06_writer.py` (4 tests: writes_markdown_table/status_pending_default/empty_inputs_writes_header_only/writes_to_planning_runs_subdir D-208 path). Failing on missing `SqliteRunWriter` + `storage.norm06_writer` module.
  - **Task 2 GREEN** (`3ee77c7`): `src/ga_crawler/storage/sqlite.py` extended with `SqliteRunWriter` class — `create()` opens running row + returns auto-incremented run_id (concrete-only NOT in Protocol per Open Q1), `patch_stats(run_id, delta)` does single-SQL `UPDATE runs SET stats = json_patch(stats, :delta) WHERE run_id = :rid` atomic RFC-7396 MergePatch (Pitfall 6) + rejects None values upfront with `ValueError("Pitfall 4: …")` (Pitfall 4 — RFC-7396 null-as-DELETE), `get_stats(run_id)` reads + JSON-decodes, `fail(run_id, reason)` idempotent UPDATE, `finalize(run_id, status="success")` with `WHERE status='running'` guard (cannot un-fail a previously-failed run). `src/ga_crawler/storage/norm06_writer.py` ships `Norm06Writer.persist(run_id, viled_unmatched, goldapple_new_slugs)` — renders 4-column markdown table at `.planning/runs/{run_id}/norm06-review.md` (D-208) with source enum `viled-unmatched | goldapple-new-slug` and status default `pending`; markdown header documents D-209 operator workflow inline (aliased / skip / reviewed → commit; git history is audit trail per D-210). 12/12 Task 2 tests green.
  - **Full suite**: 217 passed + 17 skipped + 0 failed (vs 192 + 24 + 0 baseline → **+25 GREEN, -7 skip-marked** = 7 unblocked + 3 voluntary additive). `git diff src/ga_crawler/interfaces.py` returns empty (Open Q1 Protocol drift avoided). Phase 3 frozen modules untouched.
  - 4 commits, 3 source files created (`__init__.py` + `sqlite.py` 269 LOC + `norm06_writer.py` 85 LOC = 362 LOC), 7 test files modified. Self-check PASSED.
  - SUMMARY → `.planning/phases/02-project-skeleton-viled-crawl-storage/02-02-SUMMARY.md`. **Cascading constraints for Plans 04+05**: Plan 04 viled_run.py imports SqliteSnapshotWriter (must serialize `volume_norm` tuple → str before INSERT); Plan 05 main_run.py orchestrator calls `init_db()` once at startup, `engine = make_engine(); rw = SqliteRunWriter(engine); sw = SqliteSnapshotWriter(engine)`, `run_id = rw.create()`, viled phase patches `viled.*` keys + goldapple phase patches `goldapple.*` keys (must NOT collide per Pitfall 6), on success `rw.finalize(run_id, "success")`, on exception `rw.fail(run_id, str(exc))` from try/finally, then `Norm06Writer(repo_root).persist(run_id, viled_unmatched, goldapple_new_slugs)`. Plan 06 backup script can rely on WAL active (test_wal_pragma_active confirms).

- `/gsd-execute-phase 3` plan 03-08 (Wave 7 gap_closure: NORM-06 brand-intersect bucket fix) 2026-05-06 — 3 TDD tasks executed sequentially, **0 deviations** (plan executed verbatim from copy-paste-ready `<action>` blocks):
  - **Task 1 RED** (`88176bc`): `tests/unit/test_brand_token_index.py` — 7 failing tests for new helper (longest-prefix-in-whitelist with structural disambiguation, orphan-drop, MAX_DEPTH=3, empty whitelist edge cases). Confirmed RED via ImportError.
  - **Task 1 GREEN** (`68e32c0`): `src/ga_crawler/enumeration/goldapple_sitemap.py` — added `BRAND_TOKEN_MAX_DEPTH = 3` + `index_by_brand_token(slug_map, known_brand_tokens) -> dict[brand_token, list[url]]` (Path A: tests depth-3..depth-1 prefixes against whitelist, longest-match wins, orphan slugs dropped). `__all__` extended. 7/7 tests pass.
  - **Task 2** (`ca719c7`): refactored `src/ga_crawler/enumeration/slug.py` and `src/ga_crawler/runner/stats.py` — param `sitemap_slugs` renamed to `brand_bucket` in both `intersect_brand_pool` and `compute_norm06_forward`; docstrings updated to point at `index_by_brand_token` as the source-of-truth indexer. `tests/unit/test_intersect_brand_pool.py` — mechanical rename `sitemap` → `brand_bucket` in 6 existing tests (data unchanged); +3 new tests: `test_intersect_against_real_sitemap_shape` (6 sub-tests covering givenchy / jo_malone_london / tom_ford with-and-without disambiguation / estee_lauder bilingual / unknown), `test_intersect_no_substring_lookup_in_function_body` (D-305 inspect.getsource gate), `test_compute_norm06_forward_with_brand_bucket_shape`. 9/9 intersect tests pass.
  - **Task 3** (`68213b4`): `src/ga_crawler/runners/goldapple_run.py` — added imports `index_by_brand_token` + `slug_fy_bilingual`; new Step 3.5 between sitemap diff and NORM-06 forward: builds `known_brand_tokens: set[str]` via `slug_fy_bilingual` over every alias of every viled brand, then `brand_bucket = index_by_brand_token(slug_map, known_brand_tokens)`, passes `brand_bucket` to `compute_norm06_forward` (was `slug_map`); new `phase3_brand_bucket_built` structlog event with `whitelist_size` + `bucket_key_count`. `tests/integration/test_run_e2e_with_phase2_mocks.py` — +1 E2E test `test_e2e_brand_intersect_against_realistic_sitemap_shape` injects a 6-entry slug_map keyed by full product-slugs (real shape) for 3 viled brands; asserts none fall to unmatched, `delta["goldapple.unmatched_viled_brands"] == 0`, status==success. 6 prior E2E tests still pass (Path X — slug-keys in those fixtures already match brand-tokens after whitelist build).
  - **All phase-level verification gates passed first try:** Truth 1 closure smoke (`PASS — Truth 1 closure + D-305 structural disambiguation proven`), D-305 source-level guards on all 3 production functions (zero `.startswith/.find/.endswith/.contains` in `intersect_brand_pool`, `index_by_brand_token`, `compute_norm06_forward`), AST orchestrator wiring check (`index_by_brand_token` + `slug_fy_bilingual` called; `compute_norm06_forward` receives `brand_bucket` AST.Name), AST param-rename check (`sitemap_slugs` no longer a parameter in slug.py or stats.py), CLI smoke (`python -m ga_crawler --help` exits 0).
  - **192/192 tests green** (181 baseline + 11 new = 192 expected per plan; achieved exactly). 4 commits, 1 file created (`tests/unit/test_brand_token_index.py`), 6 files modified. Self-check PASSED. Duration ~10 min.
  - SUMMARY → `.planning/phases/03-goldapple-crawl/03-08-SUMMARY.md`. **CRAWL-02 BLOCKER closed:** matched_url_count > 0 against realistic 45,490-slug sitemap shape proven structurally; D-305 / Pitfall 3 now a STRUCTURAL invariant (only viled-known brand-tokens become bucket keys, longest-prefix-match precludes cross-contamination by construction). **Open follow-up:** verifier re-run for Truth 4 (operator runs `goldapple-run --run-id 43 --viled-brands givenchy,jo_malone_london --sanity-gate-m 10` to obtain the 1-hour-run data point); Phase 4 (matcher) unblocked.

- `/gsd-execute-phase 3` plan 03-06 (Wave 5 orchestrator + CLI) 2026-05-06 — 2 tasks executed sequentially, **0 deviations** (plan executed verbatim from copy-paste-ready `<action>` blocks):
  - **Task 1** (`1c04bce`): `src/ga_crawler/runners/__init__.py` package marker; `src/ga_crawler/runners/goldapple_run.py` ships `run_goldapple_phase(run_id, viled_brands, repo_root, brand_alias, normalizer, snapshot_writer, run_writer, *, headless=True, M=1000, smoke_urls=None, fetcher_factory=None, sitemap_fetcher=None) -> PhaseResult` async orchestrator wiring all 14 numbered architecture-diagram steps end-to-end (sitemap fetch → persist_sitemap_slugs → find_previous_slug_file → diff_new_slugs for D-307 NORM-06 reverse → builder.set("unmatched_goldapple_slugs_new") → compute_norm06_forward via brand_alias.lookup for D-306 NORM-06 forward → builder.set("unmatched_viled_brands") → factory(run_id, headless) → async-with fetcher → smoke_probe + builder.set smoke_pass/diagnostics/camoufox_version → on smoke fail: run_writer.fail + patch_stats + return PhaseResult(failed, "smoke_probe_failed") with profile dir cleaned via __aexit__ → on smoke pass: fetcher.run_loop(matched_urls) → for each non-blocked record: parse_pdp(html, url) → on None: detect_state to disambiguate stale-sku from parse-fail → on success: build normalized dict via normalizer.brand/name/volume → snapshot_writer.append(run_id, "goldapple", final_records) → builder.from_run_loop_stats + set stale_count/parse_failures/gate_shell_count → __aexit__ teardown → final_m_gate(count, M) → _gather_prior_counts via run_writer.get_stats(prior_id) for IDs current-4..current-1 → auto_suggest_m([*history, current_count]) → builder.set("auto_suggest_m") if not None → run_writer.patch_stats(builder.delta) ATOMIC SINGLE CALL Pitfall 6 → on M-fail: run_writer.fail with "goldapple_count {n} < M={M}" + return PhaseResult(failed) → on success: return PhaseResult(success)). `PhaseResult` dataclass with status/goldapple_count/reason/stats_delta/unmatched_viled_brands/new_goldapple_slugs. `tests/integration/test_run_e2e_with_phase2_mocks.py` ships 6 E2E scenarios with `FakeFetcher` (no-op async-context, canned smoke_records and run_loop_records, `_smoke_call_idx` advances per fetch_one call so smoke probe gets 3 distinct canned responses) + Phase 2 mocks (mock_brand_alias.lookup.side_effect, mock_normalizer brand/name/volume, mock_snapshot_writer.append asserted call_count==1, mock_run_writer.patch_stats asserted call_count==1, mock_run_writer.fail asserted not_called or called once with reason): test_e2e_happy_path (2 URLs, 2 records, success, snapshot once, fail not called), test_e2e_smoke_fail_aborts (3 gate-shell smoke responses, snapshot NOT called, fail called with "smoke" in reason), test_e2e_final_gate_fail_run_to_completion (1 record + M=10, snapshot DOES write per D-309, fail called, patch_stats called once), test_e2e_norm06_forward_counts_unmatched (givenchy hits + tom_ford misses → unmatched_viled_brands=["tom_ford"] in PhaseResult + delta goldapple.unmatched_viled_brands=1), test_e2e_atomic_stats_merge_one_call (5 records → patch_stats.call_count == 1 Pitfall 6), test_e2e_auto_suggest_when_history_present (mock_run_writer.get_stats.side_effect returns {"goldapple.fetch_count": 2000} for all prior IDs → orchestrator computes int(0.7 * median([2000,2000,2000,2000,1][-4:]) = int(0.7 * 2000) = 1400). `tests/integration/test_norm06_diff_integration.py` ships 4 scenarios: test_first_run_no_diff (no predecessor → empty), test_second_run_finds_new_slugs (run-1 → run-2 detects "tom-ford"), test_third_run_only_new_in_diff (run-3 picks LATEST predecessor run-2, not run-1; diff = ["c","d"]), test_removed_slugs_not_in_diff (D-307 additions-only). 10/10 new tests green; 173/173 total at end of Task 1.
  - **Task 2** (`2ebddca`): `src/ga_crawler/cli.py` ships argparse CLI with `goldapple-smoke --run-id N [--headless false]` (boots GoldappleFetcher async-with, runs smoke_probe, prints diagnostics JSON, exit 0=pass / 1=fail) + `goldapple-run --run-id N --viled-brands a,b,c [--repo-root .] [--sanity-gate-m 1000] [--headless false]` (calls run_goldapple_phase with stub Phase 2 implementations rooted at {repo_root}/.planning, prints PhaseResult JSON, exit 0=success / 2=failed) + 4 stub Phase 2 implementations satisfying interfaces.py Protocols: StubBrandAlias.lookup → [brand_norm], StubNormalizer.brand/name lowercase+strip volume None, StubSnapshotWriter.append append-only JSONL writes to `{root}/runs/{id}/snapshots.jsonl` (DATA-03), StubRunWriter.patch_stats/get_stats/fail JSON dict-merge to `{root}/runs/{id}/runs.json` (mirrors SQLite json_patch semantics; Pitfall 6 atomic single-call merge). `_parse_bool` helper resilient to operator typos (false/0/no/off → False). `src/ga_crawler/__main__.py` re-exports cli.main for `python -m ga_crawler ...` invocation. `tests/integration/test_storage_integration.py` ships 6 stub-storage contract tests: test_snapshot_writer_appends_jsonl (retailer + run_id columns), test_snapshot_writer_append_only_no_overwrite (DATA-03 immutable: 2 calls → 2 lines), test_run_writer_patch_stats_merges (goldapple.* and viled.* coexist after sequential patch_stats), test_run_writer_fail_records_reason (status=failed + fail_reason + stats preserved), test_run_writer_get_stats_empty_when_no_run, test_cli_main_smoke_command_exists (--help shows both subcommands). 6/6 tests green; **179/179 total tests green** (Wave 0..5 = 163 prior + 16 new); `uv run python -m ga_crawler --help` shows both subcommands.
  - **0 deviations** — plan executed exactly as written. Two minor in-scope adaptations: (1) `fetcher_factory` parameter typed `Optional[Callable[..., Any]]` instead of plan's stub `Optional[object]` for type clarity at call site (semantic identity preserved — GoldappleFetcher class is itself a callable matching the (run_id, headless) signature so plan's isinstance shim was unnecessary); (2) CLI `--headless` parser uses module-level `_parse_bool` function recognizing `false/0/no/off` instead of plan's lambda `v.lower() != "false"` (resilience to operator typos; semantic identity preserved for the plan's expected `false` input).
  - 7 files created (2 source + 4 tests + 1 main.py shim), 0 modified. 2 commits. Self-check PASSED.
  - SUMMARY → `.planning/phases/03-goldapple-crawl/03-06-SUMMARY.md`. **5 of 5 CRAWL-02 success-criteria infrastructure complete**: URL pool from viled snapshot (orchestrator step 4), goldapple snapshots written to same `snapshots` table (orchestrator step 10 via SnapshotWriterProtocol.append), sanity-gate marks runs.status=failed when goldapple_count < M (orchestrator step 12 via final_m_gate + run_writer.fail), per-SKU isolation/retry/rate-limit/parse-quality reused from Phase 2 modules (orchestrator step 7 wires Wave 3 GoldappleFetcher.run_loop), NORM-06 review queue populated with reverse-direction NEW slugs (orchestrator steps 2-3 + result.new_goldapple_slugs). Wave 6 (manual live smoke + limited live run) is the empirical verification gate before Phase 7 prod-cron deploy.

- `/gsd-execute-phase 3` plan 03-05 (Wave 4 runtime gates + stats namespace) 2026-05-06 — 2 tasks executed sequentially:
  - **Task 1** (`11dca8c`): `src/ga_crawler/runner/__init__.py` package marker; `src/ga_crawler/runner/gates.py` ships `SMOKE_URLS` (frozen 3-Givenchy tuple, `7680100018-very-irresistible-givenchy` + `7681000001-givenchy-pour-homme-blue-label` + `19000032744-givenchy-gentleman-reserve-privee-eau-de-parfum`; A12 stale-row-0 `7681000002` explicitly excluded), `load_smoke_urls_from_config(repo_root)` Phase 7 rotation surface (reads `config/smoke_urls.txt` one-URL-per-line + strip `#` comments, falls back to SMOKE_URLS), `_camoufox_version_at_runtime()` via `importlib.metadata.version("camoufox")` (Rule 1 fix), `smoke_probe(fetcher, smoke_urls=SMOKE_URLS) -> {pass, diagnostics}` async pre-crawl gate (pass = ALL urls have `status in (200, 304)` AND `not block` AND `parse_pdp(html, url).current_price > 0`; diagnostics dict has `camoufox_version` + per-URL `responses` list with url/status/size/title/block/price_extracted), `final_m_gate(count, M=1000) -> bool` inclusive `count >= M` boundary (D-308/D-309), `auto_suggest_m(history) -> Optional[int]` returns None for <4 runs else `int(0.7 * statistics.median(history[-4:]))` (D-310; Rule 1 fix in test: 0.7 not exactly representable in IEEE 754, int(0.7 * 2700.0) = 1889 not 1890). 7 smoke probe tests + 18 final_m_gate/auto_suggest tests = 25/25 green.
  - **Task 2** (`5d5ba6c`): `src/ga_crawler/runner/stats.py` ships `GOLDAPPLE_STATS_KEYS` frozen 13-key tuple per RESEARCH §Q4 lines 1051-1066 (fetch_count, fetch_failures, gate_shell_count, stale_count, parse_failures, unmatched_viled_brands, unmatched_goldapple_slugs_new, smoke_pass, smoke_diagnostics, fetch_duration_seconds, mean_fetch_seconds, camoufox_version, auto_suggest_m); `_BARE_TO_NAMESPACED` reverse-map for auto-prefix; `StatsNamespaceError(KeyError)` raised on any non-namespace key (Pitfall 6 mitigation against accidental clobber of viled.* keys); `GoldappleStatsBuilder` one-shot accumulator with `.set(bare_or_full, value)` (auto-prefix), `.inc(bare_or_full, n=1)` (zero-init), `.get(bare, default)`, `.from_run_loop_stats(dict)` (defensive ignore of unknown keys), `.delta` ready for `RunWriter.patch_stats(run_id, builder.delta)` atomic JSON-merge; `compute_norm06_forward(viled_brands, aliases, sitemap_slugs) -> tuple[list[str], int, list[str]]` wraps Wave 1's `intersect_brand_pool` for D-306 forward direction (lazy-import to avoid future circular dep). 28 stats namespace tests + 5 NORM-06 forward tests = 33/33 green.
  - 2 Rule 1 deviations, both auto-fixed inline: camoufox version submodule shape (plan's `getattr(camoufox, "__version__", ...)` returned a module object — switched to `importlib.metadata.version("camoufox")`); IEEE 754 truncation in `auto_suggest_m([100,200,300,400,5000,6000])` (plan-expected 1890, actual `int(0.7 * 2700.0) = 1889` because 0.7 is not exactly representable in binary float; updated test expected value with explanatory comment, acceptable per D-310 operator-confirmed-not-auto-tune).
  - **163/163 tests green** (Wave 0+1+2+3+4 = 105 prior + 25 Task 1 + 33 Task 2). 2 commits, 7 files created, 0 modified. Self-check PASSED.
  - SUMMARY → `.planning/phases/03-goldapple-crawl/03-05-SUMMARY.md`. 5 of 13 LOCKED Phase 3 decisions delivered (D-306 NORM-06 forward; D-308 M=1000 default; D-309 final M-gate post-crawl shape; D-310 auto-suggest formula+gating; D-312 smoke probe pass-criteria). Wave 5 orchestrator (Plan 03-06) wires smoke_probe → run_loop → final_m_gate → patch_stats end-to-end.
- `/gsd-execute-phase 3` plan 03-04 (Wave 3 Camoufox fetcher) 2026-05-06 — 2 tasks executed sequentially, **0 deviations** (plan executed verbatim from copy-paste-ready `<action>` blocks):
  - **Task 1** (`b178a97`): `src/ga_crawler/fetchers/__init__.py` package marker; `src/ga_crawler/fetchers/goldapple.py` retry + isolation primitives — `TransientFetchError`, `_goto_with_retry` tenacity decorator (`stop_after_attempt(RETRY_MAX_ATTEMPTS=3) + wait_exponential_jitter(initial=2.0, max=30.0) + retry_if_exception_type((TransientFetchError, PWTimeout)) + reraise=True`; lazy `_make_retry_decorator()` factory catches `ImportError` for `playwright.async_api.TimeoutError` and falls back to private `class PWTimeout(Exception)`), `fetch_one_isolated` module-level free function (catch arbitrary `Exception` → `log.error("fetch_failed", url=..., error=str(e), error_type=...)` → `stats["fetch_failures"] += 1` → `return None`), `GoldappleFetcher` class skeleton (Task 2 fills lifecycle + fetch_one + run_loop). Operational constants pinned: `PAUSE_RANGE=(3.0, 5.0)`, `PAGE_TIMEOUT_MS=60_000`, `GATE_POLL_DEADLINE_MS=25_000`, `GATE_POLL_STEP_MS=500`, `RETRY_*` triple, mirrors pyproject.toml [tool.ga_crawler.crawl.goldapple]. 7 retry-policy tests + 4 isolation tests = 11/11 green.
  - **Task 2** (`e250e27`): `src/ga_crawler/fetchers/goldapple.py` extended with `__aenter__` (lazy `from camoufox.async_api import AsyncCamoufox` + boot with all six SKILL kwargs `geoip=True, locale=["ru-RU","kk-KZ","en-US"], humanize=True, persistent_context=True, user_data_dir=str(self.profile_dir)`; tmp profile cleanup on boot failure), `__aexit__` (try/finally `shutil.rmtree(self.profile_dir, ignore_errors=True)` always — Pitfall 7), `fetch_one(page, url)` (spike-style dict — `_goto_with_retry` for transport, optional `wait_for_load_state("networkidle", 10s)`, gate-poll `while elapsed < GATE_POLL_DEADLINE_MS: page.title()` until `GATE_TITLE_MARKER not in title.lower()`, state classification via `block_reason ∈ {gate_shell_not_cleared, http_*, exception}`; broad outer try/except sets `block=True`+`block_reason="exception"` so isolation is purely counter+log), instance-method `fetch_one_isolated(url, stats)` delegates to module-level free function, `run_loop(urls, stats, sleep_fn=None)` sequential drive with `random.uniform(*PAUSE_RANGE)` between URLs (NOT after last), accumulates `fetch_count` + `gate_shell_count` + `fetch_failures` into stats. `tests/integration/__init__.py` package marker; `tests/integration/test_goldapple_fetch_loop_mocked.py` (10 tests via `FakeCamoufoxCM` context-manager replacement: lifecycle clean on success + on exception, locked Camoufox kwargs assertion, fetch_one happy/gate-shell/404/exception, run_loop pacing-between-not-after-last, run_loop isolation continues, run_loop accumulates `gate_shell_count`). 105/105 tests green (84 prior + 21 new).
  - SUMMARY → `.planning/phases/03-goldapple-crawl/03-04-SUMMARY.md`. Self-check PASSED. Wave 3 ships infrastructure for CRAWL-02 (per-SKU isolation chain wired end-to-end with retry + counter); CRAWL-02 itself closes when Wave 5 orchestrator wires the brand-pool intersect from Wave 1 into the run_loop.
- `/gsd-execute-phase 3` plan 03-03 (Wave 2 microdata parser) 2026-05-06 — 2 tasks executed sequentially:
  - **Task 1** (`cb6da19`): `parsers/goldapple_microdata.py` shipped with `GoldappleRawProduct` frozen dataclass (9 fields), `detect_state(html, title)` three-axis classifier (gate-shell / stale-sku / real-pdp — Pitfall 4 / D-303 / RESEARCH §Pattern 4 verbatim), `has_microdata_price(html)` foundation primitive (re-implementation of spike notebook.py L94-110), constants `GATE_SHELL_MAX_BYTES=30_000` and `GATE_TITLE_MARKER="checking"` pinned. 11 gate-detection tests + 4 stale-SKU detection tests anchored to spike `tier2_results_json` row 0 (`7681000002-givenchy-pour-homme-blue-label`: status=200, html_size=18027, title="Loading <url>"). All 15 tests green.
  - **Task 2** (`ed7f959`): `parse_pdp(html, url) -> Optional[GoldappleRawProduct]` with priceType-aware extraction (Pitfall 2). Helpers: `_walks_into_priceSpecification` (skip nested priceSpec descendants), `_has_excluded_priceType_sibling` (scope-bounded — rejects priceType inside nested priceSpecification per ancestor-chain check), `_is_in_gold_card_section` (walk-up bounded by `[itemprop='offers']` ancestor), `_extract_top_level_offer`, `_extract_strikethrough`, `_extract_availability`. PARSE-04 enforces `100 <= price <= 1_000_000` inclusive boundaries. PARSE-06 maps schema.org availability URLs to enum {InStock, OutOfStock, Discontinued, PreOrder, Unknown}. **Real Givenchy PDP round-trip:** brand_raw='Givenchy', sku_id='7681000002', current_price=46920, was_price=60410 (StrikethroughPrice), currency='KZT', availability='InStock'. 30 parser tests (7 round-trip + 2 gate/stale rejection + 3 priceType + 7 sanity-range parametrize + 6 enum parametrize + 1 no-link Unknown + 1 JSON-LD anti-fixture + 3 strikethrough extractor).
  - 2 deviations both auto-fixed: Rule 1 — naive `parent.css_first('link[itemprop="priceType"]')` reaches into nested priceSpecification descendant and falsely matches StrikethroughPrice as a "sibling" of the public price → fix iterate `parent.css(...)` and walk ancestor chain rejecting nested priceSpec; Rule 1 — `_is_in_gold_card_section` walk-up reads `<body>` text and falsely matches "при авторизации" from a SIBLING offer block → fix bound walk-up at nearest `[itemprop="offers"]` ancestor.
  - Plan-spec deviation (intentional, not a Rule violation): Task 1 wrote the FULL parser module up-front (including Task-2 helpers) for module-level coherence; Task 1 commit `cb6da19` only ships the dataclass + classifier tests, Task 2 commit `ed7f959` ships parser tests + the bug-fix edits. Both tasks logically separated at test/commit level.
  - 2 commits, 5 files created, 0 modified; 84/84 unit tests green (Wave 0+1+2). Self-check PASSED.
  - SUMMARY → `.planning/phases/03-goldapple-crawl/03-03-SUMMARY.md`
- `/gsd-execute-phase 3` plan 03-02 (Wave 1 enumeration) 2026-05-06 — 2 tasks executed sequentially:
  - **Task 1** (`954d0ea`): `enumeration/slug.py` — `slug_fy_bilingual` (NFKD + accent strip + Cyrillic→Latin transliterate, with explicit KZ-glyph list ә ғ қ ң ө ұ ү һ і) + `intersect_brand_pool` (EXACT-match via `dict.get`, NOT substring iteration — Pitfall 3 / D-305 guard against `tom-ford ⊆ tom-ford-beauty`). 11 RESEARCH-mandated parametrized cases + 4 supplementary (idempotency, KZ glyph round-trip, hyphen-collapse, map keys). Plus 6 intersect cases (exact-match guard, bilingual hit, unmatched surfacing, alias-fallback to brand_norm, empty-alias unmatched, multi-URL slug).
  - **Task 2** (`a7fa06d`): `enumeration/goldapple_sitemap.py` — `fetch_sitemap_slugs` (curl_cffi `impersonate="chrome"`, tenacity `stop_after_attempt(3) + wait_exponential_jitter`, SITEMAP_TIMEOUT_S=30s); `PRODUCT_URL_RE` whitelist (Threat T-04 sitemap-poisoning mitigation: rejects /brands/x facets, wrong domains, non-numeric IDs); `persist_sitemap_slugs / find_previous_slug_file / diff_new_slugs` (D-307 NORM-06 reverse direction, on-disk under `{root}/runs/{run_id}/sitemap-slugs.txt`, sorted). 9 sitemap-parser tests (6 regex whitelist cases + namespaced fixture extraction + mocked end-to-end + 503-retry-then-raise) + 9 norm06-diff tests (persist sorted, predecessor finder with non-numeric/future skip, first-run-empty, additions-sorted, removals-ignored, blank-line-tolerant).
  - 4 deviations all auto-fixed: Rule 1 — `[ә-і]` regex range is `re.error: bad character range` (KZ glyphs non-adjacent), fix = explicit `[а-яёәғқңөұүһі]` alternation; Rule 1 — `L'Oréal Paris → l-oreal-paris` instead of `loreal-paris`, fix = strip apostrophes BEFORE hyphen-replacement; Rule 1 — slug whitelist `[^a-z0-9а-я]+` drops KZ glyphs, fix = extend to `[^a-z0-9а-яёәғқңөұүһі]+`; Rule 3 — fixture is etree-serialized (`<ns0:loc>`), production sitemap is plain `<loc>`; fix = test regex matches both shapes, mocked end-to-end exercises production shape. 2 commits, 8 files created, 39/39 tests green, self-check PASSED.
  - SUMMARY → `.planning/phases/03-goldapple-crawl/03-02-SUMMARY.md`
- `/gsd-execute-phase 3` plan 03-01 (Wave 0 bootstrap) 2026-05-06 — 3 tasks executed sequentially:
  - **Task 1** (`36d8f56`): pinned `camoufox[geoip]==0.4.11` (bundles Firefox 135.0.1-beta.24 per D-313, verified via `pkgman.installed_verstr()`); added `tenacity>=9`, `sqlmodel>=0.0.24`, `pydantic>=2.10`; dev: `pytest 8`, `pytest-asyncio 1.3.0` (asyncio_mode=auto), `pytest-mock`, `respx`. `[tool.pytest.ini_options]` block + `[tool.ga_crawler.crawl.goldapple]` namespace with 15 operational constants + 3 hardcoded smoke URLs (D-312).
  - **Task 2** (`c2716c5`): created `src/ga_crawler/__init__.py` + `interfaces.py` with 6 Protocol contracts (BrandAlias, Normalizer, SnapshotWriter, RunWriter, ParseDispatcher — `@runtime_checkable`; CrawlerProtocol — static-only). Rule 3 deviation: added `[build-system]` hatchling block to make `src/`-layout package importable as `ga_crawler` (without it, all Wave 1+ imports fail).
  - **Task 3** (`6ac04c0`): copied 5 spike sample-payloads as test fixtures verbatim (`_debug-product-page.html`, `gate-shell.html`, `sitemap-1-excerpt.xml`, `tier2-camoufox-kz-results.json`, `_debug-jsonld-blocks.json`), synthesized `stale-sku-9.5kb.html` (9189 B, "Loading" title, no microdata) per PATTERNS fixture-gap fallback. `tests/conftest.py` with 11 fixtures: 6 HTML/JSON loaders (session-scope), 4 Phase 2 protocol mocks (function-scope), 1 `tmp_camoufox_profile_dir` factory.
  - 3 deviations all auto-fixed (Rule 1: Camoufox PEP 440 string mismatch; Rule 3: hatchling build-system required for src/-layout; Rule 3: skip redundant `camoufox fetch` — binary already cached). 3 commits, 13 files created, 2 modified, self-check PASSED.
  - SUMMARY → `.planning/phases/03-goldapple-crawl/03-01-SUMMARY.md`
- `/gsd-discuss-phase 3` 2026-05-06 — 4 gray areas обсуждены (URL-pool, Brand-alias coverage, Sanity-gate threshold M, Camoufox profile lifecycle). 13 implementation decisions D-301..D-313 locked: sitemap-only URL pool, full re-crawl weekly, slug-эвристика bilingual exact-match, NORM-06 viled-side + week-over-week NEW goldapple-slug diff, M=1000 static + run-to-completion + auto-suggest after 4 weeks, fresh Camoufox profile per run + integrated smoke probe before crawl + pinned Camoufox version. Created `.planning/phases/03-goldapple-crawl/03-CONTEXT.md` (canonical refs to spike-MEMO + spike-skill + research/* + Phase 1 context, code_context with reusable assets/patterns/integration points, deferred ideas list including v2 incremental + fuzzy + persistent-profile-warm). Created `.planning/phases/03-goldapple-crawl/03-DISCUSSION-LOG.md` audit trail. Committed `b100932`.
- `/gsd-execute-phase 1` Phase 1 closure session 2026-05-06 (continuation of session that committed `1ff7d4d` Camoufox-spike + `789e51b` re-route session note):
  - **Plan 01-08 rewritten** Patchright→Camoufox (commit `532b37c`); URL pool collected (`649eb6c`); notebook.py replaced with Camoufox 100-fetch implementation including D-14 revision to microdata-or-JSON-LD (`90f112d`); 100-fetch run executed at 99/100 success, 0% gate-shell, NOT FRAGILE (`f9ace33`); MEMO Options-tested table updated with both Patchright (historical 0/7) and Camoufox (actual 99/100) rows + Open Risks (post 01-08) subsection (`e13e47a`); SUMMARY → `.planning/phases/01-goldapple-reconnaissance-spike/01-08-SUMMARY.md` (`175d6de`).
  - **Plan 01-11 finalized** MEMO sign-off (commit `70fdffa`) — TL;DR + Chosen + robots/ToS rate-limits + Next-step impact + Open risks + Appendix Challenge-rate + Sign-off block; all 12 obligatory sections populated, zero TBDs; SUMMARY → `01-11-SUMMARY.md` (`e4a5a1b`).
  - **Plan 01-12 wrap-up** (this commit) — Obsidian decision note `knowledge/decisions/Goldapple — Tier 2 Camoufox без proxy, 99 из 100.md` created with frontmatter (tags, date, project, phase, tier, source_memo); project-local skill `.claude/skills/spike-01-goldapple/SKILL.md` created for Phase 3 discuss/plan auto-discovery; STATE.md (this file) updated — Phase 1 closed, Tier 2 in Key Decisions, Phase 2 next; SUMMARY pending.
  - **Plans skipped (formal):** 01-03 (IPRoyal trial — D-08 cancelled), 01-09 (multi-geo proxy — VOI ≈ 0), 01-10 (Tier 3 escalation — trigger never fires). Documented in MEMO Options-tested status block.
- `/gsd-execute-phase 1` plan 01-06 executed (sequential mode, Task 1 substituted by programmatic Patchright network capture per user pre-authorization in spawn prompt):
  - **Task 1 (substituted):** `scripts/01-06-network-hunt.py` — Patchright (chromium persistent context per D-04, headless=False, KZ-laptop direct per D-06, no proxy) probes 7 URLs (home, brands index, 2 brand listings, 3 product/facet pages from selected brands per D-12) at 3-5s rate-limit per 01-04. Page-on-request/page-on-response captures 256 events. Initial 5s wait extended to 20s poll-loop watching for title change off "checking device" + 5s networkidle settle. Commit `0439cb2`.
  - **Loud-flag finding:** 0/7 pages cleared the gate even at 20-25s wait. Deferral gate "≥98/100 + challenge<10%" decisively fails. **Anti-bot vendor identified as GroupIB / F.A.C.C.T.** (Russian-market rebrand of Singapore-based GroupIB) via inline `window.gib.init({cid:'w-goldapple', gafUrl:'ru.id.facct.ru/id.html'})` in challenge HTML — material reorder of escalation tree.
  - **Task 2:** `goldapple-network-trace.md` (human-readable summary: per-page metrics, anti-bot vendor identification, XHR endpoint table including `/web/api/v1/settings` 24/24=403 + telemetry sinks, D-14 ALERT, 5 implications for 01-08, re-run instructions); `goldapple-jsonld-sample.json` is `[]` per D-14 ALERT (challenge shell has no schema.org markup). Commit `42763ca`.
  - **Task 3:** MEMO `## JSON-endpoint hunt verdict (D-09, D-10)` populated — Variant B (Tier 0 not viable, Tier 2+ required) PLUS additional finding that Tier 2 baseline is itself insufficient. 5 explicit implications for 01-08 captured. Commit `a22034a`.
  - 5 files created (1 helper script + 4 sample payloads), 1 modified (`MEMO.md`), 3 deviations (1 blocking-issue resolution = checkpoint substitution; 1 bug-fix = wait-loop logic; 1 artifact-hygiene = byte-equivalent challenge-shell dedup), self-check PASSED.
  - SUMMARY → `.planning/phases/01-goldapple-reconnaissance-spike/01-06-SUMMARY.md`
- Earlier this session: `/gsd-execute-phase 1` plan 01-07 executed (sequential mode, autonomous, 3 tasks; Task 1 substituted by autonomous viled-sitemap probe per user YOLO preference + 01-05 precedent):
  - Task 1 (substituted): `_fetch_viled_urls.py` fetches viled.kz/sitemap.xml via curl_cffi → sitemapindex with 9 sub-sitemaps → 42,294 product URLs (women=22,378 / men=11,845 / kids=8,071 + collection/lookbook/news) under `/item/<numeric_id>` route → step-stride sample of 15 diversified URLs (IDs 148026 oldest → 409206 newest). Sitemap plain-deliverable confirmed (1.2 KB index, 200 OK). Commit `021c354`.
  - Task 2: `notebook-viled.py` replaced 01-01 stub with real feasibility script; ran 15 fetches at 2s pause: **15/15 HTTP 200, avg 485ms (min 300, max 671), 35.3s wall-clock**. Critical finding: **0/15 product pages have JSON-LD** (D-14-style proxy НЕ применим к viled), but **15/15 have `__NEXT_DATA__`** Next.js SSR blob. `_inspect_viled_nextdata.py` extracted Phase 2 PARSE-02 hot-data from 4 cross-category samples (apparel/perfumery/cosmetics/watch): canonical paths `props.pageProps.attributes[0].{price, realPrice, currency}` + `props.pageProps.item.{brandName, name, sizeType}` + nested `attributes[].name=='Размер'` for beauty volume. Currency display `"₸"` (unicode tenge); pricing integer KZT (no decimal). Commit `a7f6d43`.
  - Task 3: MEMO `## viled.kz feasibility (RECON-02)` populated — verdict CONFIRMED (Tier 0), per-URL outcomes, timing, critical findings, Phase 2 schema hot-data table (8 v1-schema fields → __NEXT_DATA__ paths), side-deliverables table (pagination = sitemap-driven, URL pattern, UA strictness, was_price availability via realPrice, pricing format). Commit `28d7ee5`.
  - 7 files created (2 helper scripts + 5 sample payloads), 2 modified (`notebook-viled.py` + `MEMO.md`), 3 deviations (1 blocking-issue resolution = checkpoint substitution; 1 missing-critical = Phase 2 hot-data inspector; 1 artifact-hygiene = strip 1.3 MB intermediate URLs file), self-check PASSED.
  - SUMMARY → `.planning/phases/01-goldapple-reconnaissance-spike/01-07-SUMMARY.md`
- Earlier this session: plan 01-05 executed (sequential mode, autonomous, 3 tasks; Task 1 substituted by autonomous viled probe per user YOLO preference):
  - Task 1 (substituted): `_fetch_viled_brands.py` probes viled.kz/ via curl_cffi → `__NEXT_DATA__` parse → 58 brand entries → discovered viled is luxury fashion + niche perfumery; selected 5 brands (Jo Malone London / Tom Ford / Creed / Frederic Malle / Givenchy). Default plan_context list (Lancôme/La Roche-Posay/Vichy etc) rejected as not-representative.
  - Task 2: goldapple sitemap fetch + per-brand counts — commit `b9cb355`. **Critical empirical finding:** goldapple.kz/sitemap.xml IS plain-deliverable via curl_cffi (HTTP 200, no JS-challenge — sitemap is exempt from anti-bot layer). Sitemapindex with 3 sub-sitemaps; 112,317 URLs total; 100,779 product-numeric URLs (89.7%); 1,461 distinct brand slugs; ~69 products/brand catalog-wide. Per-brand facet counts: Givenchy 40, Tom Ford 33, Frederic Malle 19, Creed 8, Jo Malone London 1.
  - Task 3: MEMO `## Page-volume estimate (RECON-03)` populated — commit `f00d947`. Section includes brand-selection methodology, per-brand table, catalog-wide aggregates, Phase 3 implications (~600 MB/week, ~$2.10 proxy, ~4.4h duration), sitemap-as-enumeration-strategy validation.
  - 9 files created (3 helper scripts + 6 sample payloads), 1 modified (`MEMO.md`), 5 deviations (1 blocking-issue resolution = checkpoint substitution; 1 bug = encoding; 3 artifact-hygiene; 1 critical = JSON shape), self-check PASSED.
  - SUMMARY → `.planning/phases/01-goldapple-reconnaissance-spike/01-05-SUMMARY.md`
- Earlier this session: plan 01-04 executed (sequential mode, autonomous, 2 tasks):
  - Task 1 (snapshot robots.txt): viled (508 B HTTP 200, no Crawl-delay, sitemap declared) + goldapple (7303 B HTTP 200, Magento-style, no Crawl-delay, blocks 38 bots incl. SemrushBot/MJ12bot/BLEXBot/DotBot, sitemap declared) — commit `198f579`
  - Task 2 (ToS audit + committed rate-limits): viled `/privacy` extracted via Next.js `__NEXT_DATA__` (16066 chars, **no anti-scraping clauses**, only KZ Law 94-V personal-data); goldapple — all 11 ToS-slug candidates return identical 18 912-byte JS-challenge shell ("Gold Apple — checking device", DataDome-style UUID JS bundle), text deferred to post-01-08 — commit `83c5150`
  - Committed rate-limits: viled=2s sequential, goldapple=3-5s random uniform concurrency=1
  - 9 files created (4 helper scripts + 5 sample payloads), 1 modified (`tos-audit.md`), 3 deviations (all artifact-hygiene cleanups, zero scope creep), self-check PASSED
  - SUMMARY → `.planning/phases/01-goldapple-reconnaissance-spike/01-04-SUMMARY.md`
- Earlier (this session): plan 01-02 → 4 файла, 2 коммита (`d47b800`, `0b98407`); plan 01-01 (spike skeleton) → 7 файлов, 3 коммита (c2da755, 02e8cf5, 8a2d5c5)
- Earlier (planning): `/gsd-plan-phase 1` создал 12 атомарных плана (01-01..01-12) в 5 волнах; gsd-plan-checker VERIFICATION PASSED

### Earlier (this session)

- Phase 1 discuss session: 4 gray areas обсуждены (Tier escalation & timebox, IP-гео, JSON-endpoint hunt, success criteria)
- Locked-in 16 implementation decisions (D-01..D-16) для recon-спайка
- Создан `.planning/phases/01-goldapple-reconnaissance-spike/01-CONTEXT.md` с canonical refs на research/* и project docs
- Создан `.planning/phases/01-goldapple-reconnaissance-spike/01-DISCUSSION-LOG.md` (audit trail)

### Earlier (initialization)

- Read PROJECT.md and REQUIREMENTS.md
- Read research/SUMMARY.md, STACK.md, FEATURES.md, ARCHITECTURE.md, PITFALLS.md
- Derived 7-phase roadmap aligned with research synthesis (Phase 1 spike, viled-first build, goldapple-second, matcher, reporter, delivery, scheduler)
- Mapped all 48 enumerated v1 requirements to phases (no orphans, no duplicates)
- Wrote ROADMAP.md with phase details and success criteria
- Updated REQUIREMENTS.md traceability section

### What's Next

1. **`/gsd-execute-phase 3 07`** — Wave 6 manual live smoke checkpoint: operator runs `uv run python -m ga_crawler goldapple-smoke --run-id 999` from KZ-laptop direct (no proxy, fresh Camoufox profile per D-311) to empirically verify D-312 smoke probe passes against live goldapple.kz (3 known-good Givenchy URLs return 200 + microdata price extracted). On smoke pass, operator runs limited live run e.g. `uv run python -m ga_crawler goldapple-run --run-id 1 --viled-brands givenchy,tom_ford --sanity-gate-m 50` — exercises orchestrator end-to-end at small scale (~70 fetches × 3-5s ≈ 5min wall-clock), verifying CRAWL-02 success criteria 4 (1-hour live run completes without 429/503 spikes — scaled to 5-min subset) and 5 (NORM-06 review queue populated by real run with NEW goldapple slugs from sitemap). Output: `.planning/runs/1/snapshots.jsonl` + `runs.json` + `sitemap-slugs.txt`. Phase 3 closure gate.
2. **`/gsd-discuss-phase 2`** — Project Skeleton + viled Crawl + Storage. Phase 2 viled stack is hot-data ready from 01-07 (curl_cffi + selectolax + `__NEXT_DATA__` parser; 8 canonical field paths; sitemap-driven enumeration; was_price via realPrice; currency map "₸"→"KZT"). Phase 2 does NOT depend on Phase 3 and can run in parallel — Phase 3 codes against `interfaces.py` Protocols, Phase 2 implementations swap in at Wave 5.
3. **Phase 7 hosting decision (pending one smoke fetch):** Hetzner CX22 EU is the working hypothesis. Before locking, one Camoufox+EU smoke fetch against goldapple. If gate passes → no proxy, $0/week. If gate fails → revive D-08 (IPRoyal KZ residential, ~$2/week steady-state). If both fail → managed unblocker (ZenRows / Bright Data Web Unlocker, pay-per-page; reframes economics per D-02).
4. **Phase 3 ops playbook items (deferred to Phase 7):**
   - Weekly Camoufox-vs-goldapple smoke ("does the gate still pass?") — alert if pass-rate <90% or gate-shell rate >5%
   - Camoufox upstream check (daijro releases vs coryking fork) — switch fork if daijro stalls
   - Stale-SKU 200-but-9.5KB rate tracking — if >5%, sitemap may have stale entries needing prune
5. Open follow-ups (Phase 7 backlog): KZ-legal review (30 min с юристом) with bundle = `tos-audit.md` + `viled-privacy.txt` + both `*-robots.txt` snapshots + GroupIB/F.A.C.C.T. vendor flag + Camoufox-fingerprint-spoof note for legal nuance.

### Resume Instructions

To continue this project from a fresh session:

1. Read `.planning/PROJECT.md` for core value and constraints.
2. Read `.planning/ROADMAP.md` for phase structure.
3. Read this STATE.md for current position.
4. Read `.planning/spikes/01-goldapple/MEMO.md` — signed-off decision memo, source-of-truth for Phase 3 stack constraints.
5. Read `.claude/skills/spike-01-goldapple/SKILL.md` — quick-reference Phase 3 entry-point.
6. Run `/gsd-discuss-phase 2` (viled crawl + storage) OR `/gsd-discuss-phase 3` (goldapple crawl). Phase 2 and Phase 3 are independent — order is operator preference.

---
*State initialized: 2026-05-05 by gsd-roadmapper; updated by gsd-plan-phase 2026-05-05; updated by gsd-executor (plan 01-01) 2026-05-05; updated by gsd-executor (plan 01-02) 2026-05-05; updated by gsd-executor (plan 01-04) 2026-05-05; updated by gsd-executor (plan 01-05) 2026-05-05; updated by gsd-executor (plan 01-07) 2026-05-05; updated by gsd-executor (plan 01-06) 2026-05-06; updated by gsd-executor (plans 01-08, 01-11, 01-12) 2026-05-06 — Phase 1 CLOSED; updated by gsd-executor (plan 03-01) 2026-05-06 — Phase 3 Wave 0 done; updated by gsd-executor (plan 03-02) 2026-05-06 — Phase 3 Wave 1 done; updated by gsd-executor (plan 03-03) 2026-05-06 — Phase 3 Wave 2 done; updated by gsd-executor (plan 03-04) 2026-05-06 — Phase 3 Wave 3 done; updated by gsd-executor (plan 03-05) 2026-05-06 — Phase 3 Wave 4 done; updated by gsd-executor (plan 03-06) 2026-05-06 — Phase 3 Wave 5 done*
