---
phase: 08-parser-bug-fixes
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/spikes/v1.1-brand-name-shapes/MEMO.md
  - .planning/spikes/v1.1-brand-name-shapes/shape-table.md
  - .planning/spikes/v1.1-brand-name-shapes/capture.py
  - tests/fixtures/goldapple/_live-2026-05-13-stereotype.html
  - tests/fixtures/goldapple/_live-2026-05-13-armani-code.html
  - tests/fixtures/viled/_live-2026-05-13-contre-jour.html
  - tests/conftest.py
  - .claude/skills/spike-findings-v1.1-brand-name-shapes/SKILL.md
autonomous: false
requirements: [PARSE-FIX-01, PARSE-FIX-02, PARSE-FIX-03]
must_haves:
  truths:
    - "30 goldapple PDPs captured live via Camoufox 0.4.11, stratified 5x6 (lux/mass-market/niche/RU-brands/multi-word brands) per D-801"
    - "shape-table.md categorizes every PDP into a shape bucket with concrete brand_raw / name_raw / volume_block columns filled"
    - "3 live fixtures committed under tests/fixtures/ in retailer-grouped (NOT date-grouped) directories per CONTEXT.md `<code_context>` Established Patterns append-only convention"
    - "conftest.py exposes 3 session-scoped fixture loaders for the new _live-2026-05-13-* variants, mirroring existing 6-line loader shape"
    - "Project-local skill .claude/skills/spike-findings-v1.1-brand-name-shapes/SKILL.md exists with frontmatter + same H2 structure as spike-01-goldapple"
    - "Output gate satisfied: shape-table.md commitable + 3 fixtures committed + SKILL.md created (CONTEXT.md D-808)"
    - "No PII / cf_clearance / UUID-shaped session tokens in any committed fixture (V12 ASVS L1)"
  artifacts:
    - path: ".planning/spikes/v1.1-brand-name-shapes/MEMO.md"
      provides: "Spike decision memo with TL;DR, bucket-by-bucket counts, fallback decision"
      min_lines: 30
    - path: ".planning/spikes/v1.1-brand-name-shapes/shape-table.md"
      provides: "30 PDP x 8 columns survey table"
      contains: "shape bucket"
    - path: "tests/fixtures/goldapple/_live-2026-05-13-stereotype.html"
      provides: "STEREOTYPE/sago Bug #1+#2 evidence fixture"
    - path: "tests/fixtures/goldapple/_live-2026-05-13-armani-code.html"
      provides: "Armani-code Bug #2 evidence fixture"
    - path: "tests/fixtures/viled/_live-2026-05-13-contre-jour.html"
      provides: "Frederic Malle Contre-Jour Bug #3 legitimate-None evidence"
    - path: ".claude/skills/spike-findings-v1.1-brand-name-shapes/SKILL.md"
      provides: "Project-local skill wrap-up for downstream agents (gsd-planner, gsd-executor)"
      contains: "name: spike-findings-v1.1-brand-name-shapes"
    - path: "tests/conftest.py"
      provides: "3 new session-scoped fixture loaders"
      contains: "goldapple_pdp_html_live_stereotype"
  key_links:
    - from: "tests/conftest.py::goldapple_pdp_html_live_stereotype"
      to: "tests/fixtures/goldapple/_live-2026-05-13-stereotype.html"
      via: "session-scoped pytest fixture loader"
      pattern: "FIXTURES_DIR / \"_live-2026-05-13-stereotype.html\""
    - from: "tests/conftest.py::viled_pdp_html_live_contre_jour"
      to: "tests/fixtures/viled/_live-2026-05-13-contre-jour.html"
      via: "session-scoped pytest fixture loader"
      pattern: "VILED_FIXTURES_DIR / \"_live-2026-05-13-contre-jour.html\""
    - from: ".claude/skills/spike-findings-v1.1-brand-name-shapes/SKILL.md"
      to: ".planning/spikes/v1.1-brand-name-shapes/MEMO.md"
      via: "Obsidian wiki-link"
      pattern: "\\[\\[.planning/spikes/v1.1-brand-name-shapes/MEMO\\]\\]"
---

<objective>
Run the mandatory 30-PDP shape-sampling sub-spike that BLOCKS all of Wave 1 per D-804/D-808. Capture live goldapple PDPs via Camoufox 0.4.11, categorize them into shape buckets (Givenchy-baseline / STEREOTYPE-style / Armani-style / discovered), commit 3 evidence fixtures, and wrap up findings in a project-local skill so Plans 08-02/03/04 can target real DOM shapes instead of guessing from a single STEREOTYPE screenshot.

Purpose: Prevent over-fitting parser fixes to a single user-reported PDP. Pitfall #1 from RESEARCH.md ("parser-fix overfitting") is mitigated by sample-first protocol.

Output: Spike directory + 3 fixtures + SKILL.md + conftest.py extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-parser-bug-fixes/08-CONTEXT.md
@.planning/phases/08-parser-bug-fixes/08-RESEARCH.md
@.planning/phases/08-parser-bug-fixes/08-PATTERNS.md
@.planning/phases/08-parser-bug-fixes/08-VALIDATION.md
@.claude/skills/spike-01-goldapple/SKILL.md
@.planning/spikes/01-goldapple/MEMO.md
@src/ga_crawler/fetchers/goldapple.py
@tests/conftest.py

<interfaces>
<!-- Existing fixture-loader pattern at tests/conftest.py:23-37 (analog) -->
```python
@pytest.fixture(scope="session")
def goldapple_pdp_html() -> str:
    """Real Givenchy PDP captured during spike 01-08 (~200 KB)."""
    return (FIXTURES_DIR / "_debug-product-page.html").read_text(encoding="utf-8")
```

<!-- GoldappleFetcher async-context-manager usage pattern -->
```python
fetcher = GoldappleFetcher(run_id=0)
async with fetcher:
    rec = await fetcher.fetch_one(fetcher._page, url)
    html = rec.get("html") or ""
    status = rec.get("status")
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator curates 30 goldapple PDP URLs across 5 buckets</name>
  <read_first>
    - `.planning/phases/08-parser-bug-fixes/08-CONTEXT.md` lines 31-38 (D-801..D-804 spike spec)
    - `.planning/phases/08-parser-bug-fixes/08-RESEARCH.md` § "W0 Sub-Spike Protocol" (lines 714-829)
    - `.claude/skills/spike-01-goldapple/SKILL.md` (analog spike conventions, rate-limit reference)
  </read_first>
  <what-built>
    Skeleton spike directory at `.planning/spikes/v1.1-brand-name-shapes/` with capture.py script template containing 30 placeholder URL tuples organized into 5 buckets of 6 each.
  </what-built>
  <how-to-verify>
    Operator action (cannot be automated): manually curate 30 goldapple.kz PDP URLs across the 5 stratification buckets defined in D-801:

    1. **Lux (6 URLs):** Tom Ford, Creed, Frederic Malle, Maison Margiela, Chanel, Dior — high-margin SKUs likely to exhibit STEREOTYPE-style uppercase brand UI
    2. **Mass-market (6 URLs):** Givenchy, YSL, Versace, Calvin Klein, Hugo Boss, Armani — largest viled overlap; expected to exhibit Armani-style mixed-case concat
    3. **Niche (6 URLs):** STEREOTYPE/sago confirmed; plus Byredo, Le Labo, Maison Crivelli, Profumum Roma, Memo
    4. **RU-brands (6 URLs):** Натура Сибирика, Чистая Линия, Black Pearl, Лошадиная Сила, Невская Косметика, Сибирские Травы — tests Cyrillic-uppercase brand text against Lexbor case-insensitive flag
    5. **Multi-word brands (6 URLs):** Tom Ford, Jo Malone, Atelier Cologne, Maison Margiela, By Kilian, Diptyque — tests `_strip_brand_prefix` if microdata coverage <95%

    Source URLs from goldapple.kz catalog browse, sitemap (`goldapple-sitemap.xml`), or manual category navigation. Each URL MUST match `PRODUCT_URL_RE` from `src/ga_crawler/enumeration/goldapple_sitemap.py`.

    Fill `URLS: list[tuple[str, str]]` in `.planning/spikes/v1.1-brand-name-shapes/capture.py` with `(slug-tag, full-URL)` pairs. Verify `assert len(URLS) == 30` holds.
  </how-to-verify>
  <resume-signal>Type "approved" once 30 URLs are filled in capture.py, or describe blockers (e.g. "cannot find 6 RU-brands on goldapple.kz today — fewer available than expected — propose substitution from {bucket}")</resume-signal>
  <acceptance_criteria>
    - File `.planning/spikes/v1.1-brand-name-shapes/capture.py` exists
    - `python -c "exec(open('.planning/spikes/v1.1-brand-name-shapes/capture.py').read()); print(len(URLS))"` outputs `30`
    - `python -c "import re; from src.ga_crawler.enumeration.goldapple_sitemap import PRODUCT_URL_RE; exec(open('.planning/spikes/v1.1-brand-name-shapes/capture.py').read()); assert all(PRODUCT_URL_RE.match(u) for _, u in URLS), 'invalid URL'"` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build capture.py script reusing GoldappleFetcher verbatim</name>
  <read_first>
    - `src/ga_crawler/fetchers/goldapple.py` (entire `GoldappleFetcher` class — verbatim consumer)
    - `.planning/phases/08-parser-bug-fixes/08-PATTERNS.md` § "scripts/capture_spike_pdps.py" (capture-script template, lines 1002-1050)
    - `.planning/phases/08-parser-bug-fixes/08-RESEARCH.md` § "Capture Script Outline" (lines 722-771)
    - `tests/integration/test_run_e2e_with_phase2_mocks.py` lines 28-60 (analog: existing fetcher-consumer pattern)
  </read_first>
  <action>
    Write `.planning/spikes/v1.1-brand-name-shapes/capture.py` per the verbatim template from PATTERNS.md. Use the `URLS` tuple list populated in Task 1.

    The script MUST:
    - Use `from ga_crawler.fetchers.goldapple import GoldappleFetcher` — do NOT re-implement Camoufox launch
    - Use `async with fetcher:` context manager (rate-limit 3-5s already enforced by `fetch_one` per Phase 3 D-307)
    - Write each captured HTML to `pdp-NN-{slug}.html` (zero-padded NN, 01-30) into `.planning/spikes/v1.1-brand-name-shapes/`
    - Log `status` and `len(html)` per fetch so the operator sees Cloudflare interstitials immediately
    - Total wall-clock budget: ~3 min fetch + 10-15 min write-up = ~30-60 min per RESEARCH.md

    Per CONTEXT.md "Reusable Assets": Camoufox 0.4.11 is LOCKED per Phase 3 D-313 — do NOT propose Patchright or Playwright alternative. Per CLAUDE.md tech-stack lock-ins, no alternatives.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('.planning/spikes/v1.1-brand-name-shapes/capture.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/spikes/v1.1-brand-name-shapes/capture.py` exists and parses as valid Python
    - `grep -q 'from ga_crawler.fetchers.goldapple import GoldappleFetcher' .planning/spikes/v1.1-brand-name-shapes/capture.py`
    - `grep -q 'async with fetcher' .planning/spikes/v1.1-brand-name-shapes/capture.py`
    - `grep -q 'pdp-' .planning/spikes/v1.1-brand-name-shapes/capture.py`
    - Script does NOT introduce new Camoufox launch code (must reuse `GoldappleFetcher`):
      - `grep -c 'from camoufox' .planning/spikes/v1.1-brand-name-shapes/capture.py` returns `0`
  </acceptance_criteria>
  <done>Capture script written, imports GoldappleFetcher verbatim, produces 30 zero-padded pdp-NN-*.html files when run.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator runs capture.py and fills shape-table.md</name>
  <read_first>
    - `.planning/spikes/v1.1-brand-name-shapes/capture.py` (output from Task 2)
    - `.planning/phases/08-parser-bug-fixes/08-RESEARCH.md` § "Output Format" — `shape-table.md` template (lines 798-829)
    - `.planning/research/v1.1-PARSER-BUG-FINDINGS.md` (STEREOTYPE/Armani evidence — informs column values)
  </read_first>
  <what-built>
    Empty `shape-table.md` skeleton with the 8-column survey table header (per RESEARCH.md template) and 30 empty data rows numbered 01-30.
  </what-built>
  <how-to-verify>
    Operator action (cannot be automated):

    1. Run `uv run python .planning/spikes/v1.1-brand-name-shapes/capture.py` — produces 30 `pdp-NN-{slug}.html` files
    2. For each PDP, open the HTML and extract:
       - `brand_raw` from `<meta itemprop="name">` inside `[itemprop="brand"]` (look for the content attribute)
       - `brand_displayed_in_h1` from `<h1>` text (verbatim, including any CSS-uppercase rendering)
       - `name_raw` from product-level `<meta itemprop="name">` (sibling of `[itemprop="brand"]`, NOT inside it)
       - `<h1>` text verbatim
       - Volume block present? (search HTML for `ОБЪЁМ` literal — note: confirm Ё U+0401 vs Е U+0415 per RESEARCH.md Pitfall 2)
       - Volume label text exact byte sequence (e.g. `ОБЪЁМ / МЛ` or `ОБЪЕМ / МЛ`)
       - Shape bucket: one of `Givenchy-baseline` / `STEREOTYPE-style` / `Armani-style` / `(discovered: name)`
    3. Fill all 30 rows in `shape-table.md`
    4. Add summary section: "Shape Buckets Identified" with N=X count per bucket
    5. Add "Selector Validation" section with: lex-contains match rate (X/30); `<meta itemprop="name">` coverage (X/30); fallback needed Y/N (per D-806: <95% threshold)
    6. Add "Selected Fixture URLs for v1.1 Plans" section identifying:
       - Which PDP # becomes `_live-2026-05-13-stereotype.html`
       - Which PDP # becomes `_live-2026-05-13-armani-code.html`
       - SMOKE_URLs rotation slot 1 (STEREOTYPE-style URL)
       - SMOKE_URLs rotation slot 2 (Armani-style URL)
  </how-to-verify>
  <resume-signal>Type "approved" once shape-table.md is filled for all 30 PDPs and SMOKE rotation URLs identified, or describe blockers (e.g. "only 27/30 PDPs returned non-error HTML — request approval to substitute 3 from {bucket}")</resume-signal>
  <acceptance_criteria>
    - File `.planning/spikes/v1.1-brand-name-shapes/shape-table.md` exists
    - 30 data rows present (excluding header): `grep -E '^\| [0-9]{2} \|' .planning/spikes/v1.1-brand-name-shapes/shape-table.md | wc -l` returns `30`
    - "Shape Buckets Identified" section exists: `grep -q '^## Shape Buckets Identified' .planning/spikes/v1.1-brand-name-shapes/shape-table.md`
    - "Selected Fixture URLs for v1.1 Plans" section exists: `grep -q '^## Selected Fixture URLs' .planning/spikes/v1.1-brand-name-shapes/shape-table.md`
    - Captured PDP files exist: `ls .planning/spikes/v1.1-brand-name-shapes/pdp-*.html | wc -l` returns `30`
    - **SMOKE rotation coverage (required for Plan 08-05 Task 2 Part C):** "Selected Fixture URLs" section MUST identify at least 1 STEREOTYPE-style URL AND at least 1 Armani-style URL, OR explicitly document why such a URL does not exist in goldapple.kz (e.g., "STEREOTYPE bucket only found 0/30 PDPs — operator approves single-shape rotation with Givenchy-baseline ×2 + STEREOTYPE-substitute ×1 from sitemap fallback"). Plan 08-05 RED test asserting `len({slug for url in SMOKE_URLS for slug in [url.rsplit('/',1)[-1]]}) == 3` (3 distinct slugs) depends on this gate.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Curate + commit 3 evidence fixtures with PII canary scan</name>
  <read_first>
    - `.planning/spikes/v1.1-brand-name-shapes/shape-table.md` (output from Task 3 — identifies which PDP # maps to which fixture name)
    - `.planning/phases/08-parser-bug-fixes/08-RESEARCH.md` § "Fixture-Capture Pattern" (lines 917-991)
    - `.planning/phases/08-parser-bug-fixes/08-PATTERNS.md` § "tests/fixtures/...html" (lines 902-913 — placement rules)
    - RESEARCH.md § "Pitfall 7: Fixture commit safety" (lines 1139-1143)
    - existing `tests/fixtures/goldapple/_debug-product-page.html` (size + shape reference)
    - existing `tests/fixtures/viled/viled-pdp-discounted.html` (size + shape reference)
  </read_first>
  <action>
    Per CONTEXT.md D-808 "Established Patterns" — append-only, retailer-grouped (NOT `tests/fixtures/live/`).

    Copy 3 PDPs from `.planning/spikes/v1.1-brand-name-shapes/` into their committed test-fixture locations:
    1. **Goldapple STEREOTYPE PDP** (per shape-table Selected Fixture URLs §):
       `cp .planning/spikes/v1.1-brand-name-shapes/pdp-{NN}-{slug}.html tests/fixtures/goldapple/_live-2026-05-13-stereotype.html`
    2. **Goldapple Armani PDP** (per shape-table):
       `cp .planning/spikes/v1.1-brand-name-shapes/pdp-{NN}-{slug}.html tests/fixtures/goldapple/_live-2026-05-13-armani-code.html`
    3. **Viled Frederic Malle Contre-Jour PDP** — this is a separate viled fetch (NOT in the 30-PDP goldapple capture). Fetch the Contre-Jour PDP from viled.kz via `curl_cffi` (existing viled fetcher pattern in `src/ga_crawler/fetchers/viled.py`) or operator browser-save:
       `tests/fixtures/viled/_live-2026-05-13-contre-jour.html`

    **PII canary scan (per V12 ASVS L1 + RESEARCH.md Pitfall 7):**
    Before `git add`, scan each fixture for forbidden patterns:
    ```
    grep -lE '(cf_clearance=|bot[0-9]+:|Bearer |eyJ[A-Za-z0-9_-]{20,}|[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4})' tests/fixtures/goldapple/_live-2026-05-13-*.html tests/fixtures/viled/_live-2026-05-13-*.html
    ```
    If grep matches → ABORT, redact or recapture. If no matches → proceed.

    Naming convention `_live-YYYY-MM-DD-{slug}.html` MUST use leading underscore (matches `_debug-product-page.html` convention).
  </action>
  <verify>
    <automated>test -f tests/fixtures/goldapple/_live-2026-05-13-stereotype.html && test -f tests/fixtures/goldapple/_live-2026-05-13-armani-code.html && test -f tests/fixtures/viled/_live-2026-05-13-contre-jour.html && ! grep -lE '(cf_clearance=|bot[0-9]+:|Bearer |eyJ[A-Za-z0-9_-]{20,})' tests/fixtures/goldapple/_live-2026-05-13-*.html tests/fixtures/viled/_live-2026-05-13-*.html</automated>
  </verify>
  <acceptance_criteria>
    - All 3 fixtures exist (test -f for each path above)
    - PII canary grep returns no matches (exit 1 from `grep -l` means no matches found)
    - Each goldapple fixture >50 KB: `wc -c tests/fixtures/goldapple/_live-2026-05-13-stereotype.html | awk '{exit !($1 > 50000)}'` exits 0
    - Each viled fixture >30 KB: same shape for viled fixture
    - Fixtures contain product structure: `grep -q 'itemprop="brand"' tests/fixtures/goldapple/_live-2026-05-13-stereotype.html`
  </acceptance_criteria>
  <done>3 fixtures committed under correct retailer directories; PII canary clean; structure validation passes.</done>
</task>

<task type="auto">
  <name>Task 5: Extend tests/conftest.py with 3 new fixture loaders</name>
  <read_first>
    - `tests/conftest.py` lines 23-37 (existing `goldapple_pdp_html` fixture loader analog) and lines 164-189 (existing viled trio)
    - `.planning/phases/08-parser-bug-fixes/08-PATTERNS.md` § "tests/conftest.py (extension)" (lines 847-899 — verbatim insertion templates)
    - `.planning/phases/08-parser-bug-fixes/08-RESEARCH.md` § "conftest.py Extension" (lines 940-984)
  </read_first>
  <action>
    Extend `tests/conftest.py` with 3 new session-scoped fixture loaders. Insert AFTER line 91 (`jsonld_blocks_anti_fixture` — last goldapple fixture before Phase-2 contract-mocks section) for the 2 goldapple loaders, and AFTER line 189 (`viled_pdp_multipack_html`) for the viled loader.

    Use the verbatim insertion templates from PATTERNS.md lines 866-899:
    - `goldapple_pdp_html_live_stereotype()` reading `FIXTURES_DIR / "_live-2026-05-13-stereotype.html"`
    - `goldapple_pdp_html_live_armani()` reading `FIXTURES_DIR / "_live-2026-05-13-armani-code.html"`
    - `viled_pdp_html_live_contre_jour()` reading `VILED_FIXTURES_DIR / "_live-2026-05-13-contre-jour.html"`

    Each fixture is `@pytest.fixture(scope="session")` with `-> str` return type. Docstrings cite source (Plan 08-01 W0 spike) per existing convention.

    Do NOT touch any existing fixture or import — additive only.
  </action>
  <verify>
    <automated>uv run python -c "from tests.conftest import goldapple_pdp_html_live_stereotype, goldapple_pdp_html_live_armani, viled_pdp_html_live_contre_jour"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def goldapple_pdp_html_live_stereotype' tests/conftest.py`
    - `grep -q 'def goldapple_pdp_html_live_armani' tests/conftest.py`
    - `grep -q 'def viled_pdp_html_live_contre_jour' tests/conftest.py`
    - All 3 are `@pytest.fixture(scope="session")`: `grep -B1 'def goldapple_pdp_html_live_stereotype' tests/conftest.py | grep -q 'scope="session"'`
    - Existing 803 tests still pass (no fixture-loading regression): `uv run pytest -m "not live" -q --collect-only 2>&1 | tail -1 | grep -qE '[0-9]+ tests collected'`
  </acceptance_criteria>
  <done>3 fixture loaders added; module imports clean; pytest collection unchanged in count.</done>
</task>

<task type="auto">
  <name>Task 6: Write MEMO.md spike decision memo</name>
  <read_first>
    - `.planning/spikes/v1.1-brand-name-shapes/shape-table.md` (output from Task 3)
    - `.planning/spikes/01-goldapple/MEMO.md` (verbatim shape analog — frontmatter + TL;DR + Options-tested table)
    - `.planning/phases/08-parser-bug-fixes/08-RESEARCH.md` § "MEMO.md template" (lines 833-862)
    - `.planning/phases/08-parser-bug-fixes/08-PATTERNS.md` § "MEMO.md (spike artifact, new)" (lines 945-992)
  </read_first>
  <action>
    Write `.planning/spikes/v1.1-brand-name-shapes/MEMO.md` per RESEARCH.md template (lines 833-862) mirroring the Phase 1 spike MEMO shape.

    Required H2 sections (in this order):
    1. `## TL;DR` — 1-paragraph verdict: selector validated / fallback needed / new shape discovered + Lex-contains match rate + microdata coverage rate + fallback decision
    2. `## Buckets (5 × 6 stratification per D-801)` — 5-row table with bucket / URLs sampled / lex-contains match rate / microdata-name presence / shape variant
    3. `## What works` — concrete selector evidence (verbatim CSS strings + match counts)
    4. `## What doesn't` — edge cases flagged for v1.2 (e.g. JSON-encoded volume in some PDPs)
    5. `## Decisions` — `_extract_volume_block` parent-iter strategy confirmed/refined; `_strip_brand_prefix` fallback needed Y/N; final SMOKE_URLs rotation selection
    6. `## Handoff` — list of 3 fixtures committed + skill wrap-up location

    Frontmatter (top of file) must include:
    - `**Completed:** 2026-05-13`
    - `**Spike start:** 2026-05-13`
    - `**Spike end:** 2026-05-13`
    - `**Fetched:** 30 PDPs across 5 stratification buckets via Camoufox 0.4.11 from {operator-location}`

    Per Obsidian vault convention from user MEMORY.md (`feedback_obsidian_vault_style.md`): assertion-style filename, wiki-links to related notes.
  </action>
  <verify>
    <automated>test -f .planning/spikes/v1.1-brand-name-shapes/MEMO.md && grep -q '^## TL;DR' .planning/spikes/v1.1-brand-name-shapes/MEMO.md && grep -q '^## Decisions' .planning/spikes/v1.1-brand-name-shapes/MEMO.md && grep -q '^## Handoff' .planning/spikes/v1.1-brand-name-shapes/MEMO.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/spikes/v1.1-brand-name-shapes/MEMO.md`
    - All 6 required H2 sections present: `for h in 'TL;DR' 'Buckets' 'What works' 'What doesn' 'Decisions' 'Handoff'; do grep -q "^## .*$h" .planning/spikes/v1.1-brand-name-shapes/MEMO.md || exit 1; done`
    - Frontmatter Completed line present: `grep -q '\*\*Completed:\*\* 2026-05-13' .planning/spikes/v1.1-brand-name-shapes/MEMO.md`
    - Min lines: `wc -l .planning/spikes/v1.1-brand-name-shapes/MEMO.md | awk '{exit !($1 >= 30)}'` exits 0
  </acceptance_criteria>
  <done>MEMO.md authored with all required sections; verdict captured; downstream plans can reference it.</done>
</task>

<task type="auto">
  <name>Task 7: Wrap up findings in project-local skill</name>
  <read_first>
    - `.claude/skills/spike-01-goldapple/SKILL.md` (full file — VERBATIM structural analog)
    - `.planning/phases/08-parser-bug-fixes/08-RESEARCH.md` § "Project-Local Skill Wrap-Up" template (lines 866-899)
    - `.planning/phases/08-parser-bug-fixes/08-PATTERNS.md` § "spike-findings-v1.1-brand-name-shapes/SKILL.md" (lines 1054-1096)
    - `.planning/spikes/v1.1-brand-name-shapes/MEMO.md` (just-written, source of TL;DR text)
  </read_first>
  <action>
    Create `.claude/skills/spike-findings-v1.1-brand-name-shapes/SKILL.md` mirroring the verbatim structure of `.claude/skills/spike-01-goldapple/SKILL.md`.

    YAML frontmatter (REQUIRED — discoverable via `Skill` MCP tool):
    ```yaml
    ---
    name: spike-findings-v1.1-brand-name-shapes
    description: Phase 8 W0 spike findings — goldapple PDP brand/name/volume shape buckets. Use when planning Phase 8 W1 plans (08-02/03), Phase 9 brand-coverage canary, or any future parser-drift investigation.
    ---
    ```

    Required H2 sections (mirror spike-01-goldapple/SKILL.md):
    1. `## What was decided` — TL;DR verbatim from MEMO.md
    2. `## Shape buckets identified` — 4-row table (Givenchy-baseline / STEREOTYPE-style / Armani-style / discovered) with frequency + selector strategy
    3. `## Operational constants` — `<meta itemprop="name">` coverage Y%; `:lexbor-contains("ОБЪЁМ" i)` match rate; fallback NEEDED/NOT NEEDED
    4. `## When to consult this skill` — list at minimum: `/gsd-plan-phase 8`, `/gsd-discuss-phase 9`, "did v1.1 W0 cover bucket X?" questions
    5. `## Critical files` — Obsidian wiki-links to MEMO, shape-table, 3 fixture paths

    Per D-803: the skill is the system-discoverable index that lets downstream agents (gsd-planner, gsd-executor in Wave 1+2) see findings WITHOUT reading the full MEMO/table.
  </action>
  <verify>
    <automated>test -f .claude/skills/spike-findings-v1.1-brand-name-shapes/SKILL.md && head -5 .claude/skills/spike-findings-v1.1-brand-name-shapes/SKILL.md | grep -q '^name: spike-findings-v1.1-brand-name-shapes'</automated>
  </verify>
  <acceptance_criteria>
    - File exists at correct path: `test -f .claude/skills/spike-findings-v1.1-brand-name-shapes/SKILL.md`
    - YAML frontmatter `name:` line present: `grep -q '^name: spike-findings-v1.1-brand-name-shapes' .claude/skills/spike-findings-v1.1-brand-name-shapes/SKILL.md`
    - YAML frontmatter `description:` present and includes "Phase 8 W0": `grep -q '^description:.*Phase 8 W0' .claude/skills/spike-findings-v1.1-brand-name-shapes/SKILL.md`
    - 5 required H2 sections: `for h in 'What was decided' 'Shape buckets identified' 'Operational constants' 'When to consult' 'Critical files'; do grep -q "^## .*$h" .claude/skills/spike-findings-v1.1-brand-name-shapes/SKILL.md || exit 1; done`
    - Obsidian wiki-link to MEMO present: `grep -q '\[\[.planning/spikes/v1.1-brand-name-shapes/MEMO\]\]' .claude/skills/spike-findings-v1.1-brand-name-shapes/SKILL.md`
  </acceptance_criteria>
  <done>Skill committed at `.claude/skills/spike-findings-v1.1-brand-name-shapes/SKILL.md`; downstream agents can discover findings via skill registry.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: Output gate review — verify W0 unblocks W1</name>
  <what-built>
    All W0 artifacts:
    - `.planning/spikes/v1.1-brand-name-shapes/MEMO.md`
    - `.planning/spikes/v1.1-brand-name-shapes/shape-table.md`
    - `.planning/spikes/v1.1-brand-name-shapes/capture.py`
    - `.planning/spikes/v1.1-brand-name-shapes/pdp-NN-*.html` (×30)
    - `tests/fixtures/goldapple/_live-2026-05-13-stereotype.html`
    - `tests/fixtures/goldapple/_live-2026-05-13-armani-code.html`
    - `tests/fixtures/viled/_live-2026-05-13-contre-jour.html`
    - `tests/conftest.py` (3 new fixture loaders)
    - `.claude/skills/spike-findings-v1.1-brand-name-shapes/SKILL.md`
  </what-built>
  <how-to-verify>
    Operator visual review per CONTEXT.md D-808 output gate + RESEARCH.md Pitfall 10:

    1. Open all 3 `_live-2026-05-13-*.html` fixtures in a browser. Confirm:
       - Real product PDPs render (not Cloudflare interstitial, not 403 error page)
       - File size >50 KB for goldapple, >30 KB for viled (rough sanity)
       - `<meta itemprop="name">` and product microdata structure present in HTML source

    2. Spot-check 5 random rows in `shape-table.md` against the corresponding `pdp-NN-*.html` files — verify the categorization (brand_raw, shape_bucket) matches what the HTML actually contains

    3. Confirm `MEMO.md "## Decisions"` section makes a clear `_strip_brand_prefix` Y/N call AND identifies the 2 SMOKE_URLs rotation URLs (slot 1 STEREOTYPE-style + slot 2 Armani-style)

    4. Confirm `.claude/skills/spike-findings-v1.1-brand-name-shapes/SKILL.md` shows up via skill registry: `ls .claude/skills/` should list both `spike-01-goldapple/` and `spike-findings-v1.1-brand-name-shapes/`

    5. Verify W1 can start: Plans 08-02 / 08-03 / 08-04 will reference these fixtures by name — opening each plan should not surface "fixture not found" issues
  </how-to-verify>
  <resume-signal>Type "approved" once all 4 visual checks pass. If MEMO.md decisions section is ambiguous (e.g. "fallback NEEDED?" left as TBD), describe what's missing — Wave 1 cannot proceed until Plan 08-03 has a clear microdata-coverage answer.</resume-signal>
  <acceptance_criteria>
    - All required files present: `for f in .planning/spikes/v1.1-brand-name-shapes/{MEMO.md,shape-table.md} tests/fixtures/goldapple/_live-2026-05-13-stereotype.html tests/fixtures/goldapple/_live-2026-05-13-armani-code.html tests/fixtures/viled/_live-2026-05-13-contre-jour.html .claude/skills/spike-findings-v1.1-brand-name-shapes/SKILL.md; do test -f $f || exit 1; done`
    - 30 PDP captures present: `ls .planning/spikes/v1.1-brand-name-shapes/pdp-*.html | wc -l` returns `30`
    - Existing test suite still green: `uv run pytest -m "not live" -q 2>&1 | tail -1 | grep -qE 'passed'`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| goldapple.kz/viled.kz → repo | Untrusted HTML crosses here; will be committed verbatim as fixtures |
| local filesystem → git index | Fixture files become permanent repo artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | `tests/fixtures/**/_live-2026-05-13-*.html` (PII / cf_clearance / session tokens) | mitigate | Task 4 includes PII canary grep for `cf_clearance=`, `bot\d+:`, `Bearer `, `eyJ` JWT prefix, UUID-shaped hex; ABORT-and-redact policy if any match. ASVS V12.1.1 (file content review). |
| T-08-02 | Denial of Service | `capture.py` rate-limit bypass against goldapple.kz | accept | Camoufox 0.4.11 `fetch_one` enforces Phase 3 D-307 rate-limit (3-5s random uniform); operator runs 1× sequential 30-PDP capture; aggregate 30 requests at 4s avg = 2 min total — well within polite-crawl norms. |
| T-08-03 | Tampering | Forged `<meta itemprop="name">` content from goldapple.kz | accept | Goldapple is a trusted commercial retailer; injection of brand misinformation has no business benefit. The PARSE-FIX-02 invariant canary (Plan 08-03) catches per-SKU regressions. |
| T-08-04 | Information Disclosure | `capture.py` URL list contains operator's IP or session cookies | mitigate | URLs are public product pages from goldapple.kz catalog; no cookies / auth required. Operator visually scans capture.py before commit to confirm no `?token=` query params. |
</threat_model>

<verification>
- All 30 PDP captures present in `.planning/spikes/v1.1-brand-name-shapes/`
- `shape-table.md` filled with 30 categorized rows (operator-verified per Task 3)
- 3 evidence fixtures committed and pass PII canary
- `tests/conftest.py` exposes 3 new fixture loaders, importable cleanly
- `.claude/skills/spike-findings-v1.1-brand-name-shapes/SKILL.md` discoverable via skill registry
- `uv run pytest -m "not live" -q` still shows ≥803 tests passing (no regression)
- Operator approves W0 output gate (Task 8 checkpoint)
</verification>

<success_criteria>
- W0 spike output gate per CONTEXT.md D-808 satisfied:
  - shape-table.md commitable ✓
  - 3 fixtures committed ✓
  - SKILL.md created ✓
- W1 unblocked: Plans 08-02 / 08-03 / 08-04 can target real DOM shapes
- No regression: existing 803 tests still green
- No PII leaks in fixtures (V12 ASVS L1)
- Contributes to Phase 8 Success Criteria #1 (enables matched-pair production) — does not satisfy it directly; W1 + W2 do
</success_criteria>

<output>
After completion, create `.planning/phases/08-parser-bug-fixes/08-01-SUMMARY.md` documenting:
- Number of PDPs successfully captured (30 or operator-substituted count)
- Shape buckets identified with frequencies
- `_strip_brand_prefix` decision (NEEDED / NOT NEEDED)
- 2 SMOKE_URLs rotation slot selections
- Any unexpected shape variants discovered (flag for v1.2)
- Test count delta (should be 803 → 803; no new tests in W0, only fixtures + loaders)
</output>
