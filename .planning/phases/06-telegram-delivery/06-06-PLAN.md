---
phase: 06-telegram-delivery
plan: 06
type: execute
wave: 5
depends_on: [06-05]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/phases/06-telegram-delivery/06-CONTEXT.md  # B5 FIX — D-603 formula drift
autonomous: true
requirements: [DELIVER-01, DELIVER-02, DELIVER-03, DELIVER-04, DELIVER-05]
requirements_addressed: [DELIVER-01, DELIVER-02, DELIVER-03, DELIVER-04, DELIVER-05]
must_haves:
  truths:
    - "B5 FIX — CONTEXT.md D-603 updated from `wait_exponential(multiplier=5, min=5, max=45)` to `wait_chain(wait_fixed(5), wait_fixed(15), wait_fixed(45))` (RESEARCH caveat #2 — wait_exponential gives 10/20, not 5/15/45). Cross-link to 06-RESEARCH §Library Verification caveat #2 added."
    - "REQUIREMENTS.md DELIVER-01..05 marked [x] with per-plan citation (06-CONTEXT.md D-601..D-616 references)"
    - "REQUIREMENTS.md Traceability table updated: all 5 DELIVER-* entries → 'Done' with plan IDs (Plan 06-02..06-05)"
    - "STATE.md §Accumulated Key Decisions appended with D-605 (delivery_status decoupled from runs.status), D-606 (6-value enum), D-607 (8-key namespace + 5-way disjoint)"
    - "ROADMAP.md Phase 6 plan list filled (6 plans across 6 waves Wave 0..5)"
    - "ROADMAP.md Progress table Phase 6 → Complete 2026-05-12 (or actual date)"
    - "Coverage block updated: 37/48 → 42/48 v1 requirements satisfied (Phase 6 closes 5 of remaining 11)"
    - "Phase 7 SCHED-01..05 remain Pending — Phase 6 unblocks Phase 7 (cron + healthchecks)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "DELIVER-01..05 closed; Traceability table updated"
      contains: "DELIVER-01"
    - path: ".planning/STATE.md"
      provides: "Accumulated Key Decisions extended with D-605/D-606/D-607 cascade rows for Phase 7 inheritance"
      contains: "D-605"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6 plan list filled + Progress table updated"
      contains: "06-06-PLAN.md"
  key_links:
    - from: "REQUIREMENTS.md DELIVER-* closure annotations"
      to: "06-CONTEXT.md decisions D-601..D-616"
      via: "citations in each [x] DELIVER-NN row"
      pattern: "Plan 05-06 doc cascade mirror"
    - from: "STATE.md §Accumulated Key Decisions §D-605"
      to: "Phase 7 Healthchecks integration (SCHED-03)"
      via: "doc cascade — Phase 7 planner reads STATE.md for inheritance"
      pattern: "Plan 04-06 D-405 cascade precedent"
---

<objective>
Wave 5 of Phase 6: финальный doc cascade. После Wave 4 фаза функционально готова — production code shipped, integration tests green. Plan 06-06 закрывает фазу в документации: помечает REQUIREMENTS.md DELIVER-01..05 как closed (with citations), записывает в STATE.md новые накопленные decisions для Phase 7 inheritance (D-605/D-606/D-607), заполняет ROADMAP.md Phase 6 plan list и обновляет Progress table.

Purpose: Без doc cascade Phase 7 planner не получит правильный контекст наследования (D-605 health-probe semantics, D-606 enum values, D-607 namespace shape). Также REQUIREMENTS.md — единственный source-of-truth для overall project completion progress (37/48 → 42/48 v1).

Output: 3 файла модифицированы (no code). Никаких новых тестов — это документная фаза.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-telegram-delivery/06-CONTEXT.md
@.planning/phases/06-telegram-delivery/06-01-PLAN.md
@.planning/phases/06-telegram-delivery/06-02-PLAN.md
@.planning/phases/06-telegram-delivery/06-03-PLAN.md
@.planning/phases/06-telegram-delivery/06-04-PLAN.md
@.planning/phases/06-telegram-delivery/06-05-PLAN.md

<interfaces>
<!-- Doc cascade target sites. -->

REQUIREMENTS.md §Deliver block (current `- [ ]` rows for DELIVER-01..05) — convert to `- [x]` with closure annotations matching Plan 05-06 style.

REQUIREMENTS.md §Traceability table 5 DELIVER-* rows — replace Pending → Done with plan citations.

ROADMAP.md Phase 6 details — current "Plans: TBD" replaced with 6-row plan list (Wave 0..5).

ROADMAP.md Progress table Phase 6 row — `0/0 | Not started | -` → `6/6 | Complete | 2026-05-12`.

STATE.md §Accumulated Key Decisions — append 3 rows (D-605/D-606/D-607).

STATE.md §Plan Execution Metrics — append 6 rows for Plan 06-01..06-06.

STATE.md §Current Position — update phase status to COMPLETE.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md — close DELIVER-01..05 with citations + Traceability table + Coverage bump</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` §Deliver block (DELIVER-01..05 `- [ ]` rows ~lines 67-73)
    - `.planning/REQUIREMENTS.md` §Traceability table DELIVER-* rows ~lines 174-178
    - `.planning/REQUIREMENTS.md` §Coverage block ~lines 185-189
    - `.planning/REQUIREMENTS.md` Phase 5 closure style at REPORT-01..06 (~lines 60-66) — VERBOSE annotation pattern with plan citations
    - `.planning/phases/06-telegram-delivery/06-CONTEXT.md` §Action Items (REQUIREMENTS.md guidance)
    - Plan 06-01..06-05 frontmatter `must_haves` to identify which files closed which DELIVER-*
  </read_first>
  <action>
    **Edit 1 — Replace DELIVER-* rows** in REQUIREMENTS.md §Deliver. Use verbose closure style mirroring Plan 05-06 REPORT-* closures. Five replacements (each `- [ ]` → `- [x]` with citation paragraph):

    ```markdown
    - [x] **DELIVER-01**: Telegram-бот отправляет business-чат: текстовая сводка + xlsx-вложение через `send_document` — Plan 06-02 ships `delivery/message_builder.py::business_caption` (D-514 verbatim consumption of `runs.stats.report.summary_text`); Plan 06-03 ships `delivery/telegram_client.py::send_document_with_policy` with FSInputFile(Path) per RESEARCH §3; Plan 06-04 orchestrator wires `_send_async` business-route branch with caption-split fallback (Claude's Discretion: summary_text > 1024 chars → `send_message` + `send_document(caption="См. сводку выше")`). aiogram 3.27 locked per D-601 + CLAUDE.md §Telegram Delivery. `async with Bot()` lifecycle prevents unclosed-session warnings (Pitfall B, RESEARCH §2).
    - [x] **DELIVER-02**: Отдельный ops-чат получает уведомления о падениях, sanity-gate failures, отсутствующих ENV-переменных — Plan 06-02 ships `delivery/message_builder.py::build_ops_alert` (D-610 single-template with reason-field + REASON_SHORT mapping + html.escape per Pitfall A + Asia/Almaty tz per Pitfall E + 3500-char traceback truncation per D-614); golden-file `tests/fixtures/delivery/ops-alert-templates.txt` source-locks 5 scenarios. Plan 06-04 orchestrator routes ops-only via `_send_async` ops branch. parse_mode=HTML (D-609) — escape only `<>&`, NOT MarkdownV2's 16-char escape surface.
    - [x] **DELIVER-03**: Pre-send sanity-gate — если `runs.status != 'success'`, в business-чат ничего не отправляется, в ops-чат идёт алерт — Plan 06-03 ships `delivery/gate.py::evaluate_gate` composing 4 checks (D-604, first-fail-wins): runs.status=='success' (REUSE `matcher.strict_key.read_run_status` D-411 helper); `report.xlsx_path` non-empty; `report.size_guard_passed==True` (D-515 cascade from Phase 5); `report.summary_text` non-empty after strip. `GateDecision` frozen dataclass with route + gate_failed_check + gate_failure_reason. Plan 06-04 orchestrator gates routing; Plan 06-05 E2E test SC#2 (deliberate-failure → ops_only) pins behavior.
    - [x] **DELIVER-04**: Retry с учётом Telegram rate-limit (`retry-after` header); если Telegram недоступен — отчёт остаётся на диске и помечается как недоставленный — Plan 06-03 ships tenacity 3-retry on `(TelegramNetworkError, TelegramServerError)` ONLY via `wait_chain(wait_fixed(5), wait_fixed(15), wait_fixed(45))` (RESEARCH caveat #2 — NOT `wait_exponential`). `TelegramRetryAfter` handled OUTSIDE tenacity via `_send_with_retry_after_loop` (RESEARCH §11). Pitfall A: `TelegramBadRequest` / `TelegramForbiddenError` / `TelegramNotFound` / `TelegramUnauthorizedError` excluded from retry-set (fail-fast → undelivered). Plan 06-04 orchestrator on exhaustion → `delivery_status='undelivered_telegram_unreachable'` per D-606; xlsx remains on disk for `deliver-run --run-id N` recovery (D-608); Plan 06-05 E2E test pins D-605 invariant (runs.status='success' even when Telegram fails).
    - [x] **DELIVER-05**: Конфигурация двух чатов через ENV (`TG_BOT_TOKEN`, `TG_BUSINESS_CHAT_ID`, `TG_OPS_CHAT_ID`) — Plan 06-01 ships `.env.example` template (D-612) + `.gitignore` audit confirms `.env` excluded; Plan 06-02 ships `delivery/config.py::DeliverEnvConfig.from_env` reading via `os.getenv` ONLY (RESEARCH caveat #4 — `load_dotenv` lives only in `cli.py::_cmd_deliver` startup); Plan 06-04 orchestrator applies D-611 asymmetric handling: `TG_BOT_TOKEN` missing → `delivery_status='skipped_no_credentials'` + CLI exit 3; `TG_BUSINESS_CHAT_ID` missing on business route → degrade to ops alert with reason `missing_env_TG_BUSINESS_CHAT_ID`; `TG_OPS_CHAT_ID` missing on ops route → exit 3.
    ```

    **Edit 2 — Update Traceability table** rows ~174-178. Replace 5 `Pending` entries with these Done rows:

    ```markdown
    | DELIVER-01 | Phase 6 | Done (Plan 06-02 message_builder + Plan 06-03 telegram_client + Plan 06-04 orchestrator — D-601/D-602/D-614 aiogram 3.27 + FSInputFile + async-with Bot lifecycle; caption-split for summary_text>1024) |
    | DELIVER-02 | Phase 6 | Done (Plan 06-02 build_ops_alert single-template + D-610 reason-field + D-609 parse_mode=HTML + Pitfall A html.escape + Pitfall E Asia/Almaty tz + golden-file source-lock) |
    | DELIVER-03 | Phase 6 | Done (Plan 06-03 evaluate_gate D-604 4-check first-fail-wins + REUSES matcher.strict_key.read_run_status D-411 helper + D-515 size_guard cascade from Phase 5) |
    | DELIVER-04 | Phase 6 | Done (Plan 06-03 tenacity wait_chain(5,15,45) per RESEARCH caveat #2 + Pitfall A fail-fast classes excluded + TelegramRetryAfter outside-tenacity loop + Plan 06-04 D-606 delivery_status='undelivered_telegram_unreachable' + Plan 06-05 D-605 E2E invariant) |
    | DELIVER-05 | Phase 6 | Done (Plan 06-01 .env.example D-612 + Plan 06-02 DeliverEnvConfig.from_env + Plan 06-04 D-611 asymmetric handling: token=fail-loud, chat_ids=degradable; load_dotenv ONLY in cli.py::_cmd_deliver per RESEARCH caveat #4) |
    ```

    **Edit 3 — Update Coverage block** ~lines 185-189. Add "Closed: N/48" line. Compute exact current closed count by reading all `- [x]` markers in REQUIREMENTS.md BEFORE this plan executes:

    Run: `grep -c "^- \[x\]" .planning/REQUIREMENTS.md` to get current count; add 5 (DELIVER-01..05 closures); insert "Closed: {count+5}/48" line.

    Replace current Coverage block with (substituting actual `{prior_closed + 5}` value):
    ```markdown
    **Coverage:**
    - v1 requirements: 48 total (RECON 4 + CRAWL 6 + PARSE 6 + NORM 6 + MATCH 4 + DATA 6 + REPORT 6 + DELIVER 5 + SCHED 5)
    - Mapped to phases: 48
    - Closed: {N}/48 (Phase 1-6 complete; Phase 7 SCHED-01..05 + Phase 1 RECON-01 conditional plans pending)
    - Unmapped: 0
    - Note: previous "47 total" count was an off-by-one in the initial summary; the enumerated IDs above sum to 48.
    ```

    **Edit 4 — Append Phase 6 footer line** AFTER existing footer block (preserve Phase 4 / Phase 5 footers):

    ```markdown
    *Phase 6 update: 2026-05-12 — DELIVER-01..05 closed; Plans 06-01..06-06 shipped Wave 0..5 (Wave 0 setup → Wave 1 foundations → Wave 2 gate+client → Wave 3 orchestrator+CLI → Wave 4 composition+E2E → Wave 5 doc cascade). Phase 6 closure unblocks Phase 7 (Scheduler + Observability Hardening) — SCHED-01..05 inheritance points: D-605 delivery_status decoupling, D-606 6-value enum for Healthchecks SCHED-03 routing, D-607 8-key deliver.* namespace, D-608 `deliver-run --run-id N` standalone recovery tool, D-611 asymmetric ENV handling. New dep: aiogram>=3.27,<4.0.*
    ```
  </action>
  <verify>
    <automated>uv run python -c "from pathlib import Path; t=Path('.planning/REQUIREMENTS.md').read_text(encoding='utf-8'); assert all(f'- [x] **DELIVER-0{i}**' in t for i in (1,2,3,4,5)); print('OK 5 DELIVER closed')"</automated>
    <automated>uv run python -c "from pathlib import Path; t=Path('.planning/REQUIREMENTS.md').read_text(encoding='utf-8'); rows=[l for l in t.splitlines() if l.startswith('| DELIVER-')]; assert len(rows) == 5 and all('Done' in r for r in rows); print('OK Traceability 5/5 Done')"</automated>
    <automated>uv run python -c "from pathlib import Path; t=Path('.planning/REQUIREMENTS.md').read_text(encoding='utf-8'); assert 'Closed:' in t and 'Phase 6 update' in t; print('OK Coverage + footer present')"</automated>
  </verify>
  <done>
    REQUIREMENTS.md DELIVER-01..05 closed with verbose citations; Traceability table 5/5 Done; Coverage shows closed-count up by 5; Phase 6 update footer line appended.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md — append D-605/D-606/D-607 + Plan Execution Metrics + Current Position</name>
  <read_first>
    - `.planning/STATE.md` §Current Position section (early in file — frontmatter + first ~50 lines)
    - `.planning/STATE.md` §Accumulated Key Decisions section (find via Grep)
    - `.planning/STATE.md` §Plan Execution Metrics table (find via Grep)
    - Plan 04-06 + 05-06 STATE.md amendment examples — read recent git commits/diffs (`git log -p .planning/STATE.md | head -200` for style precedent)
    - `.planning/phases/06-telegram-delivery/06-CONTEXT.md` §Action Items (STATE.md guidance — 2 explicit entries for D-605/D-606)
  </read_first>
  <action>
    1. **Grep for section anchors:**
    Use Grep tool: pattern `^## Accumulated Key Decisions`, `^## Plan Execution Metrics`, `^### Current Position` (or `^## Current Position`) in `.planning/STATE.md` to locate insertion points.

    2. **Edit 1 — Append 3 rows to §Accumulated Key Decisions** (preserve existing ordering — insert AFTER the most recent Phase 5 entries; format matches table-row style of existing rows):

    ```markdown
    | D-605 (Phase 6) | **Delivery failure ≠ run failure** — Telegram outage → `runs.status` remains `success` + `runs.stats.deliver.delivery_status='undelivered_telegram_unreachable'` + xlsx persists on disk. ARCHITECTURE.md "reporter independent of delivery" extends inverted: "delivery independent of run correctness". Recovery via `python -m ga_crawler deliver-run --run-id N` (D-608). | Phase 7 Healthchecks SCHED-03 SHALL probe `deliver.delivery_status` (NOT `runs.status`) — `delivered_business`/`delivered_ops_only` = healthy; `undelivered_*`/`skipped_*` = unhealthy. |
    | D-606 (Phase 6) | **delivery_status enum — 6 exhaustive values:** `pending` / `delivered_business` / `delivered_ops_only` / `undelivered_telegram_unreachable` / `skipped_no_credentials` / `skipped_already_delivered`. Pitfall 4 sentinel `""` rejected; `pending` is the explicit default before any send attempt. | Phase 7 SCHED-03 health-probe enum mapping; SCHED-05 README documents enum semantics for operators. |
    | D-607 (Phase 6) | **`deliver.*` stats namespace — 8 keys disjoint from {viled,goldapple,match,report}.** Keys: delivery_status / route / business_caption_message_id / business_document_message_id / ops_message_id / attempt_count / last_error / delivered_at. All written via SINGLE atomic `patch_stats` per `run_delivery_phase` invocation (Pitfall 6). 5-way disjoint canary `test_five_way_namespaces_disjoint` enforces. | Phase 7+ analytics + dashboards (v2 out-of-scope) can query `deliver.delivery_status` history for delivery-failure-rate KPI. |
    ```

    Adapt the table-row format to match the existing STATE.md schema (likely `| Decision | Detail | Cascade target |` or similar — read first 5 rows to verify exact column count).

    3. **Edit 2 — Append Plan Execution Metrics rows** (one row per Plan 06-NN, mirror Plan 04-06/05-06 style):

    ```markdown
    | Phase 06 P01 | ~10 min | 3 tasks | 14 modified (pyproject.toml + .env.example + .gitignore audit + tests/conftest.py 3 new fixtures + 10 test stubs + tests/fixtures/delivery/ops-alert-templates.txt golden-file scaffold); aiogram>=3.27,<4.0 dep added; ~70 sub-deps via uv.lock; 610 existing tests preserved + 10 skip-marked stubs |
    | Phase 06 P02 | ~15 min | 3 tasks | 4 created (delivery/__init__.py + config.py + stats.py + message_builder.py) + 4 modified (test_delivery_stats + test_delivery_config + test_message_builder + test_stats_namespace_five_way); ~33 new tests GREEN; D-607 8-key namespace + D-610 ops-alert template + 5-way disjoint canary; 0 deviations |
    | Phase 06 P03 | ~15 min | 2 tasks | 2 created (delivery/gate.py + telegram_client.py) + 2 modified (test_gate + test_telegram_client); ~22 new tests GREEN; tenacity wait_chain(5,15,45) locked per RESEARCH caveat #2; Pitfall A fail-fast 4-class exclusion pinned |
    | Phase 06 P04 | ~20 min | 2 tasks | 1 created (runners/delivery_run.py) + 1 amended (cli.py 5 amend points) + 2 modified (integration test_delivery_run + test_cli_deliver); ~21 new integration tests GREEN; all 6 D-606 enum transitions pinned end-to-end; Pitfall 6 single patch_stats invariant spy-verified |
    | Phase 06 P05 | ~12 min | 2 tasks | 1 amended (main_run.py 5 amend points: imports + dataclass D-616 + pre-init + composition D-615 + 5 return-sites) + 2 modified (test_weekly_run_with_delivery E2E + test_delivery_source_lock structural); ~10 new E2E tests GREEN; D-605 invariant + SC#1 + SC#2 deliberate-failure pinned |
    | Phase 06 P06 | ~6 min | 3 tasks | 0 created + 3 modified (REQUIREMENTS.md DELIVER-01..05 closure + STATE.md D-605/D-606/D-607 cascade + ROADMAP.md Phase 6 plan list + Progress 6/6 Complete); ~700/700 tests preserved (docs-only) |
    ```

    4. **Edit 3 — Update §Current Position** (frontmatter + first-section "Resume file" + status lines):

    Modify these specific lines (use Edit tool with exact match strings):

    - Replace `Phase: 06 — **CONTEXT GATHERED**` with `Phase: 06 — **COMPLETE** (2026-05-12; all 6 plans shipped Wave 0..5; DELIVER-01..05 closed; D-605/D-606/D-607 cascade persisted; Phase 7 Scheduler + Observability Hardening unblocked)`.
    - Replace the `Plan: TBD — /gsd-plan-phase 6` line with `Plan: 06-06 — COMPLETE 2026-05-12. Phase 6 deliverable: weekly-run end-to-end now includes Telegram delivery; deliver-run --run-id N standalone recovery tool ships per D-608. Next: /gsd-discuss-phase 7 (Scheduler + Observability Hardening — SCHED-01..05).`
    - Replace `Phase 5 status: **COMPLETE**` with `Phase 6 status: **COMPLETE** (2026-05-12; all 6 plans shipped Wave 0..5; DELIVER-01..05 closed; aiogram 3.27 SDK; tenacity wait_chain(5,15,45); D-606 6-value enum; D-607 8-key deliver.* namespace; D-605 delivery decoupled from runs.status — Phase 7 Healthchecks inheritance point)`.
    - Update Resume file to: `.planning/phases/06-telegram-delivery/06-06-SUMMARY.md (Phase 6 close-out 2026-05-12; DELIVER-01..05 closed; ready for /gsd-discuss-phase 7)`.

    Use Edit tool for each surgical replacement; do NOT rewrite the entire STATE.md.

    5. **Verify edits applied** via Grep checks below.
  </action>
  <verify>
    <automated>uv run python -c "from pathlib import Path; t=Path('.planning/STATE.md').read_text(encoding='utf-8'); assert 'D-605' in t and 'D-606' in t and 'D-607' in t; assert 'Phase 06 P01' in t and 'Phase 06 P06' in t; assert 'Phase 6 status: **COMPLETE**' in t or 'Phase: 06 — **COMPLETE**' in t; print('OK STATE.md cascade present')"</automated>
    <automated>uv run python -c "from pathlib import Path; t=Path('.planning/STATE.md').read_text(encoding='utf-8'); assert 'CONTEXT GATHERED' not in t.split('Phase 6')[1][:500] if 'Phase 6' in t else True; assert '06-06-SUMMARY.md' in t; print('OK Resume file updated')"</automated>
  </verify>
  <done>
    STATE.md §Accumulated Key Decisions has 3 new rows (D-605/D-606/D-607); §Plan Execution Metrics has 6 new rows (Phase 06 P01..P06); §Current Position shows Phase 6 COMPLETE with 06-06-SUMMARY.md as Resume file.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP.md — Phase 6 plan list filled + Progress table updated to 6/6 Complete</name>
  <read_first>
    - `.planning/ROADMAP.md` §Phase 6 details (lines 133-144 current with "Plans: TBD")
    - `.planning/ROADMAP.md` Progress table (line 189 current `| 6. Telegram Delivery + Ops/Business Split | 0/0 | Not started | - |`)
    - `.planning/ROADMAP.md` footer / update annotations (Plan 05-06 inserted Phase 5 update lines — mirror style)
    - All 6 plan frontmatter files (`06-01-PLAN.md` through `06-06-PLAN.md`) for brief plan summaries
  </read_first>
  <action>
    **Edit 1 — Replace ROADMAP.md Phase 6 §Plans section** (currently `**Plans**: TBD`). Replace with:

    ```markdown
    **Plans**: 6 plans across 6 waves (Wave 0 setup → Wave 1 foundations → Wave 2 gate+client → Wave 3 orchestrator+CLI → Wave 4 composition+E2E → Wave 5 doc cascade)
    - [x] 06-01-PLAN.md - Wave 0: pyproject [tool.ga_crawler.deliver] namespace (D-614 6 keys) + aiogram>=3.27,<4.0 dep + .env.example (D-612) + .gitignore audit + 3 new tests/conftest.py fixtures (mock_aiogram_bot + mock_tg_env + synthetic_delivered_run) + 10 skip-marked test stubs + tests/fixtures/delivery/ops-alert-templates.txt golden-file scaffold
    - [x] 06-02-PLAN.md - Wave 1: delivery/__init__.py + config.py (DeliverConfig.from_pyproject + DeliverEnvConfig.from_env — D-611 + D-614 + RESEARCH caveat #4 load_dotenv-only-in-cli) + stats.py (DELIVER_STATS_KEYS 8-tuple + DeliverStatsBuilder + StatsNamespaceError reuse per D-607) + message_builder.py (build_ops_alert + REASON_SHORT + business_caption per D-609 + D-610 + Pitfall A html.escape + Pitfall E Asia/Almaty tz); ~33 tests GREEN
    - [x] 06-03-PLAN.md - Wave 2: delivery/gate.py (evaluate_gate D-604 4-check first-fail-wins REUSES matcher.strict_key.read_run_status D-411 helper + GateDecision frozen dataclass) + delivery/telegram_client.py (open_bot + send_message_with_policy + send_document_with_policy + tenacity wait_chain(5,15,45) per RESEARCH caveat #2 + Pitfall A fail-fast exclusions + _send_with_retry_after_loop outside tenacity per §11); ~22 tests GREEN
    - [x] 06-04-PLAN.md - Wave 3: runners/delivery_run.py (run_delivery_phase 7-step sync orchestrator + _send_async + DeliveryPhaseResult + Pitfall C path containment + Pitfall 6 single patch_stats + D-605/D-606/D-608 idempotency dispatch) + cli.py AMEND (deliver-run subcommand + _cmd_deliver handler + load_dotenv ONLY here per RESEARCH caveat #4 + 6 argparse flags + exit codes 0/2/3); ~21 integration tests GREEN
    - [x] 06-05-PLAN.md - Wave 4: main_run.py AMEND (5 amend points: imports + MainRunResult dataclass 2 new fields D-616 + pre-init outcome vars above try + delivery composition D-615 inside `if m_result.status == 'success': if r_result.status == 'success' and r_result.xlsx_path:` block + 5 return-sites populated) + tests/integration/test_weekly_run_with_delivery.py E2E (SC#1 happy-path + SC#2 deliberate-failure + D-605 invariant pinned) + tests/test_delivery_source_lock.py structural canaries (D-514 inverted invariant + aiogram isolation + load_dotenv hygiene); ~10 E2E tests GREEN
    - [x] 06-06-PLAN.md - Wave 5: doc cascade — REQUIREMENTS.md DELIVER-01..05 closed with per-plan citations + Traceability table 5/5 Done + Coverage 42/48; STATE.md §Accumulated Key Decisions 3 new rows (D-605/D-606/D-607) + Plan Execution Metrics 6 new rows + Current Position COMPLETE; ROADMAP.md Phase 6 plan list filled + Progress 6/6 Complete 2026-05-12
    ```

    **Edit 2 — Update ROADMAP.md Progress table row** (line ~189 currently `| 6. Telegram Delivery + Ops/Business Split | 0/0 | Not started | - |`). Replace with:

    ```markdown
    | 6. Telegram Delivery + Ops/Business Split | 6/6 | Complete (all 6 plans shipped Wave 0..5; DELIVER-01..05 closed; aiogram 3.27 SDK + tenacity wait_chain(5,15,45) + D-606 6-value enum + D-607 8-key namespace + D-605 delivery decoupled from runs.status for Phase 7 Healthchecks inheritance; D-608 `deliver-run --run-id N` standalone recovery; ~118 net new tests Wave 0..5; ARCHITECTURE.md "reporter independent of delivery" extended inverted "delivery independent of run-status correctness") | 2026-05-12 |
    ```

    **Edit 3 — Append Phase 6 footer line** AFTER existing footers (preserve Phase 4/5 entries) at end of file:

    ```markdown
    *Phase 6 plan list filled + close-out: 2026-05-12 — all 6 plans shipped Wave 0..5; DELIVER-01..05 closed in REQUIREMENTS.md with per-plan citations; D-605/D-606/D-607 cascade persisted to STATE.md Accumulated Key Decisions for Phase 7 planner inheritance; project total 42/48 v1 requirements satisfied; Phase 7 Scheduler + Observability Hardening unblocked.*
    ```
  </action>
  <verify>
    <automated>uv run python -c "from pathlib import Path; t=Path('.planning/ROADMAP.md').read_text(encoding='utf-8'); assert '- [x] 06-01-PLAN.md' in t and '- [x] 06-06-PLAN.md' in t; assert '| 6. Telegram Delivery + Ops/Business Split | 6/6 | Complete' in t; assert 'Phase 6 plan list filled + close-out' in t; print('OK ROADMAP.md cascade complete')"</automated>
  </verify>
  <done>
    ROADMAP.md Phase 6 section shows 6/6 plans with detailed line per plan; Progress table row updated to 6/6 Complete with completion date; Phase 6 footer appended.
  </done>
</task>

<task type="auto">
  <name>Task 4 (B5 FIX): Fix D-603 wait_exponential formula drift in 06-CONTEXT.md</name>
  <read_first>
    - `.planning/phases/06-telegram-delivery/06-CONTEXT.md` D-603 block (lines 22-31) — current text contains `wait=wait_exponential(multiplier=5, min=5, max=45),  # 5/15/45 backoff` which RESEARCH caveat #2 explicitly disproves (wait_exponential with these params produces 10/20, NOT 5/15/45)
    - `.planning/phases/06-telegram-delivery/06-RESEARCH.md` §"Library Verification" caveat #2 — the canonical source-of-truth for the tenacity wait formula choice
  </read_first>
  <action>
    **B5 FIX — Eliminate formula drift between CONTEXT.md and PLAN files.**

    All planning artifacts (06-01..06-05) and verify-canaries reference `wait_chain(wait_fixed(5), wait_fixed(15), wait_fixed(45))` per RESEARCH caveat #2. But D-603 in CONTEXT.md still cites the WRONG formula (`wait_exponential(multiplier=5, min=5, max=45)`). This task closes the cascade by syncing CONTEXT.md to the corrected formula.

    Apply a surgical Edit to `.planning/phases/06-telegram-delivery/06-CONTEXT.md`:

    **Old text (in D-603 code block, lines ~25-28):**
    ```python
    @retry(
        retry=retry_if_exception_type((TelegramNetworkError, TelegramServerError)),
        stop=stop_after_attempt(3),
        wait=wait_exponential(multiplier=5, min=5, max=45),  # 5/15/45 backoff
        reraise=True,
    )
    ```

    **New text (RESEARCH caveat #2-aligned):**
    ```python
    @retry(
        retry=retry_if_exception_type((TelegramNetworkError, TelegramServerError)),
        stop=stop_after_attempt(3),
        wait=wait_chain(wait_fixed(5), wait_fixed(15), wait_fixed(45)),  # 5/15/45 backoff per RESEARCH caveat #2 (wait_exponential with multiplier=5 gives 10/20, NOT 5/15/45)
        reraise=True,
    )
    ```

    **Add a footnote** below the D-603 code block (preserving the rest of the D-603 paragraph that follows the code block):
    > **Note (B5 FIX, revision 2026-05-12):** Earlier draft of D-603 cited `wait_exponential(multiplier=5, min=5, max=45)`. Empirical verification in `06-RESEARCH.md §Library Verification caveat #2` showed that formula produces the sequence `10s, 20s` (capped at max=45 only past attempt 4), not the intended `5s, 15s, 45s`. Plans 06-01..06-05 already use the corrected `wait_chain(wait_fixed(5), wait_fixed(15), wait_fixed(45))`; this footnote brings D-603 into sync.

    Use the Edit tool with the exact `wait_exponential(multiplier=5, min=5, max=45),  # 5/15/45 backoff` substring as the anchor.
  </action>
  <verify>
    <automated>uv run python -c "from pathlib import Path; ctx=Path('.planning/phases/06-telegram-delivery/06-CONTEXT.md').read_text(encoding='utf-8'); assert 'wait_chain(wait_fixed(5), wait_fixed(15), wait_fixed(45))' in ctx, 'B5 FIX not applied — D-603 still references old formula'; assert 'wait_exponential(multiplier=5, min=5, max=45)' not in ctx, 'B5 FIX incomplete — old wait_exponential string still present in CONTEXT.md'; assert 'B5 FIX, revision 2026-05-12' in ctx, 'B5 footnote missing'; print('OK D-603 formula drift resolved')"</automated>
  </verify>
  <done>
    CONTEXT.md D-603 block uses `wait_chain(wait_fixed(5), wait_fixed(15), wait_fixed(45))` matching Plans 06-01..06-05 + RESEARCH caveat #2. Footnote documents the correction. Old `wait_exponential(multiplier=5, min=5, max=45)` string fully removed from CONTEXT.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 06-06 → REQUIREMENTS.md/STATE.md/ROADMAP.md | Doc-only edits; no code change; no production risk |
| STATE.md §Accumulated Key Decisions → Phase 7 planner | Phase 7 reads STATE.md for inheritance — incorrect/missing rows → Phase 7 misplans |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-21 | Repudiation | Phase 7 planner missing D-605 inheritance → wrong Healthchecks routing | mitigate | D-605 cascade row explicit + verbose; cascade-target column points at SCHED-03; verify via Task 2 grep canary. |
| T-6-22 | Tampering | REQUIREMENTS.md `- [x]` count mis-incremented | mitigate | Task 1 verify step counts both before-and-after `- [x]` markers; difference must be exactly 5. Manual recount in case of edge cases. |
</threat_model>

<verification>
1. **REQUIREMENTS.md cascade green:** all 4 automated checks in Task 1 verify pass.
2. **STATE.md cascade green:** both checks in Task 2 verify pass.
3. **ROADMAP.md cascade green:** check in Task 3 verify pass.
4. **No code regression:** `uv run pytest -x` exits 0 (sanity — docs-only edit shouldn't affect tests, but verify).
5. **Doc consistency:** counts align across REQUIREMENTS.md Coverage line + ROADMAP.md Progress table + STATE.md Current Position.
</verification>

<success_criteria>
- [ ] REQUIREMENTS.md DELIVER-01..05 all marked `- [x]` with verbose per-plan citations
- [ ] REQUIREMENTS.md Traceability table 5/5 DELIVER-* rows marked Done with plan + decision citations
- [ ] REQUIREMENTS.md Coverage block updated with "Closed: 42/48" (or correct count)
- [ ] REQUIREMENTS.md Phase 6 update footer line appended
- [ ] STATE.md §Accumulated Key Decisions has 3 new rows (D-605/D-606/D-607) with cascade-target column populated
- [ ] STATE.md §Plan Execution Metrics has 6 new rows (Phase 06 P01..P06)
- [ ] STATE.md §Current Position shows Phase 6 COMPLETE + Resume file = 06-06-SUMMARY.md
- [ ] ROADMAP.md Phase 6 section shows 6/6 plans (Wave 0..5) with brief per-plan summaries
- [ ] ROADMAP.md Progress table row 6 → "6/6 Complete 2026-05-12"
- [ ] ROADMAP.md Phase 6 footer line appended
- [ ] **B5 FIX:** CONTEXT.md D-603 updated to `wait_chain(wait_fixed(5), wait_fixed(15), wait_fixed(45))`; old `wait_exponential(multiplier=5, min=5, max=45)` string removed; B5 footnote present
- [ ] `uv run pytest -x` still exits 0 (sanity — docs-only changes)
</success_criteria>

<output>
After completion, create `.planning/phases/06-telegram-delivery/06-06-SUMMARY.md` documenting:
- 3 files modified (REQUIREMENTS.md + STATE.md + ROADMAP.md)
- 0 tests added (docs-only)
- Exact `- [x]` count delta in REQUIREMENTS.md
- Phase 6 → Phase 7 handoff status (READY)
- Total Phase 6 net new test count across all 6 plans (sum of plan metrics ~118)
- Any deviations from cascade-target columns in STATE.md
</output>
</content>
</invoke>