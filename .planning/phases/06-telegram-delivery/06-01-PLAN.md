---
phase: 6
plan: 01
type: execute
wave: 1
autonomous: true
reconstructed: 2026-05-14
requirements: [DELIVER-01, DELIVER-02, DELIVER-03, DELIVER-04, DELIVER-05]
---

<objective>
Phase 6 retroactive stub — Telegram delivery pipeline. Original implementation shipped 2026-05-12. This stub provides the `<threat_model>` block reconstructed from production code docstring in `src/ga_crawler/delivery/message_builder.py` "Pitfall A" section for the `/gsd-secure-phase 6` skill (AUDIT-DEBT-03 precondition).

CRITICAL: T-06-03 (`business_caption()` summary_text not _esc()'d) is pre-classified as `disposition: accept` — see threat register below. This is the D-1002 auto-flip insurance per RESEARCH §2 Claim 3.
</objective>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline stats → Telegram bot message | Dynamic fields from `runs.stats` are interpolated into HTML-parsed Telegram messages |
| `.env` file → process environment | Bot token and chat IDs loaded from `.env`; file must be chmod 0600 |
| Internal pipeline → `business_caption` | `summary_text` comes from `summary_builder.py` internal output, not user-supplied input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation / Evidence |
|-----------|----------|-----------|-------------|----------------------|
| T-06-01 | Injection | `delivery/message_builder.py` — `build_ops_alert` HTML fields | mitigate | `_esc(value)` wraps `stdlib_html.escape(value, quote=False)` (lines 75-82); applied to EVERY dynamic str interpolation in `build_ops_alert`: `reason_short` (line 115), `_format_almaty(started_at_utc)` (line 117), `run_status` (line 118), `gate_failed_check` (line 119), truncated error (line 132). Pitfall A explicitly documented in module docstring. Golden-file `tests/fixtures/delivery/ops-alert-templates.txt` source-locks the template including escaped fields. `grep -n "_esc(" src/ga_crawler/delivery/message_builder.py` confirms coverage at every interpolation point. |
| T-06-02 | Information Disclosure | `delivery/config.py` — bot token in `.env` | accept | Bot token read via `os.getenv("TG_BOT_TOKEN")` in `delivery/config.py:97`; never hardcoded. `.env` gitignored; `.env.example` committed with placeholder. Operator required to `chmod 0600 .env` per `README.md:51` (T-07-08 mitigation from Phase 7). OS-level file permission protection — not enforceable at the Python code layer. Accept disposition is consistent with Phase 7 `07-SECURITY.md` treatment of the same token-in-.env pattern. |
| T-06-03 | Injection | `delivery/message_builder.py` — `business_caption()` `summary_text` not `_esc()`'d | accept | `summary_text` is internal pipeline output from `summary_builder.py`, not user-supplied input. `_esc()` is applied where user-tainted data enters (ops alert template) per `message_builder.py` Pitfall A. The `summary_text` field is generated deterministically by `summary_builder.build_summary()` from structured stats fields (integers, floats, and pre-validated string constants from `D-504` emoji template) — it is not a reflection of any external input. Classifying this as `mitigate` would require adding `_esc()` calls that would break the valid HTML in the summary (e.g. `<b>`, `<code>` tags intentionally embedded by `summary_builder.py`). RESEARCH §2 Claim 3 nuance: accept is the correct disposition here. |
</threat_model>
