---
phase: 2
plan: 01
type: execute
wave: 1
autonomous: true
reconstructed: 2026-05-14
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05, DATA-06, CRAWL-01, CRAWL-03, CRAWL-04, CRAWL-05, CRAWL-06, PARSE-01, PARSE-02, PARSE-03, PARSE-04, PARSE-05, PARSE-06, NORM-01, NORM-02, NORM-03, NORM-04, NORM-05, NORM-06]
---

<objective>
Phase 2 retroactive stub — project skeleton + viled crawl + storage pipeline. Original implementation shipped 2026-05-07. This stub provides the `<threat_model>` block reconstructed from production code docstrings in `src/ga_crawler/storage/sqlite.py` and `src/ga_crawler/matcher/strict_key.py` for the `/gsd-secure-phase 2` skill (AUDIT-DEBT-01 precondition).
</objective>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External HTML → parser | Untrusted HTML from viled.kz ingested via `selectolax` / `__NEXT_DATA__` JSON deserialization |
| Operator `.env` → process | Bot token and credentials loaded from `.env` file; file must be chmod 0600 |
| Application → SQLite | All SQL queries parameterized; no external network data reaches SQL layer unescaped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation / Evidence |
|-----------|----------|-----------|-------------|----------------------|
| T-02-01 | Injection | `storage/sqlite.py` — SQL layer | mitigate | Bind-param SQL throughout: `text(":rid")` + `params={"rid": run_id}` in every `SqliteRunWriter` method (`patch_stats`, `fail`, `finalize`, lines 279-321). Zero f-string or %-formatted SQL anywhere in the storage layer. Mirrors D-215 explicit SQL-injection note in Phase 2 RESEARCH. `grep -n "text(\"" src/ga_crawler/storage/sqlite.py` yields bind-param form for all queries. |
| T-02-02 | Tampering | `parsers/viled_nextdata.py` — untrusted HTML | accept | `selectolax` / `__NEXT_DATA__` JSON deserialization on untrusted HTML. Risk mitigated by: (a) tenacity retry isolation in `fetchers/viled.py::fetch_one_isolated` (CRAWL-03 per-SKU isolation — one bad product does not crash run); (b) sanity-gate threshold (CRAWL-05 / D-203) catches wholesale parse failures; (c) parse-quality gate (PARSE-05 / D-218) aborts run if >5% required fields null. No sandbox needed for parsing public product catalog HTML. |
| T-02-03 | Information Disclosure | `cli.py` + `.env` | mitigate | `load_dotenv(find_dotenv(usecwd=True))` called ONLY in `cli.py` entrypoint, never in modules (hazard_dotenv_walks_from_file.md). `.env` file gitignored; `.env.example` committed with placeholder values. Operator required to `chmod 0600 .env` per README.md §2 step 7. |
</threat_model>
