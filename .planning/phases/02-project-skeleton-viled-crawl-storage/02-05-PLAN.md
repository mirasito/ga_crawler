---
phase: 02
plan: 05
type: execute
wave: 2
depends_on: ["02-02", "02-03", "02-04"]
files_modified:
  - src/ga_crawler/runner/gates.py
  - src/ga_crawler/runner/stats.py
  - src/ga_crawler/runners/viled_run.py
  - src/ga_crawler/runners/main_run.py
  - src/ga_crawler/cli.py
  - tests/unit/test_sanity_n_gate.py
  - tests/unit/test_parse_quality_gate.py
  - tests/unit/test_auto_suggest_threshold.py
  - tests/unit/test_viled_stats_builder.py
  - tests/integration/test_viled_run_e2e_with_real_storage.py
  - tests/integration/test_main_run_e2e.py
autonomous: true
requirements:
  - CRAWL-05
  - PARSE-05
  - NORM-06

must_haves:
  goal: "Gates + orchestrator + CLI cutover land. Refactor `runner/gates.py` (D-203) to retailer-agnostic `auto_suggest_threshold` + `final_threshold_gate` (Phase 3 backward-compat shims kept). Add `ViledStatsBuilder` mirror of `GoldappleStatsBuilder`. Ship `runners/viled_run.py` orchestrator (mirror of `runners/goldapple_run.py`, sync inside) implementing PARSE-05 parse-quality gate + CRAWL-05 sanity-N gate (D-218 sequential). Ship `runners/main_run.py` composing viled→goldapple→Norm06Writer→finalize. Cutover `cli.py`: DELETE 4 stub Phase 2 implementations (D-212), DELETE `goldapple-run` subcommand, ADD `weekly-run` subcommand, KEEP `goldapple-smoke`."
  truths:
    - "src/ga_crawler/runner/gates.py exports new retailer-agnostic helpers `auto_suggest_threshold(history, factor=0.7, min_runs=4)` and `final_threshold_gate(count, threshold)` per D-203"
    - "Backward-compat shims kept: `auto_suggest_m(history) → auto_suggest_threshold(history, 0.7, 4)`; `final_m_gate(c, M)` → `final_threshold_gate(c, M)`. Phase 3 callers (runner/gates.py imports + goldapple_run.py) stay green."
    - "src/ga_crawler/runner/stats.py adds `VILED_STATS_KEYS` 9-tuple namespace + `ViledStatsBuilder` class mirror of `GoldappleStatsBuilder` per RESEARCH Open Q7"
    - "Existing `GOLDAPPLE_STATS_KEYS` and `GoldappleStatsBuilder` UNCHANGED (only additions)"
    - "src/ga_crawler/runners/viled_run.py exports `run_viled_phase(run_id, config, brand_alias, normalizer, snapshot_writer, run_writer, *, dispatcher=None, fetcher=None, fetch_catalog=None) → ViledPhaseResult` — mirror of run_goldapple_phase shape (sync inside)"
    - "viled_run.py implements 8-step pipeline: 1. fetch_catalog_urls(both endpoints) → 2. fetcher.run_loop → 3. parser.dispatch('viled', ...) per record → 4. normalizer.brand/name/volume → 5. snapshot_writer.append(retailer='viled', ...) — per-batch — → 6. parse_quality gate (D-218 >5% null required-field rate → fail) → 7. final_threshold_gate(viled_count, N) (CRAWL-05 D-201) → 8. atomic patch_stats(viled.* delta) — single call (Pitfall 6)"
    - "Two gates checked sequentially in viled_run.py: parse_quality FIRST (D-218); if pass → sanity_gate_n SECOND (D-201). Either failing → run_writer.fail(reason) and PhaseResult.status='failed'. Snapshot rows still persist (audit trail per RESEARCH §System Architecture)."
    - "src/ga_crawler/runners/main_run.py exports `run_weekly(repo_root, *, db_path='prices.db', headless=True, viled_only=False, goldapple_only=False) → MainRunResult` composing both retailers + Norm06Writer + finalize"
    - "main_run.py owns `runs` row lifecycle: try { create → viled_run → goldapple_run → norm06_writer → finalize('success') } except { run_writer.fail(stack-trace) — IDEMPOTENT } — DATA-05 invariant satisfied via try/finally semantics"
    - "main_run.py reads viled brand list AFTER viled phase (D-221): `SELECT DISTINCT brand_norm FROM v_current_snapshots WHERE retailer='viled'` — passed to goldapple_run as viled_brands"
    - "main_run.py wires Norm06Writer.persist (D-211 — Phase 2 owns write-path; Phase 3 stub no longer writes review queue) AFTER both retailer phases complete"
    - "src/ga_crawler/cli.py: 4 stub classes (StubBrandAlias / StubNormalizer / StubSnapshotWriter / StubRunWriter) DELETED (D-212); `goldapple-run` subcommand DELETED; `weekly-run` subcommand ADDED with --run-id, --db-path, --sanity-gate-n, --sanity-gate-m, --headless, --viled-only, --goldapple-only flags; `goldapple-smoke` subcommand KEPT verbatim"
    - "Existing 192/192 non-live tests + Plan 02/03/04 additions stay GREEN after stub deletion (mocks in tests/conftest.py from Phase 3 take over the testability role per D-212)"
  artifacts:
    - path: src/ga_crawler/runner/gates.py
      provides: "auto_suggest_threshold + final_threshold_gate retailer-agnostic helpers + backward-compat shims"
      contains: "auto_suggest_threshold"
    - path: src/ga_crawler/runner/stats.py
      provides: "VILED_STATS_KEYS + ViledStatsBuilder + StatsNamespaceError (existing)"
      contains: "VILED_STATS_KEYS"
    - path: src/ga_crawler/runners/viled_run.py
      provides: "run_viled_phase + ViledPhaseResult dataclass"
      contains: "ViledPhaseResult"
      min_lines: 150
    - path: src/ga_crawler/runners/main_run.py
      provides: "run_weekly + MainRunResult composition orchestrator"
      contains: "Norm06Writer"
    - path: src/ga_crawler/cli.py
      provides: "weekly-run + goldapple-smoke subcommands; stubs DELETED"
  key_links:
    - from: src/ga_crawler/runners/viled_run.py
      to: src/ga_crawler/runner/gates.py
      via: "imports final_threshold_gate + auto_suggest_threshold"
      pattern: "from ga_crawler.runner.gates import"
    - from: src/ga_crawler/runners/viled_run.py
      to: src/ga_crawler/storage/sqlite.py
      via: "calls SqliteSnapshotWriter.append + SqliteRunWriter.patch_stats"
      pattern: "snapshot_writer.append\\|run_writer.patch_stats"
    - from: src/ga_crawler/runners/main_run.py
      to: src/ga_crawler/runners/goldapple_run.py
      via: "imports run_goldapple_phase (Phase 3 frozen)"
      pattern: "from ga_crawler.runners.goldapple_run import"
    - from: src/ga_crawler/runners/main_run.py
      to: src/ga_crawler/storage/norm06_writer.py
      via: "Norm06Writer.persist invoked after both retailer phases"
      pattern: "Norm06Writer"
    - from: src/ga_crawler/cli.py
      to: src/ga_crawler/runners/main_run.py
      via: "weekly-run subcommand calls run_weekly"
      pattern: "weekly-run\\|run_weekly"
---

<objective>
Wave 2 ORCHESTRATION + STUB CUTOVER. The keystone wave: gates refactored (D-203 retailer-agnostic + Phase 3 shim), ViledStatsBuilder mirrors GoldappleStatsBuilder, viled_run.py orchestrator implements 2 gates (parse-quality + sanity-N) per D-218 sequence, main_run.py composes viled→goldapple→Norm06→finalize with single runs-row lifecycle, cli.py delivers production-ready `weekly-run` subcommand and DELETES 4 Phase 3 stubs (D-212).

Purpose: Make `python -m ga_crawler weekly-run --run-id N` actually work end-to-end against the in-memory or on-disk DB with real Phase 2 storage (no Stubs). PARSE-05 + CRAWL-05 + NORM-06 close in this wave; DATA-05 lifecycle integrity verified by integration test that simulates a mid-run crash.

Output: 5 modified source files (gates, stats, cli) + 2 new (viled_run, main_run) + 6 GREEN test files (4 unit + 2 integration). Stub deletion verified by `grep -c "class Stub" src/ga_crawler/cli.py` == 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-project-skeleton-viled-crawl-storage/02-RESEARCH.md
@.planning/phases/02-project-skeleton-viled-crawl-storage/02-PATTERNS.md
@.planning/phases/02-project-skeleton-viled-crawl-storage/02-CONTEXT.md
@.planning/phases/02-project-skeleton-viled-crawl-storage/02-02-SUMMARY.md
@.planning/phases/02-project-skeleton-viled-crawl-storage/02-03-SUMMARY.md
@.planning/phases/02-project-skeleton-viled-crawl-storage/02-04-SUMMARY.md
@src/ga_crawler/runner/gates.py
@src/ga_crawler/runner/stats.py
@src/ga_crawler/runners/goldapple_run.py
@src/ga_crawler/cli.py
@src/ga_crawler/interfaces.py
@src/ga_crawler/storage/sqlite.py
@src/ga_crawler/storage/norm06_writer.py
@src/ga_crawler/normalizers/facade.py
@src/ga_crawler/alias/yaml_loader.py
@src/ga_crawler/parsers/dispatcher.py
@src/ga_crawler/fetchers/viled.py
@src/ga_crawler/enumeration/viled_catalog.py
@src/ga_crawler/config.py

<interfaces>
<!-- Phase 3 frozen orchestrator gives us the orchestrator template to mirror. -->

From src/ga_crawler/runners/goldapple_run.py (FROZEN — DO NOT MODIFY; reference pattern):
```python
@dataclass
class PhaseResult:
    status: str  # "success" | "failed"
    goldapple_count: int = 0
    reason: Optional[str] = None
    stats_delta: dict = field(default_factory=dict)
    unmatched_viled_brands: list[str] = field(default_factory=list)
    new_goldapple_slugs: list[str] = field(default_factory=list)

async def run_goldapple_phase(
    run_id, viled_brands, repo_root, brand_alias, normalizer,
    snapshot_writer, run_writer, *, headless=True, M=1000,
    smoke_urls=None, fetcher_factory=None, sitemap_fetcher=None,
) -> PhaseResult: ...
```

From src/ga_crawler/runner/gates.py (CURRENT — refactor in Task 1):
```python
def final_m_gate(goldapple_count: int, M: int = 1000) -> bool: ...
def auto_suggest_m(history_counts: list[int]) -> Optional[int]: ...
```

From src/ga_crawler/runner/stats.py (CURRENT — extend in Task 1):
```python
GOLDAPPLE_STATS_KEYS: tuple[str, ...] = (...)  # 13 keys
class StatsNamespaceError(KeyError): ...
class GoldappleStatsBuilder: ...
```

From src/ga_crawler/cli.py (CURRENT — overhaul in Task 3):
- 4 Stub classes (DELETE)
- `goldapple-smoke` subcommand (KEEP)
- `goldapple-run` subcommand + `_cmd_run` (DELETE)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor gates.py (D-203) + add ViledStatsBuilder + write parse-quality + sanity-N + auto-suggest tests</name>
  <files>src/ga_crawler/runner/gates.py, src/ga_crawler/runner/stats.py, tests/unit/test_sanity_n_gate.py, tests/unit/test_parse_quality_gate.py, tests/unit/test_auto_suggest_threshold.py, tests/unit/test_viled_stats_builder.py</files>
  <read_first>
    src/ga_crawler/runner/gates.py (FULL — current `final_m_gate`, `auto_suggest_m`, and any other helpers; refactor target),
    src/ga_crawler/runner/stats.py (FULL — `GOLDAPPLE_STATS_KEYS`, `GoldappleStatsBuilder`, `StatsNamespaceError` — extend without modifying),
    .planning/phases/02-project-skeleton-viled-crawl-storage/02-RESEARCH.md (§"Code Examples" lines 980-1014 — verbatim refactor; §Open Q7 — VILED_STATS_KEYS 9-tuple),
    .planning/phases/02-project-skeleton-viled-crawl-storage/02-PATTERNS.md (lines 481-528 — refactor target; lines 532-589 — ViledStatsBuilder mirror),
    .planning/phases/02-project-skeleton-viled-crawl-storage/02-CONTEXT.md (D-203 refactor; D-218 parse-quality gate spec)
  </read_first>
  <behavior>
    - Test 1 (test_auto_suggest_threshold.py::test_returns_int_factor_median): auto_suggest_threshold([100,200,300,400], 0.7, 4) → int(0.7 * 250) = 175
    - Test 2 (test_auto_suggest_threshold.py::test_min_runs_guard): auto_suggest_threshold([100,200,300], 0.7, 4) → None (<min_runs)
    - Test 3 (test_auto_suggest_threshold.py::test_only_last_min_runs_used): auto_suggest_threshold([1000, 100, 200, 300, 400], 0.7, 4) uses [100,200,300,400] not [1000,...]
    - Test 4 (test_auto_suggest_threshold.py::test_backward_compat_auto_suggest_m): auto_suggest_m == auto_suggest_threshold(history, 0.7, 4) (Phase 3 shim works)
    - Test 5 (test_auto_suggest_threshold.py::test_backward_compat_final_m_gate): final_m_gate(1500, 1000) is True; final_m_gate(500, 1000) is False
    - Test 6 (test_sanity_n_gate.py::test_above_threshold_passes): final_threshold_gate(150, 100) is True
    - Test 7 (test_sanity_n_gate.py::test_below_threshold_fails): final_threshold_gate(50, 100) is False
    - Test 8 (test_sanity_n_gate.py::test_equal_threshold_passes): final_threshold_gate(100, 100) is True (>= guarantee)
    - Test 9 (test_parse_quality_gate.py::test_low_null_rate_passes): null_rate=0.03 (3% of 100 rows) → gate passes (D-218 ≤5%)
    - Test 10 (test_parse_quality_gate.py::test_high_null_rate_fails): null_rate=0.08 (8% of 100 rows) → gate fails
    - Test 11 (test_parse_quality_gate.py::test_threshold_boundary): null_rate=0.05 → passes (≤ not <); 0.0501 → fails
    - Test 12 (test_parse_quality_gate.py::test_zero_rows_passes_or_skips): given 0 rows total, gate passes (no data → no quality assertion); document as N/A
    - Test 13 (test_viled_stats_builder.py::test_set_resolves_bare_key): builder.set("fetch_count", 100) stores as "viled.fetch_count"
    - Test 14 (test_viled_stats_builder.py::test_set_namespaced_key_passes): builder.set("viled.parse_failures", 2) accepted
    - Test 15 (test_viled_stats_builder.py::test_set_unknown_key_raises): builder.set("nonsense", 1) raises StatsNamespaceError
    - Test 16 (test_viled_stats_builder.py::test_delta_separated_from_goldapple): ViledStatsBuilder cannot accept goldapple.* keys (StatsNamespaceError)
    - Test 17 (test_viled_stats_builder.py::test_viled_stats_keys_count): len(VILED_STATS_KEYS) == 9
  </behavior>
  <action>
**Step A — Refactor `src/ga_crawler/runner/gates.py`** — APPEND new helpers + parse_quality_gate; KEEP old `final_m_gate` and `auto_suggest_m` as backward-compat shims:

Read the existing file first; preserve all current function bodies. Then APPEND these (NEW or REPLACING `auto_suggest_m` / `final_m_gate` with shim impls):

```python
# ---- D-203 retailer-agnostic refactor ----

def auto_suggest_threshold(
    history_counts: list[int],
    factor: float = 0.7,
    min_runs: int = 4,
) -> Optional[int]:
    """Retailer-agnostic auto-suggest. Returns int(factor × median(last min_runs counts)).

    Returns None if history < min_runs.

    NOTE on float arithmetic (STATE.md plan 03-05 lesson): `int(0.7 * value)` truncates
    toward 0 due to IEEE 754; documented behavior. Don't pre-compute test expectations.

    Source: 02-RESEARCH.md §"auto_suggest_threshold refactor" lines 980-998.
    Decision: D-203.
    """
    import statistics
    if len(history_counts) < min_runs:
        return None
    last = history_counts[-min_runs:]
    return int(factor * statistics.median(last))


def final_threshold_gate(count: int, threshold: int) -> bool:
    """Retailer-agnostic. count >= threshold → True (gate PASSES).

    Source: 02-RESEARCH.md §"final_threshold_gate refactor" lines 1004-1014.
    """
    return count >= threshold


# ---- Phase 3 backward-compat shims (DO NOT REMOVE) ----

def auto_suggest_m(history_counts: list[int]) -> Optional[int]:
    """Phase 3 shim. Forwards to auto_suggest_threshold(history, 0.7, 4)."""
    return auto_suggest_threshold(history_counts, factor=0.7, min_runs=4)


def final_m_gate(goldapple_count: int, M: int = 1000) -> bool:
    """Phase 3 shim. Forwards to final_threshold_gate(count, M)."""
    return final_threshold_gate(goldapple_count, M)


def final_n_gate(viled_count: int, N: int = 100) -> bool:
    """Phase 2 viled-side sanity gate. Forwards to final_threshold_gate(count, N).
    Decision: D-201 seed N=100."""
    return final_threshold_gate(viled_count, N)


# ---- D-218 parse-quality gate ----

def parse_quality_gate(
    null_rate_required_fields: float,
    *,
    threshold: float = 0.05,
) -> bool:
    """D-218: returns True iff null_rate <= threshold (gate PASSES).

    null_rate_required_fields = (rows where name OR current_price OR url is NULL)
                              / total_count

    >5% null rate → run marked failed with reason='parse_quality_below_threshold'.

    Source: 02-CONTEXT.md D-218; 02-RESEARCH.md §Pattern 1 PARSE-05.
    """
    return null_rate_required_fields <= threshold
```

If the file currently *defines* `auto_suggest_m` and `final_m_gate` as concrete bodies (not shims), REPLACE those bodies with the shim forms above. Read the file first to determine which.

**Step B — Extend `src/ga_crawler/runner/stats.py`** — APPEND VILED_STATS_KEYS + ViledStatsBuilder; do NOT modify GOLDAPPLE_STATS_KEYS or GoldappleStatsBuilder:

```python
# ---- Phase 2 viled.* namespace ----

VILED_STATS_KEYS: tuple[str, ...] = (
    "viled.fetch_count",
    "viled.fetch_failures",
    "viled.parse_failures",
    "viled.fetch_duration_seconds",
    "viled.mean_fetch_seconds",
    "viled.sanity_gate_n_pass",
    "viled.parse_quality_pass",
    "viled.null_rate_required_fields",
    "viled.auto_suggest_n",
)

_VILED_BARE_TO_NAMESPACED: dict[str, str] = {
    k.split(".", 1)[1]: k for k in VILED_STATS_KEYS
}


class ViledStatsBuilder:
    """Mirror of GoldappleStatsBuilder — viled.* namespace.

    Source: 02-RESEARCH.md §Open Q7; 02-PATTERNS.md lines 532-589.
    """

    def __init__(self) -> None:
        self.delta: dict[str, Any] = {}

    def _resolve(self, bare_key: str) -> str:
        if bare_key in _VILED_BARE_TO_NAMESPACED:
            return _VILED_BARE_TO_NAMESPACED[bare_key]
        if bare_key in VILED_STATS_KEYS:
            return bare_key
        raise StatsNamespaceError(
            f"key {bare_key!r} not in VILED_STATS_KEYS; "
            f"allowed: {sorted(VILED_STATS_KEYS)}"
        )

    def set(self, bare_key: str, value: Any) -> None:
        full = self._resolve(bare_key)
        self.delta[full] = value
```

**Step C — Tests** (REMOVE skip markers from each):

`tests/unit/test_auto_suggest_threshold.py`:
```python
"""D-203 retailer-agnostic auto-suggest verification."""
from ga_crawler.runner.gates import (
    auto_suggest_threshold, auto_suggest_m, final_m_gate, final_n_gate,
)


def test_returns_int_factor_median():
    # median([100,200,300,400]) = 250; int(0.7 * 250) = 175
    assert auto_suggest_threshold([100, 200, 300, 400], 0.7, 4) == 175


def test_min_runs_guard():
    assert auto_suggest_threshold([100, 200, 300], 0.7, 4) is None


def test_only_last_min_runs_used():
    """First entry should NOT skew the median."""
    result = auto_suggest_threshold([99999, 100, 200, 300, 400], 0.7, 4)
    # median of last 4 = 250; int(0.7 * 250) = 175
    assert result == 175


def test_backward_compat_auto_suggest_m():
    """Phase 3 shim: auto_suggest_m == auto_suggest_threshold(h, 0.7, 4)."""
    history = [1000, 1000, 1000, 1000]
    assert auto_suggest_m(history) == auto_suggest_threshold(history, 0.7, 4)


def test_backward_compat_final_m_gate():
    assert final_m_gate(1500, 1000) is True
    assert final_m_gate(500, 1000) is False


def test_final_n_gate_seed():
    """D-201 N=100 seed; final_n_gate is the viled-side shim."""
    assert final_n_gate(150, 100) is True
    assert final_n_gate(99, 100) is False
```

`tests/unit/test_sanity_n_gate.py`:
```python
"""CRAWL-05 + D-201 verification — final_threshold_gate."""
from ga_crawler.runner.gates import final_threshold_gate


def test_above_threshold_passes():
    assert final_threshold_gate(150, 100) is True


def test_below_threshold_fails():
    assert final_threshold_gate(50, 100) is False


def test_equal_threshold_passes():
    """>= semantics."""
    assert final_threshold_gate(100, 100) is True


def test_zero_count_below_any_positive_threshold():
    assert final_threshold_gate(0, 100) is False
```

`tests/unit/test_parse_quality_gate.py`:
```python
"""PARSE-05 + D-218 aggregate gate."""
from ga_crawler.runner.gates import parse_quality_gate


def test_low_null_rate_passes():
    assert parse_quality_gate(0.03) is True


def test_high_null_rate_fails():
    assert parse_quality_gate(0.08) is False


def test_threshold_boundary():
    assert parse_quality_gate(0.05) is True   # ≤ semantics
    assert parse_quality_gate(0.0501) is False


def test_zero_null_rate_passes():
    assert parse_quality_gate(0.0) is True


def test_custom_threshold():
    assert parse_quality_gate(0.10, threshold=0.10) is True
    assert parse_quality_gate(0.11, threshold=0.10) is False
```

`tests/unit/test_viled_stats_builder.py`:
```python
"""Open Q7 — VILED_STATS_KEYS namespace + ViledStatsBuilder."""
import pytest

from ga_crawler.runner.stats import (
    StatsNamespaceError, ViledStatsBuilder, VILED_STATS_KEYS,
    GoldappleStatsBuilder, GOLDAPPLE_STATS_KEYS,
)


def test_viled_stats_keys_count():
    assert len(VILED_STATS_KEYS) == 9


def test_viled_stats_keys_all_namespaced():
    for k in VILED_STATS_KEYS:
        assert k.startswith("viled.")


def test_set_resolves_bare_key():
    b = ViledStatsBuilder()
    b.set("fetch_count", 100)
    assert b.delta == {"viled.fetch_count": 100}


def test_set_namespaced_key_passes():
    b = ViledStatsBuilder()
    b.set("viled.parse_failures", 2)
    assert b.delta == {"viled.parse_failures": 2}


def test_set_unknown_key_raises():
    b = ViledStatsBuilder()
    with pytest.raises(StatsNamespaceError):
        b.set("nonsense", 1)


def test_delta_separated_from_goldapple():
    b = ViledStatsBuilder()
    with pytest.raises(StatsNamespaceError):
        b.set("goldapple.fetch_count", 100)


def test_existing_goldapple_unchanged():
    """Phase 3 GoldappleStatsBuilder must still accept its keys."""
    g = GoldappleStatsBuilder()
    g.set("fetch_count", 100)  # bare-key-resolved to goldapple.fetch_count
    assert g.delta.get("goldapple.fetch_count") == 100
    assert len(GOLDAPPLE_STATS_KEYS) >= 9  # Phase 3 baseline
```

**Step D — Run** all 4 test files + full suite:

```bash
uv run pytest tests/unit/test_auto_suggest_threshold.py tests/unit/test_sanity_n_gate.py tests/unit/test_parse_quality_gate.py tests/unit/test_viled_stats_builder.py -x -q
uv run pytest -m "not live" -q
```

Verify Phase 3's existing tests for goldapple gates/stats still green (no regression from refactor).
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_auto_suggest_threshold.py tests/unit/test_sanity_n_gate.py tests/unit/test_parse_quality_gate.py tests/unit/test_viled_stats_builder.py -x -q 2>&1 | tail -5 && grep -q "def auto_suggest_threshold" src/ga_crawler/runner/gates.py && grep -q "def parse_quality_gate" src/ga_crawler/runner/gates.py && grep -q "VILED_STATS_KEYS" src/ga_crawler/runner/stats.py && grep -q "class ViledStatsBuilder" src/ga_crawler/runner/stats.py && grep -q "GOLDAPPLE_STATS_KEYS" src/ga_crawler/runner/stats.py && uv run pytest -m "not live" -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def auto_suggest_threshold" src/ga_crawler/runner/gates.py`
    - `grep -q "def final_threshold_gate" src/ga_crawler/runner/gates.py`
    - `grep -q "def parse_quality_gate" src/ga_crawler/runner/gates.py`
    - `grep -q "def auto_suggest_m" src/ga_crawler/runner/gates.py` (Phase 3 shim retained)
    - `grep -q "def final_m_gate" src/ga_crawler/runner/gates.py` (Phase 3 shim retained)
    - `grep -q "def final_n_gate" src/ga_crawler/runner/gates.py` (Phase 2 shim added)
    - `grep -q "VILED_STATS_KEYS" src/ga_crawler/runner/stats.py`
    - `grep -q "class ViledStatsBuilder" src/ga_crawler/runner/stats.py`
    - `grep -q "GOLDAPPLE_STATS_KEYS" src/ga_crawler/runner/stats.py` (Phase 3 untouched)
    - `grep -q "class GoldappleStatsBuilder" src/ga_crawler/runner/stats.py` (Phase 3 untouched)
    - 4 test files no longer carry pytest.mark.skip
    - `pytest tests/unit/test_auto_suggest_threshold.py tests/unit/test_sanity_n_gate.py tests/unit/test_parse_quality_gate.py tests/unit/test_viled_stats_builder.py -x -q` exits 0
    - `pytest -m "not live" -q` exits 0
  </acceptance_criteria>
  <done>gates.py refactored: D-203 retailer-agnostic helpers + 3 backward-compat shims + parse_quality_gate (D-218); stats.py extends with VILED_STATS_KEYS + ViledStatsBuilder mirror; tests across 4 files all GREEN; Phase 3 GoldappleStatsBuilder untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: viled_run orchestrator + main_run composition + integration tests</name>
  <files>src/ga_crawler/runners/viled_run.py, src/ga_crawler/runners/main_run.py, tests/integration/test_viled_run_e2e_with_real_storage.py, tests/integration/test_main_run_e2e.py</files>
  <read_first>
    src/ga_crawler/runners/goldapple_run.py (FULL — orchestrator template; do NOT modify),
    src/ga_crawler/storage/sqlite.py (FULL — SqliteRunWriter + SqliteSnapshotWriter from Plan 02),
    src/ga_crawler/storage/norm06_writer.py (FULL — Norm06Writer.persist signature),
    src/ga_crawler/normalizers/facade.py, src/ga_crawler/alias/yaml_loader.py,
    src/ga_crawler/parsers/dispatcher.py, src/ga_crawler/fetchers/viled.py,
    src/ga_crawler/enumeration/viled_catalog.py, src/ga_crawler/config.py,
    .planning/phases/02-project-skeleton-viled-crawl-storage/02-RESEARCH.md (System Architecture Diagram lines 186-242),
    .planning/phases/02-project-skeleton-viled-crawl-storage/02-PATTERNS.md (lines 275-348 — viled_run analog),
    .planning/phases/02-project-skeleton-viled-crawl-storage/02-CONTEXT.md (D-218 sequential gates; D-221 brand-list provenance; D-211 norm06 write-path)
  </read_first>
  <behavior>
    - test_viled_run_e2e_with_real_storage.py::test_happy_path: with mocked fetch_catalog_urls (10 URLs) + mocked fetcher returning synthetic __NEXT_DATA__ html, run_viled_phase produces ViledPhaseResult(status=success, viled_count=10) and writes 10 rows with retailer=viled.
    - test_viled_run_e2e_with_real_storage.py::test_sanity_n_gate_fails: viled_count=5, N=100 → run failed; reason mentions sanity_gate_n; snapshot table still has 5 rows (audit trail).
    - test_viled_run_e2e_with_real_storage.py::test_atomic_stats_merge_pitfall_6: spy on RunWriter.patch_stats — exactly 1 call on success path with viled.* keys merged.
    - test_viled_run_e2e_with_real_storage.py::test_per_sku_isolation: fetcher skips one record mid-loop; other 9 still inserted; stats.fetch_failures=1; run still completes.
    - test_main_run_e2e.py::test_full_run_cycle_viled_only: run_weekly(viled_only=True) → status=success; v_current_snapshots populated; .planning/runs/{N}/norm06-review.md exists.
    - test_main_run_e2e.py::test_viled_failure_blocks_goldapple: viled_run returns failed → goldapple_run NOT invoked; runs.status=failed.
    - test_main_run_e2e.py::test_crash_finalizes_run: inject Exception in middle of viled_run → run_writer.fail invoked from except (DATA-05 invariant); status=failed with error reason.
  </behavior>
  <action>
**Step A — Create `src/ga_crawler/runners/viled_run.py`** (mirror of goldapple_run.py, sync inside).

Imports: time, dataclass/field, Callable/Optional, structlog. From ga_crawler: ViledConfig (config.py), ViledFetcher (fetchers/viled.py), fetch_catalog_urls (enumeration/viled_catalog.py), detect_multipack (normalizers/volume.py), ParseDispatcher (parsers/dispatcher.py), auto_suggest_threshold + final_threshold_gate + parse_quality_gate (runner/gates.py), ViledStatsBuilder (runner/stats.py).

Define `@dataclass class ViledPhaseResult` with `status: str`, `viled_count: int = 0`, `reason: Optional[str] = None`, `stats_delta: dict = field(default_factory=dict)`.

Define `_compute_null_rate(records: list[dict]) -> float` per D-218: count rows where `name` OR `current_price` IS NULL OR `url` IS NULL, divide by len. Returns 0.0 if records empty.

Define `_normalize_record(parsed: dict, normalizer) -> dict`: build SnapshotWriter dict mirroring goldapple_run.py:237-250 shape but EXTENDED with `multipack_flag` (via `detect_multipack`), `volume_raw` (the raw_volume_text passthrough), `parse_error_flag=False`. Maps `parsed["raw_volume_text"]` OR `parsed["name"]` for raw_volume_text; `volume_norm = normalizer.volume(...)`; coerced to `str()` when not None.

Define `def run_viled_phase(*, run_id, config, brand_alias, normalizer, snapshot_writer, run_writer, dispatcher=None, fetcher=None, fetch_catalog=None) -> ViledPhaseResult` implementing 8 steps:

1. Default-construct ParseDispatcher / ViledFetcher / fetch_catalog_urls if None passed (injectable for testing).
2. Builder: `builder = ViledStatsBuilder()`; `started = time.perf_counter()`.
3. Steps 1+2: `for catalog_base in config.catalog_urls: urls = fetch_catalog(catalog_base, pause_seconds=config.pause_seconds); all_urls.extend(urls)`.
4. Step 3 fetch loop: `stats={}; fetched = fetcher.run_loop(all_urls, stats, sleep_fn=time.sleep); builder.set("fetch_count", stats.get("fetch_count", 0)); builder.set("fetch_failures", stats.get("fetch_failures", 0))`.
5. Steps 4+5 parse + normalize: `for rec in fetched: parsed = dispatcher.dispatch("viled", rec.get("html", ""), rec.get("url", ""))`; on None → `parse_failures += 1; continue`; else append `_normalize_record(parsed, normalizer)`. Record `builder.set("parse_failures", parse_failures)`.
6. Step 6 persist: `if snapshot_rows: inserted = snapshot_writer.append(run_id, "viled", snapshot_rows)`. Log `snapshots_persisted`.
7. Stats decoration: `elapsed = time.perf_counter() - started; builder.set("fetch_duration_seconds", round(elapsed, 2))`; if fetch_count > 0 → `builder.set("mean_fetch_seconds", round(elapsed/fetch_count, 3))`.
8. Step 7a parse-quality gate FIRST (D-218): `null_rate = _compute_null_rate(snapshot_rows); builder.set("null_rate_required_fields", round(null_rate, 4))`; `parse_quality_pass = parse_quality_gate(null_rate); builder.set("parse_quality_pass", 1 if parse_quality_pass else 0)`. If NOT pass → `reason = f"parse_quality_below_threshold (null_rate={null_rate:.4f})"`; `run_writer.fail(run_id, reason); run_writer.patch_stats(run_id, dict(builder.delta))`; return ViledPhaseResult(status="failed", ...).
9. Step 7b sanity-N gate SECOND (D-201): `sanity_pass = final_threshold_gate(inserted, config.sanity_gate_n); builder.set("sanity_gate_n_pass", 1 if sanity_pass else 0)`. If NOT pass → `reason = f"viled_count {inserted} < N={config.sanity_gate_n}"`; `run_writer.fail(...); run_writer.patch_stats(...)`; return failed.
10. Step 7c auto-suggest (informational): `history = []` (orchestrator main_run.py supplies multi-run history if needed); `suggested = auto_suggest_threshold(history, factor=config.n_auto_suggest_factor, min_runs=config.n_auto_suggest_after_runs)`; if not None → `builder.set("auto_suggest_n", suggested)`.
11. Step 8 atomic stats merge (Pitfall 6): `run_writer.patch_stats(run_id, dict(builder.delta))` — SINGLE CALL on success path.
12. Return `ViledPhaseResult(status="success", viled_count=inserted, stats_delta=dict(builder.delta))`.

**Step B — Create `src/ga_crawler/runners/main_run.py`**:

Imports: asyncio, traceback, dataclass/field, Path, Optional, structlog, sqlalchemy.text, sqlmodel.Session. From ga_crawler: YamlBrandAlias, ViledConfig, Normalizer, run_viled_phase, Norm06Writer, SqliteRunWriter, SqliteSnapshotWriter, init_db, make_engine.

Define `@dataclass class MainRunResult`: status, run_id, viled_count=0, goldapple_count=0, reason=None, norm06_path=None, stats_delta=field(default_factory=dict).

Define `_derive_viled_brands_from_snapshots(engine, run_id) -> list[str]` (D-221): execute `SELECT DISTINCT brand_norm FROM snapshots WHERE retailer='viled' AND run_id=:rid`; return sorted set with empty-strings filtered.

Define `run_weekly(repo_root, *, db_path='prices.db', headless=True, viled_only=False, goldapple_only=False, sanity_gate_n=None, sanity_gate_m=None, aliases_path=None, pyproject_path='pyproject.toml') -> MainRunResult`:

- repo_root coerced to Path; aliases_path defaults to `repo_root/'config'/'brand-aliases.yaml'`
- `init_db(db_path)`; `engine = make_engine(db_path)`
- Construct `run_writer = SqliteRunWriter(engine)`, `snapshot_writer = SqliteSnapshotWriter(engine)`, `brand_alias = YamlBrandAlias(aliases_path)`, `normalizer = Normalizer(brand_alias)`, `config = ViledConfig.from_pyproject(pyproject_path)`. If sanity_gate_n override → reconstruct ViledConfig with that field swapped.
- `run_id = run_writer.create()`; log `weekly_run_started`.
- TRY block:
  - If not goldapple_only: `v_result = run_viled_phase(run_id=run_id, config=config, brand_alias=brand_alias, normalizer=normalizer, snapshot_writer=snapshot_writer, run_writer=run_writer)`; `viled_count = v_result.viled_count; stats_delta_acc.update(v_result.stats_delta)`. If `v_result.status == "failed"`: `Norm06Writer(repo_root).persist(run_id, [], [])`; return MainRunResult(status="failed", reason=v_result.reason, ...).
  - If not viled_only: `from ga_crawler.runners.goldapple_run import run_goldapple_phase`; `viled_brands = _derive_viled_brands_from_snapshots(engine, run_id)`; log `goldapple_phase_starting`; if sanity_gate_m override → pass `M=sanity_gate_m`; `g_result = asyncio.run(run_goldapple_phase(run_id=run_id, viled_brands=viled_brands, repo_root=repo_root, brand_alias=brand_alias, normalizer=normalizer, snapshot_writer=snapshot_writer, run_writer=run_writer, headless=headless, **m_kwargs))`; collect goldapple_count, viled_unmatched, goldapple_new_slugs. If failed → `Norm06Writer(repo_root).persist(run_id, viled_unmatched, goldapple_new_slugs)`; return failed.
  - `norm06_path = Norm06Writer(repo_root).persist(run_id, viled_unmatched, goldapple_new_slugs)` (D-211: Phase 2 owns write-path).
  - `run_writer.finalize(run_id, status="success")`; return `MainRunResult(status="success", ..., norm06_path=norm06_path)`.
- EXCEPT Exception as e:
  - `tb = traceback.format_exc(); reason = f"{type(e).__name__}: {e}"`
  - `log.error("weekly_run_crashed", run_id=run_id, error=reason, traceback=tb)`
  - `run_writer.fail(run_id, reason)` (idempotent — DATA-05 invariant)
  - try: `Norm06Writer(repo_root).persist(run_id, [], [])`; except: pass
  - return `MainRunResult(status="failed", run_id=run_id, reason=reason)`

**Step C — Tests** (REMOVE skip markers from each):

`tests/integration/test_viled_run_e2e_with_real_storage.py`:
- `_synthetic_pdp(price=10000, brand="Givenchy", name="Eau de Parfum 50 ml")`: returns minimal HTML with `<script id="__NEXT_DATA__">` carrying `props.pageProps.{item: {name, brandName}, attributes: [{realPrice, price, currency: "₸", in_stock: True}]}`.
- `setup` fixture: tmp_path db + init_db + engine + SqliteRunWriter + SqliteSnapshotWriter + YamlBrandAlias(brand_alias_yaml_fixture) + Normalizer + run_writer.create() + ViledConfig(sanity_gate_n=5, pause_seconds=0, catalog_urls=("https://viled.kz/men/catalog/1310",)).
- `_fake_catalog`: returns 10 fake URLs.
- `_FakeFetcher`: __init__ noop; run_loop sets stats["fetch_count"]=len(urls); returns 10 records with html=_synthetic_pdp().
- `test_happy_path`: assert result.status=="success", result.viled_count==10; SELECT count(*) from snapshots returns 10.
- `test_sanity_n_gate_fails`: ViledConfig(sanity_gate_n=100); fetcher returns 5; assert result.status=="failed"; "100" in reason or "sanity_gate_n" in reason; Run row status='failed'; snapshots count == 5 (audit trail).
- `test_atomic_stats_merge_pitfall_6`: spy_calls=[]; rw.patch_stats wrapped; assert len(spy_calls)==1 and "viled.fetch_count" + "viled.sanity_gate_n_pass" in spy_calls[0] (Pitfall 6).
- `test_per_sku_isolation`: FlakyFetcher.run_loop sets stats["fetch_failures"]=1 and skips index 5 (returns 9 records); assert result.status=="success", viled_count==9, stats_delta["viled.fetch_failures"]==1.

`tests/integration/test_main_run_e2e.py`:
- `setup` fixture: tmp_path repo_root with config/brand-aliases.yaml planted from brand_alias_yaml_fixture; pyproject.toml planted with [tool.ga_crawler.crawl.viled] (sanity_gate_n=5, pause_seconds=0.0, catalog_urls list, etc.); db_path=repo_root/prices.db.
- `test_full_run_cycle_viled_only`: patch ga_crawler.runners.viled_run.fetch_catalog_urls + ga_crawler.fetchers.viled.ViledFetcher; run_weekly(repo_root, db_path, viled_only=True, pyproject_path=...); assert result.status=='success', viled_count==10, norm06_path exists; Run row status='success' AND finished_at is not None.
- `test_viled_failure_blocks_goldapple`: patch with TooFewFetcher returning 2 records + sanity_gate_n=100 override; spy on run_goldapple_phase; assert result.status=='failed' and goldapple_called["yes"] is False.
- `test_crash_finalizes_run`: patch fetch_catalog_urls to raise RuntimeError("simulated crash"); run_weekly(viled_only=True); assert result.status=='failed' and "simulated crash" in result.reason; Run row status='failed' with fail_reason containing "simulated crash".

**Step D — Run** integration tests + full suite:

```bash
uv run pytest tests/integration/test_viled_run_e2e_with_real_storage.py tests/integration/test_main_run_e2e.py -x -q
uv run pytest -m "not live" -q
```
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_viled_run_e2e_with_real_storage.py tests/integration/test_main_run_e2e.py -x -q 2>&1 | tail -5 && grep -q "class ViledPhaseResult" src/ga_crawler/runners/viled_run.py && grep -q "parse_quality_gate" src/ga_crawler/runners/viled_run.py && grep -q "final_threshold_gate" src/ga_crawler/runners/viled_run.py && grep -q "patch_stats" src/ga_crawler/runners/viled_run.py && grep -q "def run_weekly" src/ga_crawler/runners/main_run.py && grep -q "Norm06Writer" src/ga_crawler/runners/main_run.py && grep -q "from ga_crawler.runners.goldapple_run import" src/ga_crawler/runners/main_run.py && grep -q "DISTINCT brand_norm" src/ga_crawler/runners/main_run.py && uv run pytest -m "not live" -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "class ViledPhaseResult" src/ga_crawler/runners/viled_run.py`
    - `grep -q "parse_quality_gate" src/ga_crawler/runners/viled_run.py` (D-218)
    - `grep -q "final_threshold_gate" src/ga_crawler/runners/viled_run.py` (CRAWL-05)
    - test_atomic_stats_merge_pitfall_6 verifies 1 patch_stats call on success path
    - `grep -q "Norm06Writer" src/ga_crawler/runners/main_run.py`
    - `grep -q "DISTINCT brand_norm" src/ga_crawler/runners/main_run.py` (D-221)
    - `grep -q "from ga_crawler.runners.goldapple_run import" src/ga_crawler/runners/main_run.py`
    - `grep -q "run_writer.fail" src/ga_crawler/runners/main_run.py` (DATA-05 lifecycle)
    - `git diff src/ga_crawler/runners/goldapple_run.py` empty (FROZEN)
    - `git diff src/ga_crawler/interfaces.py` empty (FROZEN)
    - `pytest tests/integration/test_viled_run_e2e_with_real_storage.py tests/integration/test_main_run_e2e.py -x` exits 0
    - 2 test files no longer carry pytest.mark.skip
    - `pytest -m "not live"` exits 0
  </acceptance_criteria>
  <done>viled_run orchestrator implements 8-step pipeline with sequential parse-quality + sanity-N gates and SINGLE atomic patch_stats; main_run composes viled→goldapple→Norm06→finalize with try/except DATA-05 lifecycle; D-221 brand-list derived from v_current_snapshots; integration tests GREEN; Phase 3 frozen modules untouched.</done>
</task>

<task type="auto">
  <name>Task 3: cli.py cutover — DELETE 4 stubs + DELETE goldapple-run + ADD weekly-run subcommand</name>
  <files>src/ga_crawler/cli.py</files>
  <read_first>
    src/ga_crawler/cli.py (FULL — current state with stubs and goldapple-run; D-212 cutover target),
    src/ga_crawler/runners/main_run.py (FULL — run_weekly signature),
    .planning/phases/02-project-skeleton-viled-crawl-storage/02-CONTEXT.md (D-212 stub deletion),
    .planning/phases/02-project-skeleton-viled-crawl-storage/02-PATTERNS.md (lines 595-628 — cutover plan)
  </read_first>
  <action>
**Step A — Replace `src/ga_crawler/cli.py` entirely** with a clean module that:

1. Module docstring documents D-212 cutover (stubs deleted, goldapple-run removed, weekly-run added, goldapple-smoke kept).
2. Imports: argparse, asyncio, json, sys, Path, Optional, structlog. From ga_crawler: GoldappleFetcher (fetchers/goldapple.py — KEEP), smoke_probe (runner/gates.py — KEEP).
3. **DELETE all 4 Stub classes** from current cli.py (StubBrandAlias, StubNormalizer, StubSnapshotWriter, StubRunWriter).
4. **KEEP `_cmd_smoke` verbatim** from current cli.py (Phase 3 `_cmd_smoke` at lines 126-130).
5. **DELETE `_cmd_run`** function (Phase 3 stub-bound handler).
6. **ADD `_cmd_weekly(args) -> int`** that:
   - Imports `from ga_crawler.runners.main_run import run_weekly` lazily.
   - Calls `run_weekly(repo_root=Path(args.repo_root).resolve(), db_path=args.db_path, headless=args.headless, viled_only=args.viled_only, goldapple_only=args.goldapple_only, sanity_gate_n=args.sanity_gate_n, sanity_gate_m=args.sanity_gate_m)`.
   - Prints JSON dict with status, run_id, viled_count, goldapple_count, reason, norm06_path (str or None), stats_delta_keys (sorted list).
   - Returns 0 on success, 2 on failure.
7. **KEEP `_configure_logging` and `_parse_bool` verbatim** from current cli.py.
8. **REPLACE `main()` function**:
   - argparse with `--prog="python -m ga_crawler"`, description="GA Crawler — Phase 2 (production weekly run)"
   - subparsers required=True
   - `goldapple-smoke` subcommand: --run-id (int, default=999), --headless (_parse_bool, default=True). KEEP help text from current.
   - `weekly-run` subcommand: --repo-root (default="."), --db-path (default="prices.db"), --sanity-gate-n (int, default=None), --sanity-gate-m (int, default=None), --headless (_parse_bool, default=True), --viled-only (action="store_true"), --goldapple-only (action="store_true"). Each with helpful description.
   - **REMOVE the `goldapple-run` subcommand registration AND its handler dispatch**.
   - Dispatch: if cmd == "goldapple-smoke" → asyncio.run(_cmd_smoke(args)); if cmd == "weekly-run" → _cmd_weekly(args); else → print_help + return 2.
9. KEEP `if __name__ == "__main__": sys.exit(main())` at bottom.

**Step B — Verify no Phase 3 test directly imports deleted Stubs.** Run `grep -r "StubBrandAlias\|StubNormalizer\|StubSnapshotWriter\|StubRunWriter" tests/`. Expected output: empty (Phase 3 tests use conftest.py mock_* fixtures per D-212). If any test does import a Stub directly from `ga_crawler.cli`, FIX it inline (replace with conftest mock fixture) and document in summary.

**Step C — Run** full suite:

```bash
uv run pytest -m "not live" -q
```

If any cascading test fails, fix it.

**Step D — Smoke-test the CLI**:

```bash
uv run python -m ga_crawler --help
# Must show: goldapple-smoke, weekly-run (NOT goldapple-run)
uv run python -m ga_crawler weekly-run --help
# Must show all 7 weekly-run flags
```
  </action>
  <verify>
    <automated>grep -v '^#' src/ga_crawler/cli.py | grep -c "class Stub" | grep -q "^0$" && grep -v '^#' src/ga_crawler/cli.py | grep -c "goldapple-run" | grep -q "^0$" && grep -q "weekly-run" src/ga_crawler/cli.py && grep -q "goldapple-smoke" src/ga_crawler/cli.py && grep -q "from ga_crawler.runners.main_run import run_weekly" src/ga_crawler/cli.py && uv run python -m ga_crawler --help 2>&1 | grep -q "weekly-run" && uv run python -m ga_crawler --help 2>&1 | grep -q "goldapple-smoke" && uv run pytest -m "not live" -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' src/ga_crawler/cli.py | grep -c "class Stub"` == 0 (D-212 stub deletion)
    - `grep -v '^#' src/ga_crawler/cli.py | grep -c "goldapple-run"` == 0 (subcommand removed)
    - `grep -q "weekly-run" src/ga_crawler/cli.py` (new subcommand)
    - `grep -q "goldapple-smoke" src/ga_crawler/cli.py` (kept verbatim)
    - `grep -q "from ga_crawler.runners.main_run import run_weekly" src/ga_crawler/cli.py`
    - `python -m ga_crawler --help` shows `weekly-run` and `goldapple-smoke`, NOT `goldapple-run`
    - `python -m ga_crawler weekly-run --help` shows --sanity-gate-n, --sanity-gate-m, --viled-only, --goldapple-only flags
    - `pytest -m "not live" -q` exits 0 (cascading test fixes documented in summary if any)
    - `git diff src/ga_crawler/runners/goldapple_run.py` empty (FROZEN)
    - `git diff src/ga_crawler/interfaces.py` empty (FROZEN)
  </acceptance_criteria>
  <done>4 Stub classes deleted (D-212); goldapple-run subcommand removed; weekly-run subcommand added with 7 flags; goldapple-smoke kept; --help output validated; full pytest suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viled_run + goldapple_run → runs.stats | Both phases write to same row stats; concurrent merge must be atomic |
| Phase 3 backward-compat shims | auto_suggest_m, final_m_gate still imported by Phase 3 modules — refactor must not break them |
| Operator CLI → run_weekly | --sanity-gate-n / --sanity-gate-m overrides config |
| try/except on uncaught exception → run_writer.fail | DATA-05 invariant: every code path closes the run row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | Sanity gate threshold misconfigured (operator sets N=0) | mitigate | parse_quality_gate runs FIRST regardless of N — bad parse-quality run still fails. Production cron uses pyproject defaults (sanity_gate_n=100). |
| T-02-06 | Tampering | viled_run + goldapple_run race on runs.stats | mitigate | Pitfall 6: json_patch is atomic SQL; ViledStatsBuilder + GoldappleStatsBuilder share NO keys. Tested in test_atomic_stats_merge_pitfall_6. |
| T-02-DATA05 | Repudiation | Crash mid-run leaves runs.status='running' forever | mitigate | main_run.py wraps body in try/except; uncaught Exception → run_writer.fail(stack-trace). Idempotent fail() catches double-call. test_crash_finalizes_run verifies. |
| T-02-Phase3-Regression | Tampering | gates.py refactor breaks Phase 3 callers | mitigate | Backward-compat shims auto_suggest_m, final_m_gate retained. Phase 3 tests stay green (verified by pytest -m not live). |
| T-02-D212-Cascade | Tampering | Phase 3 test files importing deleted Stub classes break | mitigate | Phase 3 plan 03-06 SUMMARY documents that all e2e tests use conftest.py mocks. Cascading fix applied in Task 3 if any direct import surfaces. |
</threat_model>

<verification>
1. `grep -q "def auto_suggest_threshold" src/ga_crawler/runner/gates.py` (D-203)
2. `grep -q "def final_threshold_gate" src/ga_crawler/runner/gates.py`
3. `grep -q "def parse_quality_gate" src/ga_crawler/runner/gates.py` (D-218)
4. `grep -q "def auto_suggest_m" src/ga_crawler/runner/gates.py` (Phase 3 shim retained)
5. `grep -q "VILED_STATS_KEYS" src/ga_crawler/runner/stats.py` AND `grep -q "class ViledStatsBuilder" src/ga_crawler/runner/stats.py`
6. `grep -q "class ViledPhaseResult" src/ga_crawler/runners/viled_run.py`
7. `grep -q "def run_weekly" src/ga_crawler/runners/main_run.py`
8. `grep -q "DISTINCT brand_norm" src/ga_crawler/runners/main_run.py` (D-221)
9. `grep -v '^#' src/ga_crawler/cli.py | grep -c "class Stub"` == 0 (D-212)
10. `grep -v '^#' src/ga_crawler/cli.py | grep -c "goldapple-run"` == 0
11. `python -m ga_crawler --help` shows weekly-run + goldapple-smoke
12. `pytest -m "not live"` exits 0
13. `git diff src/ga_crawler/runners/goldapple_run.py src/ga_crawler/interfaces.py` empty (FROZEN)
</verification>

<success_criteria>
- runner/gates.py: D-203 retailer-agnostic helpers (auto_suggest_threshold, final_threshold_gate, parse_quality_gate) + 3 backward-compat shims (auto_suggest_m, final_m_gate, final_n_gate)
- runner/stats.py: VILED_STATS_KEYS (9 keys) + ViledStatsBuilder mirror; GOLDAPPLE_STATS_KEYS + GoldappleStatsBuilder UNCHANGED
- runners/viled_run.py: run_viled_phase implements 8-step pipeline with sequential parse-quality + sanity-N gates; SINGLE atomic patch_stats call (Pitfall 6)
- runners/main_run.py: run_weekly composes viled→goldapple→Norm06Writer.persist→finalize; DATA-05 try/except lifecycle; D-221 brand-list derived from v_current_snapshots
- cli.py: 4 Stub classes DELETED (D-212), goldapple-run subcommand DELETED, weekly-run subcommand ADDED with 7 flags, goldapple-smoke KEPT verbatim
- 6 new test files all GREEN (4 unit + 2 integration)
- Existing 192 + Plans 02-04 tests all green; Phase 3 frozen modules untouched
- python -m ga_crawler --help shows new + kept subcommands; goldapple-run is gone
- Pitfall 6 verified: viled_run calls run_writer.patch_stats exactly 1 time on success path
- DATA-05 verified: simulated crash → run_writer.fail invoked → runs.status='failed' with reason
</success_criteria>

<output>
After completion, create `.planning/phases/02-project-skeleton-viled-crawl-storage/02-05-SUMMARY.md`:
- Modules shipped/modified + LOC counts; stubs deleted + LOC reduction
- Test count delta; any cascading Phase 3 test fixes documented
- Confirmation of CLI output: `python -m ga_crawler --help` listing
- Confirmation: Pitfall 6 verified (single patch_stats call); DATA-05 verified (crash path)
- Cascading constraint for Plan 06: brand-aliases.yaml seed + backup script + final E2E verification
</output>
