---
phase: 02
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - tests/conftest.py
  - tests/fixtures/viled/viled-pdp-discounted.html
  - tests/fixtures/viled/viled-pdp-out-of-stock.html
  - tests/fixtures/viled/viled-pdp-multipack.html
  - tests/fixtures/viled/viled-catalog-men-1310-page1.html
  - tests/fixtures/viled/viled-nextdata-shape.json
  - tests/fixtures/viled/brand-aliases-fixture.yaml
  - tests/fixtures/normalize/volume-corpus.yaml
  - tests/fixtures/normalize/brand-corpus.yaml
  - tests/unit/test_viled_nextdata_parser.py
  - tests/unit/test_viled_catalog_paginate.py
  - tests/unit/test_volume_normalizer.py
  - tests/unit/test_brand_normalizer.py
  - tests/unit/test_name_normalizer.py
  - tests/unit/test_yaml_brand_alias.py
  - tests/unit/test_storage_models.py
  - tests/unit/test_run_writer.py
  - tests/unit/test_snapshot_writer.py
  - tests/unit/test_norm06_writer.py
  - tests/unit/test_parse_quality_gate.py
  - tests/unit/test_sanity_n_gate.py
  - tests/unit/test_parse_dispatcher.py
  - tests/integration/test_storage_wal.py
  - tests/integration/test_v_current_snapshots.py
  - tests/integration/test_viled_fetcher_mocked.py
  - tests/integration/test_viled_run_e2e_with_real_storage.py
  - tests/integration/test_main_run_e2e.py
  - tests/integration/test_backup_script.py
  - .planning/phases/02-project-skeleton-viled-crawl-storage/02-WAVE0-PROBE.md
autonomous: true
requirements: []  # Wave 0 ships test infrastructure only — no user-facing reqs delivered.
                  # Requirements are carried by Plans 02-02..02-06 which implement them.
                  # Skip-marked test stubs created here are RED scaffolding.
enables_requirements:
  # Full list of REQ-IDs whose tests are scaffolded here (skip-marked).
  # Every ID below appears in at least one downstream plan's  field.
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06
  - CRAWL-01
  - CRAWL-03
  - CRAWL-04
  - CRAWL-05
  - CRAWL-06
  - PARSE-01
  - PARSE-02
  - PARSE-03
  - PARSE-04
  - PARSE-05
  - PARSE-06
  - NORM-01
  - NORM-02
  - NORM-03
  - NORM-04
  - NORM-05
  - NORM-06

must_haves:
  goal: "Wave 0 bootstrap: pyproject namespace + PyYAML installed; viled probe (A1, A2, A3, A4, A10) verifies field paths and pins fixtures; conftest extended with viled fixtures; all Phase 2 test files exist as RED stubs (skip-marked or xfail) so Waves 1-5 can flip them GREEN incrementally."
  truths:
    - "pyproject.toml has [tool.ga_crawler.crawl.viled] namespace with sanity_gate_n=100, pause_seconds=2.0, concurrency=1, retry_attempts=3, catalog_urls=[men/catalog/1310, women/catalog/1310] (per D-201, D-202, D-225, D-227)"
    - "PyYAML pinned in [project.dependencies] via `uv add pyyaml` (RESEARCH §Standard Stack — only new dep)"
    - "Wave 0 live probe captured 5 viled fixtures: PDP (canonical, discounted, OOS, multipack/набор if findable), catalog/1310 page-1 HTML; fixtures saved under tests/fixtures/viled/ (per D-222 + RESEARCH §Wave 0 Gaps)"
    - "02-WAVE0-PROBE.md documents A1 (in_stock field path), A2 (price>realPrice for discounted SKU), A3 (catalog/1310 returns 200 + __NEXT_DATA__), A4 (totalCount/pageSize keys), A10 (curl_cffi exception class import paths) — each downgraded ASSUMED→VERIFIED or REVISED-WITH-FOUND-PATHS"
    - "tests/conftest.py extends with viled_pdp_html, viled_catalog_html, brand_alias_yaml_fixture, in_memory_sqlite_session, volume_corpus_cases fixtures (per D-222)"
    - "tests/fixtures/normalize/volume-corpus.yaml ships ≥15 cases including kits/sets per ROADMAP success criterion 4 (`30 мл`, `30мл`, `30ml`, `1.0 oz`, `1,5 л`, `3 шт x 50мл`, `Set of 3 × 50ml`, `набор пробников`, edge non-volumes) — drives test_volume_normalizer.py via parametrize"
    - "tests/fixtures/normalize/brand-corpus.yaml ships ≥10 cases for Cyrillic↔Latin alias resolution (Estée Lauder, Эсте Лаудер, Givenchy, Живанши, etc.)"
    - "All 24 Phase 2 test files exist as skip-marked stubs (one stub test per file with `@pytest.mark.skip(reason='Wave N not implemented yet')`) so the test discovery layout is final from Wave 0; subsequent waves remove skip markers"
    - "respx is NOT used for any viled curl_cffi mock — monkey-patch _fetch_html-style wrapper functions directly (RESEARCH Pitfall 1, Phase 3 D-302 lesson)"
    - "Existing 192/192 non-live test suite remains green after Wave 0 (no regressions)"
    - "Wave 0 probe documents URL pool size estimate (~100-600 SKUs across men+women catalog/1310 endpoints per D-226), refining the seed N=100 (D-201) baseline if first probe reveals a different magnitude"
  artifacts:
    - path: pyproject.toml
      provides: "[tool.ga_crawler.crawl.viled] namespace + pyyaml dep"
      contains: "[tool.ga_crawler.crawl.viled]"
    - path: tests/conftest.py
      provides: "viled_pdp_html / viled_catalog_html / brand_alias_yaml_fixture / in_memory_sqlite_session / volume_corpus_cases fixtures"
    - path: tests/fixtures/viled/viled-pdp-discounted.html
      provides: "Discounted SKU fixture for PARSE-03 was_price verification (price > realPrice)"
    - path: tests/fixtures/viled/viled-pdp-out-of-stock.html
      provides: "OOS SKU fixture for PARSE-06 stock-state verification"
    - path: tests/fixtures/viled/viled-catalog-men-1310-page1.html
      provides: "Catalog page-1 fixture for catalog enumeration tests (CRAWL-01)"
    - path: tests/fixtures/normalize/volume-corpus.yaml
      provides: "≥15-case test corpus driving NORM-03/NORM-04 tests"
    - path: tests/fixtures/normalize/brand-corpus.yaml
      provides: "≥10-case test corpus driving NORM-02 tests"
    - path: .planning/phases/02-project-skeleton-viled-crawl-storage/02-WAVE0-PROBE.md
      provides: "A1-A4 + A10 probe outcomes; field paths VERIFIED or REVISED"
  key_links:
    - from: tests/fixtures/viled/viled-pdp-*.html
      to: tests/unit/test_viled_nextdata_parser.py
      via: "viled_pdp_html fixture loaded by parametrize"
      pattern: "viled_pdp_html"
    - from: tests/fixtures/normalize/volume-corpus.yaml
      to: tests/unit/test_volume_normalizer.py
      via: "volume_corpus_cases fixture parametrize"
      pattern: "volume_corpus_cases"
    - from: pyproject.toml [tool.ga_crawler.crawl.viled]
      to: src/ga_crawler/config.py (created Wave 4)
      via: "tomllib.load reads namespace"
      pattern: "\\[tool.ga_crawler.crawl.viled\\]"
---

<objective>
Wave 0 BOOTSTRAP. This plan ships infrastructure that every subsequent wave depends on: pyproject namespace for viled crawler config (D-202, D-227), PyYAML dependency (only new dep per RESEARCH §Standard Stack), 5 captured viled HTML fixtures (live probe pins A1, A2, A3, A4, A10 from RESEARCH §Assumptions Log), test-corpus YAMLs (volume + brand) per ROADMAP criterion #4, and 24 RED test stubs so the test layout is frozen from Wave 0 forward. Subsequent waves flip skip markers as production code lands.

Purpose: De-risk the four MEDIUM-confidence paths in RESEARCH (in_stock field name, discount semantics, catalog endpoint accessibility, pagination metadata key names) BEFORE writing production code. Without Wave 0, Plan 04's parser would be guessing — Plan 04 instead consumes verified fixtures and field paths.

Output: pyproject.toml diff + 5 fixture HTMLs + 2 corpus YAMLs + extended conftest.py + 24 skip-marked test stubs + 02-WAVE0-PROBE.md memo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-project-skeleton-viled-crawl-storage/02-CONTEXT.md
@.planning/phases/02-project-skeleton-viled-crawl-storage/02-RESEARCH.md
@.planning/phases/02-project-skeleton-viled-crawl-storage/02-PATTERNS.md
@.planning/phases/02-project-skeleton-viled-crawl-storage/02-VALIDATION.md
@.planning/spikes/01-goldapple/sample-payloads/viled-nextdata-shape.json
@.planning/spikes/01-goldapple/sample-payloads/viled-fetch-results.json
@.planning/spikes/01-goldapple/sample-payloads/viled-product-urls.txt
@.planning/spikes/01-goldapple/sample-payloads/viled-home-brands-extract.json
@pyproject.toml
@tests/conftest.py

<interfaces>
<!-- Existing infrastructure Wave 0 EXTENDS without modifying. -->

From src/ga_crawler/interfaces.py (FROZEN — do not modify):
```python
class BrandAliasProtocol(Protocol):
    def lookup(self, brand_norm: str) -> list[str]: ...

class NormalizerProtocol(Protocol):
    def brand(self, raw: str) -> str: ...
    def name(self, raw: str) -> str: ...
    def volume(self, raw: str) -> Optional[tuple[Decimal, str, int]]: ...

class SnapshotWriterProtocol(Protocol):
    def append(self, run_id: int, retailer: str, products: list) -> int: ...

class RunWriterProtocol(Protocol):
    def patch_stats(self, run_id: int, delta: dict) -> None: ...
    def get_stats(self, run_id: int) -> dict: ...
    def fail(self, run_id: int, reason: str) -> None: ...

class ParseDispatcherProtocol(Protocol):
    def dispatch(self, retailer: str, html_or_data: str) -> Optional[dict]: ...
```

From pyproject.toml [tool.ga_crawler.crawl.goldapple] (existing pattern to MIRROR):
```toml
[tool.ga_crawler.crawl.goldapple]
sanity_gate_m = 1000
pause_min_seconds = 3.0
pause_max_seconds = 5.0
# ... (mirror with viled.* keys)
```

From tests/conftest.py (existing 11 fixtures — KEEP AS-IS, ADD new ones below them):
- mock_brand_alias, mock_normalizer, mock_snapshot_writer, mock_run_writer (Phase 3 mocks; Phase 2 keeps)
- 7 other Phase 3 fixtures
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 live probe + fixture capture + 02-WAVE0-PROBE.md memo</name>
  <files>tests/fixtures/viled/viled-pdp-407682.html, tests/fixtures/viled/viled-pdp-discounted.html, tests/fixtures/viled/viled-pdp-out-of-stock.html, tests/fixtures/viled/viled-pdp-multipack.html, tests/fixtures/viled/viled-catalog-men-1310-page1.html, tests/fixtures/viled/viled-catalog-women-1310-page1.html, tests/fixtures/viled/viled-nextdata-shape.json, .planning/phases/02-project-skeleton-viled-crawl-storage/02-WAVE0-PROBE.md</files>
  <read_first>
    .planning/spikes/01-goldapple/sample-payloads/viled-nextdata-shape.json (FULL — current ASSUMED field shape),
    .planning/spikes/01-goldapple/sample-payloads/viled-fetch-results.json (full — 15-fetch sample including 4 working /item/* URLs to re-fetch),
    .planning/spikes/01-goldapple/sample-payloads/viled-product-urls.txt (full — 15 sample /item/{id} URLs),
    .planning/phases/02-project-skeleton-viled-crawl-storage/02-RESEARCH.md (§Assumptions Log A1-A10, §Pattern 1, §Pattern 2, §Pitfall 8),
    .planning/phases/02-project-skeleton-viled-crawl-storage/02-CONTEXT.md (D-217 stock-state mapping, D-223..D-227 catalog enumeration)
  </read_first>
  <action>
**Live probe against viled.kz** (Wave 0 timeboxed to ~30 min):

1. Write a one-shot probe script at `.planning/spikes/02-wave0-probe.py` (NOT under src/; throwaway):
   ```python
   from curl_cffi import requests
   from pathlib import Path
   import json, time
   FX = Path("tests/fixtures/viled"); FX.mkdir(parents=True, exist_ok=True)
   def fetch(url, fname, pause=2.0):
       r = requests.get(url, impersonate="chrome", timeout=30)
       (FX / fname).write_text(r.text, encoding="utf-8")
       time.sleep(pause)
       return r.status_code
   # 1. Canonical PDP (re-fetch a known-good /item/{id} from spike viled-fetch-results.json)
   fetch("https://viled.kz/item/407682", "viled-pdp-407682.html")
   # 2. Catalog page-1 (men + women)
   fetch("https://viled.kz/men/catalog/1310", "viled-catalog-men-1310-page1.html")
   fetch("https://viled.kz/women/catalog/1310", "viled-catalog-women-1310-page1.html")
   # 3. Hunt for discounted, OOS, kit/набор SKUs by browsing catalog pages and picking
   #    candidates from __NEXT_DATA__.pageProps.products[]
   #    (operator-driven; if not found in 5 attempts, use canonical fixture as placeholder
   #    and document in 02-WAVE0-PROBE.md)
   ```

2. Run probe (use `uv run python .planning/spikes/02-wave0-probe.py`).

3. Verify A1, A2, A3, A4, A10:
   - **A1 in_stock field name:** open `viled-pdp-407682.html`, extract `__NEXT_DATA__`, walk `props.pageProps.attributes[0]` keys. Confirm field name (`in_stock` / `count` / `purchaseType` / etc.). DOCUMENT exact path.
   - **A2 discount semantics:** if discounted SKU found, confirm `attributes[0].price > attributes[0].realPrice`. If no discounted SKU found, document in PROBE.md with note "discounted-fixture deferred to first weekly run".
   - **A3 catalog accessibility:** confirm `viled-catalog-men-1310-page1.html` HTTP 200 + non-empty `__NEXT_DATA__`. If 403/redirect, fall back to sitemap path (Pitfall 8) and DOCUMENT alternate strategy.
   - **A4 pagination metadata:** dump `__NEXT_DATA__.props.pageProps` top-level keys from catalog HTML. Document actual key names (`totalCount` / `pagination.total` / `meta.totalItems` / etc.) and `products[]` location.
   - **A10 curl_cffi exception classes:** run `uv run python -c "from curl_cffi.requests.errors import RequestsError, Timeout; print('ok')"`. If fails, find correct paths via `python -c "import curl_cffi.requests.errors; print(dir(curl_cffi.requests.errors))"` and document.

4. Save canonical viled `__NEXT_DATA__` JSON to `tests/fixtures/viled/viled-nextdata-shape.json` (extract from PDP fixture's __NEXT_DATA__ script tag — pretty-printed for readability).

5. Write `.planning/phases/02-project-skeleton-viled-crawl-storage/02-WAVE0-PROBE.md`:
   - Frontmatter: `wave: 0`, `probe_date: <today>`, `phase: 02`
   - Section "A1 in_stock field path" — verified path or revised path
   - Section "A2 discount semantics" — confirmation or deferral
   - Section "A3 catalog accessibility" — HTTP status + content notes
   - Section "A4 pagination metadata" — exact key names found
   - Section "A10 curl_cffi exceptions" — verified import path
   - Section "Decisions cascading to Plan 04" — any field-path corrections to RESEARCH §Pattern 1/§Pattern 2

6. Delete probe script (`.planning/spikes/02-wave0-probe.py`) — throwaway per spike convention.

7. **If catalog/1310 returns 403 or login redirect (Pitfall 8 trigger):** STOP, write `02-WAVE0-PROBE.md` documenting the failure, and exit. The orchestrator must replan Plan 04's enumeration to use sitemap+brand-allowlist fallback. The other Wave 0 outputs (corpus YAMLs, conftest, test stubs) are still useful and Plan 04 adapts.
  </action>
  <verify>
    <automated>test -f tests/fixtures/viled/viled-pdp-407682.html && test -f tests/fixtures/viled/viled-catalog-men-1310-page1.html && test -f tests/fixtures/viled/viled-nextdata-shape.json && test -f .planning/phases/02-project-skeleton-viled-crawl-storage/02-WAVE0-PROBE.md && grep -q "A1" .planning/phases/02-project-skeleton-viled-crawl-storage/02-WAVE0-PROBE.md && grep -q "A4" .planning/phases/02-project-skeleton-viled-crawl-storage/02-WAVE0-PROBE.md && uv run python -c "from curl_cffi.requests.errors import RequestsError, Timeout; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/viled/viled-pdp-407682.html exists and is non-empty (>10KB; PDP HTML)
    - tests/fixtures/viled/viled-catalog-men-1310-page1.html exists and is non-empty
    - tests/fixtures/viled/viled-nextdata-shape.json contains valid JSON with `props.pageProps.attributes` key
    - 02-WAVE0-PROBE.md contains all 5 sections (A1, A2, A3, A4, A10)
    - 02-WAVE0-PROBE.md documents the actual `in_stock` (or replacement) field name found in attributes[0]
    - 02-WAVE0-PROBE.md documents the actual pagination key names found in catalog page __NEXT_DATA__
    - `from curl_cffi.requests.errors import RequestsError, Timeout` succeeds (or 02-WAVE0-PROBE.md documents the corrected import path with `cascading: yes` flag)
    - `.planning/spikes/02-wave0-probe.py` does NOT exist (deleted after use)
  </acceptance_criteria>
  <done>5 fixture HTMLs captured, 02-WAVE0-PROBE.md memo committed; all 5 assumptions (A1, A2, A3, A4, A10) downgraded to VERIFIED or explicitly REVISED with corrected paths.</done>
</task>

<task type="auto">
  <name>Task 2: pyproject.toml namespace + PyYAML dep</name>
  <files>pyproject.toml</files>
  <read_first>
    pyproject.toml (FULL — current `[tool.ga_crawler.crawl.goldapple]` namespace and `[project.dependencies]`),
    .planning/phases/02-project-skeleton-viled-crawl-storage/02-PATTERNS.md (§"Pattern: pyproject.toml Namespace Mirror" lines 786-806 — verbatim TOML excerpt),
    .planning/phases/02-project-skeleton-viled-crawl-storage/02-CONTEXT.md (D-202, D-225, D-227)
  </read_first>
  <action>
1. **Add new dependency** via `uv add pyyaml` (this updates pyproject.toml `[project.dependencies]` and uv.lock atomically).

2. **Add `[tool.ga_crawler.crawl.viled]` namespace** to pyproject.toml directly under the existing `[tool.ga_crawler.crawl.goldapple]` block:

   ```toml
   [tool.ga_crawler.crawl.viled]
   sanity_gate_n = 100
   pause_seconds = 2.0
   concurrency = 1
   retry_attempts = 3
   catalog_urls = [
       "https://viled.kz/men/catalog/1310",
       "https://viled.kz/women/catalog/1310",
   ]
   n_auto_suggest_factor = 0.7
   n_auto_suggest_after_runs = 4
   ```

   - `sanity_gate_n = 100` per D-201 (revised seed for beauty-only sub-catalog)
   - `pause_seconds = 2.0` per D-225 + spike 01-07 (15/15 success at 2s)
   - `concurrency = 1` per D-225
   - `catalog_urls = [...]` per D-227 (operator-managed list)
   - `n_auto_suggest_factor / n_auto_suggest_after_runs` mirror Phase 3 keys for D-203 helper

3. Run `uv lock` to refresh lockfile (in case `uv add pyyaml` did not).

4. Verify with `uv run python -c "import yaml; print(yaml.__version__)"` and `uv run python -c "import tomllib; cfg=tomllib.load(open('pyproject.toml','rb')); print(cfg['tool']['ga_crawler']['crawl']['viled']['sanity_gate_n'])"` — must print `100`.
  </action>
  <verify>
    <automated>grep -q "\[tool.ga_crawler.crawl.viled\]" pyproject.toml && grep -q "sanity_gate_n = 100" pyproject.toml && grep -q "men/catalog/1310" pyproject.toml && uv run python -c "import yaml" && uv run python -c "import tomllib; cfg=tomllib.load(open('pyproject.toml','rb')); assert cfg['tool']['ga_crawler']['crawl']['viled']['sanity_gate_n']==100; assert cfg['tool']['ga_crawler']['crawl']['viled']['pause_seconds']==2.0; assert len(cfg['tool']['ga_crawler']['crawl']['viled']['catalog_urls'])==2"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tool.ga_crawler.crawl.viled" pyproject.toml` == 1 (header present)
    - `grep -q "sanity_gate_n = 100" pyproject.toml` (D-201 seed)
    - `grep -q "pyyaml" pyproject.toml` (dep added)
    - `uv run python -c "import yaml"` exits 0
    - `tomllib.load`-parsed `[tool.ga_crawler.crawl.viled]` has `sanity_gate_n=100`, `pause_seconds=2.0`, `len(catalog_urls)==2`
    - Existing `[tool.ga_crawler.crawl.goldapple]` namespace untouched (`grep -q "sanity_gate_m" pyproject.toml`)
  </acceptance_criteria>
  <done>PyYAML installed and importable; `[tool.ga_crawler.crawl.viled]` namespace present with all 7 keys; existing goldapple namespace unchanged; uv.lock refreshed.</done>
</task>

<task type="auto">
  <name>Task 3: Test corpus YAMLs + conftest fixtures + 24 skip-marked test stubs</name>
  <files>tests/fixtures/normalize/volume-corpus.yaml, tests/fixtures/normalize/brand-corpus.yaml, tests/fixtures/viled/brand-aliases-fixture.yaml, tests/conftest.py, tests/unit/test_viled_nextdata_parser.py, tests/unit/test_viled_catalog_paginate.py, tests/unit/test_volume_normalizer.py, tests/unit/test_brand_normalizer.py, tests/unit/test_name_normalizer.py, tests/unit/test_yaml_brand_alias.py, tests/unit/test_storage_models.py, tests/unit/test_run_writer.py, tests/unit/test_snapshot_writer.py, tests/unit/test_norm06_writer.py, tests/unit/test_parse_quality_gate.py, tests/unit/test_sanity_n_gate.py, tests/unit/test_parse_dispatcher.py, tests/unit/test_viled_fetcher_isolation.py, tests/unit/test_viled_retry_policy.py, tests/unit/test_viled_rate_limit.py, tests/integration/test_storage_wal.py, tests/integration/test_v_current_snapshots.py, tests/integration/test_run_writer_lifecycle.py, tests/integration/test_viled_fetcher_mocked.py, tests/integration/test_viled_run_e2e_with_real_storage.py, tests/integration/test_main_run_e2e.py, tests/integration/test_backup_script.py</files>
  <read_first>
    tests/conftest.py (FULL — Phase 3's 11 fixtures must be preserved verbatim),
    .planning/phases/02-project-skeleton-viled-crawl-storage/02-RESEARCH.md (§"Validation Architecture" lines 1116-1145 — the 24-row Phase Requirements → Test Map; §"Pattern 6 Volume Normalizer" lines 705-710 corpus examples),
    .planning/phases/02-project-skeleton-viled-crawl-storage/02-CONTEXT.md (D-205 brand-alias schema flat-dict, D-208 norm06 schema, D-217 stock-state, D-222 conftest fixture list)
  </read_first>
  <action>
**Step A — `tests/fixtures/normalize/volume-corpus.yaml`** (≥15 cases per ROADMAP criterion #4):

```yaml
# Volume parser corpus. Drives tests/unit/test_volume_normalizer.py via parametrize.
# Format: list of {input: str, expected_volume: [amount, unit, count] | null, expected_multipack: bool}
# Source: 02-RESEARCH.md §Pattern 6 Volume Normalizer.

cases:
  - input: "30 мл"
    expected_volume: ["30", "ml", 1]
    expected_multipack: false
  - input: "30мл"
    expected_volume: ["30", "ml", 1]
    expected_multipack: false
  - input: "30ml"
    expected_volume: ["30", "ml", 1]
    expected_multipack: false
  - input: "1.0 oz"
    expected_volume: ["1.0", "oz", 1]
    expected_multipack: false
  - input: "1,5 л"
    expected_volume: ["1.5", "l", 1]
    expected_multipack: false
  - input: "100 ml"
    expected_volume: ["100", "ml", 1]
    expected_multipack: false
  - input: "50 g"
    expected_volume: ["50", "g", 1]
    expected_multipack: false
  - input: "3 шт x 50мл"
    expected_volume: ["50", "ml", 3]
    expected_multipack: true
  - input: "3 x 50 мл"
    expected_volume: ["50", "ml", 3]
    expected_multipack: true
  - input: "Set of 3 × 50ml"
    expected_volume: ["50", "ml", 3]
    expected_multipack: true
  - input: "набор пробников"
    expected_volume: null
    expected_multipack: true
  - input: "kit travel size"
    expected_volume: null
    expected_multipack: true
  - input: "комплект для лица"
    expected_volume: null
    expected_multipack: true
  - input: "Кружевное боди"
    expected_volume: null
    expected_multipack: false
  - input: ""
    expected_volume: null
    expected_multipack: false
  - input: "Eau de Parfum 75 ml Givenchy"
    expected_volume: ["75", "ml", 1]
    expected_multipack: false
  - input: "10 шт"
    expected_volume: null
    expected_multipack: true
```

**Step B — `tests/fixtures/normalize/brand-corpus.yaml`** (≥10 cases for NORM-02):

```yaml
# Brand normalizer corpus. Drives tests/unit/test_brand_normalizer.py via parametrize.
# Format: list of {raw: str, expected_brand_norm: str, alias_map: {canonical: [aliases]}}
# Source: 02-RESEARCH.md §Pattern 7 + 02-CONTEXT.md D-205 D-206.

cases:
  - raw: "Estée Lauder"
    expected_brand_norm: "estee_lauder"
    alias_present: true
  - raw: "Estee Lauder"
    expected_brand_norm: "estee_lauder"
    alias_present: true
  - raw: "Эсте Лаудер"
    expected_brand_norm: "estee_lauder"
    alias_present: true
  - raw: "Givenchy"
    expected_brand_norm: "givenchy"
    alias_present: true
  - raw: "Живанши"
    expected_brand_norm: "givenchy"
    alias_present: true
  - raw: "Tom Ford"
    expected_brand_norm: "tom-ford"
    alias_present: false
  - raw: "L'Oréal Paris"
    expected_brand_norm: "loreal-paris"
    alias_present: false
  - raw: "JO MALONE LONDON"
    expected_brand_norm: "jo-malone-london"
    alias_present: false
  - raw: "Frédéric Malle"
    expected_brand_norm: "frederic-malle"
    alias_present: false
  - raw: "Creed"
    expected_brand_norm: "creed"
    alias_present: false
  - raw: ""
    expected_brand_norm: ""
    alias_present: false
```

**Step C — `tests/fixtures/viled/brand-aliases-fixture.yaml`** (small test seed for YamlBrandAlias unit tests):

```yaml
# Test fixture — small flat-dict alias seed.
# Schema per 02-CONTEXT.md D-205.
estee_lauder:
  - "Estée Lauder"
  - "Estee Lauder"
  - "Эсте Лаудер"
givenchy:
  - "Givenchy"
  - "Живанши"
chanel:
  - "Chanel"
  - "Шанель"
```

**Step D — Extend `tests/conftest.py`**: APPEND (do NOT delete existing 11 fixtures):

```python
# ===== Phase 2 fixtures (D-222) =====
import yaml
from pathlib import Path
from sqlmodel import create_engine, Session, SQLModel
import pytest


@pytest.fixture
def viled_pdp_html() -> str:
    """Canonical viled PDP HTML captured Wave 0 (item/407682)."""
    return Path("tests/fixtures/viled/viled-pdp-407682.html").read_text(encoding="utf-8")


@pytest.fixture
def viled_catalog_html() -> str:
    """Canonical viled catalog page-1 HTML captured Wave 0 (men/catalog/1310)."""
    return Path("tests/fixtures/viled/viled-catalog-men-1310-page1.html").read_text(encoding="utf-8")


@pytest.fixture
def brand_alias_yaml_fixture(tmp_path: Path) -> Path:
    """Materialize tests/fixtures/viled/brand-aliases-fixture.yaml into tmp_path."""
    src = Path("tests/fixtures/viled/brand-aliases-fixture.yaml").read_text(encoding="utf-8")
    dst = tmp_path / "brand-aliases.yaml"
    dst.write_text(src, encoding="utf-8")
    return dst


@pytest.fixture
def in_memory_sqlite_session():
    """Yield a Session bound to an in-memory SQLite engine with WAL/foreign-keys
    PRAGMAs applied and SQLModel tables created. Tear down on test exit.

    Source: 02-RESEARCH.md §Pattern 3 + §"Initializing the WAL session".
    """
    # NOTE: Wave 1 imports the real engine factory; for now stub via vanilla SQLite.
    # Tests that require Wave 1's storage MUST skip until Wave 1 lands the module.
    from sqlalchemy import event
    engine = create_engine("sqlite:///:memory:", echo=False)

    @event.listens_for(engine, "connect")
    def _set_pragma(dbapi_conn, _):
        cur = dbapi_conn.cursor()
        # WAL is moot for :memory: but PRAGMA call is harmless and matches prod
        cur.execute("PRAGMA foreign_keys=ON")
        cur.close()

    # Tables are created lazily by Wave 1 module; for Wave 0 stub yield bare session.
    try:
        from ga_crawler.storage.sqlite import SQLModel as _SQL
        _SQL.metadata.create_all(engine)
    except ImportError:
        pass  # Wave 1 not yet shipped
    with Session(engine) as session:
        yield session


@pytest.fixture
def volume_corpus_cases() -> list[dict]:
    """Load tests/fixtures/normalize/volume-corpus.yaml. Source: 02-RESEARCH.md Open Q5."""
    raw = Path("tests/fixtures/normalize/volume-corpus.yaml").read_text(encoding="utf-8")
    return yaml.safe_load(raw)["cases"]


@pytest.fixture
def brand_corpus_cases() -> list[dict]:
    """Load tests/fixtures/normalize/brand-corpus.yaml."""
    raw = Path("tests/fixtures/normalize/brand-corpus.yaml").read_text(encoding="utf-8")
    return yaml.safe_load(raw)["cases"]
```

**Step E — Create 24 skip-marked test stubs** (one minimal stub per file from RESEARCH §Validation Architecture line 1117-1142). Each file follows this template:

```python
# tests/unit/test_<module>.py
"""<Req IDs covered>: <description>. Source: 02-RESEARCH.md §Validation Architecture."""
import pytest

pytestmark = pytest.mark.skip(reason="Wave <N> not implemented yet — Plan 02-NN")


def test_placeholder():
    """Placeholder. Plan 02-NN flips this from skip to GREEN."""
    assert False, "implement in Plan 02-NN"
```

For each file, fill `<Req IDs>`, `<N>`, and `02-NN`:

| File | Wave | Plan | Reqs |
|------|------|------|------|
| tests/unit/test_storage_models.py | 1 | 02 | DATA-01, DATA-02 |
| tests/unit/test_run_writer.py | 1 | 02 | DATA-05 |
| tests/unit/test_snapshot_writer.py | 1 | 02 | DATA-03 |
| tests/unit/test_norm06_writer.py | 1 | 02 | NORM-06 |
| tests/integration/test_storage_wal.py | 1 | 02 | DATA-04 |
| tests/integration/test_v_current_snapshots.py | 1 | 02 | DATA-03, CRAWL-01 (provenance) |
| tests/integration/test_run_writer_lifecycle.py | 1 | 02 | DATA-05 |
| tests/unit/test_volume_normalizer.py | 2 | 03 | NORM-03, NORM-04 |
| tests/unit/test_brand_normalizer.py | 2 | 03 | NORM-02 |
| tests/unit/test_name_normalizer.py | 2 | 03 | NORM-05 |
| tests/unit/test_yaml_brand_alias.py | 2 | 03 | NORM-01 |
| tests/unit/test_viled_nextdata_parser.py | 3 | 04 | PARSE-01..04, PARSE-06 |
| tests/unit/test_viled_catalog_paginate.py | 3 | 04 | CRAWL-01 |
| tests/unit/test_viled_fetcher_isolation.py | 3 | 04 | CRAWL-03 |
| tests/unit/test_viled_retry_policy.py | 3 | 04 | CRAWL-04 |
| tests/unit/test_viled_rate_limit.py | 3 | 04 | CRAWL-06 |
| tests/unit/test_parse_dispatcher.py | 3 | 04 | PARSE-02 |
| tests/integration/test_viled_fetcher_mocked.py | 3 | 04 | CRAWL-01, CRAWL-04 |
| tests/unit/test_sanity_n_gate.py | 4 | 05 | CRAWL-05 |
| tests/unit/test_parse_quality_gate.py | 4 | 05 | PARSE-05 |
| tests/integration/test_viled_run_e2e_with_real_storage.py | 4 | 05 | end-to-end viled |
| tests/integration/test_main_run_e2e.py | 5 | 06 | end-to-end full |
| tests/integration/test_backup_script.py | 5 | 06 | DATA-06 |

**Step F — Run full suite**: `uv run pytest -m "not live" -q`. Expect:
- Existing 192 tests still GREEN
- 24 new tests SKIPPED (not red)
- 0 errors, 0 failures.
  </action>
  <verify>
    <automated>uv run python -c "import yaml; cases=yaml.safe_load(open('tests/fixtures/normalize/volume-corpus.yaml'))['cases']; assert len(cases) >= 15; print(len(cases))" && uv run python -c "import yaml; cases=yaml.safe_load(open('tests/fixtures/normalize/brand-corpus.yaml'))['cases']; assert len(cases) >= 10; print(len(cases))" && uv run pytest -m "not live" -q --co 2>&1 | grep -E "test_(volume_normalizer|brand_normalizer|storage_models|run_writer|snapshot_writer|viled_nextdata_parser|sanity_n_gate|parse_quality_gate|main_run_e2e|backup_script)" | wc -l && uv run pytest -m "not live" --tb=line 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/fixtures/normalize/volume-corpus.yaml has ≥15 cases under `cases:` key
    - tests/fixtures/normalize/brand-corpus.yaml has ≥10 cases under `cases:` key
    - tests/fixtures/viled/brand-aliases-fixture.yaml exists with at least 3 canonical brands (estee_lauder, givenchy, chanel)
    - tests/conftest.py contains all 6 new fixture defs: `viled_pdp_html`, `viled_catalog_html`, `brand_alias_yaml_fixture`, `in_memory_sqlite_session`, `volume_corpus_cases`, `brand_corpus_cases` (`grep -c "^def \\(viled_pdp_html\\|viled_catalog_html\\|brand_alias_yaml_fixture\\|in_memory_sqlite_session\\|volume_corpus_cases\\|brand_corpus_cases\\)" tests/conftest.py` == 6 OR all 6 names appear in `grep "@pytest.fixture" -A 1 tests/conftest.py`)
    - `tests/conftest.py` retains existing 11 Phase 3 fixtures (e.g., `mock_brand_alias`, `mock_normalizer`, `mock_snapshot_writer`, `mock_run_writer` still grep-able)
    - 24 new test stubs created (count: `find tests/unit tests/integration -newer pyproject.toml -name "test_*.py" | wc -l` ≥ 24)
    - Each stub uses `pytestmark = pytest.mark.skip(reason="Wave ... not implemented")` (grep `pytest.mark.skip` returns ≥24 hits across new files)
    - `uv run pytest -m "not live" -q` exits 0; output shows ≥192 passed + ≥24 skipped + 0 failed
  </acceptance_criteria>
  <done>Volume + brand corpus YAMLs ship ≥15 + ≥10 cases; conftest extended with 6 new fixtures (existing 11 preserved); 24 RED skip-marked stubs cover every Phase 2 requirement. Full pytest suite green: existing 192 pass + 24 skipped, 0 failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| viled.kz → curl_cffi probe | Untrusted HTML response crosses into local fixtures + parser |
| operator → pyproject.toml | Operator-edited TOML config crosses into runtime constants (catalog_urls, sanity_gate_n) |
| disk → tests/fixtures/ | Captured HTML loaded by parser tests; could carry malicious payloads if attacker controls viled.kz |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | viled `__NEXT_DATA__` schema drift | mitigate | Wave 0 PROBE pins exact field paths in 02-WAVE0-PROBE.md; >5% null sentinel (D-218 PARSE-05 gate, Plan 05) catches silent drift on subsequent runs |
| T-02-04b | Tampering | catalog/1310 endpoint becomes auth-gated mid-week | accept | Pitfall 8 documented; if Wave 0 probe fails, replanning trigger before any production code lands. Risk window = Wave 0 probe day only. |
| T-02-01a | Information Disclosure | brand-aliases fixture YAML | accept | Test-only fixture; contains public brand-name aliases (no PII, no secrets). Operator-edited YAML production file (Plan 06) ships under config/ in git, not data/, by design. |
| T-02-A10 | Denial of Service | curl_cffi exception class import path drift between curl_cffi versions | mitigate | Wave 0 verifies `from curl_cffi.requests.errors import RequestsError, Timeout` succeeds; if not, 02-WAVE0-PROBE.md documents corrected path and Plan 04 imports from there. |
| T-02-W0 | Repudiation | Operator runs Wave 0 probe, can't reproduce later | mitigate | 02-WAVE0-PROBE.md committed to git; fixtures committed to git. Probe outcome is auditable artifact. |
</threat_model>

<verification>
1. `grep -q "tool.ga_crawler.crawl.viled" pyproject.toml` → namespace exists
2. `uv run python -c "import yaml"` → PyYAML installed
3. `find tests/fixtures/viled tests/fixtures/normalize -type f | wc -l` ≥ 6
4. `uv run pytest -m "not live" -q` exits 0 with ≥192 passed + ≥24 skipped
5. `test -f .planning/phases/02-project-skeleton-viled-crawl-storage/02-WAVE0-PROBE.md` and grep finds A1, A2, A3, A4, A10 sections
6. `from curl_cffi.requests.errors import RequestsError, Timeout` succeeds OR PROBE.md flags corrected path
</verification>

<success_criteria>
- pyproject.toml has `[tool.ga_crawler.crawl.viled]` namespace with 7 keys (sanity_gate_n=100, pause_seconds=2.0, concurrency=1, retry_attempts=3, catalog_urls list of 2, n_auto_suggest_factor=0.7, n_auto_suggest_after_runs=4)
- PyYAML installed via uv add (only new dep per RESEARCH §Standard Stack)
- 5 viled HTML fixtures captured (PDP canonical, PDP discounted or deferral note, PDP OOS or deferral note, catalog men, catalog women)
- 02-WAVE0-PROBE.md commits A1, A2, A3, A4, A10 outcomes — VERIFIED or REVISED
- tests/conftest.py adds 6 new fixtures, retains existing 11 Phase 3 fixtures
- tests/fixtures/normalize/volume-corpus.yaml ships ≥15 cases; brand-corpus.yaml ≥10 cases
- 24 skip-marked test stubs exist, mapping 1:1 to RESEARCH §Validation Architecture rows
- `uv run pytest -m "not live"` exits 0: ≥192 pass + ≥24 skip + 0 fail
- 02-WAVE0-PROBE.md cascades any field-path corrections to RESEARCH §Pattern 1 / §Pattern 2 (footer "Decisions cascading to Plan 04")
</success_criteria>

<output>
After completion, create `.planning/phases/02-project-skeleton-viled-crawl-storage/02-01-SUMMARY.md` documenting:
- Probe outcomes for A1-A4 + A10
- Any revisions to RESEARCH §Pattern 1 or §Pattern 2 field paths
- File inventory: fixtures captured, test stubs created
- Cascading constraints for Plan 04 (e.g. "use `count > 0` instead of `in_stock` field" if A1 revised)
</output>
</content>
</invoke>