---
phase: 02
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - src/ga_crawler/storage/__init__.py
  - src/ga_crawler/storage/sqlite.py
  - src/ga_crawler/storage/norm06_writer.py
  - tests/unit/test_storage_models.py
  - tests/unit/test_run_writer.py
  - tests/unit/test_snapshot_writer.py
  - tests/unit/test_norm06_writer.py
  - tests/integration/test_storage_wal.py
  - tests/integration/test_v_current_snapshots.py
  - tests/integration/test_run_writer_lifecycle.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - NORM-06

must_haves:
  goal: "SQLModel storage layer ships: Run + Snapshot tables (DATA-01..02), append-only SnapshotWriter (DATA-03), per-batch transactions + WAL (DATA-04), RunWriter with create/finalize/fail/atomic-json_patch lifecycle (DATA-05 + Pitfall 6), v_current_snapshots view (D-221 brand-list provenance), Norm06Writer markdown ledger (NORM-06 + D-208/D-211)."
  truths:
    - "src/ga_crawler/storage/sqlite.py exports SQLModel tables Run + Snapshot per RESEARCH §Pattern 3 (DATA-01)"
    - "Snapshot row has all 13 required fields: name, brand, volume_raw, current_price, was_price, currency, stock_state, url, brand_norm, name_norm, volume_norm, multipack_flag, scraped_at — plus parse_error_flag, retailer, sku_id, run_id (DATA-02)"
    - "SnapshotWriter.append() is INSERT-only (DATA-03); UNIQUE constraint on (run_id, retailer, sku_id) prevents duplicates"
    - "Per-batch commit (default batch_size=100) means mid-run failure preserves prior batches (DATA-04 + Anti-Pattern in RESEARCH)"
    - "PRAGMA journal_mode=WAL + synchronous=NORMAL + foreign_keys=ON applied via SQLAlchemy event listener on every connect (DATA-04 + RESEARCH §Initializing the WAL session)"
    - "RunWriter.patch_stats uses raw SQL `UPDATE runs SET stats = json_patch(stats, :delta)` — atomic merge between viled.* and goldapple.* keys (Pitfall 6 D-203 D-214)"
    - "RunWriter has create() and finalize() concrete methods (NOT in Protocol per RESEARCH Open Q1 — Phase 3 contract drift avoided); has patch_stats/get_stats/fail per Protocol (DATA-05)"
    - "fail() and finalize() are idempotent — safe to call from try/finally even on crash (DATA-05)"
    - "v_current_snapshots SQL VIEW created during init_db: `SELECT * FROM snapshots WHERE run_id = (SELECT MAX(run_id) FROM runs WHERE status='success')` — single source of truth for Phase 3 brand-pool (D-221)"
    - "src/ga_crawler/storage/norm06_writer.py exports Norm06Writer.persist(run_id, viled_unmatched, goldapple_new_slugs) → writes .planning/runs/{run_id}/norm06-review.md per D-208 schema (NORM-06)"
    - "Norm06Writer markdown table has 4 columns: brand_or_slug | source | run_id | status; status defaults to `pending`; source enum is `viled-unmatched` or `goldapple-new-slug` (D-208)"
    - "Norm06Writer documents D-209 operator workflow in markdown header: operator edits status to aliased / skip / reviewed; no automated workflows around review (pure markdown ledger via git history)"
    - "alembic NOT added — schema bootstraps via SQLModel.metadata.create_all (D-220 + CLAUDE.md)"
  artifacts:
    - path: src/ga_crawler/storage/__init__.py
      provides: "Package marker"
    - path: src/ga_crawler/storage/sqlite.py
      provides: "Run, Snapshot SQLModel tables; SqliteRunWriter, SqliteSnapshotWriter; init_db, make_engine; v_current_snapshots VIEW"
      contains: "class Run(SQLModel, table=True)"
      min_lines: 200
    - path: src/ga_crawler/storage/norm06_writer.py
      provides: "Norm06Writer.persist() markdown ledger writer"
      contains: "class Norm06Writer"
  key_links:
    - from: src/ga_crawler/storage/sqlite.py
      to: src/ga_crawler/interfaces.py
      via: "SqliteRunWriter implements RunWriterProtocol; SqliteSnapshotWriter implements SnapshotWriterProtocol"
      pattern: "patch_stats.*delta"
    - from: src/ga_crawler/storage/sqlite.py
      to: src/ga_crawler/runners/goldapple_run.py
      via: "Phase 3 already calls .patch_stats / .append; Phase 2 storage must accept dict-shape from goldapple_run.py:237-250"
      pattern: "snapshot_writer.append"
    - from: src/ga_crawler/storage/norm06_writer.py
      to: src/ga_crawler/runners/main_run.py (created Plan 05)
      via: "main_run orchestrator calls Norm06Writer.persist after both retailer phases"
      pattern: "Norm06Writer"
---

<objective>
Wave 1 STORAGE LAYER. Single-module impl per D-214 (`src/ga_crawler/storage/sqlite.py`, ~200-300 LOC) of SQLModel tables (Run, Snapshot), atomic SqliteRunWriter (json_patch via raw SQL — Pitfall 6), append-only SqliteSnapshotWriter (per-batch commits — DATA-04), v_current_snapshots SQL view (D-221), plus a separate `storage/norm06_writer.py` for the markdown ledger (NORM-06 + D-208/D-211). No alembic on day 1 (D-220).

Purpose: Replace Phase 3's StubRunWriter + StubSnapshotWriter (currently in cli.py) with real SQLite-backed impls behind unchanged interfaces.py Protocols. Phase 3 stays green: Pitfall 7 (schema drift between Stub and Real) caught by integration test against goldapple_run.py:237-250 dict-shape.

Output: 3 new source files (storage/__init__.py, storage/sqlite.py, storage/norm06_writer.py) + 7 GREEN test files (4 unit + 3 integration) — flipping the skip markers seeded in Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-project-skeleton-viled-crawl-storage/02-RESEARCH.md
@.planning/phases/02-project-skeleton-viled-crawl-storage/02-PATTERNS.md
@.planning/phases/02-project-skeleton-viled-crawl-storage/02-CONTEXT.md
@.planning/phases/02-project-skeleton-viled-crawl-storage/02-01-SUMMARY.md
@src/ga_crawler/interfaces.py
@src/ga_crawler/cli.py
@src/ga_crawler/runners/goldapple_run.py

<interfaces>
<!-- Protocol contracts FROZEN — do not modify interfaces.py -->

From src/ga_crawler/interfaces.py:
```python
class SnapshotWriterProtocol(Protocol):
    def append(self, run_id: int, retailer: str, products: list) -> int: ...

class RunWriterProtocol(Protocol):
    def patch_stats(self, run_id: int, delta: dict) -> None: ...
    def get_stats(self, run_id: int) -> dict: ...
    def fail(self, run_id: int, reason: str) -> None: ...
```

From src/ga_crawler/runners/goldapple_run.py (~lines 237-250) — DICT-SHAPE CONTRACT
that SqliteSnapshotWriter.append MUST accept verbatim (Pitfall 7):
```python
normalized = {
    "sku_id": product.sku_id,
    "url": product.url,
    "name": product.name,
    "brand": product.brand_raw,
    "brand_norm": normalizer.brand(product.brand_raw),
    "name_norm": normalizer.name(product.name),
    "current_price": product.current_price,
    "was_price": product.was_price,
    "currency": product.currency,
    "stock_state": product.availability,
    "volume_norm": normalizer.volume(product.raw_volume_text or ""),
    "raw_volume_text": product.raw_volume_text,
}
```

Note: Phase 2 EXTENDS Snapshot with `multipack_flag`, `parse_error_flag`, `volume_raw`. The
Phase 3 dict-shape above does NOT include these; the writer's signature must accept extra
fields gracefully or default them. Plan 04's viled_run.py will produce a richer dict
including multipack_flag.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SQLModel tables + WAL session + v_current_snapshots view + SqliteSnapshotWriter</name>
  <files>src/ga_crawler/storage/__init__.py, src/ga_crawler/storage/sqlite.py, tests/unit/test_storage_models.py, tests/unit/test_snapshot_writer.py, tests/integration/test_storage_wal.py, tests/integration/test_v_current_snapshots.py</files>
  <read_first>
    .planning/phases/02-project-skeleton-viled-crawl-storage/02-RESEARCH.md (§Pattern 3 lines 482-546 — verbatim Run + Snapshot SQLModel spec; §Pattern 5 lines 612-643 — SqliteSnapshotWriter; §"Initializing the WAL session" lines 956-972; §Anti-Patterns lines 859-868),
    .planning/phases/02-project-skeleton-viled-crawl-storage/02-PATTERNS.md (lines 394-477 — analog pointers + verbatim model code),
    .planning/phases/02-project-skeleton-viled-crawl-storage/02-CONTEXT.md (D-214 single-module storage; D-220 no alembic; D-221 v_current_snapshots SQL),
    src/ga_crawler/cli.py (StubSnapshotWriter lines 57-70 — behavioral parity contract: append-only, len(products) return value),
    src/ga_crawler/runners/goldapple_run.py (lines 200-260 — to confirm exact dict-shape produced; integration test will replay this shape)
  </read_first>
  <behavior>
    - Test 1 (test_storage_models.py::test_snapshot_columns): Snapshot has all 17 columns: id, run_id, retailer, sku_id, url, name, brand, brand_norm, name_norm, volume_raw, volume_norm, multipack_flag, parse_error_flag, current_price, was_price, currency, stock_state, scraped_at
    - Test 2 (test_storage_models.py::test_snapshot_unique_constraint): Inserting duplicate (run_id=1, retailer='viled', sku_id='X') raises IntegrityError
    - Test 3 (test_storage_models.py::test_run_table): Run row has run_id, started_at, finished_at, status, fail_reason, stats (default '{}')
    - Test 4 (test_snapshot_writer.py::test_append_returns_count): writer.append(1, 'viled', [{...}, {...}]) returns 2
    - Test 5 (test_snapshot_writer.py::test_append_only_no_update): writer never UPDATEs; second call with same sku_id raises IntegrityError (DATA-03 invariant)
    - Test 6 (test_snapshot_writer.py::test_append_accepts_phase3_dict_shape): writer.append accepts the dict shape from goldapple_run.py:237-250 verbatim — Pitfall 7 protection
    - Test 7 (test_snapshot_writer.py::test_per_batch_commit): batch_size=2 + 5 products + bad row at index 4 → first 2 batches (4 rows) are durable after rollback (DATA-04)
    - Test 8 (test_storage_wal.py::test_wal_pragma_active): after make_engine + connect, `PRAGMA journal_mode` returns "wal"
    - Test 9 (test_storage_wal.py::test_synchronous_normal): `PRAGMA synchronous` returns 1 (NORMAL)
    - Test 10 (test_v_current_snapshots.py::test_view_returns_latest_success_run): create runs 1 (success), 2 (failed), 3 (success); insert snapshots for each; v_current_snapshots returns rows from run 3 only
    - Test 11 (test_v_current_snapshots.py::test_view_empty_when_no_success_runs): no successful runs → view returns 0 rows
  </behavior>
  <action>
**Step A — Create `src/ga_crawler/storage/__init__.py`** (empty package marker):

```python
"""Phase 2 storage layer.

Single-module impl per D-214: `sqlite.py` houses SQLModel tables (Run, Snapshot)
+ writers (SqliteRunWriter, SqliteSnapshotWriter) + engine factory + view bootstrap.
`norm06_writer.py` houses the markdown ledger writer (per D-208 / NORM-06).

Source: 02-RESEARCH.md §Pattern 3, §Pattern 4, §Pattern 5; 02-CONTEXT.md D-214.
"""
```

**Step B — Create `src/ga_crawler/storage/sqlite.py`** (verbatim per RESEARCH §Pattern 3 + §Pattern 5 + §"Initializing the WAL session"):

```python
"""SQLite storage layer: SQLModel tables, WAL engine, atomic writers, current-view bootstrap.

Source: 02-RESEARCH.md §Pattern 3 (lines 482-546), §Pattern 4 (lines 555-606),
§Pattern 5 (lines 612-643), §"Initializing the WAL session" (lines 956-972).
Decisions: D-214 single-module storage, D-220 alembic-skip-on-day-1, D-221 v_current_snapshots.
"""

from __future__ import annotations

import json
from datetime import datetime, timezone
from pathlib import Path
from typing import Optional

import structlog
from sqlalchemy import event
from sqlmodel import (
    Field,
    Index,
    Session,
    SQLModel,
    UniqueConstraint,
    create_engine,
    text,
)

log = structlog.get_logger(__name__)


# ---- SQLModel tables (DATA-01, DATA-02) ----


class Run(SQLModel, table=True):
    """Single row per weekly run. DATA-05 lifecycle: create → patch_stats → finalize/fail."""

    __tablename__ = "runs"
    run_id: Optional[int] = Field(default=None, primary_key=True)
    started_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))
    finished_at: Optional[datetime] = None
    status: str = Field(default="running")  # running | success | failed | partial
    fail_reason: Optional[str] = None
    stats: str = Field(default="{}")  # JSON-encoded text; SQLite TEXT column


class Snapshot(SQLModel, table=True):
    """Append-only snapshot row per (run_id, retailer, sku_id). DATA-01..03."""

    __tablename__ = "snapshots"
    id: Optional[int] = Field(default=None, primary_key=True)
    run_id: int = Field(foreign_key="runs.run_id", index=True)
    retailer: str = Field(index=True)  # "viled" | "goldapple"
    sku_id: str
    url: str
    name: str
    brand: str
    brand_norm: str = Field(index=True)
    name_norm: str
    volume_raw: Optional[str] = None
    volume_norm: Optional[str] = None  # serialized "(amount, unit, count)" or NULL
    multipack_flag: bool = Field(default=False)
    parse_error_flag: bool = Field(default=False)
    current_price: Optional[int] = None
    was_price: Optional[int] = None
    currency: str = Field(default="KZT")
    stock_state: str = Field(default="UNKNOWN")
    scraped_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))

    __table_args__ = (
        UniqueConstraint("run_id", "retailer", "sku_id", name="uq_snapshot_run_retailer_sku"),
        Index("ix_snapshot_retailer_brand_norm", "retailer", "brand_norm"),
        Index("ix_snapshot_run_retailer", "run_id", "retailer"),
    )


# ---- Engine factory + PRAGMAs (DATA-04) ----


def make_engine(db_path: str | Path = "prices.db", *, echo: bool = False):
    """Create SQLAlchemy engine with WAL + synchronous=NORMAL + foreign_keys=ON.

    PRAGMA application via event listener so EVERY connection (incl. pool refills)
    gets them. Source: SQLAlchemy events doc + sqlite.org PRAGMA docs.
    """
    engine = create_engine(f"sqlite:///{db_path}", echo=echo)

    @event.listens_for(engine, "connect")
    def _set_pragma(dbapi_conn, _):  # noqa: D401
        cur = dbapi_conn.cursor()
        cur.execute("PRAGMA journal_mode=WAL")
        cur.execute("PRAGMA synchronous=NORMAL")
        cur.execute("PRAGMA foreign_keys=ON")
        cur.close()

    return engine


def init_db(db_path: str | Path = "prices.db") -> None:
    """Idempotent schema bootstrap. SQLModel.metadata.create_all + create v_current_snapshots VIEW.

    D-220: NO alembic on day 1. CLAUDE.md explicit. Add when first migration is needed (v2).
    D-221: v_current_snapshots returns rows from MAX(run_id) where status='success'.
    """
    engine = make_engine(db_path)
    SQLModel.metadata.create_all(engine)
    with engine.connect() as conn:
        # CREATE VIEW IF NOT EXISTS — idempotent
        conn.exec_driver_sql(
            """
            CREATE VIEW IF NOT EXISTS v_current_snapshots AS
            SELECT * FROM snapshots
            WHERE run_id = (SELECT MAX(run_id) FROM runs WHERE status = 'success')
            """
        )
        conn.commit()
    log.info("db_initialized", db_path=str(db_path))


# ---- SqliteSnapshotWriter (DATA-03, DATA-04) ----


class SqliteSnapshotWriter:
    """Append-only INSERT writer. Per-batch commit so mid-run failure keeps prior batches.

    Implements SnapshotWriterProtocol (interfaces.py). Accepts the same dict-shape
    that runners/goldapple_run.py:237-250 produces (Pitfall 7 contract).
    """

    def __init__(self, engine, *, batch_size: int = 100):
        self.engine = engine
        self.batch_size = batch_size

    def append(self, run_id: int, retailer: str, products: list) -> int:
        if not products:
            return 0
        inserted = 0
        # Filter unknown keys so Phase 3 dict-shape (which lacks multipack_flag /
        # parse_error_flag / volume_raw) is accepted gracefully via defaults.
        valid_fields = set(Snapshot.model_fields.keys())
        with Session(self.engine) as session:
            try:
                for product in products:
                    payload = {k: v for k, v in product.items() if k in valid_fields}
                    payload["run_id"] = run_id
                    payload["retailer"] = retailer
                    row = Snapshot(**payload)
                    session.add(row)
                    inserted += 1
                    if inserted % self.batch_size == 0:
                        session.commit()
                session.commit()  # final partial batch
            except Exception:
                session.rollback()
                raise
        return inserted
```

**Step C — Append SqliteRunWriter** (Pattern 4 lines 555-606) to the SAME file (single-module D-214). [See Task 2 for the rest — split here only because the file already crosses 150 lines and Task 2 adds another 100.]

The init code above MUST end with the SqliteSnapshotWriter class so test_snapshot_writer/test_storage_models can run independently of Task 2.

**Step D — Write tests for Snapshot/Run + WAL + view + SnapshotWriter**:

`tests/unit/test_storage_models.py` (REMOVE skip marker, replace stub):
```python
"""DATA-01, DATA-02 verification. Source: 02-RESEARCH.md §Pattern 3."""
from sqlalchemy.exc import IntegrityError
import pytest
from sqlmodel import Session
from ga_crawler.storage.sqlite import Run, Snapshot, init_db, make_engine


@pytest.fixture
def engine(tmp_path):
    db = tmp_path / "test.db"
    init_db(db)
    return make_engine(db)


def test_snapshot_columns():
    cols = set(Snapshot.model_fields.keys())
    expected = {
        "id", "run_id", "retailer", "sku_id", "url", "name", "brand",
        "brand_norm", "name_norm", "volume_raw", "volume_norm",
        "multipack_flag", "parse_error_flag", "current_price", "was_price",
        "currency", "stock_state", "scraped_at",
    }
    assert expected.issubset(cols), f"missing: {expected - cols}"


def test_run_table():
    cols = set(Run.model_fields.keys())
    expected = {"run_id", "started_at", "finished_at", "status", "fail_reason", "stats"}
    assert expected.issubset(cols), f"missing: {expected - cols}"


def test_snapshot_unique_constraint(engine):
    with Session(engine) as s:
        s.add(Run(run_id=1, status="running"))
        s.commit()
        s.add(Snapshot(run_id=1, retailer="viled", sku_id="X1", url="u", name="n", brand="b", brand_norm="b", name_norm="n"))
        s.commit()
        s.add(Snapshot(run_id=1, retailer="viled", sku_id="X1", url="u2", name="n2", brand="b", brand_norm="b", name_norm="n2"))
        with pytest.raises(IntegrityError):
            s.commit()
```

`tests/unit/test_snapshot_writer.py` (REMOVE skip marker):
```python
"""DATA-03, DATA-04 verification. Source: 02-RESEARCH.md §Pattern 5 + Anti-Patterns."""
import pytest
from sqlalchemy.exc import IntegrityError
from sqlmodel import Session, select
from ga_crawler.storage.sqlite import (
    Run, Snapshot, SqliteSnapshotWriter, init_db, make_engine,
)


@pytest.fixture
def writer_setup(tmp_path):
    db = tmp_path / "test.db"
    init_db(db)
    engine = make_engine(db)
    with Session(engine) as s:
        s.add(Run(run_id=1, status="running"))
        s.commit()
    return engine, SqliteSnapshotWriter(engine, batch_size=2)


def _record(sku_id: str, **overrides) -> dict:
    base = dict(
        sku_id=sku_id, url=f"https://x/{sku_id}", name="n", brand="B",
        brand_norm="b", name_norm="n", current_price=1000, was_price=None,
        currency="KZT", stock_state="IN_STOCK",
    )
    base.update(overrides)
    return base


def test_append_returns_count(writer_setup):
    _, writer = writer_setup
    n = writer.append(1, "viled", [_record("A"), _record("B"), _record("C")])
    assert n == 3


def test_append_only_no_update(writer_setup):
    engine, writer = writer_setup
    writer.append(1, "viled", [_record("X")])
    with pytest.raises(IntegrityError):
        writer.append(1, "viled", [_record("X", name="changed")])


def test_append_accepts_phase3_dict_shape(writer_setup):
    """Pitfall 7: Phase 3 goldapple_run.py:237-250 dict shape lacks multipack_flag /
    parse_error_flag / volume_raw — Phase 2 writer must accept gracefully via defaults."""
    _, writer = writer_setup
    p3_shape = {
        "sku_id": "P3", "url": "u", "name": "n", "brand": "b",
        "brand_norm": "b", "name_norm": "n",
        "current_price": 999, "was_price": None, "currency": "KZT",
        "stock_state": "IN_STOCK",
        "volume_norm": None, "raw_volume_text": None,
        # NOTE: no multipack_flag, parse_error_flag, volume_raw — must NOT crash
    }
    n = writer.append(1, "goldapple", [p3_shape])
    assert n == 1


def test_per_batch_commit(writer_setup):
    """DATA-04: mid-run failure preserves prior batches. batch_size=2."""
    engine, writer = writer_setup
    good = [_record(f"K{i}") for i in range(4)]
    bad = _record("K0")  # duplicate of K0 — will trigger IntegrityError
    with pytest.raises(IntegrityError):
        writer.append(1, "viled", good + [bad])
    with Session(engine) as s:
        rows = s.exec(select(Snapshot).where(Snapshot.run_id == 1)).all()
        # First 2 batches (K0, K1, K2, K3) were committed at batch boundary;
        # the failing 5th row rolled back. Expected: 4 rows durable.
        assert len(rows) == 4
```

`tests/integration/test_storage_wal.py` (REMOVE skip marker):
```python
"""DATA-04 verification. Source: 02-RESEARCH.md §Initializing the WAL session."""
import pytest
from ga_crawler.storage.sqlite import init_db, make_engine


def test_wal_pragma_active(tmp_path):
    db = tmp_path / "wal.db"
    init_db(db)
    engine = make_engine(db)
    with engine.connect() as conn:
        result = conn.exec_driver_sql("PRAGMA journal_mode").fetchone()
        assert result[0].lower() == "wal"


def test_synchronous_normal(tmp_path):
    db = tmp_path / "sync.db"
    init_db(db)
    engine = make_engine(db)
    with engine.connect() as conn:
        result = conn.exec_driver_sql("PRAGMA synchronous").fetchone()
        # NORMAL = 1
        assert result[0] == 1


def test_foreign_keys_on(tmp_path):
    db = tmp_path / "fk.db"
    init_db(db)
    engine = make_engine(db)
    with engine.connect() as conn:
        result = conn.exec_driver_sql("PRAGMA foreign_keys").fetchone()
        assert result[0] == 1
```

`tests/integration/test_v_current_snapshots.py` (REMOVE skip marker):
```python
"""D-221 verification: v_current_snapshots returns latest successful run only."""
from sqlmodel import Session
from sqlalchemy import text
from ga_crawler.storage.sqlite import Run, Snapshot, init_db, make_engine


def _add_run_with_snapshot(s, run_id: int, status: str, sku: str):
    s.add(Run(run_id=run_id, status=status))
    s.commit()
    s.add(Snapshot(run_id=run_id, retailer="viled", sku_id=sku, url="u", name="n",
                   brand="b", brand_norm="b", name_norm="n"))
    s.commit()


def test_view_returns_latest_success_run(tmp_path):
    db = tmp_path / "view.db"
    init_db(db)
    engine = make_engine(db)
    with Session(engine) as s:
        _add_run_with_snapshot(s, 1, "success", "RUN1")
        _add_run_with_snapshot(s, 2, "failed", "RUN2")
        _add_run_with_snapshot(s, 3, "success", "RUN3")
    with engine.connect() as conn:
        rows = conn.exec_driver_sql("SELECT sku_id FROM v_current_snapshots").fetchall()
    assert {r[0] for r in rows} == {"RUN3"}


def test_view_empty_when_no_success_runs(tmp_path):
    db = tmp_path / "empty.db"
    init_db(db)
    engine = make_engine(db)
    with Session(engine) as s:
        _add_run_with_snapshot(s, 1, "failed", "X")
    with engine.connect() as conn:
        rows = conn.exec_driver_sql("SELECT sku_id FROM v_current_snapshots").fetchall()
    # No successful run → MAX(run_id) is NULL → WHERE run_id = NULL → 0 rows
    assert rows == []
```

**Step E — Run the new tests** (`uv run pytest tests/unit/test_storage_models.py tests/unit/test_snapshot_writer.py tests/integration/test_storage_wal.py tests/integration/test_v_current_snapshots.py -x -q`). All must pass.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_storage_models.py tests/unit/test_snapshot_writer.py tests/integration/test_storage_wal.py tests/integration/test_v_current_snapshots.py -x -q 2>&1 | tail -5 && grep -q "class Snapshot(SQLModel, table=True)" src/ga_crawler/storage/sqlite.py && grep -q "PRAGMA journal_mode=WAL" src/ga_crawler/storage/sqlite.py && grep -q "v_current_snapshots" src/ga_crawler/storage/sqlite.py && grep -q "UniqueConstraint" src/ga_crawler/storage/sqlite.py && grep -q "batch_size" src/ga_crawler/storage/sqlite.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "class Run(SQLModel, table=True):" src/ga_crawler/storage/sqlite.py`
    - `grep -q "class Snapshot(SQLModel, table=True):" src/ga_crawler/storage/sqlite.py`
    - `grep -q 'UniqueConstraint("run_id", "retailer", "sku_id"' src/ga_crawler/storage/sqlite.py`
    - `grep -q "PRAGMA journal_mode=WAL" src/ga_crawler/storage/sqlite.py`
    - `grep -q "PRAGMA synchronous=NORMAL" src/ga_crawler/storage/sqlite.py`
    - `grep -q "v_current_snapshots" src/ga_crawler/storage/sqlite.py`
    - `grep -q "class SqliteSnapshotWriter:" src/ga_crawler/storage/sqlite.py`
    - `grep -q "batch_size" src/ga_crawler/storage/sqlite.py`
    - `pytest tests/unit/test_storage_models.py tests/unit/test_snapshot_writer.py tests/integration/test_storage_wal.py tests/integration/test_v_current_snapshots.py -x` exits 0
    - 4 test files no longer carry `pytestmark = pytest.mark.skip` (`grep -L "pytestmark = pytest.mark.skip" tests/unit/test_storage_models.py tests/unit/test_snapshot_writer.py tests/integration/test_storage_wal.py tests/integration/test_v_current_snapshots.py | wc -l` == 4)
    - Existing 192 Phase 1+3 tests stay green (`pytest -m "not live" -q` exits 0)
  </acceptance_criteria>
  <done>SQLModel Run + Snapshot tables shipped with all required columns and UNIQUE constraint; make_engine applies WAL/synchronous/foreign_keys PRAGMAs; init_db creates v_current_snapshots VIEW; SqliteSnapshotWriter is append-only with per-batch commits and accepts Phase 3 dict-shape; 11 tests across 4 files all GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SqliteRunWriter (atomic json_patch) + Norm06Writer markdown ledger</name>
  <files>src/ga_crawler/storage/sqlite.py, src/ga_crawler/storage/norm06_writer.py, tests/unit/test_run_writer.py, tests/integration/test_run_writer_lifecycle.py, tests/unit/test_norm06_writer.py</files>
  <read_first>
    .planning/phases/02-project-skeleton-viled-crawl-storage/02-RESEARCH.md (§Pattern 4 lines 548-608 — SqliteRunWriter spec; §Pitfall 4 RFC-7396 null-deletion gotcha; §Open Q1 — concrete-only create/finalize NOT in Protocol),
    .planning/phases/02-project-skeleton-viled-crawl-storage/02-PATTERNS.md (lines 463-477 — atomic json_patch verbatim; storage/norm06_writer.py role-match — file-write per run-dir),
    .planning/phases/02-project-skeleton-viled-crawl-storage/02-CONTEXT.md (D-208 markdown table schema 4 columns; D-211 Norm06Writer.persist signature),
    src/ga_crawler/cli.py (StubRunWriter lines 73-120 — behavioral parity contract),
    src/ga_crawler/storage/sqlite.py (existing from Task 1 — APPEND SqliteRunWriter, do not duplicate Run model)
  </read_first>
  <behavior>
    - Test 1 (test_run_writer.py::test_create_returns_run_id): writer.create() → int run_id; second create() returns different id
    - Test 2 (test_run_writer.py::test_patch_stats_atomic_merge_pitfall_6): seed runs.stats={"viled.x":1}; patch_stats({"goldapple.y":2}) → stats={"viled.x":1, "goldapple.y":2} (NO key loss — Pitfall 6 verified)
    - Test 3 (test_run_writer.py::test_patch_stats_overrides_existing_key): seed {"k":1}; patch {"k":2} → {"k":2}
    - Test 4 (test_run_writer.py::test_get_stats_returns_dict): patch then get_stats → expected dict
    - Test 5 (test_run_writer.py::test_get_stats_missing_run): get_stats(999) → {}
    - Test 6 (test_run_writer.py::test_fail_idempotent): fail(1, "r1"); fail(1, "r2") → status='failed', reason='r2', no error
    - Test 7 (test_run_writer.py::test_finalize_only_running): finalize(1, "success") on running run → status='success'; finalize on already-failed run → status stays 'failed' (idempotent guard via WHERE status='running')
    - Test 8 (test_run_writer_lifecycle.py::test_full_run_cycle): create → patch_stats(viled.*) → patch_stats(goldapple.*) → finalize → all stats merged, status='success', finished_at populated
    - Test 9 (test_norm06_writer.py::test_writes_markdown_table): persist(run_id=44, viled_unmatched=["jo_malone"], goldapple_new_slugs=["tom-ford-private-blend"]) writes file with 4 columns and 2 rows + correct source values
    - Test 10 (test_norm06_writer.py::test_status_pending_default): all rows in markdown have `pending` in status column
    - Test 11 (test_norm06_writer.py::test_empty_inputs_writes_header_only): persist(run_id=99, [], []) writes a file with the table header but no data rows
  </behavior>
  <action>
**Step A — Append SqliteRunWriter to `src/ga_crawler/storage/sqlite.py`** (verbatim per RESEARCH §Pattern 4):

```python
# ---- SqliteRunWriter (DATA-05; atomic json_patch — Pitfall 6) ----


class SqliteRunWriter:
    """RunWriterProtocol implementer + concrete create()/finalize() (NOT in Protocol per Open Q1).

    Atomic stats merge via SQLite json_patch (RFC-7396 MergePatch). Phase 2 writes
    only viled.* keys; Phase 3 writes only goldapple.* keys; merge cleanly.

    Pitfall 4: passing None/null in delta DELETES the key (RFC-7396 semantics).
    Callers MUST NOT pass None values; use sentinels (-1, "", []) instead.
    """

    def __init__(self, engine):
        self.engine = engine

    def create(self, run_id: Optional[int] = None) -> int:
        """Concrete-only (NOT in Protocol per Open Q1). Open a new runs row.
        Returns the assigned run_id (auto-increment if not provided)."""
        with Session(self.engine) as s:
            row = Run(run_id=run_id, status="running")
            s.add(row)
            s.commit()
            s.refresh(row)
            return row.run_id  # type: ignore[return-value]

    def patch_stats(self, run_id: int, delta: dict) -> None:
        """Atomic JSON-merge into runs.stats (Pitfall 6 RFC-7396 MergePatch).

        Pitfall 4: delta MUST NOT contain None/null values (would DELETE keys).
        """
        if any(v is None for v in delta.values()):
            raise ValueError(
                "Pitfall 4: delta contains None — RFC-7396 MergePatch DELETES "
                "the key. Use sentinels (-1, '', []) or omit the key."
            )
        delta_json = json.dumps(delta, ensure_ascii=False, default=str)
        with Session(self.engine) as s:
            s.exec(  # type: ignore[call-overload]
                text("UPDATE runs SET stats = json_patch(stats, :delta) WHERE run_id = :rid"),
                params={"delta": delta_json, "rid": run_id},
            )
            s.commit()

    def get_stats(self, run_id: int) -> dict:
        with Session(self.engine) as s:
            row = s.get(Run, run_id)
            if row is None:
                return {}
            return json.loads(row.stats or "{}")

    def fail(self, run_id: int, reason: str) -> None:
        """Idempotent — safe to call from try/finally even if already failed."""
        now = datetime.now(timezone.utc)
        with Session(self.engine) as s:
            s.exec(  # type: ignore[call-overload]
                text(
                    "UPDATE runs SET status='failed', fail_reason=:r, finished_at=:t "
                    "WHERE run_id=:rid"
                ),
                params={"r": reason, "rid": run_id, "t": now},
            )
            s.commit()

    def finalize(self, run_id: int, status: str = "success") -> None:
        """Concrete-only (NOT in Protocol). Close run with explicit status.
        WHERE status='running' guard makes this idempotent — a previously failed run
        cannot be 'unfailed' by a subsequent finalize call.
        """
        now = datetime.now(timezone.utc)
        with Session(self.engine) as s:
            s.exec(  # type: ignore[call-overload]
                text(
                    "UPDATE runs SET status=:s, finished_at=:t "
                    "WHERE run_id=:rid AND status='running'"
                ),
                params={"s": status, "rid": run_id, "t": now},
            )
            s.commit()


__all__ = [
    "Run", "Snapshot",
    "SqliteSnapshotWriter", "SqliteRunWriter",
    "make_engine", "init_db",
]
```

**Step B — Create `src/ga_crawler/storage/norm06_writer.py`** (NORM-06 + D-208 + D-211):

```python
"""NORM-06 review queue writer — markdown ledger per run.

Source: 02-CONTEXT.md D-208 (markdown table schema), D-211 (Phase 2 owns write-path,
not Phase 3 stub). Mirror role of enumeration/goldapple_sitemap.py:persist_sitemap_slugs
(file-I/O per run-dir).

Schema (D-208):

    # NORM-06 Review Queue — Run {run_id} ({YYYY-MM-DD})

    | brand_or_slug | source              | run_id | status  |
    |---------------|---------------------|--------|---------|
    | jo_malone     | viled-unmatched     | 44     | pending |
    | tom-ford-...  | goldapple-new-slug  | 44     | pending |

Source enum: viled-unmatched | goldapple-new-slug
Status default: pending; operator edits to aliased | skip | reviewed.
"""

from __future__ import annotations

from datetime import date
from pathlib import Path

import structlog

log = structlog.get_logger(__name__)


class Norm06Writer:
    """Markdown ledger writer for NORM-06 review queue.

    File-based ledger (D-210: NO DB-table backup on v1; git history is audit trail).
    """

    def __init__(self, repo_root: Path | str = "."):
        self.repo_root = Path(repo_root)

    def persist(
        self,
        run_id: int,
        viled_unmatched: list[str],
        goldapple_new_slugs: list[str],
    ) -> Path:
        """Render markdown to .planning/runs/{run_id}/norm06-review.md.

        Always writes the header even if both lists are empty (creates the audit
        artifact for the run regardless).
        """
        out_dir = self.repo_root / ".planning" / "runs" / str(run_id)
        out_dir.mkdir(parents=True, exist_ok=True)
        out_path = out_dir / "norm06-review.md"

        today = date.today().isoformat()
        lines = [
            f"# NORM-06 Review Queue — Run {run_id} ({today})",
            "",
            "| brand_or_slug | source | run_id | status |",
            "|---------------|--------|--------|--------|",
        ]
        for brand in viled_unmatched:
            lines.append(f"| {brand} | viled-unmatched | {run_id} | pending |")
        for slug in goldapple_new_slugs:
            lines.append(f"| {slug} | goldapple-new-slug | {run_id} | pending |")

        out_path.write_text("\n".join(lines) + "\n", encoding="utf-8")
        log.info(
            "norm06_persisted",
            run_id=run_id,
            out_path=str(out_path),
            viled_unmatched_count=len(viled_unmatched),
            goldapple_new_slugs_count=len(goldapple_new_slugs),
        )
        return out_path


__all__ = ["Norm06Writer"]
```

**Step C — Write `tests/unit/test_run_writer.py`** (REMOVE skip marker):

```python
"""DATA-05 + Pitfall 4 + Pitfall 6 verification. Source: 02-RESEARCH.md §Pattern 4."""
import pytest
from sqlmodel import Session
from ga_crawler.storage.sqlite import (
    Run, SqliteRunWriter, init_db, make_engine,
)


@pytest.fixture
def writer(tmp_path):
    db = tmp_path / "rw.db"
    init_db(db)
    engine = make_engine(db)
    return SqliteRunWriter(engine), engine


def test_create_returns_run_id(writer):
    rw, _ = writer
    rid1 = rw.create()
    rid2 = rw.create()
    assert isinstance(rid1, int) and isinstance(rid2, int)
    assert rid1 != rid2


def test_patch_stats_atomic_merge_pitfall_6(writer):
    """Pitfall 6: viled.* and goldapple.* keys merge cleanly via json_patch."""
    rw, engine = writer
    rid = rw.create()
    rw.patch_stats(rid, {"viled.fetch_count": 100, "viled.parse_failures": 2})
    rw.patch_stats(rid, {"goldapple.fetch_count": 1000, "goldapple.fetch_failures": 5})
    stats = rw.get_stats(rid)
    assert stats["viled.fetch_count"] == 100
    assert stats["viled.parse_failures"] == 2
    assert stats["goldapple.fetch_count"] == 1000
    assert stats["goldapple.fetch_failures"] == 5


def test_patch_stats_overrides_existing_key(writer):
    rw, _ = writer
    rid = rw.create()
    rw.patch_stats(rid, {"k": 1})
    rw.patch_stats(rid, {"k": 2})
    assert rw.get_stats(rid)["k"] == 2


def test_patch_stats_rejects_none_pitfall_4(writer):
    """Pitfall 4: RFC-7396 treats null as DELETE; we reject upstream."""
    rw, _ = writer
    rid = rw.create()
    with pytest.raises(ValueError, match="Pitfall 4"):
        rw.patch_stats(rid, {"viled.foo": None})


def test_get_stats_missing_run(writer):
    rw, _ = writer
    assert rw.get_stats(9999) == {}


def test_fail_idempotent(writer):
    rw, engine = writer
    rid = rw.create()
    rw.fail(rid, "first reason")
    rw.fail(rid, "second reason")  # must not raise
    with Session(engine) as s:
        row = s.get(Run, rid)
        assert row.status == "failed"
        assert row.fail_reason == "second reason"


def test_finalize_only_running(writer):
    """finalize() WHERE status='running' guard: cannot un-fail a failed run."""
    rw, engine = writer
    rid = rw.create()
    rw.fail(rid, "boom")
    rw.finalize(rid, "success")  # must NOT change status
    with Session(engine) as s:
        row = s.get(Run, rid)
        assert row.status == "failed"
```

**Step D — Write `tests/integration/test_run_writer_lifecycle.py`** (REMOVE skip):

```python
"""DATA-05 lifecycle integration: create → patch_stats(viled+goldapple) → finalize.
Source: 02-RESEARCH.md §System Architecture Diagram steps 1+2e+4+6."""
from sqlmodel import Session
from ga_crawler.storage.sqlite import (
    Run, SqliteRunWriter, init_db, make_engine,
)


def test_full_run_cycle(tmp_path):
    db = tmp_path / "lifecycle.db"
    init_db(db)
    engine = make_engine(db)
    rw = SqliteRunWriter(engine)
    rid = rw.create()
    rw.patch_stats(rid, {"viled.fetch_count": 500, "viled.sanity_gate_n_pass": 1})
    rw.patch_stats(rid, {"goldapple.fetch_count": 3000, "goldapple.sanity_gate_m_pass": 1})
    rw.finalize(rid, "success")
    with Session(engine) as s:
        row = s.get(Run, rid)
        assert row.status == "success"
        assert row.finished_at is not None
    stats = rw.get_stats(rid)
    assert "viled.fetch_count" in stats
    assert "goldapple.fetch_count" in stats
```

**Step E — Write `tests/unit/test_norm06_writer.py`** (REMOVE skip):

```python
"""NORM-06 + D-208 markdown schema verification."""
from ga_crawler.storage.norm06_writer import Norm06Writer


def test_writes_markdown_table(tmp_path):
    w = Norm06Writer(repo_root=tmp_path)
    out = w.persist(
        run_id=44,
        viled_unmatched=["jo_malone_london"],
        goldapple_new_slugs=["tom-ford-private-blend"],
    )
    assert out.exists()
    content = out.read_text(encoding="utf-8")
    assert "# NORM-06 Review Queue — Run 44" in content
    assert "| brand_or_slug | source | run_id | status |" in content
    assert "| jo_malone_london | viled-unmatched | 44 | pending |" in content
    assert "| tom-ford-private-blend | goldapple-new-slug | 44 | pending |" in content


def test_status_pending_default(tmp_path):
    w = Norm06Writer(repo_root=tmp_path)
    out = w.persist(run_id=10, viled_unmatched=["b1", "b2"], goldapple_new_slugs=["s1"])
    content = out.read_text(encoding="utf-8")
    # 3 data rows; each ends with " pending |"
    pending_count = content.count("| pending |")
    assert pending_count == 3


def test_empty_inputs_writes_header_only(tmp_path):
    w = Norm06Writer(repo_root=tmp_path)
    out = w.persist(run_id=99, viled_unmatched=[], goldapple_new_slugs=[])
    content = out.read_text(encoding="utf-8")
    assert "# NORM-06 Review Queue — Run 99" in content
    assert "| brand_or_slug | source | run_id | status |" in content
    # No "viled-unmatched" or "goldapple-new-slug" data rows
    assert "viled-unmatched" not in content
    assert "goldapple-new-slug" not in content
```

**Step F — Run all 7 storage tests**: `uv run pytest tests/unit/test_storage_models.py tests/unit/test_run_writer.py tests/unit/test_snapshot_writer.py tests/unit/test_norm06_writer.py tests/integration/test_storage_wal.py tests/integration/test_v_current_snapshots.py tests/integration/test_run_writer_lifecycle.py -x -q`. All must pass.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_run_writer.py tests/unit/test_norm06_writer.py tests/integration/test_run_writer_lifecycle.py -x -q 2>&1 | tail -5 && grep -q "json_patch(stats" src/ga_crawler/storage/sqlite.py && grep -q "class SqliteRunWriter:" src/ga_crawler/storage/sqlite.py && grep -q "Pitfall 4" src/ga_crawler/storage/sqlite.py && grep -q "WHERE status='running'" src/ga_crawler/storage/sqlite.py && grep -q "class Norm06Writer" src/ga_crawler/storage/norm06_writer.py && grep -q "norm06-review.md" src/ga_crawler/storage/norm06_writer.py && uv run pytest -m "not live" -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "class SqliteRunWriter:" src/ga_crawler/storage/sqlite.py`
    - `grep -q "json_patch(stats, :delta)" src/ga_crawler/storage/sqlite.py` (Pitfall 6 atomic merge)
    - `grep -q "Pitfall 4" src/ga_crawler/storage/sqlite.py` (None-rejection comment present)
    - `grep -q "WHERE status='running'" src/ga_crawler/storage/sqlite.py` (finalize idempotency guard)
    - `grep -q "def create(self" src/ga_crawler/storage/sqlite.py` AND `grep -q "def finalize(self" src/ga_crawler/storage/sqlite.py` (concrete-only methods present)
    - `grep -L "RunWriterProtocol" src/ga_crawler/interfaces.py` returns nothing — interfaces.py UNCHANGED (Protocol drift avoided per Open Q1)
    - `git diff src/ga_crawler/interfaces.py` is empty (0 lines changed)
    - `grep -q "class Norm06Writer" src/ga_crawler/storage/norm06_writer.py`
    - `grep -q "norm06-review.md" src/ga_crawler/storage/norm06_writer.py`
    - `grep -q "viled-unmatched" src/ga_crawler/storage/norm06_writer.py` AND `grep -q "goldapple-new-slug" src/ga_crawler/storage/norm06_writer.py`
    - `pytest tests/unit/test_run_writer.py tests/unit/test_norm06_writer.py tests/integration/test_run_writer_lifecycle.py -x` exits 0
    - `pytest -m "not live" -q` exits 0 (all 192 prior + Plan 02 tests green)
    - 3 test files no longer carry `pytestmark = pytest.mark.skip`
  </acceptance_criteria>
  <done>SqliteRunWriter ships with atomic json_patch + Pitfall-4 None-rejection + idempotent fail/finalize; concrete create/finalize NOT added to Protocol (drift avoided); Norm06Writer renders D-208 markdown ledger; 11 new tests across 3 files all GREEN. Full pytest suite still 192+ passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → SQLite WAL | Concurrent backup tool reads WAL+main file; partial-checkpoint state visible to backup |
| Phase 2 viled writer ↔ Phase 3 goldapple writer | Both write to runs.stats simultaneously (in theory) — must merge atomically without races |
| operator-edited norm06-review.md | Operator hand-edits status column; next run reads/ignores |
| caller → patch_stats(delta) | delta dict from orchestrator; could carry None values that DELETE keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | SQLite WAL not enabled → reader sees half-written snapshot during concurrent backup | mitigate | `make_engine` registers SQLAlchemy `connect` event listener that runs `PRAGMA journal_mode=WAL` + `PRAGMA synchronous=NORMAL` on every connection — verified by test_storage_wal.py::test_wal_pragma_active |
| T-02-06 | Tampering | runs.stats race between viled and goldapple writers | mitigate | `UPDATE ... json_patch(stats, :delta)` is SQL-level atomic (Pitfall 6); never read-modify-write. Anti-pattern explicitly rejected in RESEARCH §Anti-Patterns + tested in test_run_writer.py::test_patch_stats_atomic_merge_pitfall_6 |
| T-02-07 | Tampering | caller passes None in delta → RFC-7396 DELETES key (Pitfall 4) | mitigate | `patch_stats` raises `ValueError` if any delta value is None; tested in test_run_writer.py::test_patch_stats_rejects_none_pitfall_4 |
| T-02-08 | Tampering | finalize() called on already-failed run could "un-fail" | mitigate | `WHERE status='running'` guard in finalize SQL; tested in test_run_writer.py::test_finalize_only_running |
| T-02-09 | Repudiation | norm06-review.md edited by operator without git commit | accept | D-205/D-208/D-210 explicit: file lives under .planning/runs/ in git; PR enforced operator-side. CI lint deferred to Phase 7 ops-playbook. |
| T-02-10 | DoS | per-run transaction (instead of per-batch) loses entire 600-SKU run on 1 bad row | mitigate | `SqliteSnapshotWriter` commits every `batch_size=100` rows; mid-run failure preserves prior batches. Tested in test_snapshot_writer.py::test_per_batch_commit. DATA-04 invariant. |
| T-02-W2-Pitfall7 | Tampering | Phase 3 dict shape misaligned with Phase 2 SnapshotWriter expected fields | mitigate | `append()` filters payload to `Snapshot.model_fields.keys()` → unknown keys ignored, missing optional keys default. Tested in test_snapshot_writer.py::test_append_accepts_phase3_dict_shape |
</threat_model>

<verification>
1. `grep -q "class Run(SQLModel, table=True)" src/ga_crawler/storage/sqlite.py` → DATA-01
2. `grep -q "class Snapshot(SQLModel, table=True)" src/ga_crawler/storage/sqlite.py` → DATA-01
3. `grep -q "PRAGMA journal_mode=WAL" src/ga_crawler/storage/sqlite.py` → DATA-04 WAL
4. `grep -q "json_patch(stats, :delta)" src/ga_crawler/storage/sqlite.py` → Pitfall 6
5. `grep -q "v_current_snapshots" src/ga_crawler/storage/sqlite.py` → D-221
6. `grep -q "class Norm06Writer" src/ga_crawler/storage/norm06_writer.py` → NORM-06
7. `pytest tests/unit/test_storage_models.py tests/unit/test_snapshot_writer.py tests/unit/test_run_writer.py tests/unit/test_norm06_writer.py tests/integration/test_storage_wal.py tests/integration/test_v_current_snapshots.py tests/integration/test_run_writer_lifecycle.py -x` exits 0
8. `pytest -m "not live"` exits 0 (no regressions)
9. `git diff src/ga_crawler/interfaces.py` is empty (Open Q1: concrete-only create/finalize NOT added to Protocol)
</verification>

<success_criteria>
- src/ga_crawler/storage/sqlite.py exports Run, Snapshot, SqliteSnapshotWriter, SqliteRunWriter, make_engine, init_db
- Snapshot has all 17 columns (13 required per DATA-02 + parse_error_flag + 3 audit columns)
- UNIQUE constraint on (run_id, retailer, sku_id) enforced
- WAL + synchronous=NORMAL + foreign_keys=ON applied via event listener (every connect)
- v_current_snapshots SQL VIEW created in init_db; returns rows from MAX(run_id) WHERE status='success'
- SqliteSnapshotWriter is INSERT-only with per-batch commits (DATA-03 + DATA-04)
- SqliteRunWriter.patch_stats uses raw SQL `json_patch` for atomic merge (Pitfall 6)
- SqliteRunWriter rejects None values in delta (Pitfall 4)
- SqliteRunWriter.fail and SqliteRunWriter.finalize are idempotent
- create() and finalize() are concrete-only, NOT added to RunWriterProtocol (Open Q1)
- Norm06Writer.persist(run_id, viled_unmatched, goldapple_new_slugs) writes .planning/runs/{run_id}/norm06-review.md per D-208 schema
- 11 new tests across 7 files all GREEN; existing 192 still GREEN
- alembic NOT added to deps (D-220)
</success_criteria>

<output>
After completion, create `.planning/phases/02-project-skeleton-viled-crawl-storage/02-02-SUMMARY.md` documenting:
- Storage layer files shipped (sqlite.py LOC, norm06_writer.py LOC)
- Test count delta (e.g. "192 → 203 passing, 24 → 13 skipped")
- Confirmation: interfaces.py UNCHANGED (Protocol drift avoided)
- Cascading constraints: Plans 04 + 05 import `from ga_crawler.storage.sqlite import SqliteRunWriter, SqliteSnapshotWriter, init_db, make_engine`
</output>
</content>
</invoke>