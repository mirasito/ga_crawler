---
phase: 4
plan: 01
type: execute
wave: 1
autonomous: true
reconstructed: 2026-05-14
requirements: [MATCH-01, MATCH-02, MATCH-03, MATCH-04]
---

<objective>
Phase 4 retroactive stub — strict-key matcher + match-rate KPI. Original implementation shipped 2026-05-11. This stub provides the `<threat_model>` block reconstructed verbatim from production code docstring in `src/ga_crawler/matcher/strict_key.py` lines 27-36 for the `/gsd-secure-phase 4` skill (AUDIT-DEBT-02 precondition).
</objective>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Orchestrator → matcher SQL | `run_id` value flows from orchestrator caller into SQL JOIN; must not allow injection |
| Matcher → SQLite `matches` table | DELETE+INSERT pair must be atomic; partial write leaves orphan rows |
| KPI formula → stats namespace | `price_delta_pct` formula must not silently change across refactors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation / Evidence |
|-----------|----------|-----------|-------------|----------------------|
| T-04-03-01 | Injection | `matcher/strict_key.py` — SQL layer | mitigate | Every SQL uses `text('... :rid ...')` + `params={'rid': run_id}`; no f-string interpolation reaches the SQL layer. Module-level constants `INSERT_MATCHES_SQL`, `DENOMINATOR_SQL`, `BRAND_OVERLAP_SQL`, `COMPARABLE_COUNT_SQL`, `DELETE_MATCHES_SQL`, `RUN_STATUS_SQL` all defined with `:rid`/`:retailer` placeholders (lines 58-142). `compute_comparable_counts` likewise passes `retailer` via bind param. Mirrors Phase 2 D-215. (Docstring lines 27-29 verbatim.) |
| T-04-03-02 | Tampering | `matcher/strict_key.py` — KPI formula | mitigate | KPI formula drift (silent divisor change) mitigated: symmetric-filter denominator wired in `compute_denominator`; 11/11 verification truths confirmed. INSERT and DENOMINATOR SQL live as module-level `text(...)` constants — the regression-canary test (`test_match_rate_formula_canary`) can source-lock them via `str(...)` substring asserts. (Docstring lines 31-33 verbatim.) |
| T-04-03-03 | Tampering | `matcher/strict_key.py` — transaction atomicity | mitigate | Partial INSERT on crash leaves orphan matches — mitigated: `DELETE_MATCHES_SQL` runs in same transaction as `INSERT_MATCHES_SQL` via `engine.begin()` block (D-410). D-411 explicit `run.status` pre-finalize gate before matcher reads; gate ensures matcher only runs on a finalized (success) run. (Docstring lines 34-36 verbatim.) |
</threat_model>
