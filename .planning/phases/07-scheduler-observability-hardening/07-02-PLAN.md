---
phase: 07-scheduler-observability-hardening
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - deploy/etc-cron-d-ga_crawler
  - deploy/etc-logrotate-d-ga_crawler
  - .env.example
autonomous: true
requirements: [SCHED-01, SCHED-02, SCHED-04]
threat_refs: [T-07-01, T-07-04, T-07-05]
wave_strategy_note: "Wave 2 — deploy templates + .env.example backfill. Three small static artifacts; first 'deploy/' directory in repo. Verbatim from CONTEXT.md decision blocks (D-705 logrotate, D-708 cron, Action Items line 306 .env line). After this plan: 3 of 7 Wave 0 canaries flip RED → GREEN (cron_template_shape, logrotate_template_shape, env_example_shape)."

must_haves:
  truths:
    - "Cron template file `deploy/etc-cron-d-ga_crawler` exists with verbatim D-708 content (CRON_TZ=Asia/Almaty, MAILTO=\"\", Sunday 23:00 weekly-run row, daily 01:00 backup row)."
    - "Logrotate template `deploy/etc-logrotate-d-ga_crawler` exists with 7 D-705 directives + path glob."
    - "`.env.example` retains 3 TG_* placeholders AND adds HC_PING_URL= line with comment block."
    - "All 3 Wave 0 canaries for these artifacts flip from RED to GREEN."
  artifacts:
    - path: "deploy/etc-cron-d-ga_crawler"
      provides: "Cron config-as-code template (D-708 verbatim); deployed via cp to /etc/cron.d/ga_crawler"
      contains: "CRON_TZ=Asia/Almaty"
    - path: "deploy/etc-logrotate-d-ga_crawler"
      provides: "Logrotate config-as-code template (D-705 verbatim); deployed via cp to /etc/logrotate.d/ga_crawler"
      contains: "/var/log/ga_crawler/*.log"
    - path: ".env.example"
      provides: "ENV template with Phase 6 TG_* + Phase 7 HC_PING_URL placeholders"
      contains: "HC_PING_URL="
  key_links:
    - from: "deploy/etc-cron-d-ga_crawler"
      to: "bin/weekly-run.sh"
      via: "Sunday 23:00 row references /opt/ga_crawler/bin/weekly-run.sh (Plan 07-03 ships)"
      pattern: "/opt/ga_crawler/bin/weekly-run.sh"
    - from: "deploy/etc-cron-d-ga_crawler"
      to: "bin/backup.sh"
      via: "Daily 01:00 row references existing /opt/ga_crawler/bin/backup.sh (Plan 02-06 frozen)"
      pattern: "/opt/ga_crawler/bin/backup.sh"
    - from: ".env.example"
      to: "bin/weekly-run.sh"
      via: "HC_PING_URL= placeholder is required by D-703 fail-loud in wrapper (Plan 07-03)"
      pattern: "HC_PING_URL="
---

<objective>
Ship 3 static deployment templates per locked CONTEXT.md decisions:
1. `deploy/etc-cron-d-ga_crawler` — cron config (D-708)
2. `deploy/etc-logrotate-d-ga_crawler` — logrotate config (D-705)
3. `.env.example` — add HC_PING_URL= line (Action Items line 306)

Purpose: SCHED-01 (cron weekly Sunday-23:00 Almaty), SCHED-02 (CRON_TZ no-drift), SCHED-04 (logrotate weekly keep 13) closed at template-shape level. These are config-as-code: planner provides exact content; operator copies `deploy/etc-* -> /etc/cron.d/* and /etc/logrotate.d/*` per README §2 (Plan 07-04).

Output: 3 deployment artifacts; first `deploy/` directory created in the repo.
</objective>

<execution_context>
@C:\Users\gstorepc\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\gstorepc\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-scheduler-observability-hardening/07-CONTEXT.md
@.planning/phases/07-scheduler-observability-hardening/07-RESEARCH.md
@.planning/phases/07-scheduler-observability-hardening/07-PATTERNS.md
@.env.example
@tests/test_phase07_cron_template_shape.py
@tests/test_phase07_logrotate_template_shape.py
@tests/test_phase07_env_example_shape.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Cron template — deploy/etc-cron-d-ga_crawler (D-708 verbatim)</name>
  <files>deploy/etc-cron-d-ga_crawler</files>
  <read_first>
    - .planning/phases/07-scheduler-observability-hardening/07-CONTEXT.md §D-708 lines 84–89 (cron content verbatim)
    - .planning/phases/07-scheduler-observability-hardening/07-RESEARCH.md §Example 1 lines 374–382 + §Pitfall #1 (filename no-dots) + §Pitfall #2 (MAILTO="")
    - .planning/phases/07-scheduler-observability-hardening/07-PATTERNS.md §"deploy/etc-cron-d-ga_crawler"
    - tests/test_phase07_cron_template_shape.py (the canary that this task closes)
  </read_first>
  <action>
Create new file `deploy/etc-cron-d-ga_crawler` with the EXACT content below (no trailing newline drift):

```
# /etc/cron.d/ga_crawler — Phase 7 SCHED-01 / SCHED-02.
# Source: 07-CONTEXT.md D-708; crontab(5); 07-RESEARCH.md Example 1 + Pitfall #1 + Pitfall #2.
#
# Deploy via README §4:
#   sudo cp deploy/etc-cron-d-ga_crawler /etc/cron.d/ga_crawler
#   sudo chown root:root /etc/cron.d/ga_crawler
#   sudo chmod 0644 /etc/cron.d/ga_crawler
#   sudo systemctl reload cron
#
# Pitfall #1: filename MUST NOT contain a dot — Vixie cron silently ignores
# /etc/cron.d/* with non-[A-Za-z0-9_-] characters.
# Pitfall #2: MAILTO="" (empty string) explicitly disables cron email leak
# (T-07-01 Information Disclosure mitigation).
#
# Schedule (Asia/Almaty, UTC+5):
#   Sunday 23:00 Almaty (Sunday 18:00 UTC) — weekly viled+goldapple crawl + delivery
#   Daily   01:00 Almaty (Daily 20:00 UTC prev) — sqlite3 .backup with 4-rotate retention
#
# Report lands in business chat Monday morning Almaty (~02:00–03:00 after a 3–4h run).

CRON_TZ=Asia/Almaty
MAILTO=""
0 23 * * 0 ga_crawler /opt/ga_crawler/bin/weekly-run.sh
0 1  * * * ga_crawler /opt/ga_crawler/bin/backup.sh
```

**Notes for executor:**
- The two data rows MUST match canary substrings EXACTLY:
  - `0 23 * * 0 ga_crawler /opt/ga_crawler/bin/weekly-run.sh`
  - `0 1  * * * ga_crawler /opt/ga_crawler/bin/backup.sh` (note: **two spaces** between "1" and "* * *" — matches D-708 verbatim per PATTERNS.md line 330)
- Filename MUST be `etc-cron-d-ga_crawler` (no dot, no extension) — Pitfall #1.
- Use Write tool to create the file. Ensure UTF-8 encoding. Do NOT add trailing whitespace lines beyond a single final newline.

Run the cron shape canary to flip RED → GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/test_phase07_cron_template_shape.py -x</automated>
  </verify>
  <done>
    - File `deploy/etc-cron-d-ga_crawler` exists (path has NO dot in filename).
    - All 5 tests in `tests/test_phase07_cron_template_shape.py` GREEN.
    - `grep -c '^CRON_TZ=Asia/Almaty$' deploy/etc-cron-d-ga_crawler` returns 1.
    - `grep -c '^MAILTO=""$' deploy/etc-cron-d-ga_crawler` returns 1.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Logrotate template — deploy/etc-logrotate-d-ga_crawler (D-705 verbatim)</name>
  <files>deploy/etc-logrotate-d-ga_crawler</files>
  <read_first>
    - .planning/phases/07-scheduler-observability-hardening/07-CONTEXT.md §D-705 lines 33–43 (logrotate content verbatim)
    - .planning/phases/07-scheduler-observability-hardening/07-RESEARCH.md §Example 2 lines 385–397 + §Pitfall #5 (user must exist first; notifempty + 0-byte logs)
    - .planning/phases/07-scheduler-observability-hardening/07-PATTERNS.md §"deploy/etc-logrotate-d-ga_crawler"
    - tests/test_phase07_logrotate_template_shape.py (the canary that this task closes)
  </read_first>
  <action>
Create new file `deploy/etc-logrotate-d-ga_crawler` with the EXACT content below:

```
# /etc/logrotate.d/ga_crawler — Phase 7 SCHED-04.
# Source: 07-CONTEXT.md D-705; logrotate(8); 07-RESEARCH.md Example 2 + Pitfall #5.
#
# Deploy via README §2:
#   sudo cp deploy/etc-logrotate-d-ga_crawler /etc/logrotate.d/ga_crawler
#   sudo chown root:root /etc/logrotate.d/ga_crawler
#   sudo chmod 0644 /etc/logrotate.d/ga_crawler
#
# Pitfall #5: system user `ga_crawler` MUST exist BEFORE first logrotate run
# (create 0644 ga_crawler ga_crawler fails silently otherwise).
# README §2 setup order: useradd -r -m -d /opt/ga_crawler ga_crawler  --BEFORE--  this cp.
#
# Retention: 13 archives × weekly = ~3 months history. Hetzner CX22 40GB SSD:
# ~5MB/run × 13 = ~65MB total, negligible.
#
# T-07-05 mitigation (V12 Files & Resources): no world-writable logs.
# T-07-01 inheritance: structlog JSONRenderer writes to stdout; bash wrapper
# (Plan 07-03) redirects stdout+stderr to the file pattern below.

/var/log/ga_crawler/*.log {
    weekly
    rotate 13
    compress
    delaycompress
    missingok
    notifempty
    create 0644 ga_crawler ga_crawler
}
```

**Notes for executor:**
- Each of the 7 directives is on its own line.
- 4-space indentation inside the block (standard logrotate convention).
- Final newline after the closing `}`.
- Filename MUST be `etc-logrotate-d-ga_crawler` (no dot).
- Use Write tool, UTF-8.

Run logrotate shape canary.
  </action>
  <verify>
    <automated>uv run pytest tests/test_phase07_logrotate_template_shape.py -x</automated>
  </verify>
  <done>
    - File `deploy/etc-logrotate-d-ga_crawler` exists.
    - All 8 tests in `tests/test_phase07_logrotate_template_shape.py` GREEN.
    - Manual visual: 7 directives present (weekly, rotate 13, compress, delaycompress, missingok, notifempty, create 0644 ga_crawler ga_crawler).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: .env.example — append HC_PING_URL= placeholder block</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (current Phase 6 8-line content)
    - .planning/phases/07-scheduler-observability-hardening/07-CONTEXT.md §"Action Items" line 306 (HC_PING_URL= placeholder)
    - .planning/phases/07-scheduler-observability-hardening/07-PATTERNS.md §".env.example" lines 258–297 (exact append form)
    - .planning/phases/07-scheduler-observability-hardening/07-RESEARCH.md §Pitfall #4 (no # in values, no quotes — bash/python-dotenv parity)
    - tests/test_phase07_env_example_shape.py (the canary that this task closes)
  </read_first>
  <action>
Edit `.env.example` (USE Edit tool — file already exists with 8 lines).

Append a new comment block + HC_PING_URL line AFTER the current `TG_OPS_CHAT_ID=` line. Resulting file content MUST be:

```
# Telegram delivery (Phase 6 — DELIVER-05)
# Create bot: @BotFather → /newbot
# Get chat_id: add @userinfobot to chat → it prints the chat_id

TG_BOT_TOKEN=
TG_BUSINESS_CHAT_ID=
TG_OPS_CHAT_ID=

# Healthchecks.io dead-man's-switch (Phase 7 — SCHED-03)
# Create check at healthchecks.io → copy ping URL (full URL incl. scheme)
# Required: bash wrapper bin/weekly-run.sh refuses to run if missing (exit 4 per D-703).

HC_PING_URL=
```

**Conventions to preserve (Pitfall #4 + Phase 6 invariants):**
- Empty value after `=` (no placeholder text, no quotes).
- No `#` inside the value (bash vs python-dotenv parser drift risk).
- One blank line between Phase 6 block and Phase 7 block (visual grouping).
- Comment block before placeholder describes purpose + setup pointer + required-vs-optional flag.

Implementation hint:
- Use Edit with `old_string` matching the entire current 8-line file content, `new_string` containing the full 14-line resulting content. Single edit; do not chain edits.

Run env_example canary.
  </action>
  <verify>
    <automated>uv run pytest tests/test_phase07_env_example_shape.py tests/unit/test_phase06_wave0_pyproject_envexample.py -x</automated>
  </verify>
  <done>
    - `.env.example` has 14 lines (8 Phase 6 + 1 blank + 3 comment + 1 blank + 1 placeholder + final newline).
    - All 6 tests in `tests/test_phase07_env_example_shape.py` GREEN.
    - All 4 Phase 6 tests in `tests/unit/test_phase06_wave0_pyproject_envexample.py` still GREEN (regression — 3 TG_* placeholders preserved).
    - `grep -c '^HC_PING_URL=$' .env.example` returns 1.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo → operator → VPS filesystem | Templates committed in `deploy/` are copied (via `sudo cp`) to `/etc/cron.d/` and `/etc/logrotate.d/` — only README §2 procedure crosses this boundary; templates themselves do not run code. |
| `.env.example` → operator → `.env` on VPS | `.env.example` carries blank placeholders only; operator fills `.env` (gitignored). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | cron MAILTO leak (stdout to root mailbox) | mitigate | `MAILTO=""` line in `deploy/etc-cron-d-ga_crawler` (Pitfall #2; canary `test_cron_contains_mailto_empty`). |
| T-07-04 | Information Disclosure | HC.io UUID in source / git history | mitigate | `.env.example` ships only `HC_PING_URL=` (blank value); real UUID lives in `.env` (gitignored — Phase 6 invariant). Canary `test_env_example_all_known_placeholders_blank` enforces. |
| T-07-05 | DoS (data) / V12 Files | World-writable rotated logs | mitigate | `create 0644 ga_crawler ga_crawler` directive in logrotate template (canary asserts substring presence). |
</threat_model>

<verification>
After all 3 tasks complete:
- `deploy/` directory exists with 2 files (cron + logrotate templates).
- `.env.example` has 4 placeholders (3 TG_* + 1 HC_PING_URL).
- Wave 0 canaries: 3 of 7 files flip from RED to GREEN:
  - `tests/test_phase07_cron_template_shape.py` — GREEN
  - `tests/test_phase07_logrotate_template_shape.py` — GREEN
  - `tests/test_phase07_env_example_shape.py` — GREEN
- Remaining 4 still RED (wrapper / test-failure-alert / readme / structural-canaries — addressed by Plans 07-03..07-04).
- Phase 6 suite remains GREEN.
</verification>

<success_criteria>
- 3 files committed (2 new `deploy/*`, 1 modified `.env.example`).
- SCHED-01 + SCHED-02 + SCHED-04 closed at template level (runtime verification deferred to manual operator smoke per VALIDATION.md).
- Atomic commits per task per Phase 2..6 convention:
  - `feat(07-02): deploy/etc-cron-d-ga_crawler — D-708 cron template`
  - `feat(07-02): deploy/etc-logrotate-d-ga_crawler — D-705 logrotate template`
  - `feat(07-02): .env.example — HC_PING_URL= placeholder (SCHED-03)`
</success_criteria>

<output>
After completion, create `.planning/phases/07-scheduler-observability-hardening/07-02-SUMMARY.md` listing:
- 3 files created/modified; final byte / line counts
- Which 3 Wave 0 canary files flipped RED → GREEN (named test counts before/after)
- Any deviations (e.g., spacing in cron file — should be NONE per D-708 verbatim)
</output>
