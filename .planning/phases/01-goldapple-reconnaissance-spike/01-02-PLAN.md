---
phase: 01-goldapple-reconnaissance-spike
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .python-version
  - .planning/spikes/01-goldapple/SETUP-NOTES.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "uv-проект инициализирован в repo root с Python 3.12"
    - "Установлены spike-зависимости: curl_cffi, patchright, selectolax, structlog, python-dotenv"
    - "Patchright Chromium binary установлен и видим из uv-окружения"
    - "Phase 2 переиспользует тот же uv-проект (никакого второго `uv init`)"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata + dep declarations (curl_cffi, patchright, selectolax, structlog, python-dotenv)"
      contains: "patchright"
    - path: "uv.lock"
      provides: "Lockfile для воспроизводимого окружения"
    - path: ".python-version"
      provides: "Pin Python 3.12 для uv"
      contains: "3.12"
    - path: ".planning/spikes/01-goldapple/SETUP-NOTES.md"
      provides: "Запись о выбранных версиях, команде установки Chromium-binary, и заметка для Phase 2 'не делать uv init второй раз'"
      contains: "uv init"
  key_links:
    - from: "pyproject.toml"
      to: "patchright + curl_cffi + selectolax"
      via: "[project.dependencies]"
      pattern: "patchright"
---

<objective>
Инициализировать uv-проект в корне репозитория с Python 3.12, добавить spike-зависимости (per CLAUDE.md §Technology Stack: curl_cffi 0.15.x, patchright 1.55+, selectolax 0.3.x, structlog 25.x, python-dotenv 1.0.x), установить Patchright Chromium binary. Зафиксировать setup-notes так, чтобы Phase 2 переиспользовал тот же uv-проект (никакого `uv init` повторно — иначе потеряется lockfile-история).

Purpose: Окружение готово для Tier-2 Patchright-эксперимента (plans 01-08+) и curl_cffi-feasibility-чека для viled (plan 01-07). Все три сценария (Tier 0 JSON-эндпоинт, Tier 2 Patchright, Tier 3 Patchright+proxy) используют этот же набор зависимостей.
Output: Рабочее uv-окружение, видимое через `uv run python -c "import patchright, curl_cffi, selectolax"`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/research/STACK.md
@.planning/phases/01-goldapple-reconnaissance-spike/01-CONTEXT.md
@.planning/spikes/01-goldapple/README.md

<interfaces>
<!-- Версии библиотек из CLAUDE.md §Technology Stack — авторитетный источник. -->
<!-- Не отклоняться: D-default per CONTEXT.md — версии зафиксированы. -->

Core dependencies (per CLAUDE.md):
- python = ">=3.12,<3.13"   # 3.13 ещё с rough edges для curl_cffi/lxml в 2026
- curl_cffi = ">=0.15,<0.16"   # impersonate="chrome", chrome136+ targets
- patchright = ">=1.55"          # drop-in для playwright, проходит CF/DataDome 2026
- selectolax = ">=0.3,<0.4"      # 10-30x быстрее BeautifulSoup
- structlog = ">=25.0,<26.0"     # JSON логи для recon-замеров
- python-dotenv = ">=1.0,<2.0"   # для .env.local (IPRoyal creds)

Patchright требует ОТДЕЛЬНОГО browser install: `uv run patchright install chromium`
(не `playwright install`).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Установить uv (если ещё не установлен) — пользовательский setup</name>
  <what-built>
Установка `uv` — package manager. Однократный шаг на машине разработчика. Если `uv --version` уже работает — auto-pass.
  </what-built>
  <how-to-verify>
В PowerShell:

```powershell
uv --version
```

Если команда возвращает версию (например, `uv 0.10.x`) — `approved`.

Если "не найдено" — выполнить установку (Windows PowerShell):

```powershell
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.sh | iex"
```

Или (Windows native):

```powershell
winget install --id=astral-sh.uv -e
```

После установки перезапустить терминал и снова проверить `uv --version`. Когда `uv --version` отвечает версией — написать `approved`.
  </how-to-verify>
  <resume-signal>Напишите "approved" когда `uv --version` работает; "skip" если уже установлен и проверено</resume-signal>
</task>

<task type="auto">
  <name>Task 2: uv init + Python 3.12 + установка spike-зависимостей</name>
  <files>pyproject.toml, uv.lock, .python-version</files>
  <read_first>
    - `CLAUDE.md` секция `## Technology Stack` — версии библиотек (авторитет)
    - `.planning/research/STACK.md` секция `## Installation` — порядок команд `uv add`
    - `.planning/phases/01-goldapple-reconnaissance-spike/01-CONTEXT.md` — D-16 (артефакты в `.planning/spikes/01-goldapple/`, **но uv-проект в repo root**)
  </read_first>
  <action>
Все команды из корня репозитория (`C:\Users\gstorepc\projects\ga_crawler\`).

1. Установить Python 3.12 через uv (если не установлен):
   ```bash
   uv python install 3.12
   ```

2. Инициализировать uv-проект В КОРНЕ репозитория. Если `pyproject.toml` уже существует — пропустить шаг и сразу к `uv add`. Если нет:
   ```bash
   uv init --python 3.12 --no-readme --no-workspace .
   ```
   Флаги: `--no-readme` (не создавать README — он Phase 2 будет писать), `--no-workspace` (single-package), `.` (текущий каталог).

   Это создаст: `pyproject.toml`, `.python-version` (содержит `3.12`), пустой `main.py` или `hello.py` (можно удалить — спайк не нуждается в entry-point).

3. Удалить hello.py / main.py если uv их создал (спайк-скрипты живут в `.planning/spikes/01-goldapple/notebook*.py`):
   ```bash
   rm -f main.py hello.py
   ```

4. Добавить spike-зависимости (одна команда — uv resolves всё разом, чистый lockfile):
   ```bash
   uv add "curl_cffi>=0.15,<0.16" "patchright>=1.55" "selectolax>=0.3,<0.4" "structlog>=25.0,<26.0" "python-dotenv>=1.0,<2.0"
   ```

5. **НЕ** добавлять в этот спайк: pandas, xlsxwriter, sqlmodel, aiogram, playwright (vanilla), camoufox, scrapling. Они либо для Phase 2+ (pandas/sqlmodel), либо для эскалации Tier 3+ (camoufox/scrapling — добавляются ОТДЕЛЬНО plan'ом 01-10 если нужно), либо мы не используем (vanilla playwright per D-01: skip Tier 1).

6. Проверить что lockfile создался и проект собирается:
   ```bash
   uv sync
   uv run python -c "import patchright, curl_cffi, selectolax, structlog, dotenv; print('OK')"
   ```

   Команда должна напечатать `OK`. Если ImportError — проверить версию Python (`uv run python --version` должно быть 3.12.x).

Если на Windows возникает проблема с curl_cffi (compile error) — это известная проблема некоторых старых wheel'ов; добавить `curl_cffi` отдельной командой `uv add --prerelease=allow curl_cffi` и повторить.
  </action>
  <acceptance_criteria>
    - `test -f pyproject.toml` succeeds
    - `test -f uv.lock` succeeds
    - `test -f .python-version` succeeds
    - `cat .python-version` содержит `3.12`
    - `grep -c "patchright" pyproject.toml` ≥ 1
    - `grep -c "curl-cffi\|curl_cffi" pyproject.toml` ≥ 1
    - `grep -c "selectolax" pyproject.toml` ≥ 1
    - `grep -c "structlog" pyproject.toml` ≥ 1
    - `grep -c "python-dotenv" pyproject.toml` ≥ 1
    - `uv run python -c "import patchright, curl_cffi, selectolax, structlog, dotenv"` exit code 0
  </acceptance_criteria>
  <verify>
    <automated>test -f pyproject.toml && test -f uv.lock && test -f .python-version && grep -q "3.12" .python-version && grep -q "patchright" pyproject.toml && grep -qE "curl[-_]cffi" pyproject.toml && grep -q "selectolax" pyproject.toml && grep -q "structlog" pyproject.toml && grep -q "python-dotenv" pyproject.toml && uv run python -c "import patchright, curl_cffi, selectolax, structlog, dotenv; print('OK')"</automated>
  </verify>
  <done>uv-проект в корне работает; все 5 spike-зависимостей импортируются.</done>
</task>

<task type="auto">
  <name>Task 3: Установить Patchright Chromium binary + записать SETUP-NOTES.md</name>
  <files>.planning/spikes/01-goldapple/SETUP-NOTES.md</files>
  <read_first>
    - `pyproject.toml` (созданный Task 2) — для записи реальных установленных версий в notes
    - `CLAUDE.md` секция `## Anti-Bot Strategy` — для подтверждения что используем Patchright (а не Playwright vanilla — D-01)
  </read_first>
  <action>
1. Установить Patchright Chromium binary (отдельная команда от `uv add patchright` per CLAUDE.md "Single binary trap"):

   ```bash
   uv run patchright install chromium
   ```

   Это скачает ~150MB browser-binary в `~/.cache/ms-playwright/` (Linux/Mac) или `%USERPROFILE%\AppData\Local\ms-playwright\` (Windows). Не в git.

2. На Windows может потребоваться `uv run patchright install-deps chromium` чтобы установить системные deps — но это нужно ТОЛЬКО на Linux VPS (для production). На Windows-laptop пропустить если первая команда прошла без ошибок.

3. Smoke-test что Patchright headless запускается:

   ```bash
   uv run python -c "from patchright.sync_api import sync_playwright; p = sync_playwright().start(); b = p.chromium.launch(headless=True); page = b.new_page(); page.goto('https://example.com'); print('TITLE:', page.title()); b.close(); p.stop()"
   ```

   Должно напечатать `TITLE: Example Domain`. Если не работает — проверить что Chromium установлен (см. шаг 1).

4. Создать `.planning/spikes/01-goldapple/SETUP-NOTES.md`:

   ```markdown
   # Spike 01: Setup Notes

   **Setup completed:** _<сегодняшняя дата ISO YYYY-MM-DD>_
   **Operator:** mirdbek@gmail.com
   **Baseline machine:** Windows 11 Pro (per D-06)

   ## uv project location

   **Repo root:** `pyproject.toml` живёт здесь, не в `.planning/spikes/01-goldapple/`.

   **⚠ Phase 2 instruction:** **НЕ делать `uv init` повторно.** Phase 2 переиспользует этот же uv-проект и просто добавит свои зависимости (`uv add pandas xlsxwriter sqlmodel aiogram structlog`). Lockfile-история сохраняется.

   ## Installed versions

   _Заполнить точные версии из `uv.lock` или `uv pip list`:_

   - Python: _TBD (uv run python --version)_
   - patchright: _TBD_
   - curl_cffi: _TBD_
   - selectolax: _TBD_
   - structlog: _TBD_
   - python-dotenv: _TBD_

   ## Patchright Chromium binary

   Установлен через `uv run patchright install chromium`.

   Cache location (Windows): `%USERPROFILE%\AppData\Local\ms-playwright\chromium-*\`

   На Linux VPS (Phase 7) дополнительно потребуется `uv run patchright install-deps chromium` для системных deps.

   ## Smoke test (recorded)

   `uv run python -c "from patchright.sync_api import sync_playwright; ..."` → output: _TBD (paste output here)_

   ## Excluded (NOT installed in this spike)

   - vanilla `playwright` — per D-01, Tier 1 (vanilla) skipped
   - `camoufox`, `scrapling` — Tier 4, добавляются plan'ом 01-10 ТОЛЬКО если Tier 2+3 fail
   - `pandas`, `xlsxwriter`, `sqlmodel`, `aiogram` — Phase 2+ scope
   ```

   Заполнить `_TBD_` поля реальными значениями (запустить `uv pip list | grep -iE "patchright|curl-cffi|selectolax|structlog|python-dotenv"` и вставить версии; запустить smoke-test и вставить вывод).
  </action>
  <acceptance_criteria>
    - `uv run python -c "from patchright.sync_api import sync_playwright; p = sync_playwright().start(); b = p.chromium.launch(headless=True); b.close(); p.stop()"` exit code 0
    - `test -f .planning/spikes/01-goldapple/SETUP-NOTES.md` succeeds
    - `grep -c "uv init" .planning/spikes/01-goldapple/SETUP-NOTES.md` ≥ 1 (содержит warning для Phase 2)
    - `grep -c "Phase 2" .planning/spikes/01-goldapple/SETUP-NOTES.md` ≥ 1
    - `grep -c "patchright" .planning/spikes/01-goldapple/SETUP-NOTES.md` ≥ 1
    - SETUP-NOTES.md не содержит `_TBD_` (все placeholder заполнены реальными значениями)
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from patchright.sync_api import sync_playwright; p = sync_playwright().start(); b = p.chromium.launch(headless=True); b.close(); p.stop(); print('OK')" && test -f .planning/spikes/01-goldapple/SETUP-NOTES.md && grep -q "uv init" .planning/spikes/01-goldapple/SETUP-NOTES.md && grep -q "Phase 2" .planning/spikes/01-goldapple/SETUP-NOTES.md && ! grep -q "_TBD_" .planning/spikes/01-goldapple/SETUP-NOTES.md</automated>
  </verify>
  <done>Patchright Chromium запускается headless, страница example.com отдаёт title; SETUP-NOTES.md записан с реальными версиями и явной инструкцией для Phase 2 не делать `uv init` повторно.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local→PyPI | Скачивание зависимостей через uv. Trust в Astral PyPI mirror + lockfile-pinned versions. |
| local→Microsoft Playwright CDN | Скачивание Chromium binary. Trust в playwright.dev. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | uv.lock или Chromium binary tampered | accept | Standard supply chain trust; project не публичный, риск низкий. uv hash-pinning lockfile mitigates большую часть. |
| T-01-02-02 | Information Disclosure | `pyproject.toml` случайно содержит токен/пароль | mitigate | Plan не записывает secrets в pyproject.toml. Все creds — в `.env.local` (plan 01-03), gitignored. |

Spike scope: dev-environment setup, no PII, no production. Full threat model deferred to Phase 7.
</threat_model>

<verification>
- `uv pip list | grep -iE "patchright|curl-cffi|selectolax|structlog|python-dotenv"` показывает 5 пакетов
- Smoke test Patchright headless проходит
- `.planning/spikes/01-goldapple/SETUP-NOTES.md` содержит реальные версии (не `_TBD_`)
</verification>

<success_criteria>
- pyproject.toml содержит pinned versions для curl_cffi, patchright, selectolax, structlog, python-dotenv
- uv.lock существует и lockfile-history сохраняется для Phase 2
- Patchright Chromium установлен, headless smoke-test проходит
- SETUP-NOTES.md явно предупреждает Phase 2: "не делать `uv init` повторно"
</success_criteria>

<output>
After completion, create `.planning/phases/01-goldapple-reconnaissance-spike/01-02-SUMMARY.md`
</output>
