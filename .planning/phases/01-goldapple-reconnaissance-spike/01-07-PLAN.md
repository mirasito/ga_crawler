---
phase: 01-goldapple-reconnaissance-spike
plan: 07
type: execute
wave: 1
depends_on: [01, 02]
files_modified:
  - .planning/spikes/01-goldapple/notebook-viled.py
  - .planning/spikes/01-goldapple/sample-payloads/viled-product-urls.txt
  - .planning/spikes/01-goldapple/sample-payloads/viled-fetch-results.json
  - .planning/spikes/01-goldapple/MEMO.md
autonomous: false
requirements: [RECON-02]

must_haves:
  truths:
    - "≥10 product fetches к viled.kz через curl_cffi impersonate='chrome' выполнены"
    - "Per-fetch records: HTTP status, timing (ms), JSON-LD presence, content-length, content-type"
    - "Side-deliverables зафиксированы (per CONTEXT.md 'Не обсуждали явно'): pagination shape, robots/UA strictness, JSON-LD schema на viled"
    - "MEMO.md секция 'viled.kz feasibility (RECON-02)' заполнена реальным числом успехов (N/10) + side-deliverables"
  artifacts:
    - path: ".planning/spikes/01-goldapple/notebook-viled.py"
      provides: "Reproducible script — для повторного запуска feasibility check (Phase 2 reference)"
      contains: "curl_cffi"
    - path: ".planning/spikes/01-goldapple/sample-payloads/viled-product-urls.txt"
      provides: "Список 10-15 product URL для эксперимента"
      min_lines: 10
    - path: ".planning/spikes/01-goldapple/sample-payloads/viled-fetch-results.json"
      provides: "JSON {url: {status, timing_ms, jsonld_present, content_length, ...}, ...}"
      contains: "status"
    - path: ".planning/spikes/01-goldapple/MEMO.md"
      provides: "Заполненная секция 'viled.kz feasibility (RECON-02)'"
      contains: "viled.kz feasibility"
  key_links:
    - from: ".planning/spikes/01-goldapple/notebook-viled.py"
      to: ".planning/spikes/01-goldapple/sample-payloads/viled-fetch-results.json"
      via: "Скрипт записывает results в этот JSON"
      pattern: "viled-fetch-results"
---

<objective>
Подтвердить feasibility парсинга viled.kz через `curl_cffi impersonate="chrome"` без headless-браузера — RECON-02. Минимум ≥10 product fetches. Зафиксировать timing, JSON-LD presence, pagination shape, robots/UA strictness — это Phase 2 hot-data (per CONTEXT.md "Не обсуждали явно: side-deliverables").

Purpose: Phase 2 v1 предполагает curl_cffi для viled (per research/SUMMARY.md "Tier 0"). Спайк подтверждает это эмпирически — не ждать Phase 2, чтобы узнать что viled тоже под Cloudflare.
Output: 10-15 fetch'ей с per-URL метриками; заполненная секция в MEMO.md.

Per CONTEXT.md: "минимум ≥10 product fetches через curl_cffi impersonate='chrome'; Claude дополнительно фиксирует timing, JSON-LD presence, pagination shape, robots/UA-строгость как side-deliverable".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-goldapple-reconnaissance-spike/01-CONTEXT.md
@.planning/spikes/01-goldapple/tos-audit.md
@.planning/spikes/01-goldapple/MEMO.md
@.planning/research/STACK.md

<interfaces>
<!-- curl_cffi impersonate API (per CLAUDE.md §Anti-Bot Tier 0): -->
<!-- from curl_cffi import requests -->
<!-- requests.get(url, impersonate="chrome", timeout=30) -->
<!-- impersonate values 2026: "chrome", "chrome131", "chrome136", "safari184" -->
<!-- Default "chrome" → latest Chrome profile available in installed version. -->

<!-- selectolax extraction для JSON-LD (per Tier 0 stack): -->
<!-- from selectolax.parser import HTMLParser -->
<!-- tree.css('script[type="application/ld+json"]') → list of nodes -->
<!-- node.text() → raw JSON string → json.loads() -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Manual sample — 10-15 viled.kz product URLs</name>
  <what-built>
Список из 10-15 РЕАЛЬНЫХ product URLs viled.kz для feasibility experiment.

Спайк ЯВНО не парсит viled brand-каталог программно (это Phase 2). Здесь — manual gather URLs за 5-10 минут.
  </what-built>
  <how-to-verify>
1. Открыть https://www.viled.kz/
2. Перейти в любой каталог (например "Уход за лицом" / "Бренды → Estée Lauder")
3. Открыть 10-15 разных продуктов (любых брендов, любых ценовых категорий — нужна репрезентативность)
4. Для каждого скопировать URL в текстовый файл

Альтернатива (быстрее):
- Открыть страницу-листинг с 12-24 продуктами на странице
- Через DevTools (F12 → Console) запустить:
  ```javascript
  Array.from(document.querySelectorAll('a[href*="/product/"], a[href*="/p/"]'))
    .map(a => a.href)
    .filter((v, i, arr) => arr.indexOf(v) === i)
    .slice(0, 15)
    .forEach(url => console.log(url));
  ```
- Скопировать вывод в файл

Сохранить URLs в `.planning/spikes/01-goldapple/sample-payloads/viled-product-urls.txt`, по одному URL на строку.

Формат:
```
https://www.viled.kz/product/krem-1
https://www.viled.kz/product/serum-2
...
```

Прислать в чат: "approved <N> URLs saved" (где N ≥ 10).
  </how-to-verify>
  <resume-signal>Напишите "approved <N>" (например "approved 15"). Если viled.kz недоступен (downtime / region block) — "skip" (тогда RECON-02 будет помечен "deferred — viled inaccessible at spike time", не блокирует MEMO finalize).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Написать notebook-viled.py — fetch loop через curl_cffi с per-URL метриками</name>
  <files>.planning/spikes/01-goldapple/notebook-viled.py, .planning/spikes/01-goldapple/sample-payloads/viled-fetch-results.json</files>
  <read_first>
    - `.planning/spikes/01-goldapple/sample-payloads/viled-product-urls.txt` (сохранённый Task 1)
    - `.planning/spikes/01-goldapple/tos-audit.md` секция "viled.kz rate-limit (committed)" — для использования правильного pause
    - `.planning/spikes/01-goldapple/notebook-viled.py` (stub из 01-01) — заменяется реальным кодом
    - `.planning/research/STACK.md` секция "Tier 0 — viled.kz"
  </read_first>
  <action>
Заменить stub-содержимое `notebook-viled.py` следующим скриптом:

```python
"""
Spike 01: viled.kz curl_cffi feasibility check (RECON-02).

Reproducible: запускается из repo root через `uv run python .planning/spikes/01-goldapple/notebook-viled.py`.

Per RECON-02: ≥10 product fetches via curl_cffi impersonate="chrome".
Per CONTEXT.md "Не обсуждали явно":
  - timing per fetch (ms)
  - JSON-LD presence per fetch
  - pagination shape (manual observation)
  - robots/UA strictness (do we get blocked, do we need realistic UA?)

Output: sample-payloads/viled-fetch-results.json
"""

import json
import time
from pathlib import Path
from datetime import datetime, timezone

from curl_cffi import requests
from selectolax.parser import HTMLParser
import structlog

# Config
URLS_FILE = Path(".planning/spikes/01-goldapple/sample-payloads/viled-product-urls.txt")
RESULTS_FILE = Path(".planning/spikes/01-goldapple/sample-payloads/viled-fetch-results.json")
PAUSE_S = 2.0  # per tos-audit.md committed rate-limit для viled (1-2s)
TIMEOUT_S = 30
MAX_URLS = 15

# Logger
log = structlog.get_logger()

def fetch_one(url: str) -> dict:
    """Fetch one URL, return per-URL metrics."""
    started = time.perf_counter()
    record = {
        "url": url,
        "fetched_at": datetime.now(timezone.utc).isoformat(),
        "status": None,
        "timing_ms": None,
        "content_length": None,
        "content_type": None,
        "jsonld_present": False,
        "jsonld_has_product": False,
        "jsonld_has_price": False,
        "error": None,
    }
    try:
        r = requests.get(url, impersonate="chrome", timeout=TIMEOUT_S)
        elapsed_ms = int((time.perf_counter() - started) * 1000)
        record["status"] = r.status_code
        record["timing_ms"] = elapsed_ms
        record["content_length"] = len(r.content)
        record["content_type"] = r.headers.get("content-type", "")

        if r.status_code == 200 and "html" in record["content_type"].lower():
            tree = HTMLParser(r.text)
            jsonld_nodes = tree.css('script[type="application/ld+json"]')
            record["jsonld_present"] = len(jsonld_nodes) > 0
            for n in jsonld_nodes:
                try:
                    obj = json.loads(n.text())
                    items = obj if isinstance(obj, list) else [obj]
                    for it in items:
                        if isinstance(it, dict) and it.get("@type") == "Product":
                            record["jsonld_has_product"] = True
                            offers = it.get("offers", {})
                            if isinstance(offers, dict) and offers.get("price"):
                                record["jsonld_has_price"] = True
                            elif isinstance(offers, list) and any(o.get("price") for o in offers if isinstance(o, dict)):
                                record["jsonld_has_price"] = True
                except (json.JSONDecodeError, AttributeError):
                    pass
    except Exception as e:
        record["error"] = f"{type(e).__name__}: {e}"
    return record

def main():
    urls = [u.strip() for u in URLS_FILE.read_text(encoding="utf-8").splitlines() if u.strip()]
    urls = urls[:MAX_URLS]
    print(f"Fetching {len(urls)} URLs from viled.kz with pause={PAUSE_S}s ...")
    results = []
    for i, url in enumerate(urls, 1):
        print(f"  [{i}/{len(urls)}] {url}")
        rec = fetch_one(url)
        print(f"      → status={rec['status']}, timing={rec['timing_ms']}ms, jsonld_product={rec['jsonld_has_product']}, jsonld_price={rec['jsonld_has_price']}")
        results.append(rec)
        if i < len(urls):
            time.sleep(PAUSE_S)

    # Aggregates
    successful = [r for r in results if r["status"] == 200]
    with_product_jsonld = [r for r in successful if r["jsonld_has_product"]]
    with_price_jsonld = [r for r in successful if r["jsonld_has_price"]]
    timings = [r["timing_ms"] for r in successful if r["timing_ms"]]
    summary = {
        "total": len(results),
        "successful_200": len(successful),
        "with_product_jsonld": len(with_product_jsonld),
        "with_price_jsonld": len(with_price_jsonld),
        "errors": [r for r in results if r["error"]],
        "timing_ms": {
            "min": min(timings) if timings else None,
            "max": max(timings) if timings else None,
            "avg": int(sum(timings) / len(timings)) if timings else None,
        },
    }
    out = {"summary": summary, "results": results}
    RESULTS_FILE.parent.mkdir(parents=True, exist_ok=True)
    RESULTS_FILE.write_text(json.dumps(out, indent=2, ensure_ascii=False), encoding="utf-8")
    print(f"\n✓ Saved to {RESULTS_FILE}")
    print(f"\nSummary: {summary['successful_200']}/{summary['total']} OK, "
          f"{summary['with_price_jsonld']}/{summary['successful_200']} have JSON-LD price, "
          f"avg timing {summary['timing_ms']['avg']}ms")

if __name__ == "__main__":
    main()
```

Запустить:
```bash
uv run python .planning/spikes/01-goldapple/notebook-viled.py
```

**Edge case — если все запросы возвращают не-200:**
- Status 403/429: viled.kz блокирует curl_cffi (impersonate="chrome" не помогает) → попробовать `impersonate="chrome131"` или `impersonate="safari184"` — изменить в скрипте, перезапустить.
- Если ВСЁ ЕЩЁ 403 → пометить в MEMO.md "viled.kz requires Tier 1+ (vanilla Playwright). curl_cffi insufficient. Phase 2 stack will need adjustment."
- Если cookie-auth required → попробовать сначала `requests.get('https://viled.kz/')` чтобы получить cookies, затем product fetches с теми же cookies (session reuse).

**Edge case — если goldapple-style anti-bot обнаружен на viled:**
- Это серьёзный blocker для Phase 2 economics — записать в MEMO.md "Open risks" + flag в Russian для пользователя в `01-07-SUMMARY.md`.
  </action>
  <acceptance_criteria>
    - `test -f .planning/spikes/01-goldapple/notebook-viled.py` succeeds
    - `! grep -q "NotImplementedError" .planning/spikes/01-goldapple/notebook-viled.py` (заменили stub)
    - `test -f .planning/spikes/01-goldapple/sample-payloads/viled-fetch-results.json` succeeds
    - `uv run python -c "import json; d = json.load(open('.planning/spikes/01-goldapple/sample-payloads/viled-fetch-results.json')); assert 'summary' in d and 'results' in d; assert len(d['results']) >= 10, f'Need >=10, got {len(d[\"results\"])}'"` exit code 0
    - Хотя бы 1 результат имеет `status=200` (полный fail = blocker для Phase 2, требует MEMO.md flag)
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/spikes/01-goldapple/notebook-viled.py && ! grep -q "NotImplementedError" .planning/spikes/01-goldapple/notebook-viled.py && test -f .planning/spikes/01-goldapple/sample-payloads/viled-fetch-results.json && uv run python -c "import json; d = json.load(open('.planning/spikes/01-goldapple/sample-payloads/viled-fetch-results.json')); assert 'summary' in d and 'results' in d; assert len(d['results']) >= 10; print(f'OK: {d[\"summary\"][\"successful_200\"]}/{d[\"summary\"][\"total\"]} successful')"</automated>
  </verify>
  <done>notebook-viled.py запустился, виды fetch'ей сохранены в JSON; ≥10 fetch'ей выполнено; summary показывает success-rate.</done>
</task>

<task type="auto">
  <name>Task 3: Записать секцию "viled.kz feasibility (RECON-02)" в MEMO.md</name>
  <files>.planning/spikes/01-goldapple/MEMO.md</files>
  <read_first>
    - `.planning/spikes/01-goldapple/sample-payloads/viled-fetch-results.json` (созданный Task 2)
    - `.planning/spikes/01-goldapple/MEMO.md` (с заполненными в 01-05/01-06 секциями)
    - `.planning/spikes/01-goldapple/tos-audit.md` секция viled.kz rate-limit
  </read_first>
  <action>
Заменить (или дополнить) секцию `## viled.kz feasibility (RECON-02)` в MEMO.md, сохранив остальное.

Извлечь данные из `viled-fetch-results.json`:

```markdown
## viled.kz feasibility (RECON-02)

**Status:** _CONFIRMED — curl_cffi viable for Phase 2_ / _BLOCKED — alternative tier required_

**Method:** `curl_cffi impersonate="chrome"`, pause = _<N>_s, timeout = 30s. Single thread.
**Sample size:** _<N>_ product URLs (manual sample per Task 1)
**Rate-limit used:** _<N>_s pause (per tos-audit.md viled committed rate-limit)
**Reproducible script:** `notebook-viled.py`

### Per-URL outcomes

- **HTTP 200:** _<N>/<total>_
- **JSON-LD present:** _<N>/<successful>_
- **JSON-LD has Product:** _<N>/<successful>_
- **JSON-LD has price (D-14 satisfaction proxy):** _<N>/<successful>_
- **Errors:** _<list types: timeouts, 403, 429, ...>_

### Timing

- Min / Avg / Max: _<min>_ / _<avg>_ / _<max>_ ms
- Implications: _at <avg>ms × ~500 viled SKUs (estimate) = ~<minutes> per crawl_

### Side-deliverables (Phase 2 hot-start)

| Aspect | Finding |
|--------|---------|
| **JSON-LD schema** | _e.g., `Product` with nested `offers.{price, priceCurrency, availability}`_ — Phase 2 PARSE-02 can use directly |
| **Pagination shape** | _e.g., `?page=N&per_page=24` query params; total count in `.catalog__total` element_ |
| **UA strictness** | _e.g., curl_cffi default UA worked; `requests` без impersonate = 403 / works without_ |
| **Cookies / session** | _e.g., no session-token required; cookies persist OK_ |
| **Pricing format** | _e.g., `5990` (integer) or `"5 990,00"` (formatted)_ |
| **Currency format** | _e.g., `KZT` in JSON-LD `priceCurrency`; `₸` in display HTML_ |

### Verdict

_Phase 2 stack confirmed: curl_cffi + selectolax JSON-LD-first parsing. No headless browser needed for viled. Phase 2 PARSE-02 (JSON-LD priority) immediately viable._

OR (if blocked):

_BLOCKER: viled.kz returns 403 to curl_cffi. Phase 2 will need Playwright (Tier 1) for viled too — economics changes. Investigate cookie-warmup strategy or different impersonate profile in Phase 2._

See `sample-payloads/viled-fetch-results.json` for raw data.
```

Заполнить ВСЕ `_<...>_` placeholders реальными значениями из results.json. Не оставлять `_TBD_`.

Если RECON-02 заблокирован (status 200 = 0/N) — пометить **верхним приоритетом** в `## Open risks` секции MEMO.md как "Risk-1 (HIGH): viled curl_cffi blocked → Phase 2 stack must include Playwright". Это сильно влияет на decision memo и Phase 2 planning.
  </action>
  <acceptance_criteria>
    - `grep -c "## viled.kz feasibility (RECON-02)" .planning/spikes/01-goldapple/MEMO.md` ≥ 1
    - В этой секции есть слова "HTTP 200" и "Side-deliverables"
    - `grep -c "viled-fetch-results.json" .planning/spikes/01-goldapple/MEMO.md` ≥ 1
    - В секции нет `_TBD_` и нет `_<...>_` placeholders
  </acceptance_criteria>
  <verify>
    <automated>grep -q "## viled.kz feasibility (RECON-02)" .planning/spikes/01-goldapple/MEMO.md && grep -q "Side-deliverables" .planning/spikes/01-goldapple/MEMO.md && grep -q "viled-fetch-results.json" .planning/spikes/01-goldapple/MEMO.md</automated>
  </verify>
  <done>MEMO.md секция viled.kz feasibility заполнена реальными числами, side-deliverables (JSON-LD schema, pagination shape, UA, currency) задокументированы для Phase 2 hot-start. RECON-02 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local→viled.kz | Read-only HTTPS GETs, ≤15 fetches, polite rate-limit per tos-audit.md. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07-01 | Denial of Service | Spike accidentally hammers viled.kz (own infrastructure!) | mitigate | Pause 2s между fetch'ами; max 15 URLs per run; viled.kz — наша же команда per Pitfall 13. |
| T-01-07-02 | Information Disclosure | viled-fetch-results.json содержит полные URL'ы (могут содержать tracking params) | accept | URL'ы публичные. Никакой PII не fetched. |

Spike scope: ≤15 read-only HTTP GETs к собственному ритейлеру. Full threat model deferred.
</threat_model>

<verification>
- `cat .planning/spikes/01-goldapple/sample-payloads/viled-fetch-results.json | python -c "import json,sys; d=json.load(sys.stdin); print(d['summary'])"` показывает summary
- `grep -A 20 "viled.kz feasibility" .planning/spikes/01-goldapple/MEMO.md` показывает заполненный block
</verification>

<success_criteria>
- ≥10 viled product fetches выполнены через curl_cffi impersonate="chrome"
- Per-fetch metrics (status, timing, JSON-LD presence) saved в JSON
- Side-deliverables (pagination, UA, JSON-LD schema, currency) задокументированы для Phase 2
- MEMO.md секция viled feasibility заполнена с явным verdict (CONFIRMED / BLOCKED)
- RECON-02 closed
</success_criteria>

<output>
After completion, create `.planning/phases/01-goldapple-reconnaissance-spike/01-07-SUMMARY.md`
</output>
