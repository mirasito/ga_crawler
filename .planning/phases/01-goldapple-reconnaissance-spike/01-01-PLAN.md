---
phase: 01-goldapple-reconnaissance-spike
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/spikes/01-goldapple/README.md
  - .planning/spikes/01-goldapple/MEMO.md
  - .planning/spikes/01-goldapple/notebook.py
  - .planning/spikes/01-goldapple/notebook-viled.py
  - .planning/spikes/01-goldapple/tos-audit.md
  - .planning/spikes/01-goldapple/sample-payloads/.gitkeep
  - .gitignore
autonomous: true
requirements: []

must_haves:
  truths:
    - "Папка `.planning/spikes/01-goldapple/` существует и git-tracked"
    - "Stub-файлы (MEMO.md, notebook.py, notebook-viled.py, tos-audit.md, README.md) созданы с заголовками-якорями для последующих плансов"
    - "README ясно фиксирует throwaway-границу (ничего не импортируется в Phase 2 src/)"
  artifacts:
    - path: ".planning/spikes/01-goldapple/README.md"
      provides: "Throwaway-scope, навигация по артефактам спайка, ссылки на CONTEXT.md и memo template"
      contains: "Throwaway"
    - path: ".planning/spikes/01-goldapple/MEMO.md"
      provides: "Stub decision memo с template-секциями (problem / options tested / chosen / next-step / open risks)"
      contains: "Chosen tier:"
    - path: ".planning/spikes/01-goldapple/notebook.py"
      provides: "Заголовок Patchright-100-fetch ноутбука (фактический код пишет 01-08)"
      contains: "goldapple"
    - path: ".planning/spikes/01-goldapple/notebook-viled.py"
      provides: "Заголовок viled curl_cffi-feasibility ноутбука (фактический код пишет 01-07)"
      contains: "viled"
    - path: ".planning/spikes/01-goldapple/tos-audit.md"
      provides: "Stub robots/ToS audit (заполняется в 01-04)"
      contains: "viled.kz rate-limit:"
    - path: ".planning/spikes/01-goldapple/sample-payloads/.gitkeep"
      provides: "Каталог для сохранённых образцов HTML/JSON/network-trace"
    - path: ".gitignore"
      provides: "Игнор для .env.local, *.db, browser cache, snapshot директорий"
      contains: ".env.local"
  key_links:
    - from: ".planning/spikes/01-goldapple/README.md"
      to: ".planning/phases/01-goldapple-reconnaissance-spike/01-CONTEXT.md"
      via: "ссылка на canonical refs / D-XX-decisions"
      pattern: "01-CONTEXT"
    - from: ".planning/spikes/01-goldapple/MEMO.md"
      to: "knowledge/decisions/"
      via: "пометка о дублировании в Obsidian после wrap-up"
      pattern: "knowledge/decisions"
---

<objective>
Создать скелет throwaway-спайка в `.planning/spikes/01-goldapple/` со всеми stub-файлами, которые последующие планы заполнят. Зафиксировать throwaway-границу (D-16): код спайка живёт ТОЛЬКО здесь, Phase 2 не импортирует из этой папки ни строки. Создать `.gitignore` для секретов (`.env.local`, IPRoyal credentials, browser-state).

Purpose: Дать всем последующим планам единое место для записи артефактов и сделать throwaway-намерение явным (read-first для каждого следующего исполнителя).
Output: 6 stub-файлов + .gitkeep + .gitignore.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-goldapple-reconnaissance-spike/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать каталог spike + README с throwaway-scope</name>
  <files>.planning/spikes/01-goldapple/README.md, .planning/spikes/01-goldapple/sample-payloads/.gitkeep</files>
  <read_first>
    - `.planning/phases/01-goldapple-reconnaissance-spike/01-CONTEXT.md` — секции `<domain>` (throwaway-scope) и `<decisions>` (D-16: пути артефактов)
    - `.planning/PROJECT.md` — core value (для контекста зачем мы это делаем)
  </read_first>
  <action>
Создать каталог `.planning/spikes/01-goldapple/` (если ещё нет) и файл `.gitkeep` внутри `.planning/spikes/01-goldapple/sample-payloads/` (чтобы пустая папка попала в git).

Записать `.planning/spikes/01-goldapple/README.md` со следующими разделами (на русском, заголовки маркдаун):

```markdown
# Spike 01: Goldapple Reconnaissance

**Status:** Active (1-week timebox per D-02)
**Output:** Signed-off `MEMO.md` + reproducible `notebook.py`

## Throwaway scope

Этот каталог — источник правды для Phase 1 спайка и **только** для него.

- Ни один файл отсюда не импортируется в `src/` или production-код Phase 2+.
- Phase 2 начинается с чистого листа; все findings переезжают через `MEMO.md` (decision memo) и project-local skill, упакованный `/gsd-spike --wrap-up`.
- Notebook — `.py` script (D-default per CONTEXT.md), не `.ipynb`. Print'ы вместо output-cells, чище diffs.

## Artifacts

| File | Purpose | Filled by plan |
|------|---------|----------------|
| `MEMO.md` | Decision memo: chosen tier, browser engine, proxy provider, prod-IP recommendation | 01-11 |
| `notebook.py` | Patchright 100-fetch experiment (Tier 2/3 measurement) | 01-08, 01-09, conditionally 01-10 |
| `notebook-viled.py` | curl_cffi feasibility check для viled.kz (≥10 fetches) | 01-07 |
| `tos-audit.md` | robots.txt + ToS findings + committed rate-limits для обоих сайтов | 01-04 |
| `sample-payloads/` | Образцы HTML/JSON/network-trace, challenge-страницы, error-варианты | 01-05, 01-06, по ходу |

## Locked decisions reference

Все 16 D-XX-решений зафиксированы в `../phases/01-goldapple-reconnaissance-spike/01-CONTEXT.md`. **Любое отклонение — ошибка плана, а не решения исполнителя.**

Ключевые:
- **D-01:** Стартуем с Tier 2 (Patchright). Tier 1 (vanilla) скипаем.
- **D-02:** Hard timebox = 1 week. Если стабильный tier ≤3 не достигнут — вердикт "Tier 4 / managed unblocker".
- **D-03:** Stop-rule = 5 подряд блоков (403/429) или первая Cloudflare/DataDome challenge без auto-resolve → tier failed.
- **D-04:** Persistent (warm) browser context, slow rate (3–5s pause), cookies live across fetches.
- **D-12:** 100-fetch эксперимент = 3–5 брендов из пересечения с viled top-10.
- **D-13:** Threshold = ≥95/100 успехов с разрешёнными 5xx/timeout-ретраями.
- **D-14:** Успешный fetch = HTTP 200 + product JSON-LD в `<script type="application/ld+json">`.
- **D-16:** На завершении спайка — `/gsd-spike --wrap-up` + копия MEMO.md в Obsidian `knowledge/decisions/`.

## Setup notes

`uv` проект инициализируется в **корне репозитория** (`pyproject.toml` в repo root) — Phase 2 переиспользует тот же uv project. См. plan 01-02. Не делайте `uv init` повторно в Phase 2.

`.env.local` (IPRoyal credentials) лежит в repo root, в git **не** коммитится (см. `.gitignore`).
```

Важно: если каталог `.planning/spikes/01-goldapple/` уже существует с какими-либо файлами — оставить их (не перезаписывать). Если файла README.md нет — создать. Если есть — прочитать и заменить только если содержимое не соответствует выше.
  </action>
  <acceptance_criteria>
    - Файл `.planning/spikes/01-goldapple/README.md` существует
    - `grep -c "Throwaway scope" .planning/spikes/01-goldapple/README.md` возвращает ≥ 1
    - `grep -c "D-01" .planning/spikes/01-goldapple/README.md` возвращает ≥ 1
    - `grep -c "D-16" .planning/spikes/01-goldapple/README.md` возвращает ≥ 1
    - Файл `.planning/spikes/01-goldapple/sample-payloads/.gitkeep` существует
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/spikes/01-goldapple/README.md && test -f .planning/spikes/01-goldapple/sample-payloads/.gitkeep && grep -q "Throwaway scope" .planning/spikes/01-goldapple/README.md && grep -q "D-01" .planning/spikes/01-goldapple/README.md && grep -q "D-16" .planning/spikes/01-goldapple/README.md</automated>
  </verify>
  <done>README с throwaway-scope, navigation table артефактов и ссылкой на 16 D-XX-решений из CONTEXT.md создан; sample-payloads/ закоммичен через .gitkeep.</done>
</task>

<task type="auto">
  <name>Task 2: Создать stub-файлы MEMO.md / notebook.py / notebook-viled.py / tos-audit.md</name>
  <files>.planning/spikes/01-goldapple/MEMO.md, .planning/spikes/01-goldapple/notebook.py, .planning/spikes/01-goldapple/notebook-viled.py, .planning/spikes/01-goldapple/tos-audit.md</files>
  <read_first>
    - `.planning/spikes/01-goldapple/README.md` (создан в Task 1) — для согласованности terminologii
    - `.planning/phases/01-goldapple-reconnaissance-spike/01-CONTEXT.md` секции `<decisions>` (D-13, D-14, D-15 — что должно быть в memo) и "Не обсуждали явно" (формат decision memo template)
  </read_first>
  <action>
Создать четыре stub-файла. Каждый stub задаёт скелет, который последующие планы заполняют. Все Russian-headers.

**`.planning/spikes/01-goldapple/MEMO.md`** — decision memo template (заполняется в 01-11):

```markdown
# Spike 01: Goldapple Anti-Bot Decision Memo

**Sign-off:** _PENDING — заполнить дату + подпись после plan 01-11_
**Spike start:** _TBD_
**Spike end:** _TBD_

> Заполняется в plan 01-11 на основе всех findings 01-04..01-10.

## TL;DR

> Однострочное summary: tier, engine, proxy, prod-IP recommendation.

- **Chosen tier:** _TBD (0 / 2 / 3 / 4)_
- **Browser engine:** _TBD (curl_cffi only / vanilla Playwright / Patchright / Camoufox)_
- **Proxy provider:** _TBD (none / IPRoyal residential / Decodo / managed unblocker)_
- **Production IP recommendation (Phase 7):** _TBD_

## Problem

Goldapple.kz anti-bot tier — defining unknown проекта (см. research/SUMMARY.md, research/PITFALLS.md). Phase 3 stack гейтится этим решением.

## Options tested

| Tier | Engine | Proxy | Result | Notes |
|------|--------|-------|--------|-------|
| _TBD_ | _TBD_ | _TBD_ | _TBD/100 fetches, X challenges_ | _TBD_ |

## Chosen

**Tier:** _TBD_
**Rationale:** _TBD_
**Exact 100-fetch results:**
- KZ-laptop (D-06): _N/100, X challenges_
- EU/RU residential (D-05): _N/100, X challenges_

## JSON-endpoint hunt verdict (D-09, D-10)

_TBD — found / not found. Если found: какие эндпоинты, как используются, влияет на сценарий Tier 0._

## Page-volume estimate (RECON-03)

_TBD — products per typical brand, source: sitemap.xml / pagination meta._

## viled.kz feasibility (RECON-02)

_TBD — N/10 successful curl_cffi fetches, JSON-LD presence, timing._

## robots/ToS audit summary (RECON-04)

_См. tos-audit.md. Committed rate-limits:_
- viled.kz: _TBD_
- goldapple.kz: _TBD_

## Next-step impact

- **Phase 3 stack:** _TBD_
- **Phase 7 hosting / prod-IP:** _TBD_

## Open risks

- _TBD_

## Appendix: Challenge-rate (D-15)

_TBD — если challenge-rate >20%, tier помечается "fragile" даже на технически-passing._
```

**`.planning/spikes/01-goldapple/notebook.py`** — заголовок (контент пишет 01-08):

```python
"""
Spike 01: Goldapple Patchright 100-fetch experiment.

Throwaway script. Implementation in plans 01-08 (KZ-laptop), 01-09 (EU-proxy comparison),
conditionally 01-10 (Tier 3 escalation if 01-08/01-09 fails).

Per D-04: persistent (warm) browser context, slow rate (3-5s pause), cookies across fetches.
Per D-13: threshold ≥95/100 successful fetches (5xx/timeout retries allowed).
Per D-14: successful fetch = HTTP 200 + JSON-LD product schema present.
Per D-03: stop-rule = 5 consecutive blocks OR first Cloudflare/DataDome challenge without auto-resolve.
"""

# Implementation: see plan 01-08-PLAN.md
raise NotImplementedError("Filled by plan 01-08")
```

**`.planning/spikes/01-goldapple/notebook-viled.py`** — заголовок (контент пишет 01-07):

```python
"""
Spike 01: viled.kz curl_cffi feasibility check.

Throwaway script. Implementation in plan 01-07.

Per RECON-02: ≥10 product fetches via curl_cffi impersonate="chrome".
Side-deliverables (per CONTEXT.md "Не обсуждали явно"): timing, JSON-LD presence,
pagination shape, robots/UA strictness — all logged for Phase 2 hot-start.
"""

# Implementation: see plan 01-07-PLAN.md
raise NotImplementedError("Filled by plan 01-07")
```

**`.planning/spikes/01-goldapple/tos-audit.md`** — заголовок (контент пишет 01-04):

```markdown
# Spike 01: robots.txt + ToS Audit (RECON-04)

**Status:** Pending (filled by plan 01-04)
**Scope:** self-review обоих сайтов (viled.kz и goldapple.kz). KZ-legal review (30 мин с юристом) — DEFERRED to Phase 7 per CONTEXT.md.

## viled.kz

**robots.txt URL:** _TBD_
**Fetched:** _TBD_
**Allowed paths relevant to scraping:** _TBD_
**Disallowed paths relevant to scraping:** _TBD_
**Crawl-delay directive:** _TBD_
**ToS URL:** _TBD_
**ToS findings (anti-scraping clauses):** _TBD_
**viled.kz rate-limit:** _TBD (committed value, e.g., 1 req / 2s)_

## goldapple.kz

**robots.txt URL:** _TBD_
**Fetched:** _TBD_
**Allowed paths relevant to scraping:** _TBD_
**Disallowed paths relevant to scraping:** _TBD_
**Crawl-delay directive:** _TBD_
**Sitemap URLs from robots.txt:** _TBD (передать в plan 01-05)_
**ToS URL:** _TBD_
**ToS findings (anti-scraping clauses):** _TBD_
**goldapple.kz rate-limit:** _TBD (committed value)_

## Summary

_Заполнить на основе findings выше._
```

Все файлы на UTF-8 без BOM, LF line endings.
  </action>
  <acceptance_criteria>
    - `test -f .planning/spikes/01-goldapple/MEMO.md` succeeds
    - `test -f .planning/spikes/01-goldapple/notebook.py` succeeds
    - `test -f .planning/spikes/01-goldapple/notebook-viled.py` succeeds
    - `test -f .planning/spikes/01-goldapple/tos-audit.md` succeeds
    - `grep -c "Chosen tier:" .planning/spikes/01-goldapple/MEMO.md` ≥ 1
    - `grep -c "viled.kz rate-limit:" .planning/spikes/01-goldapple/tos-audit.md` ≥ 1
    - `grep -c "goldapple.kz rate-limit:" .planning/spikes/01-goldapple/tos-audit.md` ≥ 1
    - `grep -c "NotImplementedError" .planning/spikes/01-goldapple/notebook.py` ≥ 1
    - `grep -c "NotImplementedError" .planning/spikes/01-goldapple/notebook-viled.py` ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/spikes/01-goldapple/MEMO.md && test -f .planning/spikes/01-goldapple/notebook.py && test -f .planning/spikes/01-goldapple/notebook-viled.py && test -f .planning/spikes/01-goldapple/tos-audit.md && grep -q "Chosen tier:" .planning/spikes/01-goldapple/MEMO.md && grep -q "viled.kz rate-limit:" .planning/spikes/01-goldapple/tos-audit.md && grep -q "goldapple.kz rate-limit:" .planning/spikes/01-goldapple/tos-audit.md</automated>
  </verify>
  <done>Все 4 stub-файла созданы с раздельными секциями-якорями. Последующие планы заполняют, не перезаписывая структуру.</done>
</task>

<task type="auto">
  <name>Task 3: Создать/обновить .gitignore для secrets и spike-артефактов</name>
  <files>.gitignore</files>
  <read_first>
    - `.gitignore` (если уже существует — расширить, не перезаписать)
  </read_first>
  <action>
Создать или обновить `.gitignore` в корне репозитория. Если файл существует — добавить недостающие записи (idempotent merge), если нет — создать с нуля.

Содержимое (минимум):

```gitignore
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/

# uv
.uv-cache/

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Secrets (D-08: IPRoyal credentials)
.env
.env.local
.env.*.local

# Spike browser state (D-04: persistent context — не коммитим cookie)
.planning/spikes/01-goldapple/browser-state/
.planning/spikes/01-goldapple/.cache/

# DBs (Phase 2+ artifact, добавляем превентивно)
*.db
*.db-journal
*.db-wal
*.db-shm

# Logs
*.log
logs/

# Build artifacts
dist/
build/
*.egg-info/
```

Если `.gitignore` уже есть — прочитать его, добавить ТОЛЬКО недостающие строки (по точному совпадению строки), сохранить порядок существующих записей.
  </action>
  <acceptance_criteria>
    - Файл `.gitignore` существует в корне репо
    - `grep -c "^.env.local$" .gitignore` ≥ 1
    - `grep -c "^.planning/spikes/01-goldapple/browser-state/" .gitignore` ≥ 1
    - `grep -c "^*.db$" .gitignore` ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>test -f .gitignore && grep -q "^\.env\.local$" .gitignore && grep -q "^\.planning/spikes/01-goldapple/browser-state/" .gitignore && grep -q "^\*\.db$" .gitignore</automated>
  </verify>
  <done>.gitignore покрывает .env.local, browser-state, .db, .log; secrets D-08 (IPRoyal credentials) защищены от commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Plan creates skeleton files only; no network, no user data, no production code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Information Disclosure | `.env.local` (IPRoyal credentials, plan 01-03) committed by accident | mitigate | `.gitignore` блокирует `.env*.local` (Task 3); plan 01-03 явно создаёт credentials только в `.env.local`. |

Spike scope: skeleton-only, no PII, no production deployment. Full threat model deferred to Phase 6 (Telegram bot tokens) and Phase 7 (VPS hardening).
</threat_model>

<verification>
- `tree .planning/spikes/01-goldapple/ -L 2` показывает: README.md, MEMO.md, notebook.py, notebook-viled.py, tos-audit.md, sample-payloads/.gitkeep
- `git status` показывает 7 новых файлов готовых к commit
</verification>

<success_criteria>
- 6 stub-файлов + .gitkeep созданы в `.planning/spikes/01-goldapple/`
- README фиксирует throwaway-scope (нельзя импортировать в Phase 2 src/)
- MEMO.md имеет полный template-skeleton со всеми обязательными секциями (Chosen tier, JSON-endpoint hunt verdict, viled feasibility, page-volume estimate, robots/ToS, prod-IP recommendation, challenge-rate appendix)
- `.gitignore` защищает .env.local (IPRoyal trial credentials из plan 01-03)
</success_criteria>

<output>
After completion, create `.planning/phases/01-goldapple-reconnaissance-spike/01-01-SUMMARY.md`
</output>
