---
phase: 01-goldapple-reconnaissance-spike
plan: 11
type: execute
wave: 4
depends_on: [04, 05, 06, 07, 08, 09, 10]
files_modified:
  - .planning/spikes/01-goldapple/MEMO.md
autonomous: false
requirements: [RECON-01, RECON-02, RECON-03, RECON-04]

must_haves:
  truths:
    - "MEMO.md TL;DR заполнен: tier, browser engine, proxy provider, prod-IP recommendation для Phase 7"
    - "Sign-off date, spike start, spike end заполнены реальными датами"
    - "Все обязательные секции заполнены (нет TBD): Problem, Options tested, Chosen, JSON-endpoint hunt, Page-volume estimate, viled feasibility, robots/ToS audit summary, Next-step impact, Open risks, Challenge-rate appendix"
    - "Decision-memo template (problem / options tested / chosen tier+rationale / next-step impact / open risks) полностью применён"
    - "Sign-off line есть и заполнена"
  artifacts:
    - path: ".planning/spikes/01-goldapple/MEMO.md"
      provides: "Final signed-off decision memo"
      contains: "Sign-off:"
  key_links:
    - from: ".planning/spikes/01-goldapple/MEMO.md"
      to: "all sample-payloads/* artifacts"
      via: "TL;DR + Open risks + each section reference evidence files"
      pattern: "sample-payloads"
---

<objective>
Финализировать `MEMO.md` — главный deliverable спайка. Свести findings из 01-04..01-10 в подписанный decision memo. После этого plan'а:
- Phase 3 имеет phisically-sufficient stack-decision
- Phase 7 имеет prod-IP recommendation
- /gsd-spike --wrap-up в plan 01-12 может упаковать в skill

Per CONTEXT.md "Не обсуждали явно": "Claude использует короткий decision-memo template (problem / options tested / chosen tier+rationale / next-step impact / open risks), не freeform."
Per D-13/D-14/D-15: точные числа из run'ов фиксируются в MEMO.

Output: Signed-off MEMO.md с заполненной TL;DR, всеми обязательными секциями, sign-off line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-goldapple-reconnaissance-spike/01-CONTEXT.md
@.planning/spikes/01-goldapple/MEMO.md
@.planning/spikes/01-goldapple/tos-audit.md
@.planning/spikes/01-goldapple/SETUP-NOTES.md
@.planning/spikes/01-goldapple/PROXY-SETUP.md
@.planning/spikes/01-goldapple/sample-payloads/page-volume-raw.json
@.planning/spikes/01-goldapple/sample-payloads/goldapple-network-trace.md
@.planning/spikes/01-goldapple/sample-payloads/viled-fetch-results.json
@.planning/spikes/01-goldapple/sample-payloads/tier2-camoufox-kz-results.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Свести summary из всех result-JSONs + assemble final TL;DR</name>
  <files></files>
  <read_first>
    - `.planning/spikes/01-goldapple/sample-payloads/tier2-kz-laptop-results.json`
    - `.planning/spikes/01-goldapple/sample-payloads/tier2-eu-proxy-results.json` (может отсутствовать если plan 01-09 был skipped)
    - `.planning/spikes/01-goldapple/sample-payloads/tier2-tier3-results.json` (может отсутствовать если plan 01-10 был skipped)
    - `.planning/spikes/01-goldapple/sample-payloads/viled-fetch-results.json`
    - `.planning/spikes/01-goldapple/sample-payloads/page-volume-raw.json`
    - `.planning/spikes/01-goldapple/sample-payloads/goldapple-network-trace.md`
    - `.planning/spikes/01-goldapple/tos-audit.md`
    - `.planning/spikes/01-goldapple/PROXY-SETUP.md`
  </read_first>
  <action>
Прочитать ВСЕ result-files и составить mental model из:
- Какие tier'ы measured (Tier 2 KZ-laptop всегда; Tier 2 EU-proxy если 01-09 запускался; Tier 3 если 01-10 запускался)
- Какие passes_d13 / fragile flags
- JSON-endpoint hunt verdict (Tier 0 viable / not / partial)
- viled curl_cffi verdict (CONFIRMED / BLOCKED)
- Page-volume per brand → bandwidth estimate

**Decision tree для chosen tier:**

1. Если JSON-endpoint hunt = "Tier 0 candidate found" (D-10): **Chosen tier = 0** (curl_cffi + JSON endpoint). Browser engine = curl_cffi. Proxy = none. Это золото.

2. Иначе если Tier 2 KZ-laptop passes_d13 AND not fragile AND Tier 2 EU-proxy passes_d13 AND not fragile: **Chosen tier = 2**. Engine = Patchright. Proxy = none. Prod-IP = Hetzner EU OK.

3. Иначе если Tier 2 KZ-laptop passes_d13 AND not fragile, но EU-proxy fails OR fragile: **Chosen tier = 2 with KZ residential proxy** (effectively Tier 3 from a proxy-budget standpoint). Engine = Patchright. Proxy = IPRoyal KZ residential. Prod-IP = Hetzner EU + IPRoyal KZ residential для goldapple.

4. Иначе если Tier 3 (plan 01-10) passes_d13 AND not fragile: **Chosen tier = 3**. Engine = Patchright. Proxy = IPRoyal residential <geo>. Prod-IP = Hetzner EU + IPRoyal residential.

5. Иначе (всё провалилось ИЛИ plan 01-10 не выполнен и Tier 2 fail): **Chosen tier = 4** (managed unblocker). Engine = TBD (Camoufox/Scrapling spike OR managed API). Proxy = ZenRows / Bright Data / Decodo Web Unlocker. Prod-IP = depends on managed service. **Project economics affected per D-02**.

Сформировать TL;DR + Chosen блоки. Этот task — only mental work / data extraction; ничего не пишется. Task 2 пишет.
  </action>
  <acceptance_criteria>
    - (No file changes; preparation step)
  </acceptance_criteria>
  <verify>
    <automated>true</automated>
  </verify>
  <done>Mental model готов: chosen tier, engine, proxy, prod-IP — все four выведены из data.</done>
</task>

<task type="auto">
  <name>Task 2: Перезаписать MEMO.md TL;DR и Chosen секции с финальным verdict + sign-off</name>
  <files>.planning/spikes/01-goldapple/MEMO.md</files>
  <read_first>
    - `.planning/spikes/01-goldapple/MEMO.md` (с заполненными секциями из 01-04..01-10 — главный source)
    - All result JSONs из Task 1
  </read_first>
  <action>
Переписать секции `# Spike 01: Goldapple Anti-Bot Decision Memo`, `## TL;DR`, и `## Chosen` финальными значениями. **НЕ трогать** уже заполненные секции (Page-volume, JSON-endpoint hunt, viled feasibility, Options tested, Multi-geo finding, Tier 3 finding, robots/ToS summary).

```markdown
# Spike 01: Goldapple Anti-Bot Decision Memo

**Sign-off:** _<сегодняшняя дата ISO YYYY-MM-DD>_ — mirdbek@gmail.com (operator) — APPROVED
**Spike start:** _<date plan 01-01 был commited>_
**Spike end:** _<сегодняшняя дата>_
**Duration:** _<N>_ days (timebox 1 week per D-02)

## TL;DR

Один абзац (3-5 строк), human-readable:

> Goldapple.kz feasibly scrapeable at **Tier <N>** using **<engine>** with **<proxy or none>**. KZ-laptop measurement: <N>/100 successful fetches with <X>% challenge rate. <Multi-geo / Tier 3 caveat>. Production deployment: **<Hetzner EU + (proxy or none)>**. JSON-endpoint hunt: **<verdict>**. viled.kz feasibility: **<CONFIRMED/BLOCKED>** via curl_cffi. Phase 3 stack ready to build.

- **Chosen tier:** <0 / 2 / 3 / 4>
- **Browser engine:** <curl_cffi-only / Patchright / Camoufox / managed API>
- **Proxy provider:** <none / IPRoyal residential <geo> / Decodo / managed unblocker>
- **Production IP recommendation (Phase 7):** <Hetzner EU без proxy / Hetzner EU + IPRoyal KZ residential / TBD per Tier 4 spike>

## Problem

Goldapple.kz anti-bot tier — defining unknown проекта (см. research/SUMMARY.md, research/PITFALLS.md). Phase 3 stack гейтится этим решением. Спайк должен resolved tier <4 OR явно flag что нужен Tier 4 (per D-02).
```

[Сохранить _Options tested_ table в нынешнем виде — она уже заполнена в 01-08/01-09/01-10]

```markdown
## Chosen

**Tier:** <N>
**Engine:** <name>
**Proxy:** <none / details>
**Geo:** <KZ / EU / RU / N/A>

**Rationale (3-5 пунктов):**
- _e.g., Tier 2 KZ-laptop: 98/100 без proxy, 4% challenges all auto-resolved_
- _e.g., Tier 2 EU-proxy: 84/100, 18% challenges — geo дискриминация заметна_
- _e.g., Tier 3 не запускался — Tier 2 на KZ-IP достаточен_
- _e.g., JSON-endpoint hunt: __NEXT_DATA__ доступен в HTML — parser получит резильентность бонус_
- _e.g., page-volume avg ~120 URLs/brand × 50 брендов = ~6000 weekly fetches → ~600MB → ~$2/week если с proxy, $0/week если без_

**Exact 100-fetch results:**
- KZ-laptop (D-06): <N>/100 success, <X> challenges (<Y> auto-resolved), <Z> blocks
- EU/RU residential (D-05): <N>/100 success, <X> challenges (<Y> auto-resolved), <Z> blocks  [or "not measured — plan 01-09 skipped due to ..."]
- Tier 3 KZ residential: <N>/100 success, <X> challenges  [or "not measured — Tier 2 sufficient"]

[Сохранить остальные секции: JSON-endpoint hunt verdict, Page-volume estimate, viled feasibility, robots/ToS audit summary — они заполнены в 01-04..01-07]

## Next-step impact

**Phase 3 stack:**
- _e.g., curl_cffi + selectolax + JSON-LD parsing (Tier 0)_
- _e.g., Patchright + IPRoyal residential KZ + persistent context (Tier 3)_

**Phase 3 budget:**
- Bandwidth estimate: ~<MB>/week per page-volume × <N> brands
- Proxy cost: $<X>/week (или $0)
- Crawl duration estimate: ~<minutes>m at chosen rate-limit

**Phase 7 hosting / prod-IP:**
- _Hetzner CX22 EU без proxy (если Tier 0 / Tier 2 без proxy)_
- _Hetzner CX22 EU + IPRoyal residential KZ pool как production proxy (если Tier 3)_
- _Re-evaluate hosting если Tier 4 (managed unblocker меняет всё)_

**Phase 2 inputs from spike:**
- viled curl_cffi: CONFIRMED — proceed без headless browser per stack research
- viled JSON-LD shape: <see viled-fetch-results.json> — PARSE-02 module ready to consume
- viled pagination shape: <e.g., ?page=N&per_page=24> — CRAWL-01 имеет pattern
- viled rate-limit committed: <N>s pause — CRAWL-06 config
- goldapple rate-limit committed: <N>s — Phase 3 CRAWL-06 config

[Сохранить Open risks секцию — она была обновлена в 01-08/01-09/01-10 с CRITICAL flag если Tier 4]

## Appendix: Challenge-rate (D-15)

- KZ-laptop: <X>% challenges, all <Y/N> auto-resolved → <NOT fragile / fragile>
- EU-proxy:  <X>% challenges, <Y/N> auto-resolved → <NOT fragile / fragile>
- Tier 3:    <X>% challenges, <Y/N> auto-resolved  [or "not measured"]

Per D-15: tier flagged "fragile" если challenge-rate >20% даже на passing 95/100. Эта секция captures фактическую challenge-rate для prod-monitoring guidance.

## Sign-off

I, mirdbek@gmail.com, на дату <YYYY-MM-DD> подтверждаю что спайк завершён:
- [x] 100-fetch experiment(s) выполнены и задокументированы
- [x] Chosen tier зафиксирован
- [x] Phase 3 stack-decision можно принять без дальнейших experiments
- [x] viled curl_cffi feasibility подтверждена (RECON-02)
- [x] page-volume estimate готов (RECON-03)
- [x] robots/ToS audited, rate-limits committed (RECON-04)
- [x] Open risks задокументированы и переданы в Phase 7 backlog
```

**Шаги:**
1. Прочитать current MEMO.md
2. Identify sections (TL;DR, Chosen, Next-step impact, Sign-off) которые нужно перезаписать
3. Сформировать новый content на основе Task 1 mental model + actual data из result JSONs
4. Записать обновлённый MEMO.md (использовать Edit tool с конкретными замещениями для каждой целевой секции — НЕ Write полного файла, чтобы не потерять заполненные в 01-04..01-10 секции)

**Важно:**
- Все числа из result JSONs — реальные, не выдуманные
- Sign-off line должна быть ЯВНО подтверждена в чате — Claude может задать вопрос "Подтверждаете подпись MEMO.md как mirdbek@gmail.com на дату <today>?" перед финализацией. Если оператор подтверждает — пишет sign-off. Если нет — оставляет "Sign-off: PENDING — operator review required".

Обязательная финальная проверка:
```bash
grep -c "_TBD_" .planning/spikes/01-goldapple/MEMO.md
```
Должно вернуть 0.
  </action>
  <acceptance_criteria>
    - `grep -c "## TL;DR" .planning/spikes/01-goldapple/MEMO.md` ≥ 1
    - `grep -c "## Chosen" .planning/spikes/01-goldapple/MEMO.md` ≥ 1
    - `grep -c "## Next-step impact" .planning/spikes/01-goldapple/MEMO.md` ≥ 1
    - `grep -c "## Sign-off" .planning/spikes/01-goldapple/MEMO.md` ≥ 1
    - `grep -cE "^- \*\*Chosen tier:\*\*" .planning/spikes/01-goldapple/MEMO.md` ≥ 1 (TL;DR bullet)
    - `grep -cE "^- \*\*Production IP recommendation" .planning/spikes/01-goldapple/MEMO.md` ≥ 1
    - `! grep -q "_TBD_" .planning/spikes/01-goldapple/MEMO.md` (no remaining TBDs)
    - `! grep -q "<N>/100" .planning/spikes/01-goldapple/MEMO.md` (no remaining placeholders для чисел)
    - Sign-off line содержит конкретную дату (не "PENDING") OR явное "PENDING — operator review required"
  </acceptance_criteria>
  <verify>
    <automated>grep -q "## TL;DR" .planning/spikes/01-goldapple/MEMO.md && grep -q "## Chosen" .planning/spikes/01-goldapple/MEMO.md && grep -q "## Next-step impact" .planning/spikes/01-goldapple/MEMO.md && grep -q "## Sign-off" .planning/spikes/01-goldapple/MEMO.md && grep -qE "^- \*\*Chosen tier:\*\*" .planning/spikes/01-goldapple/MEMO.md && ! grep -q "_TBD_" .planning/spikes/01-goldapple/MEMO.md</automated>
  </verify>
  <done>MEMO.md финализирован: TL;DR + Chosen + Next-step impact + Sign-off секции заполнены конкретными данными; нет TBD'ов. Memo готов к копированию в Obsidian (plan 01-12) и к использованию в Phase 3 discuss/plan.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke-проверка финального MEMO.md — все секции заполнены, sample-payloads cross-linked</name>
  <files></files>
  <read_first>
    - `.planning/spikes/01-goldapple/MEMO.md` (final)
    - `.planning/spikes/01-goldapple/sample-payloads/` (listing)
  </read_first>
  <action>
Прогнать automatic checks через bash/grep чтобы убедиться что MEMO ready:

1. **Все обязательные секции присутствуют:**
   ```bash
   for s in "## TL;DR" "## Problem" "## Options tested" "## Chosen" "## JSON-endpoint hunt verdict" "## Page-volume estimate" "## viled.kz feasibility" "## robots/ToS audit summary" "## Next-step impact" "## Open risks" "## Appendix: Challenge-rate" "## Sign-off"; do
     grep -q "$s" .planning/spikes/01-goldapple/MEMO.md && echo "OK: $s" || echo "MISSING: $s"
   done
   ```
   Все должны быть OK.

2. **Все evidence-файлы cross-linked в MEMO:**
   ```bash
   for f in tier2-kz-laptop-results.json viled-fetch-results.json page-volume-raw.json goldapple-network-trace.md; do
     grep -q "$f" .planning/spikes/01-goldapple/MEMO.md && echo "OK: $f cross-linked" || echo "MISSING link: $f"
   done
   ```

3. **Sign-off line check:**
   ```bash
   grep -E "^\*\*Sign-off:\*\*" .planning/spikes/01-goldapple/MEMO.md
   ```
   Должна вывести строку с датой ИЛИ явным PENDING.

4. **Sanity check на размер MEMO:**
   ```bash
   wc -l .planning/spikes/01-goldapple/MEMO.md
   ```
   Заполненный MEMO должен быть ≥150 lines (template + filled-in data). Если <100 — что-то лишнее удалено в Task 2.

5. **Если что-то "MISSING" — fix и повторить шаги 1-4. Если всё OK — записать одну строку в `## Open risks` с timestamp последней проверки:**
   ```markdown
   _Last consistency check: <YYYY-MM-DD HH:MM> — all sections present, all evidence files cross-linked._
   ```

   Если есть "PENDING" в Sign-off — поднять отдельный checkpoint в чате чтобы оператор подтвердил sign-off (или явно отложил, например "review monday").
  </action>
  <acceptance_criteria>
    - Все 12 обязательных секций (## TL;DR, ## Problem, ## Options tested, ## Chosen, ## JSON-endpoint hunt verdict, ## Page-volume estimate, ## viled.kz feasibility, ## robots/ToS audit summary, ## Next-step impact, ## Open risks, ## Appendix: Challenge-rate, ## Sign-off) присутствуют
    - Минимум 4 evidence-файла (tier2-*-results.json, viled-fetch-results.json, page-volume-raw.json, goldapple-network-trace.md) cross-linked
    - MEMO.md ≥150 строк
    - Sign-off строка имеет либо конкретную дату либо явное PENDING с reason
  </acceptance_criteria>
  <verify>
    <automated>for s in "## TL;DR" "## Problem" "## Options tested" "## Chosen" "## JSON-endpoint hunt verdict" "## Page-volume estimate" "## viled.kz feasibility" "## robots/ToS audit summary" "## Next-step impact" "## Open risks" "## Appendix: Challenge-rate" "## Sign-off"; do grep -q "$s" .planning/spikes/01-goldapple/MEMO.md || (echo "MISSING: $s" && exit 1); done && [ "$(wc -l < .planning/spikes/01-goldapple/MEMO.md)" -ge 150 ] && grep -qE "^\*\*Sign-off:\*\*" .planning/spikes/01-goldapple/MEMO.md && echo "OK"</automated>
  </verify>
  <done>MEMO.md прошёл consistency check: 12 секций, 4+ evidence files cross-linked, ≥150 lines, sign-off line valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Plan только assembles existing data из spike artifacts; no new network calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11-01 | Repudiation | MEMO.md sign-off без явного operator-confirmation в чате | mitigate | Task 2 явно требует operator approval перед записью sign-off line; иначе PENDING. |
| T-01-11-02 | Tampering | MEMO.md случайно потерял заполненные в 01-04..01-10 секции при rewrite | mitigate | Task 2 использует Edit tool с конкретными секциями (не Write полного файла); Task 3 grep'ом проверяет все 12 секций. |

Spike scope: документирование. No PII collected. Full threat model deferred.
</threat_model>

<verification>
- `wc -l .planning/spikes/01-goldapple/MEMO.md` ≥ 150
- `grep -c "^## " .planning/spikes/01-goldapple/MEMO.md` ≥ 11 (12 секций минимум)
- `grep -c "sample-payloads" .planning/spikes/01-goldapple/MEMO.md` ≥ 4
- `! grep -q "_TBD_" .planning/spikes/01-goldapple/MEMO.md`
</verification>

<success_criteria>
- MEMO.md финализирован с TL;DR (tier, engine, proxy, prod-IP), Chosen с rationale + exact numbers, Next-step impact для Phase 3 + Phase 7
- Все 4 RECON requirements покрыты (RECON-01 в Options/Chosen, RECON-02 в viled feasibility, RECON-03 в Page-volume + JSON-endpoint hunt, RECON-04 в robots/ToS summary)
- Sign-off line с датой (или явное PENDING)
- Все evidence-файлы (sample-payloads/*) cross-linked
- Memo ready для копирования в Obsidian и для /gsd-spike --wrap-up в plan 01-12
</success_criteria>

<output>
After completion, create `.planning/phases/01-goldapple-reconnaissance-spike/01-11-SUMMARY.md`
</output>
