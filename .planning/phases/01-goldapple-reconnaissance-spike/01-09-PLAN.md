---
phase: 01-goldapple-reconnaissance-spike
plan: 09
type: execute
wave: 2
depends_on: [03, 08]
files_modified:
  - .planning/spikes/01-goldapple/sample-payloads/tier2-eu-proxy-results.json
  - .planning/spikes/01-goldapple/sample-payloads/tier2-eu-proxy-log.txt
  - .planning/spikes/01-goldapple/MEMO.md
autonomous: false
requirements: [RECON-01]

must_haves:
  truths:
    - "Тот же 100-URL pool из plan 01-08 запущен через notebook.py --mode eu-proxy с IPRoyal/Decodo residential proxy (D-05 multi-geo)"
    - "Per-URL records записаны идентично KZ-laptop run для apples-to-apples comparison"
    - "MEMO.md 'Options tested' таблица содержит ВТОРУЮ строку (Tier 2 EU/RU-proxy) с реальными числами"
    - "Разница между KZ-laptop и EU-proxy зафиксирована в MEMO.md как multi-geo finding"
  artifacts:
    - path: ".planning/spikes/01-goldapple/sample-payloads/tier2-eu-proxy-results.json"
      provides: "JSON с тем же shape что tier2-kz-laptop-results.json но через proxy"
      contains: "success_count"
    - path: ".planning/spikes/01-goldapple/sample-payloads/tier2-eu-proxy-log.txt"
      provides: "Per-fetch JSON log"
    - path: ".planning/spikes/01-goldapple/MEMO.md"
      provides: "Options tested таблица с второй строкой + multi-geo comparison commentary"
      contains: "EU"
  key_links:
    - from: ".planning/spikes/01-goldapple/MEMO.md"
      to: "tier2-kz-laptop-results.json + tier2-eu-proxy-results.json"
      via: "Multi-geo comparison cite оба результата"
      pattern: "tier2-eu-proxy"
---

<objective>
Multi-geo measurement (per D-05): тот же 100-URL pool через ту же `notebook.py` но с **EU/RU residential proxy** (через IPRoyal credentials из plan 01-03). Сравнить с KZ-laptop результатами (plan 01-08).

Per D-05: "Меряем Patchright с двух геолокаций для сравнения. Memo содержит **два числа** (например, 'KZ-laptop: 98/100 без challenge, EU-proxy: 84/100, 6 challenges'). Это снимает неопределённость 'а что если IP-гео меняет ответ'."

Output: `tier2-eu-proxy-results.json` с success_count + challenges, обновлённая MEMO.md "Options tested" таблица с двумя rows.

**Branch logic после этого плана:**
- Если ОБА (KZ + EU) `passes_d13` AND обе НЕ fragile → Tier 2 без proxy CONFIRMED. Plan 01-10 SKIPPED. MEMO finalize в 01-11 с recommendation "Tier 2".
- Если EU-proxy НЕ passes_d13 (а KZ passes) → goldapple geo-discriminates against EU/RU IPs. Prod-IP recommendation: KZ residential (Phase 7). Plan 01-10 запускается чтобы проверить Tier 3 c KZ-residential pool.
- Если ОБА fail → Tier 2 INSUFFICIENT. Plan 01-10 запускается с KZ-residential.
- Если обе fragile → Plan 01-10 запускается, в MEMO рекомендация Tier 3 для prod.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-goldapple-reconnaissance-spike/01-CONTEXT.md
@.planning/spikes/01-goldapple/PROXY-SETUP.md
@.planning/spikes/01-goldapple/notebook.py
@.planning/spikes/01-goldapple/sample-payloads/tier2-kz-laptop-results.json
@.planning/spikes/01-goldapple/MEMO.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — проверить .env.local credentials и notebook.py готовность</name>
  <files></files>
  <read_first>
    - `.env.local` (PROXY_* keys должны быть filled)
    - `.planning/spikes/01-goldapple/PROXY-SETUP.md` (current geo)
    - `.planning/spikes/01-goldapple/notebook.py` (создан в plan 01-08)
    - `.planning/spikes/01-goldapple/sample-payloads/goldapple-product-urls.txt` (100 URLs)
  </read_first>
  <action>
1. Проверить что `.env.local` существует и содержит все нужные переменные:
   ```bash
   uv run python -c "
   from dotenv import dotenv_values
   v = dotenv_values('.env.local')
   missing = [k for k in ['PROXY_HOST', 'PROXY_PORT', 'PROXY_USER', 'PROXY_PASS', 'PROXY_GEO'] if not v.get(k)]
   if missing:
       raise SystemExit(f'Missing env vars: {missing}')
   print(f'OK — proxy ready, geo={v[\"PROXY_GEO\"]}')
   "
   ```

   Если падает — plan 01-03 не завершён или skipped. Сообщить пользователю: "Plan 01-03 не выполнен / skipped. Этот plan (01-09) тоже skipped. См. MEMO.md Open risks: 'Multi-geo measurement не выполнено'." Не запускать Task 2/3.

2. Проверить что notebook.py существует и handle eu-proxy mode:
   ```bash
   uv run python .planning/spikes/01-goldapple/notebook.py --help 2>&1 | grep eu-proxy
   ```

3. Проверить что URL pool из plan 01-08 ещё на месте:
   ```bash
   wc -l < .planning/spikes/01-goldapple/sample-payloads/goldapple-product-urls.txt
   ```
   Должно вывести ≥100. Если файл удалён — return и перезапустить 01-08 Task 1.

4. **Очистить browser-state** (D-04 persistent context для нового geo — старые cookies от KZ-laptop run загрязнят результат через identifying fingerprint):
   ```bash
   rm -rf .planning/spikes/01-goldapple/browser-state/
   ```
   Это критично: тот же user_data_dir с KZ cookies + EU IP = inconsistent fingerprint = нечестное измерение.
  </action>
  <acceptance_criteria>
    - `uv run python -c "from dotenv import dotenv_values; v = dotenv_values('.env.local'); assert all(v.get(k) for k in ['PROXY_HOST','PROXY_PORT','PROXY_USER','PROXY_PASS','PROXY_GEO']), 'missing'"` exit code 0
    - `[ -f .planning/spikes/01-goldapple/notebook.py ]` succeeds
    - `[ "$(wc -l < .planning/spikes/01-goldapple/sample-payloads/goldapple-product-urls.txt)" -ge 100 ]` succeeds
    - `[ ! -d .planning/spikes/01-goldapple/browser-state ]` succeeds (cleared for fresh fingerprint)
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from dotenv import dotenv_values; v = dotenv_values('.env.local'); assert all(v.get(k) for k in ['PROXY_HOST','PROXY_PORT','PROXY_USER','PROXY_PASS','PROXY_GEO']), 'missing'; print('OK')" && test -f .planning/spikes/01-goldapple/notebook.py && [ "$(wc -l < .planning/spikes/01-goldapple/sample-payloads/goldapple-product-urls.txt)" -ge 100 ] && [ ! -d .planning/spikes/01-goldapple/browser-state ]</automated>
  </verify>
  <done>Pre-flight passed: credentials готовы, notebook.py готов, 100 URLs готовы, browser-state очищен для fresh-geo measurement.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Запустить notebook.py --mode eu-proxy, наблюдать browser, дождаться завершения</name>
  <files>.planning/spikes/01-goldapple/sample-payloads/tier2-eu-proxy-results.json, .planning/spikes/01-goldapple/sample-payloads/tier2-eu-proxy-log.txt</files>
  <what-built>
Те же 100 fetch'ей через тот же script, но с `--mode eu-proxy`. Patchright поднимет Chrome через IPRoyal residential endpoint. Browser open в headless=False — оператор может наблюдать challenges, особенно интересно: насколько чаще они срабатывают на EU/RU IP vs KZ IP.

Realistic timing: 15-40 минут (proxy add +200-500ms на TLS handshake per request).
  </what-built>
  <how-to-verify>
1. Запустить:
   ```bash
   uv run python .planning/spikes/01-goldapple/notebook.py --mode eu-proxy
   ```

   Console напечатает `Using proxy server: http://geo.iproyal.com:12321 (creds redacted)`. Это OK — креды берутся из .env.local через python-dotenv.

2. **Наблюдать первые 5 fetch'ей** в open browser:
   - Если goldapple явно дискриминирует non-KZ IPs — увидите Cloudflare challenge на КАЖДОМ fetch (vs ~4-8% на KZ-laptop run если он прошёл)
   - Если goldapple геonostly indifferent — challenges будут similar к KZ-laptop run

3. **Дать script идти до конца** ИЛИ stop-rule (D-03):
   - Realistic 15-40 минут с proxy
   - Console прогресс как в plan 01-08

4. **После завершения** — script печатает SUMMARY:
   ```
   SUMMARY: success=84/100, challenges=18 (18.0%), resolved=15, blocks=16, stopped_early=False
   D-13 (>=95/100): FAIL
   D-15 (fragile if >20% challenges): NO (но close)
   ```

5. **Проверить файлы:**
   ```bash
   ls .planning/spikes/01-goldapple/sample-payloads/tier2-eu-proxy-*
   ```

6. **Подтвердить в чате:**
   ```
   approved
   summary: success=N/100, ch=X (Y%), resolved=Z, blocks=W, stopped=BOOL, passes_d13=BOOL, fragile=BOOL
   ```

**Возможные проблемы:**
- `RuntimeError: Mode eu-proxy requires PROXY_*` → Task 1 проверка credentials упала или .env.local пустой. Return.
- Все 100 challenge'ы → goldapple жёстко геофильтрует non-KZ. Это серьёзный finding для prod-IP recommendation. Дать script дойти до stop-rule (D-03), записать.
- Proxy 407 (Auth Required) → wrong creds в .env.local. Re-check IPRoyal dashboard.
- Висит на одной странице >2 мин → proxy slow, OR target site blocking. Ctrl+C, fix.
  </how-to-verify>
  <resume-signal>Напишите "approved" + summary string. При проблемах — "blocked: <описание>".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Записать Tier 2 EU-proxy row в MEMO.md + multi-geo comparison commentary</name>
  <files>.planning/spikes/01-goldapple/MEMO.md</files>
  <read_first>
    - `.planning/spikes/01-goldapple/sample-payloads/tier2-eu-proxy-results.json` (созданный Task 2)
    - `.planning/spikes/01-goldapple/sample-payloads/tier2-kz-laptop-results.json` (из plan 01-08)
    - `.planning/spikes/01-goldapple/MEMO.md` (с заполненной KZ-laptop row из 01-08)
  </read_first>
  <action>
Загрузить summaries из обоих JSON файлов. Добавить ВТОРУЮ строку в `## Options tested` table в MEMO.md:

```markdown
## Options tested

| Tier | Engine | Proxy | Geo (IP) | Result | Notes |
|------|--------|-------|----------|--------|-------|
| 2 | Patchright (warm context) | none | KZ (laptop, D-06) | <kz_success>/<kz_total> success, <kz_ch> challenges (<kz_resolved> auto-resolved), <kz_blocks> blocks | <kz_status> |
| 2 | Patchright (warm context) | IPRoyal residential | <eu_geo from .env.local PROXY_GEO> | <eu_success>/<eu_total> success, <eu_ch> challenges (<eu_resolved> auto-resolved), <eu_blocks> blocks | <eu_status> |
```

Подставить РЕАЛЬНЫЕ числа из обоих JSON.

После таблицы добавить секцию `### Multi-geo finding (D-05)`:

```markdown
### Multi-geo finding (D-05)

| Metric | KZ-laptop (D-06 baseline) | EU-proxy (IPRoyal `<geo>`) | Delta |
|--------|---------------------------|----------------------------|-------|
| Success rate | <kz_success>/<kz_total> = <kz_pct>% | <eu_success>/<eu_total> = <eu_pct>% | <delta> pp |
| Challenge rate | <kz_ch_pct>% | <eu_ch_pct>% | <ch_delta> pp |
| Blocks | <kz_blocks> | <eu_blocks> | <block_delta> |

**Interpretation:**

_Выбрать одну из формулировок based on actual data:_

- **Geo-agnostic** (delta ≤ 5 pp on success, similar challenges): goldapple.kz обращает мало внимания на IP-geo для read-only product fetches. Prod-IP может быть Hetzner EU без residential proxy.
- **KZ-favored** (KZ success >> EU success OR EU challenges >> KZ challenges): goldapple дискриминирует non-KZ IPs. **Prod-IP recommendation для Phase 7: KZ residential proxy ОБЯЗАТЕЛЬНО.** Hetzner EU IP для production не сработает; нужен Tier 3.
- **Both insufficient** (оба <95): даже KZ-IP недостаточен; нужен Tier 3 (KZ residential pool через IPRoyal). Plan 01-10 запускается.
- **EU-favored** (rare): EU работает лучше KZ. Странно для KZ-домена; возможно tracking-cookie state с laptop-runа загрязнил KZ run. Запустить 01-08 повторно с очищенным browser-state для verification.

**Decision triggers:**

- Если plan 01-10 (Tier 3 escalation) запускается? **<YES / NO based on>:**
  - YES если any of: kz_success<95, eu_success<95, kz_fragile, eu_fragile
  - NO если both ≥95/100 AND both not fragile (Tier 2 без proxy = clear winner)

См. `sample-payloads/tier2-kz-laptop-results.json` и `sample-payloads/tier2-eu-proxy-results.json` для raw data.
```

Сохранить остальные секции MEMO.md без изменений.

В конце — добавить запись в `## Open risks` если EU-proxy fragile/failed:
- "Risk-N: goldapple discriminates non-KZ IPs; prod-deployment на Hetzner EU IP will fail; KZ residential proxy required (Tier 3)."
  </action>
  <acceptance_criteria>
    - `grep -c "EU\|IPRoyal\|residential" .planning/spikes/01-goldapple/MEMO.md` ≥ 1 (вторая строка добавлена)
    - `grep -c "## Multi-geo finding (D-05)\|### Multi-geo finding (D-05)" .planning/spikes/01-goldapple/MEMO.md` ≥ 1
    - `grep -c "tier2-eu-proxy-results.json" .planning/spikes/01-goldapple/MEMO.md` ≥ 1 (cross-link)
    - `grep -c "tier2-kz-laptop-results.json" .planning/spikes/01-goldapple/MEMO.md` ≥ 1
    - В Multi-geo finding section есть конкретные числа (не `_TBD_` или `<kz_success>`)
    - `grep -cE "(Geo-agnostic|KZ-favored|Both insufficient|EU-favored)" .planning/spikes/01-goldapple/MEMO.md` ≥ 1 (выбрана одна интерпретация)
  </acceptance_criteria>
  <verify>
    <automated>grep -qE "EU|IPRoyal|residential" .planning/spikes/01-goldapple/MEMO.md && grep -q "Multi-geo finding (D-05)" .planning/spikes/01-goldapple/MEMO.md && grep -q "tier2-eu-proxy-results.json" .planning/spikes/01-goldapple/MEMO.md && grep -qE "(Geo-agnostic|KZ-favored|Both insufficient|EU-favored)" .planning/spikes/01-goldapple/MEMO.md</automated>
  </verify>
  <done>MEMO.md "Options tested" содержит две строки (KZ-laptop + EU-proxy); Multi-geo finding section с реальными числами и явной interpretation; решение о plan 01-10 (запускать ли Tier 3) явно записано.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local→IPRoyal residential pool→goldapple.kz | Patchright fetches routed через third-party residential ISPs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09-01 | Information Disclosure | Proxy creds могут попасть в exception traceback при connection failure | mitigate | notebook.py логирует только `proxy['server']` (host:port) per plan 01-08; user/pass не в logs. Operator проверяет log file перед commit. |
| T-01-09-02 | Tampering | IPRoyal node modifies HTTPS response | accept | TLS encryption guarantees integrity; cert pinning не релевантен для public retail site. |
| T-01-09-03 | Denial of Service | proxy bandwidth quota exhausted mid-run | accept | Spike trial budget; если 100 fetches × 100KB = 10MB, well within $7 trial. |

Spike scope: 100 read-only HTTP GETs через residential pool, single TCP session. Full threat model deferred.
</threat_model>

<verification>
- `python -c "import json; d = json.load(open('.planning/spikes/01-goldapple/sample-payloads/tier2-eu-proxy-results.json')); print(d['summary'])"` показывает summary
- `grep -A 30 'Multi-geo finding' .planning/spikes/01-goldapple/MEMO.md` показывает comparison table с реальными числами
</verification>

<success_criteria>
- Те же 100 URLs запущены через EU-proxy (D-05 multi-geo)
- Browser-state очищен между runs (fresh fingerprint per geo)
- MEMO.md "Options tested" содержит две строки
- Multi-geo finding section с явной interpretation определяет prod-IP recommendation для Phase 7
- Решение о plan 01-10 (запускать Tier 3 или skip) документировано
- RECON-01 progress (closure в 01-11)
</success_criteria>

<output>
After completion, create `.planning/phases/01-goldapple-reconnaissance-spike/01-09-SUMMARY.md`
</output>
