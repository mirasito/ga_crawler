---
phase: 01-goldapple-reconnaissance-spike
plan: 12
type: execute
wave: 4
depends_on: [11]
files_modified:
  - .planning/STATE.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "MEMO.md скопирован в Obsidian vault knowledge/decisions/ с assertion-style filename + frontmatter (per CLAUDE.md §Obsidian)"
    - "Project-local skill упакован через /gsd-spike --wrap-up (или manual fallback если slash-command недоступен) для чтения в Phase 3 discuss/plan"
    - "STATE.md обновлён: Phase 1 marked complete, chosen tier зафиксирован в Key Decisions table"
    - "Phase 3 готова к /gsd-discuss-phase 3 с Tier-decision на руках"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Current Position, Key Decisions table — обновлены"
      contains: "Tier"
  key_links:
    - from: ".planning/STATE.md"
      to: ".planning/spikes/01-goldapple/MEMO.md"
      via: "Key Decisions reference MEMO sign-off"
      pattern: "MEMO"
    - from: "knowledge/decisions/<assertion>.md"
      to: ".planning/spikes/01-goldapple/MEMO.md"
      via: "Cross-link для Obsidian search"
      pattern: "spikes/01-goldapple"
---

<objective>
Wrap-up спайка: дублировать MEMO.md в Obsidian vault, упаковать findings в project-local skill (для Phase 3 discuss/plan), обновить STATE.md. После этого spike officially closed; Phase 1 done; project ready для Phase 2 (skeleton + viled) И Phase 3 (goldapple crawl) с полным tier-decision на руках.

Per D-16: "На завершении спайка обязательно вызывается `/gsd-spike --wrap-up` чтобы упаковать findings в project-local skill, который Phase 3 discuss/plan смогут подгрузить. Дополнительная копия MEMO.md дублируется в Obsidian-vault в `knowledge/decisions/` для поиска и cross-link'ования."

Output: Skill в .claude/skills/ (или whatever discovery path), Obsidian decision note, updated STATE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/spikes/01-goldapple/MEMO.md
@CLAUDE.md
@.planning/phases/01-goldapple-reconnaissance-spike/01-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Дублировать MEMO.md в Obsidian vault knowledge/decisions/</name>
  <what-built>
Per CLAUDE.md `## Obsidian Knowledge Vault` инструкция: "Хранилище знаний: C:\Users\...\ObsidianMyProject\". Per project-prompt: "Если решение — создай в knowledge/decisions/".

Per CLAUDE.md /memory: "[Obsidian vault conventions](feedback_obsidian_vault_style.md) — assertion-style filenames, wiki-links, frontmatter с tags+date".

Это checkpoint:human-verify потому что Claude не знает absolute path к Obsidian vault (не в `.planning/` и не в repo). Ассумпция — vault в `C:\Users\gstorepc\ObsidianMyProject\` или в parent of repo. Operator должен подтвердить путь.
  </what-built>
  <how-to-verify>
1. Подтвердить (или сообщить) absolute path к Obsidian vault. Variants:
   - `C:\Users\gstorepc\ObsidianMyProject\knowledge\decisions\` (default per CLAUDE.md)
   - `C:\Users\gstorepc\projects\Obsidian-MyProject\knowledge\decisions\`
   - Какой-то другой путь

2. Подтвердить (или сообщить) assertion-style filename — per project-skill convention. Examples:
   - `2026-05-XX-goldapple-anti-bot-tier-2-confirmed.md` (если Tier 2 chosen)
   - `2026-05-XX-goldapple-requires-tier-3-residential.md` (если Tier 3)
   - `2026-05-XX-goldapple-tier-4-managed-unblocker-required.md` (если Tier 4)
   - `2026-05-XX-goldapple-tier-0-json-endpoint-found.md` (если Tier 0 / JSON found)

   Принцип: assertion-style = "what we now believe is true", не "что мы делали".

3. Format frontmatter:
   ```yaml
   ---
   tags: [decision, anti-bot, goldapple, scraping, ga_crawler]
   date: <YYYY-MM-DD>
   project: ga_crawler
   phase: 1-goldapple-reconnaissance-spike
   tier: <chosen tier number>
   source_memo: "[[../../projects/ga_crawler/.planning/spikes/01-goldapple/MEMO.md]]"
   ---
   ```

4. Прислать в чат: "approved <vault-path> <filename>" или "manual: я скопирую сам, написал в <path>" (если оператор хочет сам сделать копию из соображений privacy/separation).
  </how-to-verify>
  <resume-signal>Напишите "approved C:\Users\<user>\ObsidianMyProject\knowledge\decisions\ <filename>.md" / "manual" / "skip" (если Obsidian не используется).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Создать assertion-style note в Obsidian vault</name>
  <files></files>
  <read_first>
    - User-confirmed path и filename из Task 1
    - `.planning/spikes/01-goldapple/MEMO.md` (source content)
  </read_first>
  <action>
Если Task 1 = `manual` или `skip` — этот task no-op, переходим к Task 3.

Иначе:

1. Если каталог `<obsidian-vault>/knowledge/decisions/` не существует — создать.

2. Прочитать `.planning/spikes/01-goldapple/MEMO.md`.

3. Сформировать новый файл в Obsidian vault как `<vault>/knowledge/decisions/<assertion-filename>.md` со структурой:

```markdown
---
tags: [decision, anti-bot, goldapple, scraping, ga_crawler]
date: <YYYY-MM-DD>
project: ga_crawler
phase: 1-goldapple-reconnaissance-spike
tier: <number>
source_memo: "[[ga_crawler/.planning/spikes/01-goldapple/MEMO]]"
---

# <assertion как title — e.g., "Goldapple.kz scrapeable at Tier 2 (Patchright, no proxy) from KZ-IP">

> Decision date: <YYYY-MM-DD>
> Source: GA Crawler Phase 1 reconnaissance spike
> Full memo: `[[ga_crawler/.planning/spikes/01-goldapple/MEMO]]` (repo-local)

## Bottom line

[вставить TL;DR из MEMO.md as is — это уже concise summary]

## Why this matters

- Phase 3 stack-decision unblocked
- Phase 7 hosting decision: <Hetzner EU + (proxy or none)>
- viled.kz curl_cffi confirmed (RECON-02 closed)

## Operational constraints

- Rate-limit goldapple: <N>s between fetches (per tos-audit)
- Rate-limit viled: <N>s
- Production budget: $<X>/week (proxy)

## Cross-references

- [[ga_crawler/.planning/PROJECT]]
- [[ga_crawler/.planning/ROADMAP]]
- [[ga_crawler/.planning/research/STACK]]
- [[ga_crawler/.planning/research/PITFALLS]]

## Related decisions

(empty — это первое decision-note для проекта)
```

4. Записать файл. Используется Write tool с absolute path.

5. **НЕ** копировать весь body MEMO.md в Obsidian — только TL;DR и pointers. MEMO.md — единый source of truth в repo. Obsidian копия — для cross-vault поиска и связывания с другими decisions/проектами.
  </action>
  <acceptance_criteria>
    - Если Task 1 был `manual` / `skip` — нет файлов модифицировано (acceptance — true autotest)
    - Иначе — `test -f <vault>/knowledge/decisions/<filename>.md` (operator verify руками)
    - Файл содержит frontmatter `tags`, `date`, `project: ga_crawler`, `tier: <N>`
    - Файл содержит TL;DR из MEMO + cross-references
  </acceptance_criteria>
  <verify>
    <automated>true</automated>
  </verify>
  <done>Obsidian decision-note создан (или skip-confirmed); поиск в Obsidian по `tag:#decision tag:#ga_crawler` показывает эту запись.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Запустить /gsd-spike --wrap-up (если slash-command существует) ИЛИ manual skill creation</name>
  <what-built>
Per D-16: "На завершении спайка обязательно вызывается `/gsd-spike --wrap-up`". Если slash-command существует — запускается; если нет — операторский fallback на manual skill creation.
  </what-built>
  <how-to-verify>
1. Проверить существует ли `/gsd-spike` команда:
   ```bash
   ls $HOME/.claude/get-shit-done/workflows/ 2>&1 | grep spike
   # или
   ls .claude/commands/ 2>&1 | grep spike
   ```

2. **Вариант A — `/gsd-spike --wrap-up` существует:**
   - Запустить slash-command в чате (Claude может только описать — пользователь нажимает в IDE/CLI):
     `/gsd-spike --wrap-up`
   - Команда (по design'у) должна:
     - Прочитать MEMO.md
     - Создать project-local skill в `.claude/skills/` (или другой путь скилл-discovery)
     - Skill = SKILL.md index + body, который phase-3 discuss/plan agent сможет загрузить
   - Прислать в чат: "approved /gsd-spike --wrap-up completed, skill created at <path>"

3. **Вариант B — `/gsd-spike --wrap-up` НЕ существует** (более вероятно для greenfield project):
   - Создать manual fallback skill в `.claude/skills/spike-01-goldapple/SKILL.md` (per project-prompt skill discovery paths):
     ```markdown
     ---
     name: spike-01-goldapple-anti-bot
     description: Phase 1 spike findings for goldapple.kz anti-bot tier. Use when planning Phase 3 (Goldapple Crawl) to recall chosen tier, browser engine, proxy provider, prod-IP recommendation, and committed rate-limits.
     ---

     # Spike 01: Goldapple Anti-Bot Findings (Reference Skill)

     **Source memo:** `[[.planning/spikes/01-goldapple/MEMO]]`
     **Spike completed:** <YYYY-MM-DD>
     **Phase 1 status:** DONE

     ## What was decided

     [Вставить TL;DR + Chosen sections из MEMO.md as-is — это весь "skill knowledge"]

     ## Operational constants для Phase 3

     - Goldapple rate-limit: <N>s between fetches (per tos-audit.md)
     - viled rate-limit: <N>s
     - Page-volume estimate: ~<avg> URLs per typical brand
     - JSON-LD: <available / not — see network-trace>

     ## Stack constraints

     - Phase 3 MUST use chosen tier — не отклоняться без re-spike
     - Browser engine: <name>
     - Proxy: <none / IPRoyal residential <geo>>
     - Prod-IP target: <Hetzner EU / KZ residential>

     ## When to consult

     - `/gsd-discuss-phase 3` — для понимания anti-bot constraints
     - `/gsd-plan-phase 3` — для использования rate-limits в crawler config
     - Phase 7 hosting decisions — для proxy provider choice
     ```

   - Файл создаётся через Write tool в `.claude/skills/spike-01-goldapple/SKILL.md`.
   - Прислать "manual skill created".

4. **Smoke-проверка skill discovery:**
   ```bash
   ls .claude/skills/spike-01-goldapple/SKILL.md
   ```
   Должен существовать.
  </how-to-verify>
  <resume-signal>Напишите "approved /gsd-spike --wrap-up completed" / "manual skill created" / "skip skill creation" (последнее = только Obsidian copy + STATE update без skill).</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Обновить .planning/STATE.md — close Phase 1, capture chosen tier в Key Decisions</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - `.planning/STATE.md` (текущее состояние)
    - `.planning/spikes/01-goldapple/MEMO.md` (source-of-truth для chosen tier)
  </read_first>
  <action>
Обновить STATE.md. Сохранить структуру (не Write всего файла; использовать Edit для конкретных полей):

1. **Header:**
   - `**Last updated:**` → сегодняшняя дата
   - `**Mode:**` → `Phase 1 complete; ready for /gsd-discuss-phase 2 OR /gsd-discuss-phase 3`

2. **Current Position table:**
   - `Phase` → `2 — Project Skeleton + viled Crawl + Storage` (next phase)
   - `Plan` → `(none yet — run /gsd-discuss-phase 2 or /gsd-plan-phase 2)`
   - `Status` → `Phase 1 (spike) signed off <YYYY-MM-DD>; awaiting Phase 2 discuss`
   - `Progress` → `[█░░░░░░░░░░░░░░░░░░░] 1/7 phases`
   - `Resume file` → `.planning/spikes/01-goldapple/MEMO.md` (signed memo как entry-point)

3. **Performance Metrics table:**
   - `Phases completed` → `1`
   - `Plans completed` → `<actual count>` (12 plans если все запущены, меньше если 01-09 / 01-10 skipped)
   - `Spawned agents (this session)` → бывшее значение + `planner, executor` (на сколько Claude знает)

4. **Accumulated Context > Key Decisions** — добавить ОДНУ новую строку:
   ```
   | Goldapple anti-bot tier = <N>; engine = <name>; proxy = <details>; prod-IP = <recommendation> | Spike 01 MEMO.md (signed <date>) | Phase 3 stack locked; Phase 7 hosting decision dependent on this |
   ```

   (Если Tier 4 — text должен явно flag "project economics affected per D-02".)

5. **Active Todos** — clear (Phase 1 complete) ИЛИ добавить:
   - `Run /gsd-discuss-phase 2 — viled-first build`
   - `[Phase 7 backlog] KZ-legal review (30 min lawyer) for ToS compliance` (deferred per CONTEXT.md)
   - Если Tier 4 — `[CRITICAL] Re-spike: test Camoufox / Scrapling / managed unblocker before Phase 3 stack lock`

6. **Active Blockers:**
   - Если Tier 1-3 chosen: `(none)`
   - Если Tier 4: `Tier 4 verdict: project economics under review per D-02`

7. **Session Continuity > What Was Just Done:**
   - "Phase 1 (Goldapple Reconnaissance Spike) выполнена и подписана"
   - "100-fetch experiment(s) measured Tier 2/3 viability"
   - "MEMO.md сигнedoff <date>; Obsidian copy at <path>; project skill at .claude/skills/spike-01-goldapple/"

8. **What's Next:**
   - `1. /gsd-discuss-phase 2 — Project Skeleton + viled Crawl + Storage`
   - `2. После Phase 2 → /gsd-discuss-phase 3 (Goldapple Crawl) — uses spike findings`

9. **Resume Instructions:**
   - Добавить шаг: `5. Read .planning/spikes/01-goldapple/MEMO.md для chosen tier и stack constraints перед Phase 3.`
  </action>
  <acceptance_criteria>
    - `grep -c "Phase 1" .planning/STATE.md` ≥ 1
    - `grep -c "Tier" .planning/STATE.md` ≥ 1 (chosen tier mentioned)
    - `grep -c "spikes/01-goldapple/MEMO.md" .planning/STATE.md` ≥ 1 (cross-link)
    - `grep -cE "Progress.*1/7" .planning/STATE.md` ≥ 1 (phase counter advanced)
    - `grep -c "Phase 2" .planning/STATE.md` ≥ 1 (next phase identified)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "spikes/01-goldapple/MEMO.md" .planning/STATE.md && grep -qE "1/7" .planning/STATE.md && grep -q "Phase 2" .planning/STATE.md</automated>
  </verify>
  <done>STATE.md обновлён: Phase 1 marked complete, chosen tier зафиксирован в Key Decisions, next steps point to Phase 2 discuss. Resume instructions включают MEMO.md как entry-point для Phase 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local→Obsidian vault | File copy на local filesystem; no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12-01 | Information Disclosure | Obsidian vault может быть synced cloud-wise (iCloud, Dropbox) — MEMO content становится более широко доступен | accept | Operator знает свою vault setup; Task 1 явно подтверждает path; project не содержит PII или secrets per spike. |
| T-01-12-02 | Tampering | STATE.md update случайно ломает structure для будущих /gsd-* commands | mitigate | Task 4 использует Edit (не Write всего файла); preserves table structure; verification grep'ы проверяют ключевые поля. |

Spike scope: documentation finalisation. Full threat model deferred.
</threat_model>

<verification>
- `find $HOME/ObsidianMyProject/knowledge/decisions/ -name "*goldapple*" 2>/dev/null` показывает 1 file (если Task 2 не skipped)
- `ls .claude/skills/spike-01-goldapple/SKILL.md` существует (если Task 3 не skipped)
- `cat .planning/STATE.md | grep -A 2 "Goldapple anti-bot"` показывает Key Decisions row
- `grep "1/7" .planning/STATE.md` присутствует
</verification>

<success_criteria>
- MEMO.md дублирован в Obsidian (или явно skipped)
- Project-local skill создан в `.claude/skills/spike-01-goldapple/SKILL.md` (или /gsd-spike --wrap-up завершён)
- STATE.md обновлён: Phase 1 closed, chosen tier зафиксирован в Key Decisions
- Phase 2 / Phase 3 могут быть начаты с MEMO.md как authoritative reference
- Spike officially closed
</success_criteria>

<output>
After completion, create `.planning/phases/01-goldapple-reconnaissance-spike/01-12-SUMMARY.md`
</output>
