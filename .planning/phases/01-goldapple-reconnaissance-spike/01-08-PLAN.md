---
phase: 01-goldapple-reconnaissance-spike
plan: 08
type: execute
wave: 2
depends_on: [02, 04, 05, 06]
files_modified:
  - .planning/spikes/01-goldapple/notebook.py
  - .planning/spikes/01-goldapple/sample-payloads/goldapple-product-urls.txt
  - .planning/spikes/01-goldapple/sample-payloads/tier2-camoufox-kz-results.json
  - .planning/spikes/01-goldapple/sample-payloads/tier2-camoufox-kz-log.txt
  - .planning/spikes/01-goldapple/MEMO.md
autonomous: false
requirements: [RECON-01]

must_haves:
  truths:
    - "100 product URLs goldapple собраны (5 целевых брендов где это возможно из sitemap; top-up из общего sitemap product-pool до 100 — brand-precision shortfall задокументирован)"
    - "Camoufox + persistent context (warm) + geoip=True/locale=KZ + humanize=True + slow rate (3-5s) запускается с KZ-laptop direct (no proxy) — per 01-06b spike confirmation"
    - "Loop с stop-rule (D-03): 5 подряд блоков ИЛИ first not-resolved gate-shell без auto-clear → abort"
    - "Per-URL records: status, timing, html_size, has_jsonld, jsonld_has_product, jsonld_has_price, gate_cleared_after_ms, block_reason"
    - "MEMO.md секция Options tested заполнена строкой 'Tier 2 Camoufox KZ-laptop: N/100 successful, X gate-shells (auto-cleared), block reason if early stop'"
    - "Plans 01-09 (EU-proxy comparison) и 01-10 (Tier 3) явно skipped с rationale в MEMO под Options tested"
  artifacts:
    - path: ".planning/spikes/01-goldapple/notebook.py"
      provides: "Reproducible Camoufox 100-fetch script с stop-rule, persistent context, JSON-LD success criterion"
      contains: "camoufox"
    - path: ".planning/spikes/01-goldapple/sample-payloads/goldapple-product-urls.txt"
      provides: "100 product URLs — приоритет 5 niche-perfumery брендов с top-up из sitemap"
      min_lines: 100
    - path: ".planning/spikes/01-goldapple/sample-payloads/tier2-camoufox-kz-results.json"
      provides: "JSON {summary: {success_count, gate_cleared_count, block_count, stopped_at}, results: [...per-URL...]}"
      contains: "success_count"
    - path: ".planning/spikes/01-goldapple/sample-payloads/tier2-camoufox-kz-log.txt"
      provides: "structlog JSON output (per-fetch INFO records)"
    - path: ".planning/spikes/01-goldapple/MEMO.md"
      provides: "Options tested table updated с Tier 2 Camoufox KZ-laptop row + 01-09/01-10 SKIP-rationale block"
      contains: "Camoufox"
  key_links:
    - from: ".planning/spikes/01-goldapple/notebook.py"
      to: ".planning/spikes/01-goldapple/sample-payloads/tier2-camoufox-kz-results.json"
      via: "Скрипт записывает results"
      pattern: "tier2-camoufox-kz-results"
    - from: ".planning/spikes/01-goldapple/notebook.py"
      to: ".planning/spikes/01-goldapple/scripts/01-06b-camoufox-spike.py"
      via: "Расширяет 01-06b baseline (3/3 confirmed) до full 100-fetch с D-03/D-13/D-14/D-15 инструментовкой"
      pattern: "AsyncCamoufox"
---

<objective>
**Главный эксперимент спайка после re-route 2026-05-06:** Tier 2 = **Camoufox** (Firefox-based, C++ fingerprint spoofing) против goldapple.kz, **без прокси, KZ-laptop direct**. 100 product URLs, persistent context (warm), 3-5s rate, D-03 stop-rule. Измерить success-rate против threshold ≥95/100 (D-13).

**Почему Camoufox, а не Patchright:**
- Plan 01-06 показал Patchright + KZ-laptop = **0/7 gate-pass**. GroupIB / F.A.C.C.T. 403 на `/web/api/v1/settings` каждый раз.
- Side-spike 01-06b (`.planning/spikes/01-goldapple/scripts/01-06b-camoufox-spike.py`) показал Camoufox + KZ-laptop = **3/3 instant gate-pass** (`wait_ms=0`), JSON-LD на всех 3, zero 403 across 688 captured response events. Hypothesis "fingerprint-based, не IP-rep" подтверждена.
- D-01 ("стартуем с Patchright") **superseded** — see [[Camoufox а не Patchright — engine для goldapple]] в Obsidian vault, commit `1ff7d4d`.

**Decision rationale (re-route session 2026-05-06):**
- D-01 → Camoufox is the new Tier 2 engine
- D-08 (IPRoyal pre-register) **CANCELLED** — no proxy needed
- Plan 01-09 (multi-geo proxy) → **SKIP** (no proxy involved; multi-geo data is value-of-information ≈0 if fingerprint alone solves the gate)
- Plan 01-10 (Tier 3 escalation) → **SKIP** (triggered only by Tier 2 failure; Camoufox passes)

Per D-03/D-04/D-13/D-14/D-15 — same success criteria apply, just on Firefox engine:
- D-03: Stop-rule = 5 consecutive blocks OR first gate-shell без auto-clear через 25s poll
- D-04: persistent context (warm), 3-5s random pause, cookies live across fetches
- D-13: ≥95/100 successful fetches (5xx/timeout retries allowed)
- D-14: success = HTTP 200 + JSON-LD product schema present
- D-15: gate-shell auto-cleared = pass; gate-shell rate logged separately. >20% = "fragile"

Output: `tier2-camoufox-kz-results.json` с success_count, gate_cleared_count, html_size distribution; обновлённая Options tested таблица в MEMO.md + явный SKIP-block для 01-09/01-10.

**Branch logic после этого плана:**
- success_count ≥ 95 AND gate-shell rate ≤ 20% → Tier 2 Camoufox PASSED. **01-09 SKIP, 01-10 SKIP, 01-11 запускается с verdict "Tier 2 Camoufox direct, no proxy".**
- success_count ≥ 95 AND gate-shell rate > 20% → PASS but FRAGILE. Все ещё SKIP 01-09/01-10, но MEMO Open risks предупреждает Phase 3 о возможном drift.
- success_count < 95 ИЛИ stop-rule сработал → НЕОЖИДАННО (vs 01-06b 3/3). Reactivate 01-03 (IPRoyal trial) + продолжать как изначально планировалось — full retest с proxy. Тогда 01-08 commit-rollback и заново-replan через `/gsd-plan-phase` или manual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-goldapple-reconnaissance-spike/01-CONTEXT.md
@.planning/spikes/01-goldapple/tos-audit.md
@.planning/spikes/01-goldapple/MEMO.md
@.planning/spikes/01-goldapple/sample-payloads/page-volume-raw.json
@.planning/spikes/01-goldapple/sample-payloads/goldapple-all-urls.txt
@.planning/spikes/01-goldapple/sample-payloads/camoufox-spike-trace.md
@.planning/spikes/01-goldapple/scripts/01-06b-camoufox-spike.py

<interfaces>
Camoufox API (Firefox-based stealth browser, async):

```python
from camoufox.async_api import AsyncCamoufox

async with AsyncCamoufox(
    headless=True,                              # batch run; switch to False для observation
    geoip=True,                                 # spoofs timezone/locale to outbound IP geo (= KZ)
    locale=['ru-RU', 'kk-KZ', 'en-US'],
    humanize=True,                              # human-like cursor/keypress jitter
    persistent_context=True,                    # warm state per D-04
    user_data_dir=str(USER_DATA_DIR),           # gitignored, reused across runs
) as browser:
    page = browser.pages[0] if browser.pages else await browser.new_page()
    await page.goto(url, wait_until="domcontentloaded", timeout=60_000)
    html = await page.content()
```

Gate-shell detection (per 01-06 + 01-06b finding):
- HTML size < 30,000 bytes AND title contains "checking device" → gate not cleared
- Poll page.title() каждые 500ms until title changes OR deadline 25,000ms
- Если title clears → real Next.js app loaded → check JSON-LD

JSON-LD parser (D-14, same as Patchright path):
```python
from selectolax.parser import HTMLParser
import json

def has_jsonld_product(html: str) -> tuple[bool, bool]:
    tree = HTMLParser(html)
    for s in tree.css('script[type="application/ld+json"]'):
        try:
            obj = json.loads(s.text())
        except (json.JSONDecodeError, AttributeError):
            continue
        items = obj if isinstance(obj, list) else [obj]
        flat = []
        for it in items:
            if isinstance(it, dict) and "@graph" in it:
                flat.extend(it["@graph"])
            else:
                flat.append(it)
        for it in flat:
            if not isinstance(it, dict):
                continue
            t = it.get("@type")
            if t == "Product" or (isinstance(t, list) and "Product" in t):
                offers = it.get("offers", {})
                price_present = (
                    isinstance(offers, dict) and offers.get("price")
                ) or (
                    isinstance(offers, list)
                    and any(isinstance(o, dict) and o.get("price") for o in offers)
                )
                return True, bool(price_present)
    return False, False
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Собрать 100 goldapple product URLs (5 niche-perfumery брендов + top-up из sitemap)</name>
  <files>.planning/spikes/01-goldapple/sample-payloads/goldapple-product-urls.txt</files>
  <read_first>
    - `.planning/spikes/01-goldapple/sample-payloads/page-volume-raw.json` — содержит 5 целевых брендов (slugs)
    - `.planning/spikes/01-goldapple/sample-payloads/goldapple-all-urls.txt` — 112,317 URLs (включает 100,784 product URLs c numeric-id pattern `/<digits>-<slug>`)
    - `.planning/phases/01-goldapple-reconnaissance-spike/01-CONTEXT.md` D-12
  </read_first>
  <action>
**Background:** D-12 хотел "3-5 брендов из пересечения с viled top-10". 5 niche-perfumery брендов выбраны в 01-05 (Jo Malone London, Tom Ford, Creed, Frederic Malle, Givenchy). Sitemap матчинг по brand-keyword дал: givenchy=41, creed=21, frederic=3 = 65 product URLs. Tom Ford и Jo Malone London **НЕ имеют brand-prefixed slug** в sitemap — их product URLs нужно собирать через brand-page rendering, который требует Camoufox (chicken-and-egg для этого спайка).

**Pragmatic decision:** взять 65 brand-matched product URLs + top-up до 100 любыми product URLs из sitemap (numeric-id pattern). Brand-precision shortfall задокументирован в Open Risks. Spike's actual hypothesis = "Camoufox passes goldapple gate at scale" — brand selection вторично; для матчинга в Phase 4 brand-alias YAML строится отдельно.

Сохранить inline скрипт как `.planning/spikes/01-goldapple/_collect_urls.py` для повторяемости:

```python
"""Collect 100 goldapple product URLs for plan 01-08 Camoufox 100-fetch run."""
import json
import re
import random
from pathlib import Path

ROOT = Path(__file__).resolve().parents[3]
SITEMAP = ROOT / ".planning" / "spikes" / "01-goldapple" / "sample-payloads" / "goldapple-all-urls.txt"
OUT = ROOT / ".planning" / "spikes" / "01-goldapple" / "sample-payloads" / "goldapple-product-urls.txt"

# numeric-id pattern, per spike convention
PRODUCT_RE = re.compile(r"^https://goldapple\.kz/\d+-")

# brand-keyword match against URL slug
BRAND_KEYWORDS = ["givenchy", "creed", "frederic", "tom-ford", "jo-malone"]

all_urls = [l.strip() for l in SITEMAP.read_text(encoding="utf-8").splitlines() if l.strip()]
products = [u for u in all_urls if PRODUCT_RE.match(u)]
print(f"Total product URLs in sitemap: {len(products)}")

selected = []
for kw in BRAND_KEYWORDS:
    matches = [u for u in products if kw in u.lower()]
    selected.extend(matches[:25])  # хватит для balance, dedup ниже
    print(f"  {kw}: {len(matches)} matches in sitemap (took {min(len(matches), 25)})")

selected = list(dict.fromkeys(selected))
print(f"After brand-match + dedup: {len(selected)}")

if len(selected) < 100:
    rng = random.Random(20260506)
    pool = [u for u in products if u not in selected]
    rng.shuffle(pool)
    need = 100 - len(selected)
    topup = pool[:need]
    selected.extend(topup)
    print(f"  + topped up with {len(topup)} random product URLs from sitemap")

selected = selected[:100]
OUT.write_text("\n".join(selected) + "\n", encoding="utf-8")
print(f"Wrote {len(selected)} URLs to {OUT}")
```

Запустить:
```bash
uv run python .planning/spikes/01-goldapple/_collect_urls.py
```

Ожидание: ~65 brand-matched + ~35 random top-up = 100. Дедуплицировано. Все начинаются с `https://goldapple.kz/<digits>-`.
  </action>
  <acceptance_criteria>
    - `test -f .planning/spikes/01-goldapple/sample-payloads/goldapple-product-urls.txt` succeeds
    - `wc -l < .planning/spikes/01-goldapple/sample-payloads/goldapple-product-urls.txt` ≥ 100
    - Каждая строка матчит `^https://goldapple.kz/[0-9]+-` (product URL pattern)
    - Хотя бы 60 строк содержат `givenchy`, `creed` или `frederic` (brand-matched portion)
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/spikes/01-goldapple/sample-payloads/goldapple-product-urls.txt && [ "$(wc -l < .planning/spikes/01-goldapple/sample-payloads/goldapple-product-urls.txt)" -ge 100 ] && [ "$(grep -cE '^https://goldapple\.kz/[0-9]+-' .planning/spikes/01-goldapple/sample-payloads/goldapple-product-urls.txt)" -ge 100 ] && [ "$(grep -cE 'givenchy|creed|frederic' .planning/spikes/01-goldapple/sample-payloads/goldapple-product-urls.txt)" -ge 60 ]</automated>
  </verify>
  <done>≥100 product URLs goldapple собраны (brand-precision shortfall на Tom Ford / Jo Malone London задокументирован в MEMO Open Risks Task 4).</done>
</task>

<task type="auto">
  <name>Task 2: Написать notebook.py — Camoufox 100-fetch loop с stop-rule, persistent context, structlog</name>
  <files>.planning/spikes/01-goldapple/notebook.py</files>
  <read_first>
    - `.planning/spikes/01-goldapple/notebook.py` (stub из 01-01 — заменяется)
    - `.planning/spikes/01-goldapple/scripts/01-06b-camoufox-spike.py` — baseline 3-URL пробежка, расширяем до 100
    - `.planning/spikes/01-goldapple/sample-payloads/goldapple-product-urls.txt` (из Task 1)
    - `.planning/phases/01-goldapple-reconnaissance-spike/01-CONTEXT.md` D-03, D-04, D-13, D-14, D-15
    - `.planning/spikes/01-goldapple/SETUP-NOTES.md` — Camoufox версия и установка
    - `.planning/research/PITFALLS.md` Pitfalls 1, 13 — anti-bot mechanics, rate-limit
  </read_first>
  <action>
Заменить stub-содержимое `notebook.py` следующим скриптом. Один скрипт, один режим — `--mode camoufox-kz` (default). `--mode` оставлен для будущего реактивирования EU-proxy теста, если Phase 7 это потребует, но сейчас только camoufox-kz активен.

```python
"""
Spike 01: Goldapple Camoufox Tier 2 fetch experiment.

Запуск из repo root:
  uv run python .planning/spikes/01-goldapple/notebook.py
  uv run python .planning/spikes/01-goldapple/notebook.py --limit 10           # smoke test
  uv run python .planning/spikes/01-goldapple/notebook.py --headless false     # observe browser

Per CONTEXT.md decisions (post-2026-05-06 re-route):
  D-01 (revised): Tier 2 = Camoufox (Firefox + C++ fingerprint spoof). Patchright superseded по 01-06.
  D-03: Stop-rule = 5 consecutive blocks OR first gate-shell без auto-clear через 25s poll.
  D-04: persistent context (warm), random 3-5s pause, cookies live across fetches.
  D-13: Threshold ≥95/100 successful fetches (timeout/exception retries allowed).
  D-14: Successful fetch = HTTP 200 + JSON-LD product schema present.
  D-15: gate-shell auto-cleared = pass; gate-shell rate logged separately. >20% = "fragile".
  D-08 (cancelled): IPRoyal не нужен — Camoufox + KZ-laptop direct passes per 01-06b spike.

Output:
  sample-payloads/tier2-camoufox-kz-results.json
  sample-payloads/tier2-camoufox-kz-log.txt
"""

import argparse
import asyncio
import json
import random
import time
from datetime import datetime, timezone
from pathlib import Path

import structlog
from camoufox.async_api import AsyncCamoufox
from selectolax.parser import HTMLParser

REPO_ROOT = Path(__file__).resolve().parents[3]
SPIKE_DIR = REPO_ROOT / ".planning" / "spikes" / "01-goldapple"
URLS_FILE = SPIKE_DIR / "sample-payloads" / "goldapple-product-urls.txt"
OUTPUT_DIR = SPIKE_DIR / "sample-payloads"
USER_DATA_DIR = SPIKE_DIR / ".camoufox-state"   # gitignored

PAUSE_RANGE = (3.0, 5.0)        # D-04 random uniform
PAGE_TIMEOUT_MS = 60_000
GATE_POLL_DEADLINE_MS = 25_000  # 01-06b proven enough; 0/3 needed it but margin OK
GATE_POLL_STEP_MS = 500
CONSECUTIVE_BLOCK_LIMIT = 5     # D-03 stop-rule
RETRY_PER_URL = 1               # D-13: timeout/exception retry разрешён
GATE_TITLE_MARKER = "checking"  # appears in "Gold Apple — checking device"
CHALLENGE_HTML_MAX_SIZE = 30_000  # GUN gate shell ~18KB; real app 200KB+

structlog.configure(
    processors=[
        structlog.processors.add_log_level,
        structlog.processors.TimeStamper(fmt="iso"),
        structlog.processors.JSONRenderer(),
    ]
)
log = structlog.get_logger()


def has_jsonld_product(html: str) -> tuple[bool, bool]:
    """D-14 success criterion. Returns (has_product, has_price)."""
    tree = HTMLParser(html)
    for s in tree.css('script[type="application/ld+json"]'):
        try:
            obj = json.loads(s.text() or "")
        except (json.JSONDecodeError, AttributeError):
            continue
        items = obj if isinstance(obj, list) else [obj]
        flat = []
        for it in items:
            if isinstance(it, dict) and "@graph" in it:
                flat.extend(it["@graph"])
            else:
                flat.append(it)
        for it in flat:
            if not isinstance(it, dict):
                continue
            t = it.get("@type")
            is_product = t == "Product" or (isinstance(t, list) and "Product" in t)
            if not is_product:
                continue
            offers = it.get("offers", {})
            price_present = (
                isinstance(offers, dict) and offers.get("price")
            ) or (
                isinstance(offers, list)
                and any(isinstance(o, dict) and o.get("price") for o in offers)
            )
            return True, bool(price_present)
    return False, False


async def fetch_one(page, url: str) -> dict:
    started = time.perf_counter()
    rec = {
        "url": url,
        "fetched_at": datetime.now(timezone.utc).isoformat(),
        "status": None,
        "timing_ms": None,
        "html_size": None,
        "title": None,
        "gate_cleared": False,
        "gate_cleared_after_ms": None,
        "jsonld_has_product": False,
        "jsonld_has_price": False,
        "block": False,
        "block_reason": None,
        "error": None,
    }
    try:
        response = await page.goto(url, wait_until="domcontentloaded", timeout=PAGE_TIMEOUT_MS)
        rec["status"] = response.status if response else None

        # Best-effort networkidle, then poll title for gate clearance (D-15)
        try:
            await page.wait_for_load_state("networkidle", timeout=10_000)
        except Exception:
            pass

        elapsed = 0
        last_title = ""
        while elapsed < GATE_POLL_DEADLINE_MS:
            last_title = await page.title()
            if GATE_TITLE_MARKER not in last_title.lower():
                rec["gate_cleared"] = True
                rec["gate_cleared_after_ms"] = elapsed
                break
            await page.wait_for_timeout(GATE_POLL_STEP_MS)
            elapsed += GATE_POLL_STEP_MS
        rec["title"] = last_title

        html = await page.content()
        rec["html_size"] = len(html)

        if not rec["gate_cleared"] and rec["html_size"] < CHALLENGE_HTML_MAX_SIZE:
            rec["block"] = True
            rec["block_reason"] = "gate_shell_not_cleared"
        elif rec["status"] not in (200, 304):
            rec["block"] = True
            rec["block_reason"] = f"http_{rec['status']}"
        else:
            has_p, has_pr = has_jsonld_product(html)
            rec["jsonld_has_product"] = has_p
            rec["jsonld_has_price"] = has_pr
            if not has_p:
                rec["block"] = True
                rec["block_reason"] = "no_jsonld_product"
    except Exception as e:
        rec["error"] = f"{type(e).__name__}: {repr(e)[:200]}"
        rec["block"] = True
        rec["block_reason"] = "exception"
    rec["timing_ms"] = int((time.perf_counter() - started) * 1000)
    return rec


async def run(args) -> None:
    urls = [u.strip() for u in URLS_FILE.read_text(encoding="utf-8").splitlines() if u.strip()][: args.limit]
    print(f"URLs: {len(urls)}, headless: {args.headless}, pause range: {PAUSE_RANGE}s")

    USER_DATA_DIR.mkdir(parents=True, exist_ok=True)
    log_file = OUTPUT_DIR / "tier2-camoufox-kz-log.txt"
    log_handle = log_file.open("w", encoding="utf-8")

    results: list[dict] = []
    consecutive_blocks = 0
    stopped_early = False
    stop_reason = None

    async with AsyncCamoufox(
        headless=args.headless,
        geoip=True,
        locale=["ru-RU", "kk-KZ", "en-US"],
        humanize=True,
        persistent_context=True,
        user_data_dir=str(USER_DATA_DIR),
    ) as browser:
        page = browser.pages[0] if browser.pages else await browser.new_page()

        for i, url in enumerate(urls, 1):
            print(f"\n[{i}/{len(urls)}] {url}")
            attempts = 0
            rec = None
            while attempts <= RETRY_PER_URL:
                attempts += 1
                rec = await fetch_one(page, url)
                if rec["block"] and rec["block_reason"] == "exception" and attempts <= RETRY_PER_URL:
                    print(f"  retry {attempts} after error: {rec['error']}")
                    await asyncio.sleep(random.uniform(*PAUSE_RANGE))
                    continue
                break
            results.append(rec)
            log_handle.write(json.dumps(rec, ensure_ascii=False) + "\n")
            log_handle.flush()
            print(
                f"  -> status={rec['status']} timing={rec['timing_ms']}ms "
                f"size={rec['html_size']} gate_cleared={rec['gate_cleared']} "
                f"({rec.get('gate_cleared_after_ms')}ms) "
                f"jsonld={rec['jsonld_has_product']} block={rec['block']} "
                f"({rec['block_reason'] or 'OK'})"
            )

            # D-03 stop-rule
            if rec["block"]:
                consecutive_blocks += 1
                if consecutive_blocks >= CONSECUTIVE_BLOCK_LIMIT:
                    stopped_early = True
                    stop_reason = f"{consecutive_blocks} consecutive blocks"
                    print(f"\n!!! STOP-RULE TRIGGERED: {stop_reason}")
                    break
                if rec["block_reason"] == "gate_shell_not_cleared":
                    stopped_early = True
                    stop_reason = f"gate-shell did not auto-clear after {GATE_POLL_DEADLINE_MS}ms at fetch {i}"
                    print(f"\n!!! STOP-RULE TRIGGERED: {stop_reason}")
                    break
            else:
                consecutive_blocks = 0

            if i < len(urls):
                await asyncio.sleep(random.uniform(*PAUSE_RANGE))

    log_handle.close()

    success = sum(1 for r in results if not r["block"] and r["jsonld_has_product"])
    gate_cleared = sum(1 for r in results if r["gate_cleared"])
    gate_required_wait = sum(
        1 for r in results
        if r["gate_cleared"] and (r["gate_cleared_after_ms"] or 0) > 0
    )
    blocks = sum(1 for r in results if r["block"])
    gate_shell_rate_pct = (
        sum(1 for r in results if not r["gate_cleared"]) / len(results) * 100
        if results else 0.0
    )

    summary = {
        "mode": "camoufox-kz",
        "engine": "camoufox",
        "proxy_used": False,
        "total_attempted": len(results),
        "total_planned": len(urls),
        "success_count": success,
        "gate_cleared_count": gate_cleared,
        "gate_required_wait_count": gate_required_wait,
        "block_count": blocks,
        "gate_shell_rate_pct": round(gate_shell_rate_pct, 1),
        "stopped_early": stopped_early,
        "stop_reason": stop_reason,
        "passes_d13_threshold": success >= 95,           # D-13
        "fragile_per_d15": gate_shell_rate_pct > 20,     # D-15 adapted to Camoufox
    }
    out = {"summary": summary, "results": results}
    out_path = OUTPUT_DIR / "tier2-camoufox-kz-results.json"
    out_path.write_text(json.dumps(out, indent=2, ensure_ascii=False), encoding="utf-8")

    print(f"\n{'=' * 60}")
    print(
        f"SUMMARY: success={success}/{len(results)}, gate_cleared={gate_cleared}, "
        f"required_wait={gate_required_wait}, blocks={blocks}, "
        f"gate_shell_rate={gate_shell_rate_pct:.1f}%, stopped_early={stopped_early}"
    )
    print(f"D-13 (>=95/100): {'PASS' if summary['passes_d13_threshold'] else 'FAIL'}")
    print(f"D-15 (fragile if >20% gate-shell): {'YES' if summary['fragile_per_d15'] else 'NO'}")
    print(f"\nSaved: {out_path}")
    print(f"Log:   {log_file}")


def parse_args():
    ap = argparse.ArgumentParser()
    ap.add_argument("--limit", type=int, default=100)
    ap.add_argument("--headless", default="true", choices=["true", "false"])
    args = ap.parse_args()
    args.headless = args.headless == "true"
    return args


if __name__ == "__main__":
    asyncio.run(run(parse_args()))
```

Smoke-проверка синтаксиса:
```bash
uv run python -c "import ast; ast.parse(open('.planning/spikes/01-goldapple/notebook.py').read()); print('SYNTAX OK')"
```
  </action>
  <acceptance_criteria>
    - `test -f .planning/spikes/01-goldapple/notebook.py` succeeds
    - `! grep -q "NotImplementedError" .planning/spikes/01-goldapple/notebook.py`
    - `grep -c "AsyncCamoufox\|camoufox" .planning/spikes/01-goldapple/notebook.py` ≥ 2
    - `grep -c "consecutive_blocks" .planning/spikes/01-goldapple/notebook.py` ≥ 1 (D-03)
    - `grep -c "persistent_context" .planning/spikes/01-goldapple/notebook.py` ≥ 1 (D-04)
    - `grep -c "gate_cleared\|gate_shell" .planning/spikes/01-goldapple/notebook.py` ≥ 2 (D-15)
    - `grep -c "jsonld_has_product" .planning/spikes/01-goldapple/notebook.py` ≥ 2 (D-14)
    - Скрипт парсится: `uv run python -c "import ast; ast.parse(open('.planning/spikes/01-goldapple/notebook.py').read())"`
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/spikes/01-goldapple/notebook.py && ! grep -q "NotImplementedError" .planning/spikes/01-goldapple/notebook.py && grep -q "AsyncCamoufox" .planning/spikes/01-goldapple/notebook.py && grep -q "consecutive_blocks" .planning/spikes/01-goldapple/notebook.py && grep -q "persistent_context" .planning/spikes/01-goldapple/notebook.py && grep -q "jsonld_has_product" .planning/spikes/01-goldapple/notebook.py && grep -q "gate_cleared" .planning/spikes/01-goldapple/notebook.py && uv run python -c "import ast; ast.parse(open('.planning/spikes/01-goldapple/notebook.py').read()); print('OK')"</automated>
  </verify>
  <done>notebook.py написан с реализацией D-03/D-04/D-13/D-14/D-15 на Camoufox; парсится без syntax errors; готов к запуску в Task 3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Запустить notebook.py (Camoufox 100-fetch), наблюдать прогресс, дождаться завершения или stop-rule</name>
  <what-built>
Запуск Tier 2 эксперимента: 100 fetches × ~3-5s pause + ~2-5s page render = 8-15 минут wall-clock в headless mode (без challenge). 01-06b показал 0ms gate-wait, так что overhead-on-gate должен быть минимальным.

**По умолчанию `headless=True`** — батч-режим, лог в console + JSON. Если хочешь смотреть browser → `--headless false`. Это checkpoint:human-verify потому что:
- Есть шанс что Camoufox при scale (100 vs 3) поведёт себя иначе — оператор видит первые fetches и может прервать
- Final summary должен совпасть с D-13/D-15 thresholds — оператор проверяет и сообщает
- Если 5 consecutive blocks или gate-shell-not-cleared trigger stop-rule — оператор подтверждает что это произошло и почему
  </what-built>
  <how-to-verify>
1. Запустить (headless для скорости):
   ```bash
   uv run python .planning/spikes/01-goldapple/notebook.py
   ```

   Или с видимым браузером (медленнее, для observation первых fetches):
   ```bash
   uv run python .planning/spikes/01-goldapple/notebook.py --headless false
   ```

   Или smoke-test на 10 URL (~1-2 мин):
   ```bash
   uv run python .planning/spikes/01-goldapple/notebook.py --limit 10
   ```

2. **Console печатает per-URL line:** `[N/100] URL\n  -> status=200 timing=Xms size=YYY gate_cleared=True (0ms) jsonld=True block=False (OK)`

3. **Дать script идти до конца** ИЛИ stop-rule:
   - 8-15 мин realistic при 0% gate-shell rate
   - Если stop-rule → script сам выйдет с `!!! STOP-RULE TRIGGERED: ...`

4. **После завершения** — script печатает SUMMARY block:
   ```
   SUMMARY: success=98/100, gate_cleared=100, required_wait=0, blocks=2, gate_shell_rate=0.0%, stopped_early=False
   D-13 (>=95/100): PASS
   D-15 (fragile if >20% gate-shell): NO
   ```

5. **Проверить файлы созданы:**
   ```bash
   ls .planning/spikes/01-goldapple/sample-payloads/tier2-camoufox-kz-*
   ```

6. **Подтвердить в чате:**
   ```
   approved
   summary: success=N/100, gate_cleared=X, blocks=Y, gate_shell_rate=Z%, passes_d13=BOOL, fragile=BOOL
   ```

**Возможные проблемы:**
- `camoufox not found` или Firefox-binary download error → проверить `uv run camoufox fetch` (per SETUP-NOTES). Camoufox на первом запуске сам качает Firefox-binary (~80-120 MB).
- Все 100 показывают `block_reason=no_jsonld_product` → goldapple JSON-LD shape не matches; smoke-проверить вручную одну страницу через `view-source:`.
- `gate_shell_rate=100%` (= regression vs 01-06b 0%) → Camoufox fingerprint drift или GroupIB обновил детект; flag в Open Risks; revive 01-03 IPRoyal обсудимо.
- Висит на одной странице >2 мин → Ctrl+C, рассказать в чате.
  </how-to-verify>
  <resume-signal>Напишите "approved" + одну строку summary (e.g. "approved success=98/100, gate_shell=0%, passes_d13=Yes, fragile=No"). При проблемах — "blocked: <описание>" и Claude поможет debug.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Записать Tier 2 Camoufox row в MEMO + явный SKIP-block для 01-09/01-10</name>
  <files>.planning/spikes/01-goldapple/MEMO.md</files>
  <read_first>
    - `.planning/spikes/01-goldapple/sample-payloads/tier2-camoufox-kz-results.json` (созданный Task 3)
    - `.planning/spikes/01-goldapple/MEMO.md` (с заполненными в 01-04..01-07 секциями)
  </read_first>
  <action>
Прочитать summary из `tier2-camoufox-kz-results.json`. Обновить таблицу `## Options tested` в MEMO.md — заменить TBD-row реальными числами:

```markdown
## Options tested

| Tier | Engine | Proxy | Geo (IP) | Result | Notes |
|------|--------|-------|----------|--------|-------|
| 2 | Patchright (warm context, headless=False, 21s gate-wait) | none | KZ (laptop, D-06) | 0/7 success, 7/7 gate-shells (0 auto-cleared) | Plan 01-06 — fingerprint-based 403 на `/web/api/v1/settings`. **Patchright superseded for goldapple.** |
| 2 | **Camoufox** (warm context, geoip+humanize, headless=True) | none | KZ (laptop, D-06) | <success>/<total> success, <gate_shells> gate-shells (<resolved> auto-cleared), <blocks> blocks | <stopped_early ? "stopped at fetch <N>: <reason>" : "completed all <total>"> — gate-shell rate <X>%. |
```

Заполнить РЕАЛЬНЫМИ числами из `tier2-camoufox-kz-results.json`.

Под таблицей добавить status-block (выбрать один по результату):

- **Если `passes_d13_threshold == True AND fragile_per_d15 == False`:**
  > **Tier 2 Camoufox KZ-laptop: PASS.** Phase 3 production engine = Camoufox direct, no proxy. Plans 01-09 (multi-geo proxy comparison) и 01-10 (Tier 3 escalation) **SKIPPED** — fingerprint alone solves the gate, multi-geo VOI ≈ 0, Tier 3 не triggered. D-08 (IPRoyal pre-register) **CANCELLED**. Plan 01-11 finalize MEMO с verdict.

- **Если `passes_d13_threshold == True AND fragile_per_d15 == True`:**
  > **Tier 2 Camoufox KZ-laptop: PASS but FRAGILE (gate-shell rate >20%).** Phase 3 stack = Camoufox direct, but Open Risk: gate-shell drift. Plans 01-09/01-10 still SKIPPED (Tier 3 не triggered automatically), but Phase 3 must monitor gate-shell rate weekly. D-08 cancelled. Plan 01-11 → MEMO с FRAGILE verdict + monitoring guidance.

- **Если `passes_d13_threshold == False` ИЛИ `stopped_early == True`:**
  > **Tier 2 Camoufox KZ-laptop: FAILED (UNEXPECTED — vs 01-06b 3/3 baseline).** D-08 status REVERTED — IPRoyal trial reactivated. Re-plan: revive 01-03, then re-run 01-08 with Camoufox + KZ residential proxy as Tier 3 hybrid. Plan 01-11 deferred until retest.

Дополнительно — добавить в MEMO под "Options tested" подсекцию **Open Risks** строку:

```markdown
### Open Risks (post 01-08)

- **Brand-precision shortfall on Tom Ford / Jo Malone London:** sitemap-derived URL pool не содержит brand-prefixed slug для этих 2 брендов; они harvested через random sitemap top-up (~35 URLs). Phase 4 brand-alias YAML строится отдельно — не влияет на матч-rate.
- **Camoufox upstream maintenance:** daijro/camoufox v135.0.1-beta.24 used. CLAUDE.md flagged daijro upstream "unmaintained as of mid-2025"; we observed fresher releases. Phase 3 ops playbook: weekly check "does Camoufox still pass goldapple?" + alternate to coryking/camoufox fork если daijro stalls.
- **Hetzner-EU + Camoufox compatibility:** не проверено. Если Phase 7 хостит на EU и GroupIB IP-rep меняет вердикт — revive D-08 (IPRoyal trial) для Phase 7 KZ-residential.
```

Сохранить остальные секции MEMO.md без изменений (особенно JSON-endpoint hunt verdict от 01-06).
  </action>
  <acceptance_criteria>
    - `grep -c "Tier | Engine | Proxy" .planning/spikes/01-goldapple/MEMO.md` ≥ 1 (table header present)
    - В таблице есть строка с "Camoufox" + "KZ" + конкретное число fetches (например "98/100")
    - В таблице сохранена строка с Patchright 0/7 (исторический record)
    - В MEMO.md под таблицей есть status-block матчящий ОДНОМУ из трёх вариантов (PASS / PASS but FRAGILE / FAILED)
    - В MEMO.md есть подсекция "Open Risks (post 01-08)" с минимум 3 пунктами
    - В MEMO.md явно упомянуты "01-09" и "01-10" со статусом SKIPPED (если PASS-сценарии) или reactivated (если FAILED-сценарий)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Tier | Engine | Proxy" .planning/spikes/01-goldapple/MEMO.md && grep -q "Camoufox" .planning/spikes/01-goldapple/MEMO.md && grep -q "Patchright" .planning/spikes/01-goldapple/MEMO.md && grep -qE "(PASS|PASS but FRAGILE|FAILED)" .planning/spikes/01-goldapple/MEMO.md && grep -q "Open Risks (post 01-08)" .planning/spikes/01-goldapple/MEMO.md && grep -qE "01-09.*SKIP|01-10.*SKIP|01-09.*reactivat|01-10.*reactivat" .planning/spikes/01-goldapple/MEMO.md</automated>
  </verify>
  <done>Options tested table в MEMO.md содержит обе строки (Patchright historical 0/7, Camoufox actual N/100); status-block определяет PASS/FAIL; 01-09 и 01-10 явно SKIPPED (или reactivated на FAIL-path); Open Risks block содержит ≥3 пункта.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local→goldapple.kz (Camoufox Firefox) | 100 read-only HTTPS GETs к публичным product-pages, 3-5s pauses, persistent context. |
| local→Camoufox upstream (daijro Github / FFI) | Camoufox tarballs and Firefox binaries fetched on first install. Trust в upstream maintenance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08-01 | Denial of Service | 100-fetch loop случайно triggers anti-bot escalation на goldapple → IP cooldown / fingerprint blacklist | mitigate | 3-5s random pause (D-04); stop-rule после 5 consecutive blocks (D-03); persistent context — реалистичный browser pattern; геоIP+locale spoofing matches outbound IP. |
| T-01-08-02 | Information Disclosure | tier2-camoufox-kz-log.txt содержит full URLs (могут содержать tracking IDs в querystring) | accept | Snapshot публичных URLs; no auth tokens; not published outside repo. |
| T-01-08-03 | Repudiation | .camoufox-state/ персистит cookies; повторный run может быть quickly detected как одна сессия | accept | Spike scope; для production проблема решается per-run-cleared user_data_dir или cookie rotation в Phase 3. |
| T-01-08-04 | Tampering | Camoufox upstream скомпрометирован → malicious Firefox build | mitigate | uv lockfile pins camoufox version; Firefox binary checksum проверка можно добавить если CI; spike scope minimal. |

Spike scope: 100 read-only HTTP GETs за 8-15 минут, single IP. Full threat model deferred to Phase 7 production hardening.
</threat_model>

<verification>
- `python -c "import json; d = json.load(open('.planning/spikes/01-goldapple/sample-payloads/tier2-camoufox-kz-results.json')); print(d['summary'])"` показывает summary с success_count
- `wc -l .planning/spikes/01-goldapple/sample-payloads/tier2-camoufox-kz-log.txt` >= количество actually attempted fetches
- `grep -A 8 "Options tested" .planning/spikes/01-goldapple/MEMO.md` показывает строку Camoufox с реальными числами
- `grep -A 3 "Open Risks (post 01-08)" .planning/spikes/01-goldapple/MEMO.md` показывает ≥3 пункта
</verification>

<success_criteria>
- ≥100 product URLs goldapple собраны и зафиксированы (brand-precision shortfall задокументирован)
- notebook.py реализует D-03/D-04/D-13/D-14/D-15 на Camoufox engine; парсится без syntax errors
- Tier 2 Camoufox KZ-laptop run выполнен; results JSON и log сохранены
- MEMO.md "Options tested" обновлена обеими строками (Patchright 0/7, Camoufox N/100); явный status-block + Open Risks
- 01-09 и 01-10 явно SKIPPED (если PASS-сценарии) с rationale в MEMO
- RECON-01 частично закрыт (full closure после memo-finalize в 01-11)
</success_criteria>

<output>
After completion, create `.planning/phases/01-goldapple-reconnaissance-spike/01-08-SUMMARY.md`
</output>
