---
phase: 04-matcher-match-rate-kpi
plan: 06
type: execute
wave: 5
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [MATCH-01, MATCH-02, MATCH-03, MATCH-04]
must_haves:
  truths:
    - "`REQUIREMENTS.md` MATCH-02 is amended to reflect the denormalized matches table per D-401 — full 13-column shape documented; status updated from Pending → Done."
    - "`REQUIREMENTS.md` MATCH-01, MATCH-03, MATCH-04 each updated to status Done with plan references."
    - "`REQUIREMENTS.md` Traceability table rows for MATCH-01..04 reflect Phase 4 plans (specific plan numbers cited)."
    - "`STATE.md` 'Accumulated Key Decisions' section gains a new entry documenting the D-405 KPI-formula freeze for week 1."
    - "`STATE.md` 'Current Position' table reflects Phase 4 plan progression."
    - "`ROADMAP.md` Phase 4 plan list reflects all 6 plans with brief descriptions; Phase 4 status updated."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "MATCH-02 amended schema + 4 MATCH-* statuses updated"
    - path: ".planning/STATE.md"
      provides: "Accumulated decision row for KPI formula freeze"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 plan list + status"
  key_links:
    - from: ".planning/REQUIREMENTS.md MATCH-02"
      to: "04-CONTEXT.md D-401 + Match SQLModel"
      via: "verbatim schema replication"
      pattern: "brand_norm, name_norm, volume_norm, viled_price"
    - from: ".planning/STATE.md Accumulated Decisions"
      to: "04-CONTEXT.md D-404 + D-405"
      via: "match-rate formula citation"
      pattern: "match-rate KPI formula"
---

<objective>
Wave 5 — doc cascade per the Action Items recorded at the bottom of `04-CONTEXT.md`. This plan does not write code; it propagates the locked decisions of Phase 4 into the project-wide artifacts:

1. REQUIREMENTS.md — amend MATCH-02 to the full denormalized schema (D-401); update statuses of MATCH-01..04 from Pending → Done with concrete plan references; update the Traceability table.
2. STATE.md — add a new "Accumulated Key Decisions" entry documenting the KPI formula freeze (D-404 + D-405); update Current Position.
3. ROADMAP.md — update Phase 4 plan count + plan list with the 6 plans shipped + completion status.

Purpose: Without this cascade, future planners/discussers operate on stale source artifacts. The CONTEXT.md "Action Items" section explicitly enumerates these as required follow-up.

Output: 3 doc files modified; no source / test / config changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-matcher-match-rate-kpi/04-CONTEXT.md

<interfaces>
<!-- Action Items from 04-CONTEXT.md (lines ~256-261, end of file). -->
<!--
- REQUIREMENTS.md MATCH-02: amend to denormalized 13-column shape per D-401.
- STATE.md: add Accumulated Decision row for the KPI formula freeze.
-->

<!-- Existing MATCH-02 line in REQUIREMENTS.md (~line 45). -->

```
- [ ] **MATCH-02**: Результат записывается в таблицу `matches(run_id, viled_sku, goldapple_sku, price_delta, price_delta_pct)`
```

<!-- Existing Traceability table rows (REQUIREMENTS.md ~lines 158-161). -->

```
| MATCH-01 | Phase 4 | Pending |
| MATCH-02 | Phase 4 | Pending |
| MATCH-03 | Phase 4 | Pending |
| MATCH-04 | Phase 4 | Pending |
```

<!-- Existing ROADMAP.md Phase 4 plan list (line ~105). -->

```
**Plans**: TBD
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend REQUIREMENTS.md MATCH-01..04 + Traceability table</name>
  <read_first>
    - .planning/REQUIREMENTS.md (file being modified; locate §Match lines 43-47 + Traceability table 158-161)
    - .planning/phases/04-matcher-match-rate-kpi/04-CONTEXT.md (D-401 schema verbatim + D-402/D-404 filters + D-409 gate-fail behavior + Action Items at end)
    - .planning/phases/04-matcher-match-rate-kpi/04-01-SUMMARY.md, 04-02-SUMMARY.md, 04-03-SUMMARY.md, 04-04-SUMMARY.md, 04-05-SUMMARY.md (if they exist — the plan-reference source for traceability)
  </read_first>
  <action>
    Make 5 edits to `.planning/REQUIREMENTS.md`:

    Edit A — locate the line starting `- [ ] **MATCH-01**:`. Replace the ENTIRE line with:

    ```
    - [x] **MATCH-01**: Strict-key матчинг по `(brand_norm, name_norm, volume_norm)` через SQL JOIN между viled и goldapple snapshots для текущего `run_id` — Plan 04-03 ships `src/ga_crawler/matcher/strict_key.py::INSERT_MATCHES_SQL` with symmetric filter per D-402 (`multipack_flag=0 AND volume_norm IS NOT NULL AND stock_state != 'DELISTED'` applied to BOTH retailers); strict-key JOIN ON `v.brand_norm = g.brand_norm AND v.name_norm = g.name_norm AND v.volume_norm = g.volume_norm`. N→1 keep-all per D-403 — composite PK `(run_id, viled_sku, goldapple_sku)` allows multiple goldapple SKUs sharing the same key to map to one viled SKU.
    ```

    Edit B — locate the line starting `- [ ] **MATCH-02**:`. Replace the ENTIRE line with:

    ```
    - [x] **MATCH-02**: Результат записывается в денормализованную таблицу `matches(run_id, viled_sku, goldapple_sku, brand_norm, name_norm, volume_norm, viled_price, goldapple_price, viled_was_price, goldapple_was_price, price_delta, price_delta_pct, matched_at)` per D-401 — Plan 04-01 ships `Match` SQLModel + composite PK; Plan 04-03 ships DELETE+INSERT in single SQLite transaction (D-410 idempotency); `price_delta = goldapple_price − viled_price` (signed per D-401); `price_delta_pct = ROUND((g.current_price − v.current_price) × 100.0 / v.current_price, 2)` (D-405 formula frozen with week-1 baseline).
    ```

    Edit C — locate the line starting `- [ ] **MATCH-03**:`. Replace the ENTIRE line with:

    ```
    - [x] **MATCH-03**: Match-rate (`matches / viled_skus_with_brand_in_goldapple_brands × 100%`) вычисляется и логируется на каждом запуске — Plan 04-03 ships `compute_denominator(engine, run_id)` per D-404 (symmetric filter: viled comparable SKUs whose brand_norm appears on goldapple this run); Plan 04-04 orchestrator computes `match.rate = round(count * 100.0 / denominator, 2)` with zero-denominator guard (rate=0.0 + structured-log warning `match_zero_denominator`); D-405 KPI formula frozen with week-1 baseline — `test_match_rate_formula_canary` pins formula structurally (source-locked SQL substring + numerical 6/5/3 → 60.0 regression fixture).
    ```

    Edit D — locate the line starting `- [ ] **MATCH-04**:`. Replace the ENTIRE line with:

    ```
    - [x] **MATCH-04**: Sanity-gate — `match_count > P` (порог конфигурируется); меньше — `runs.status = 'failed'`, отчёт в business-чат **не отправляется** — Plan 04-01 ships `[tool.ga_crawler.match] sanity_gate_p = 20` (D-408 seed) + `MatchConfig.from_pyproject`; Plan 04-04 orchestrator calls `final_threshold_gate(match_count, threshold_p)` (D-203 retailer-agnostic helper reused) + `run_writer.fail(run_id, reason='match_count_below_threshold:{count}<{threshold}')` on trip; D-409 audit-trail invariant — matches rows ALREADY inserted persist (mirror D-218 gate-fail-but-snapshot-persists). D-407 auto-suggest emits `match_auto_suggest_p` log line after 4+ runs (NEVER auto-tunes — operator-PR only).
    ```

    Edit E — in the Traceability table, locate the four rows `| MATCH-01 | Phase 4 | Pending |` through `| MATCH-04 | Phase 4 | Pending |`. Replace with:

    ```
    | MATCH-01 | Phase 4 | Done (Plan 04-03 — strict-key SQL JOIN + D-402 symmetric filter + D-403 N→1 keep-all + D-405 source-locked formula canary) |
    | MATCH-02 | Phase 4 | Done (Plan 04-01 denormalized Match SQLModel D-401 + Plan 04-03 DELETE+INSERT single TX D-410; schema amended above) |
    | MATCH-03 | Phase 4 | Done (Plan 04-03 compute_denominator D-404 + Plan 04-04 orchestrator match.rate D-405 + zero-denominator guard) |
    | MATCH-04 | Phase 4 | Done (Plan 04-01 [tool.ga_crawler.match] D-408 + Plan 04-04 final_threshold_gate D-409 + auto_suggest_threshold D-407 log-only) |
    ```

    Edit F — at the very bottom of REQUIREMENTS.md AFTER the existing italic footer `*Requirements defined: 2026-05-05*` / `*Last updated: ...*` lines, ADD a new line (do NOT delete the existing lines):

    ```
    *Phase 4 update: 2026-05-11 — MATCH-01..04 closed; MATCH-02 schema amended to denormalized 13-column shape per 04-CONTEXT.md D-401 + Action Items.*
    ```

    Do NOT modify the Coverage block (48 total / 48 mapped is unchanged — only the per-id state changes).
  </action>
  <verify>
    <automated>uv run python -c "import sys; p = open('.planning/REQUIREMENTS.md', encoding='utf-8').read(); assert '- [x] **MATCH-01**' in p; assert '- [x] **MATCH-02**' in p; assert '- [x] **MATCH-03**' in p; assert '- [x] **MATCH-04**' in p; assert 'brand_norm, name_norm, volume_norm, viled_price' in p; assert 'MATCH-01 | Phase 4 | Done' in p; assert 'Phase 4 update: 2026-05-11' in p; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[x\] \*\*MATCH-01\*\*' .planning/REQUIREMENTS.md` == 1
    - `grep -c '^- \[x\] \*\*MATCH-02\*\*' .planning/REQUIREMENTS.md` == 1
    - `grep -c '^- \[x\] \*\*MATCH-03\*\*' .planning/REQUIREMENTS.md` == 1
    - `grep -c '^- \[x\] \*\*MATCH-04\*\*' .planning/REQUIREMENTS.md` == 1
    - `grep -c 'brand_norm, name_norm, volume_norm, viled_price' .planning/REQUIREMENTS.md` >= 1 (D-401 13-col schema)
    - `grep -c '| MATCH-01 | Phase 4 | Done' .planning/REQUIREMENTS.md` == 1
    - `grep -c '| MATCH-04 | Phase 4 | Done' .planning/REQUIREMENTS.md` == 1
    - `grep -c 'Phase 4 update: 2026-05-11' .planning/REQUIREMENTS.md` == 1
    - The original `*Requirements defined: 2026-05-05*` footer line is preserved (still in file).
  </acceptance_criteria>
  <done>REQUIREMENTS.md reflects Phase 4 completion: 4 MATCH lines updated to [x] with concrete plan refs; Traceability table reflects status; new footer note added.</done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md (accumulated decision + current position) + ROADMAP.md Phase 4 plan list</name>
  <read_first>
    - .planning/STATE.md (file being modified — read first 80 lines + scan for "Accumulated Key Decisions" section)
    - .planning/ROADMAP.md (file being modified; locate Phase 4 section lines 95-106)
    - .planning/phases/04-matcher-match-rate-kpi/04-CONTEXT.md (D-401 / D-404 / D-405 — the verbatim source for the new accumulated-decision entry)
  </read_first>
  <action>
    Make these edits:

    Step A — STATE.md "Accumulated Key Decisions" entry. Locate the section in STATE.md that lists prior accumulated decisions (look for a heading like `## Accumulated Key Decisions` or `## Key Decisions` or a similar table/list). If the section exists, APPEND a new entry. If the project uses a table, add a new row using the same column shape. If it's a bulleted list, add a new bullet. The new entry text (use the row shape that already exists in the file — date/decision_id/title/rationale columns OR bullet format):

    Decision content (adapt to the file's existing format):

    - **D-405 KPI formula freeze (week-1 baseline)** — Phase 4 match-rate formula is `matches / viled_skus_with_brand_in_goldapple_brands × 100%` with symmetric filter `multipack_flag=0 AND volume_norm IS NOT NULL AND stock_state != 'DELISTED'` applied to BOTH numerator (D-402) and denominator (D-404). Frozen with week-1 baseline — changing the formula requires updating `test_match_rate_formula_canary` AND adding a v2-migration plan AND a new STATE.md entry documenting the rationale. Source: 04-CONTEXT.md D-404 + D-405.

    If no "Accumulated Key Decisions" section exists in STATE.md, scan for an analogous section (e.g., `## Decisions`, `## Notes`, `## Locked Decisions`) and append there. If absolutely no such section exists, ADD a new H2 section at the bottom of the file before the final separator/footer:

    ```
    ## Accumulated Key Decisions

    | Date | Decision ID | Title | Rationale |
    |------|-------------|-------|-----------|
    | 2026-05-11 | D-405 | KPI formula freeze (week-1) | match-rate = matches / viled_skus_with_brand_in_goldapple_brands × 100%; symmetric filter (multipack_flag=0 AND volume_norm IS NOT NULL AND stock_state != 'DELISTED') applies to numerator AND denominator. Formula change requires updating test_match_rate_formula_canary + v2-migration plan. Source: 04-CONTEXT.md D-404 + D-405. |
    ```

    Step B — STATE.md "Current Position" / progress. The header frontmatter currently shows `status: Phase 03 complete`. Update to reflect Phase 4 progression. Specifically:

    - Update `status: ...` in the YAML frontmatter to: `status: Phase 04 complete (matcher + match-rate KPI shipped)`
    - Update `last_updated:` to today's ISO date.
    - Update `progress.completed_phases` from 3 to 4.
    - Update `progress.total_plans` from 27 to 33 (27 + 6 new Phase 4 plans).
    - Update `progress.completed_plans` from 27 to 33 (assuming this plan ships at the end of Phase 4 — the executor should verify the 5 prior Phase 4 plans were actually completed at execution time; if they have NOT been completed yet, leave completed_plans at 27 + however many actually shipped).
    - Update `progress.percent` to round(completed_phases/total_phases * 100) = round(4/7*100) = 57.
    - In the "Current Position" table near the top of the file, update the Phase row(s) accordingly.

    NOTE TO EXECUTOR: STATE.md is large and richly-structured; if the exact field names differ from the snapshot above, preserve the file's actual structure and update the analogous fields. Do NOT delete history (the long descriptions of prior plans must remain).

    Step C — ROADMAP.md Phase 4 plan list. Locate the Phase 4 section (starts at `### Phase 4: Matcher + Match-Rate KPI`). Locate the line `**Plans**: TBD`. Replace with:

    ```
    **Plans**: 6 plans across 5 waves (Wave 1 foundation: Match table + pyproject + MatchConfig + MatchStatsBuilder; Wave 2 SQL primitives; Wave 3 orchestrator; Wave 4 main_run + CLI integration; Wave 5 doc cascade)
    - [ ] 04-01-PLAN.md — Wave 1: Match SQLModel table (D-401) + [tool.ga_crawler.match] in pyproject (D-408) + matcher/ package skeleton + MatchConfig loader; 9 regression tests
    - [ ] 04-02-PLAN.md — Wave 1: MATCH_STATS_KEYS 10-key tuple + MatchStatsBuilder mirror of ViledStatsBuilder (D-414); 13+ namespace tests + three-way disjoint invariant
    - [ ] 04-03-PLAN.md — Wave 2: matcher/strict_key.py SQL primitives — INSERT_MATCHES_SQL (D-401+D-402), DENOMINATOR_SQL (D-404), brand_overlap, comparable counts, read_run_status (D-411); 17 regression tests + D-405 source-locked formula canary
    - [ ] 04-04-PLAN.md — Wave 3: runners/matcher_run.py orchestrator — 7-step sync pipeline (D-411 skip → D-410 build → D-405 rate → D-409 gate → Pitfall 6 single patch_stats); 12 integration tests
    - [ ] 04-05-PLAN.md — Wave 4: main_run.py matcher composition (D-411 skip / D-409 fail handling) + CLI matcher-run subcommand (D-412) + --sanity-gate-p flag; 11+ integration tests
    - [ ] 04-06-PLAN.md — Wave 5: doc cascade — REQUIREMENTS.md MATCH-01..04 status + MATCH-02 schema amend; STATE.md accumulated decision (D-405 KPI freeze); ROADMAP.md Phase 4 plan list (this plan)
    ```

    Step D — ROADMAP.md "Progress" table. Locate the Phase 4 row in the Progress table (around line ~175):

    ```
    | 4. Matcher + Match-Rate KPI | 0/0 | Not started | - |
    ```

    Update to (executor sets the actual completion count; if all 5 prior plans shipped at the moment this plan runs, completion is 5/6 with this plan in execution):

    ```
    | 4. Matcher + Match-Rate KPI | 5/6 | In execution (Plan 04-06 doc cascade) | - |
    ```

    Or, if the executor runs this AFTER all 5 prior Phase 4 plans completed and this plan is the final shipment:

    ```
    | 4. Matcher + Match-Rate KPI | 6/6 | Complete | 2026-05-XX |
    ```

    NOTE TO EXECUTOR: Use the actual current state. If you are running this plan as the 6th and last plan of Phase 4, on completion update to 6/6 Complete with today's date. Do NOT skip ahead to mark plans complete that have not run.
  </action>
  <verify>
    <automated>uv run python -c "p = open('.planning/STATE.md', encoding='utf-8').read(); r = open('.planning/ROADMAP.md', encoding='utf-8').read(); assert 'D-405' in p or 'KPI formula' in p, 'STATE.md missing D-405 accumulated decision'; assert '04-01-PLAN.md' in r; assert '04-06-PLAN.md' in r; assert 'Matcher + Match-Rate KPI' in r; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'D-405' .planning/STATE.md` >= 1 (KPI formula freeze entry present)
    - `grep -c 'KPI formula' .planning/STATE.md` >= 1
    - `grep -c '04-01-PLAN.md' .planning/ROADMAP.md` == 1
    - `grep -c '04-02-PLAN.md' .planning/ROADMAP.md` == 1
    - `grep -c '04-03-PLAN.md' .planning/ROADMAP.md` == 1
    - `grep -c '04-04-PLAN.md' .planning/ROADMAP.md` == 1
    - `grep -c '04-05-PLAN.md' .planning/ROADMAP.md` == 1
    - `grep -c '04-06-PLAN.md' .planning/ROADMAP.md` == 1
    - `grep -c '\*\*Plans\*\*: TBD' .planning/ROADMAP.md` == 0 (TBD replaced with concrete list under Phase 4)
    - STATE.md frontmatter `completed_phases` reflects Phase 4 progression accurately (executor verifies at run time).
  </acceptance_criteria>
  <done>STATE.md gains the D-405 accumulated-decision entry + position update; ROADMAP.md Phase 4 lists all 6 plans + progress row reflects actual state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → REQUIREMENTS.md / STATE.md / ROADMAP.md | Trusted authoring; doc updates committed via git. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Tampering | Doc cascade out-of-sync with code | mitigate | Acceptance criteria use grep against literal strings cited from CONTEXT D-401 ("brand_norm, name_norm, volume_norm, viled_price") — drift between code and docs is detectable at this plan's CI verification. |
| T-04-06-02 | Repudiation | Decision drift (formula change without audit trail) | mitigate | STATE.md accumulated-decision row pins D-405 with citation to 04-CONTEXT.md D-404+D-405 and the regression-canary test. Any future formula edit must update STATE.md or fails the audit. |
| T-04-06-03 | Information disclosure | Doc files | accept | All info already public in CONTEXT.md (committed). No new disclosure. |
</threat_model>

<verification>
- `grep` acceptance criteria above all pass.
- Manual sanity: `git diff .planning/REQUIREMENTS.md` shows MATCH lines updated; `git diff .planning/ROADMAP.md` shows TBD replaced; `git diff .planning/STATE.md` shows D-405 entry added.
- Full pytest suite still green: `uv run pytest -q` (no code changes, but confirm nothing was inadvertently touched).
</verification>

<success_criteria>
- REQUIREMENTS.md MATCH-01..04 statuses are [x] with plan-number citations.
- REQUIREMENTS.md MATCH-02 line reflects the full denormalized 13-column schema.
- REQUIREMENTS.md Traceability table rows for MATCH-* reflect Phase 4 plans.
- STATE.md carries a new accumulated-decision entry for D-405 (KPI formula freeze).
- STATE.md Current Position table + frontmatter reflect Phase 4 progression.
- ROADMAP.md Phase 4 plan list shows all 6 plans with brief descriptions; Progress table reflects actual completion state.
</success_criteria>

<output>
After completion, create `.planning/phases/04-matcher-match-rate-kpi/04-06-SUMMARY.md`.
</output>
