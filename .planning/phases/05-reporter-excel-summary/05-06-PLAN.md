---
phase: 05-reporter-excel-summary
plan: 06
type: execute
wave: 5
depends_on: ["05-05"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [REPORT-01, REPORT-02, REPORT-03, REPORT-04, REPORT-05, REPORT-06]
tags: [phase-05, docs, requirements, state, roadmap, cascade]

must_haves:
  truths:
    - "REQUIREMENTS.md REPORT-01 amended per CONTEXT D-502 Action Item (brand-gap reinterpreted as SKU-level gap within brand-overlap)"
    - "REQUIREMENTS.md REPORT-01..06 all flipped from `- [ ]` to `- [x]` with closure annotations referencing plan IDs + decision IDs"
    - "REQUIREMENTS.md Traceability table rows for REPORT-01..06 updated to status='Done' with plan references"
    - "STATE.md gains 3 Accumulated Key Decisions rows: D-514 source-of-truth, D-515 size-guard delivery cascade, D-405 reporter-cites-rate-verbatim"
    - "STATE.md Plan Execution Metrics section gains entries for plans 05-01..05-06 (mirror Phase 4 format)"
    - "ROADMAP.md Phase 5 Plans block populated with 6 plan entries; Progress table updated to '6/6 Complete' with completion date"
    - "All cascade edits are documentation-only — no source code changes; no test changes; no behavioral risk"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "REPORT-01..06 closed + REPORT-01 amendment per D-502 + Traceability rows updated"
      contains: "REPORT-01"
    - path: ".planning/STATE.md"
      provides: "3 new Accumulated Key Decisions rows + Plan Execution Metrics for 05-01..05-06"
      contains: "D-514"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 Plans block filled + Progress row updated"
      contains: "05-01-PLAN.md"
  key_links:
    - from: ".planning/REQUIREMENTS.md REPORT-01"
      to: "05-CONTEXT.md D-502"
      via: "Action Items cascade — SKU-level gap interpretation within brand-overlap"
      pattern: "brand-overlap"
    - from: ".planning/STATE.md Accumulated Decisions"
      to: "05-CONTEXT.md Action Items L281-283"
      via: "D-514 + D-515 + D-405 cascade items"
      pattern: "D-514\\|D-515\\|D-405"
---

<objective>
Wave 5 — pure documentation cascade. No source code changes; no test changes; zero behavioral risk. Closes the loop on the 6 REPORT-XX requirements and propagates the 3 CONTEXT Action Items (L278-283) into the project's persistent decision record (STATE.md) so Phase 6/7 planners inherit the invariants without re-reading 05-CONTEXT.md.

**Four edits across 3 files:**

1. **`.planning/REQUIREMENTS.md`** — In the `### Report` section (L60-65), flip all 6 checkboxes from `- [ ]` to `- [x]` with closure annotations citing the plans + decisions that closed them. Amend REPORT-01 text per CONTEXT.md Action Item L280: the brand-gap → SKU-level gap reinterpretation. In the Traceability table (L168-173), update REPORT-01..06 rows from `Pending` to `Done` with plan ID + decision references.

2. **`.planning/STATE.md` — Accumulated Key Decisions** — Add 3 rows (mirroring Phase 4 row pattern in STATE.md):
   - **D-514 reporter is source-of-truth for caption** — `runs.stats.report.summary_text` written by reporter; Phase 6 reads verbatim → no drift
   - **D-515 size-guard delivery cascade** — `report.size_guard_passed` is FLAG-ONLY; xlsx ALWAYS persists; Phase 6 DELIVER-03 sanity-gate MUST read flag and route >45MB runs to ops-chat alert
   - **D-405 reporter cites match.rate verbatim** — reporter NEVER recomputes KPI formula; reads `runs.stats.match.rate` directly; week-1 baseline lock preserved

3. **`.planning/STATE.md` — Plan Execution Metrics** — Add 6 rows for plans 05-01..05-06 (mirror Phase 4 metric block format if present; if not present, append a minimal Plan Execution Metrics block).

4. **`.planning/ROADMAP.md` — Phase 5 section** — Replace `**Plans**: TBD` (L124) with `**Plans**: 6 plans across 6 waves (Wave 0 foundation → Wave 1 builders → Wave 2 archive → Wave 3 orchestrator → Wave 4 main_run + CLI composition → Wave 5 doc cascade)` and add the 6 plan list lines (one per plan with brief objective). Update Progress table (L182) from `0/0 / Not started / -` to `6/6 / Complete / 2026-MM-DD`.

After this plan: Phase 5 is fully closed in the project record. Any Phase 6/7 planner reading STATE.md sees D-514/D-515/D-405 cascade items without traversing the phase directory. CRAWL-02 + MATCH-04 doc-coverage pattern preserved (this is the standard Wave 5 cascade).

Output: 3 markdown files edited; 0 source code; 0 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-reporter-excel-summary/05-CONTEXT.md
@.planning/phases/04-matcher-match-rate-kpi/04-06-PLAN.md
@.planning/phases/04-matcher-match-rate-kpi/04-06-SUMMARY.md

<interfaces>
<!-- REPORT-01 current wording (REQUIREMENTS.md L60) — to amend per D-502 -->

- [ ] **REPORT-01**: Excel-файл с листами: `Summary`, `Per-SKU deltas` (виледд × голдэпл по совпавшим), `Assortment gaps` (бренды на goldapple, отсутствующие на viled), `Goldapple promos` (где `was_price > current_price`)

<!-- REPORT-01 AMENDED (D-502 Action Item from CONTEXT.md L280): -->

- [x] **REPORT-01**: Excel-файл с листами: `Summary`, `Per-SKU deltas` (виледд × голдэпл по совпавшим), `Assortment gaps` (**SKU на goldapple, отсутствующие на viled по strict-key (brand_norm, name_norm, volume_norm) в пределах brand-overlap CRAWL-02**), `Goldapple promos` (где `was_price > current_price`) — Plan 05-04 ships 4-sheet xlsx via pandas+xlsxwriter D-503 Russian headers + D-505 CF + D-506 always-4-sheets + D-508 CF-on-2-sheets-only.

<!-- 3 cascade Action Items to surface to STATE.md (CONTEXT.md L281-283) -->

1. STATE.md: "Phase 5 reporter — source-of-truth for Telegram caption (D-514): `runs.stats.report.summary_text` written by reporter; Phase 6 reads verbatim — защита от summary-drift между xlsx cell A1 и Telegram caption"
2. STATE.md: "Phase 5 REPORT-06 size-guard (D-515) — log warning + `report.size_guard_passed=false` flag в stats; xlsx ВСЕГДА пишется на диск. Phase 6 DELIVER-03 sanity-gate ОБЯЗАН читать `report.size_guard_passed` и роутить >45MB runs в ops-chat alert вместо business-chat"
3. STATE.md: "Phase 5 reporter cites runs.stats.match.rate verbatim (D-405) — KPI formula freeze preserved; reporter NEVER recomputes; week-1 baseline lock structurally enforced by source-locked summary_builder.SUMMARY_TEMPLATE"

<!-- Phase 4 close-out as ROADMAP.md format precedent (L107-111) -->

- [x] 04-01-PLAN.md - Wave 1: Match SQLModel table (D-401 ...) + [tool.ga_crawler.match] pyproject namespace ...
- [x] 04-02-PLAN.md - Wave 1: MATCH_STATS_KEYS frozen 10-tuple namespace ...
- [x] 04-03-PLAN.md - Wave 2: matcher/strict_key.py SQL primitives ...
- [x] 04-04-PLAN.md - Wave 3: runners/matcher_run.py sync 7-step orchestrator ...
- [x] 04-05-PLAN.md - Wave 4: runners/main_run.py matcher composition AFTER goldapple step ...
- [x] 04-06-PLAN.md - Wave 5: doc cascade — REQUIREMENTS.md MATCH-01..04 closed + ...

<!-- Phase 5 plan list to add to ROADMAP.md -->

- [ ] 05-01-PLAN.md - Wave 0: foundation — pyproject [tool.ga_crawler.report] (D-516) + reporter/__init__.py + config.py (ReportConfig mirror MatchConfig) + stats.py (REPORT_STATS_KEYS 7-tuple D-514 + ReportStatsBuilder with 4-way disjoint invariant); pandas+xlsxwriter+openpyxl+tzdata deps; synthetic_report_run + tmp_reports_dir + openpyxl_workbook_reader conftest fixtures; D-504 golden summary text fixture
- [ ] 05-02-PLAN.md - Wave 1: pure builders — reporter/queries.py 5 SQL constants (PER_SKU JOIN-back per Pitfall 9 + NOT EXISTS per Pitfall 8 + SQL discount per Pitfall 10) + reporter/excel_builder.py (D-503 Russian headers + D-505 3-color CF mid_value=0 + D-506 always-4-sheets + D-508 CF-2-sheets-only + Pitfall 1 engine=xlsxwriter explicit + T-05-injection sanitization) + reporter/summary_builder.py (D-504 template constants source-locked + zero-match Top-3 omission)
- [ ] 05-03-PLAN.md - Wave 2: filesystem service — reporter/archive.py (D-512 ISO-week derivation with Pitfall 4 year-boundary 2027-01-01→2026-W53 + D-510 atomic write via *.xlsx.tmp+os.replace + D-515 check_size_guard flag-only); reports/.gitkeep + .gitignore exclude reports/*.xlsx; 23+ tests
- [ ] 05-04-PLAN.md - Wave 3: orchestrator — runners/reporter_run.py 7-step sync mirror matcher_run + D-507 status-gate REUSE read_run_status from matcher.strict_key + path-traversal containment check + Pitfall 6 single patch_stats + D-515 flag-only never-fails + 12+ integration tests
- [ ] 05-05-PLAN.md - Wave 4: composition — main_run.py reporter step inserted post-matcher with explicit m_result.status=='success' gate + MainRunResult extended 4 fields per D-514 + DATA-05 reporter-exception path inherits Plan 02-05 outer try/except; cli.py D-509 report-run --run-id N subcommand mirror matcher-run shape (type=int required + --output-dir dataclasses.replace override)
- [ ] 05-06-PLAN.md - Wave 5: doc cascade (this plan) — REQUIREMENTS.md REPORT-01..06 closed + REPORT-01 amendment per D-502 + STATE.md gains D-514/D-515/D-405 cascade rows + ROADMAP.md Phase 5 plan list + Progress 6/6 Complete
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: REQUIREMENTS.md — amend REPORT-01 per D-502 + close all 6 REPORT-XX checkboxes + update Traceability table</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — locate `### Report` section + `## Traceability` table)
    - .planning/phases/05-reporter-excel-summary/05-CONTEXT.md (D-502 Action Item L280; D-503 Russian headers L24-30; D-504 summary template L34-48; D-505 CF L52; D-506 always-4-sheets L54; D-507 skip-protocol L56-58; D-508 CF-2-sheets L60; D-510 filename L66; D-511 composition L68-78; D-512 ISO-week L81-88; D-514 stats keys L107-115; D-515 size-guard L118-123; D-516 pyproject namespace L125-133)
    - .planning/phases/04-matcher-match-rate-kpi/04-06-PLAN.md (precedent for closure annotation style)
  </read_first>
  <action>
**Edit 1 — REPORT-01 amendment per D-502** (REQUIREMENTS.md L60):

REPLACE:
```
- [ ] **REPORT-01**: Excel-файл с листами: `Summary`, `Per-SKU deltas` (виледд × голдэпл по совпавшим), `Assortment gaps` (бренды на goldapple, отсутствующие на viled), `Goldapple promos` (где `was_price > current_price`)
```

WITH:
```
- [x] **REPORT-01**: Excel-файл с листами: `Summary`, `Per-SKU deltas` (виледд × голдэпл по совпавшим), `Assortment gaps` (**SKU на goldapple, отсутствующие на viled по strict-key (brand_norm, name_norm, volume_norm), в пределах brand-overlap CRAWL-02 scope** — D-502 reinterpretation: brand-level gap = ∅ by CRAWL-02 construction, SKU-level = корректный intent), `Goldapple promos` (где `was_price > current_price`) — Plan 05-02 ships excel_builder.py with 4-sheet workbook via `pd.ExcelWriter(BytesIO, engine='xlsxwriter')` (Pitfall 1 explicit); Plan 05-04 ships orchestrator with D-506 always-4-sheets invariant.
```

**Edit 2 — REPORT-02 closure** (REQUIREMENTS.md L61):

REPLACE:
```
- [ ] **REPORT-02**: Conditional formatting в Excel: подсветка дельт (зелёный — viled дешевле, красный — viled дороже), frozen panes, autofilter
```

WITH:
```
- [x] **REPORT-02**: Conditional formatting в Excel: подсветка дельт (зелёный — viled дешевле, красный — viled дороже), frozen panes, autofilter — Plan 05-02 excel_builder.py ships D-505 3-color-scale on `Дельта, %` + `Скидка, %` columns with `mid_type='num', mid_value=0` anchor (parity), D-508 CF-on-2-sheets-only (Per-SKU deltas + Goldapple promos; NOT Summary / Assortment gaps), `freeze_panes(1, 0)` + `autofilter` on all data sheets per Pattern 3.
```

**Edit 3 — REPORT-03 closure** (REQUIREMENTS.md L62):

REPLACE:
```
- [ ] **REPORT-03**: Заголовки колонок и текст саммари — на русском
```

WITH:
```
- [x] **REPORT-03**: Заголовки колонок и текст саммари — на русском — Plan 05-02 excel_builder.py ships D-503 verbatim PER_SKU_HEADERS_RU + GAPS_HEADERS_RU + PROMOS_HEADERS_RU dicts (Бренд / Название / Объём / Цена viled, ₸ / Старая цена viled, ₸ / URL viled / Цена goldapple, ₸ / Старая цена goldapple, ₸ / URL goldapple / Дельта, ₸ / Дельта, % / Скидка, ₸ / Скидка, %); summary_builder.py ships D-504 multi-line emoji template constants (📊/📦/🎯/🆕/💸/🔝) source-locked; `test_russian_headers_match_d503` source-lock canary + golden-file canary `tests/fixtures/reporter/expected-summary-text.txt`.
```

**Edit 4 — REPORT-04 closure** (REQUIREMENTS.md L63):

REPLACE:
```
- [ ] **REPORT-04**: Текстовая сводка: `viled_count`, `goldapple_count`, `match_count`, **`match_rate %`**, размер ассортиментного разрыва, top-3 наибольшие дельты, количество промо у goldapple
```

WITH:
```
- [x] **REPORT-04**: Текстовая сводка: `viled_count`, `goldapple_count`, `match_count`, **`match_rate %`**, размер ассортиментного разрыва, top-3 наибольшие дельты, количество промо у goldapple — Plan 05-02 summary_builder.build_summary ships D-504 canonical template; reads upstream stats `viled.fetch_count` / `goldapple.fetch_count` / `match.count` / `match.rate` flat dot-keyed (Pitfall 6) — D-405 KPI formula citation verbatim, no recompute; top-3 sorted by `ABS(price_delta_pct) DESC` via SQL `read_top_n_deltas` (Pattern 7 — doesn't materialize 50k matches into pandas); zero-match D-504 fallback (Top-3 header omitted entirely when match_count==0).
```

**Edit 5 — REPORT-05 closure** (REQUIREMENTS.md L64):

REPLACE:
```
- [ ] **REPORT-05**: Excel-файл записывается на диск в архив (`reports/YYYY-WNN.xlsx`) до отправки — отчёт независим от Telegram
```

WITH:
```
- [x] **REPORT-05**: Excel-файл записывается на диск в архив (`reports/YYYY-WNN.xlsx`) до отправки — отчёт независим от Telegram — Plan 05-03 archive.py ships D-512 ISO-week filename via `Asia/Almaty` ZoneInfo + `date.isocalendar()` (Pitfall 4 year-boundary verified: 2027-01-01 UTC → 2026-W53, 2025-12-29 → 2026-W01) + D-510 atomic write `*.xlsx.tmp` + `os.replace` (crash-safe per Pitfall 5); Plan 05-04 orchestrator path-traversal containment check `target_path.relative_to(repo_root.resolve())`; Plan 05-05 standalone `python -m ga_crawler report-run --run-id N` subcommand (D-509) for SC#3 historical regeneration; ARCHITECTURE.md "reporter independent of delivery" structurally enforced — no Telegram imports in reporter package.
```

**Edit 6 — REPORT-06 closure** (REQUIREMENTS.md L65):

REPLACE:
```
- [ ] **REPORT-06**: Размер xlsx проверяется перед отправкой — если > 45 MB, ругается явной ошибкой (Telegram limit 50 MB)
```

WITH:
```
- [x] **REPORT-06**: Размер xlsx проверяется перед отправкой — если > 45 MB, явная сигнализация (Telegram limit 50 MB) — Plan 05-03 archive.check_size_guard ships D-515 flag-only semantics (returns `(passed: bool, size_bytes: int)`, NEVER raises); orchestrator (Plan 05-04) sets `report.size_guard_passed=false` in stats + structlog warning `report_size_exceeded`; **xlsx ALWAYS persists on disk** (D-515 invariant — manual recovery / Phase 6 split-and-send-later) + Run status remains `success` (ARCHITECTURE.md "reporter independent of delivery" удерживается); **Phase 6 DELIVER-03 cascade**: must read `report.size_guard_passed` and route >45MB runs to ops-chat alert (NOT business-chat) — invariant cascaded to STATE.md Accumulated Key Decisions for Phase 6 planner.
```

**Edit 7 — Traceability table rows for REPORT-01..06** (REQUIREMENTS.md L168-173):

REPLACE rows:
```
| REPORT-01 | Phase 5 | Pending |
| REPORT-02 | Phase 5 | Pending |
| REPORT-03 | Phase 5 | Pending |
| REPORT-04 | Phase 5 | Pending |
| REPORT-05 | Phase 5 | Pending |
| REPORT-06 | Phase 5 | Pending |
```

WITH:
```
| REPORT-01 | Phase 5 | Done (Plan 05-02 excel_builder.py 4-sheet workbook + D-502 SKU-level gap interpretation amendment; D-506 always-4-sheets) |
| REPORT-02 | Phase 5 | Done (Plan 05-02 — D-505 3-color CF mid_value=0 anchor + D-508 CF-on-2-sheets-only + freeze_panes + autofilter) |
| REPORT-03 | Phase 5 | Done (Plan 05-02 — D-503 Russian headers verbatim + D-504 emoji summary template source-locked; golden file canary) |
| REPORT-04 | Phase 5 | Done (Plan 05-02 summary_builder.py — reads runs.stats.match.* flat keys per Pitfall 6 + D-405 KPI verbatim + top-3 SQL ABS LIMIT + zero-match fallback) |
| REPORT-05 | Phase 5 | Done (Plan 05-03 archive.py — D-512 ISO-week + Pitfall 4 year-boundary + D-510 atomic write *.xlsx.tmp + os.replace; Plan 05-04 path-traversal containment; Plan 05-05 standalone report-run CLI per D-509) |
| REPORT-06 | Phase 5 | Done (Plan 05-03 check_size_guard flag-only D-515 + Plan 05-04 orchestrator sets report.size_guard_passed flag + log warning; xlsx persists on disk; Phase 6 DELIVER-03 cascade invariant) |
```

**Edit 8 — Bottom-of-file metadata** (last line of REQUIREMENTS.md):

REPLACE the closing block (look for `*Phase 4 update: 2026-05-11 — MATCH-01..04 closed; ...*`):

After that line, ADD:
```
*Phase 5 update: 2026-05-MM — REPORT-01..06 closed; REPORT-01 amended per 05-CONTEXT.md D-502 (Assortment gaps reinterpreted as SKU-level within brand-overlap CRAWL-02 scope since brand-level gap=∅ by construction). Plans 05-01..05-06 shipped.*
```

(Use today's date — `Bash(date +%Y-%m-%d)` — or leave `MM` for the executor to fill at commit time.)

**Critical invariants:**
- DO NOT modify any text outside the 6 REPORT-XX checkboxes + the 6 Traceability rows + the bottom metadata line.
- DO NOT modify other phase requirements (RECON / CRAWL / PARSE / NORM / MATCH / DATA / DELIVER / SCHED).
- REPORT-01 amendment ADDS the SKU-level gap clarification but PRESERVES the original Russian wording around it.
- All closure annotations follow Phase 4 04-06 precedent: "— Plan {NN-NN} ships [feature] per D-XXX ..." pattern.
- Coverage block at the bottom (L186-189) is correct as-is (REPORT-01..06 = 6 requirements counted) — no change needed.
  </action>
  <verify>
    <automated>grep -c "^- \[x\] \*\*REPORT-" .planning/REQUIREMENTS.md &amp;&amp; echo "---" &amp;&amp; grep -c "REPORT-01.*Done\|REPORT-02.*Done\|REPORT-03.*Done\|REPORT-04.*Done\|REPORT-05.*Done\|REPORT-06.*Done" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*REPORT-" .planning/REQUIREMENTS.md` returns 6
    - `grep -c "^- \[ \] \*\*REPORT-" .planning/REQUIREMENTS.md` returns 0 (all REPORT requirements closed)
    - `grep -c "brand-overlap CRAWL-02 scope" .planning/REQUIREMENTS.md` returns ≥1 (REPORT-01 D-502 amendment)
    - `grep -c "D-505\|D-506\|D-508" .planning/REQUIREMENTS.md` returns ≥3 (closure annotations cite decisions)
    - `grep -c "REPORT-01 | Phase 5 | Done\|REPORT-02 | Phase 5 | Done\|REPORT-03 | Phase 5 | Done\|REPORT-04 | Phase 5 | Done\|REPORT-05 | Phase 5 | Done\|REPORT-06 | Phase 5 | Done" .planning/REQUIREMENTS.md` returns 6
    - `grep -c "Phase 5 update: 2026" .planning/REQUIREMENTS.md` returns 1
    - Other phase rows untouched: `grep -c "MATCH-01.*Done.*Plan 04-03\|RECON-02.*Done.*plan 01-07" .planning/REQUIREMENTS.md` returns ≥1 each
  </acceptance_criteria>
  <done>All 6 REPORT-XX checkboxes flipped to [x] with closure annotations citing plan IDs + decision IDs; REPORT-01 amended per D-502 brand-overlap reinterpretation; Traceability table 6 rows updated to Done; bottom metadata line appended; no other rows touched.</done>
</task>

<task type="auto">
  <name>Task 2: STATE.md — append 3 D-514/D-515/D-405 Accumulated Key Decisions rows + Plan Execution Metrics for 05-01..05-06</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (locate "Accumulated Key Decisions" section + "Plan Execution Metrics" section if present)
    - .planning/phases/05-reporter-excel-summary/05-CONTEXT.md (Action Items L278-283 — verbatim cascade source)
    - .planning/phases/04-matcher-match-rate-kpi/04-06-SUMMARY.md (Phase 4 close-out — analog for Plan Execution Metrics format)
  </read_first>
  <action>
**Edit 1 — Accumulated Key Decisions cascade** — locate the "Accumulated Key Decisions" section in STATE.md (search for substring `Accumulated Key Decisions`). Append 3 new rows to that section, preserving the existing format (the section is typically a bullet list or table; mirror whatever format Phase 4 used).

If the section is a **bullet list**, append:

```markdown
- **D-514 — Phase 5 reporter is source-of-truth for Telegram caption (2026-05-MM, Plan 05-04+05-05):** `runs.stats.report.summary_text` written by reporter via single atomic `patch_stats` call (Pitfall 6); Phase 6 reads this dotted-flat key verbatim for Telegram `send_document` caption. **Защита от drift между xlsx cell A1 и Telegram caption** — единственный источник истины. Cascade: Phase 6 MUST NOT regenerate summary text from raw stats; must read `report.summary_text` directly.
- **D-515 — Phase 5 REPORT-06 size-guard delivery cascade (2026-05-MM, Plan 05-03+05-04):** `archive.check_size_guard(file_path, limit_mb)` returns `(passed: bool, size_bytes: int)` and NEVER raises. Orchestrator (`runners/reporter_run.py`) sets `report.size_guard_passed = False` + emits structlog warning `report_size_exceeded` when xlsx > 45 MB (D-516 default). **xlsx ALWAYS persists on disk** (manual recovery / Phase 6 split-and-send-later). Run status remains `success`. **Phase 6 DELIVER-03 sanity-gate cascade**: MUST read `report.size_guard_passed` and route oversize runs to ops-chat alert ("xlsx too large for Telegram, run_id={N} — manual delivery required") instead of business-chat. ARCHITECTURE.md "reporter independent of delivery" structurally enforced.
- **D-405 — Phase 5 reporter cites runs.stats.match.rate verbatim (2026-05-MM, Plan 05-02+05-04):** `summary_builder.build_summary` reads `stats["match.rate"]` flat key (Pitfall 6) directly — reporter NEVER recomputes the KPI formula. D-405 week-1 baseline lock preserved through Phase 5: any future re-baseline must update `matcher/strict_key.py::INSERT_MATCHES_SQL` + `matcher_run.py::compute_match_rate` simultaneously; reporter inherits whatever value the frozen formula produces. Structural enforcement: `tests/unit/test_summary_builder.py::test_d405_kpi_verbatim_in_summary` + `tests/integration/test_reporter_run.py::test_d405_kpi_verbatim_in_summary` substring-canary on output text `"Совпало: 3 (60.0%)"` for synthetic_report_run fixture.
```

If the section is a **table**, append 3 rows mirroring the table's columns (typically `| Decision | Source Phase | Cascade Impact | Date |`):

```markdown
| D-514 reporter source-of-truth for caption | Phase 5 | Phase 6 reads runs.stats.report.summary_text verbatim — no caption regen | 2026-05-MM |
| D-515 size-guard delivery cascade | Phase 5 | Phase 6 DELIVER-03 MUST read report.size_guard_passed; oversize → ops-chat NOT business-chat | 2026-05-MM |
| D-405 reporter cites match.rate verbatim | Phase 5 | KPI formula freeze preserved; week-1 baseline lock structurally enforced via source-locked SUMMARY_TEMPLATE | 2026-05-MM |
```

(Inspect STATE.md to determine actual format; copy whichever pattern Phase 4's accumulated decisions follow.)

**Edit 2 — Plan Execution Metrics** — locate or create the section. If absent, append a new section near the end of STATE.md:

```markdown
## Plan Execution Metrics (Phase 5)

| Plan | Wave | Tasks | Files Modified | Tests Added | Status | Commit |
|------|------|-------|----------------|-------------|--------|--------|
| 05-01 | 0 | 3 | 8 (pyproject + 3 src + 2 test + conftest + golden fixture) | 15 unit | Complete | TBD |
| 05-02 | 1 | 3 | 5 (3 src + 2 test) | 24 unit (queries + excel + summary) | Complete | TBD |
| 05-03 | 2 | 3 | 6 (1 src + .gitignore + .gitkeep + 3 test) | 23 (16 unit + 7 integration) | Complete | TBD |
| 05-04 | 3 | 2 | 2 (1 src + 1 integration test) | 12 integration | Complete | TBD |
| 05-05 | 4 | 4 | 4 (2 src amend + 2 integration test) | 10 integration | Complete | TBD |
| 05-06 | 5 | 3 | 3 (REQUIREMENTS + STATE + ROADMAP) | 0 | Complete | TBD |
```

Test counts in this table are estimates from the plans — operator/executor may adjust at close-out time. The `Commit TBD` placeholder is filled by the commit hook or manually after `git commit`.

If a Plan Execution Metrics section already exists (with Phase 1-4 rows), append the 6 Phase 5 rows under a "Phase 5" sub-header or extend the existing table with continuous numbering.

**Critical invariants:**
- Use today's date — `Bash(date +%Y-%m-%d)` returns the value to fill `MM`. If running on 2026-05-12, the date string is `2026-05-12`.
- Preserve all existing STATE.md content — only APPEND. Read the section before editing to confirm structure.
- The 3 cascade rows are NON-NEGOTIABLE — they enable Phase 6 planner to read STATE.md and inherit all D-514/D-515/D-405 invariants without traversing the phase directory.
- Test counts in Plan Execution Metrics are approximate; the executor may verify by running `uv run pytest tests/unit -k "report\|summary\|archive" --collect-only | wc -l` and similar greps.
  </action>
  <verify>
    <automated>grep -c "D-514\|D-515" .planning/STATE.md &amp;&amp; grep -c "reporter is source-of-truth\|size-guard delivery cascade\|reporter cites runs.stats.match.rate verbatim" .planning/STATE.md &amp;&amp; grep -c "Plan Execution Metrics\|05-01\|05-06" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "D-514" .planning/STATE.md` returns ≥1 (cascade row mention)
    - `grep -c "D-515" .planning/STATE.md` returns ≥1
    - `grep -c "D-405.*reporter\|reporter.*D-405" .planning/STATE.md` returns ≥1
    - `grep -c "report.summary_text\|report.size_guard_passed" .planning/STATE.md` returns ≥2
    - `grep -c "05-01\|05-02\|05-03\|05-04\|05-05\|05-06" .planning/STATE.md` returns ≥6 (Plan Execution Metrics rows)
    - `grep -c "Phase 6 DELIVER-03" .planning/STATE.md` returns ≥1 (D-515 cascade hint for Phase 6 planner)
    - Pre-existing Phase 1-4 content untouched: `grep -c "D-401 13-col denormalized\|D-308 final M-gate\|D-411 skip" .planning/STATE.md` returns ≥3
  </acceptance_criteria>
  <done>3 cascade rows (D-514 / D-515 / D-405) appended to Accumulated Key Decisions with Phase 6 cascade hints; Plan Execution Metrics extended with 6 rows for 05-01..05-06; today's date filled in; no pre-existing content modified.</done>
</task>

<task type="auto">
  <name>Task 3: ROADMAP.md — populate Phase 5 Plans block + update Progress table to 6/6 Complete</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 5 section L114-125 + Progress table L176-184)
    - .planning/phases/04-matcher-match-rate-kpi/04-06-PLAN.md (precedent for `### Phase 5` plan-list format — see Phase 4 L105-111 in ROADMAP.md)
  </read_first>
  <action>
**Edit 1 — Replace `**Plans**: TBD` (L124)** with the plan list per Phase 4 precedent:

REPLACE:
```
**Plans**: TBD
**UI hint**: no
```

WITH:
```
**Plans**: 6 plans across 6 waves (Wave 0 foundation → Wave 1 builders → Wave 2 archive → Wave 3 orchestrator → Wave 4 main_run + CLI composition → Wave 5 doc cascade)
- [x] 05-01-PLAN.md - Wave 0: foundation — pyproject `[tool.ga_crawler.report]` namespace (D-516) + `reporter/__init__.py` + `config.py` (`ReportConfig` mirror `MatchConfig`) + `stats.py` (`REPORT_STATS_KEYS` 7-tuple D-514 + `ReportStatsBuilder` with 4-way disjoint invariant); pandas+xlsxwriter+openpyxl+tzdata deps; `synthetic_report_run` + `tmp_reports_dir` + `openpyxl_workbook_reader` conftest fixtures; D-504 golden summary text fixture
- [x] 05-02-PLAN.md - Wave 1: pure builders — `reporter/queries.py` 5 SQL constants (PER_SKU JOIN-back for URLs per Pitfall 9 + NOT EXISTS per Pitfall 8 + SQL discount math per Pitfall 10) + `reporter/excel_builder.py` (D-503 Russian headers + D-505 3-color CF `mid_value=0` parity anchor + D-506 always-4-sheets + D-508 CF-2-sheets-only + Pitfall 1 `engine='xlsxwriter'` explicit + T-05-injection formula sanitization) + `reporter/summary_builder.py` (D-504 template constants source-locked + zero-match Top-3 omission)
- [x] 05-03-PLAN.md - Wave 2: filesystem service — `reporter/archive.py` (D-512 ISO-week derivation with Pitfall 4 year-boundary `2027-01-01 → 2026-W53` + D-510 atomic write via `*.xlsx.tmp` + `os.replace` + D-515 `check_size_guard` flag-only never-raises); `reports/.gitkeep` + `.gitignore` exclude `reports/*.xlsx`; 23+ tests
- [x] 05-04-PLAN.md - Wave 3: orchestrator — `runners/reporter_run.py` 7-step sync mirror `matcher_run.py` shape + D-507 status-gate REUSE `read_run_status` from `matcher.strict_key` (no re-impl) + path-traversal containment check + Pitfall 6 single `patch_stats` per code path + D-515 flag-only never-fails-run + 12+ integration tests
- [x] 05-05-PLAN.md - Wave 4: composition — `main_run.py` reporter step inserted post-matcher with explicit `m_result.status=='success'` gate + `MainRunResult` extended 4 fields per D-514 + DATA-05 reporter-exception path inherits Plan 02-05 outer try/except; `cli.py` D-509 `report-run --run-id N` subcommand mirror `matcher-run` shape (`type=int` required + `--output-dir` via `dataclasses.replace`)
- [x] 05-06-PLAN.md - Wave 5: doc cascade — REQUIREMENTS.md REPORT-01..06 closed + REPORT-01 amendment per D-502 (SKU-level gap interpretation within brand-overlap CRAWL-02 scope) + STATE.md gains D-514/D-515/D-405 cascade rows + Plan Execution Metrics for 05-01..05-06 + ROADMAP.md Phase 5 plan list + Progress 6/6 Complete
**UI hint**: no
```

**Edit 2 — Progress table row** (L182):

REPLACE:
```
| 5. Reporter (Excel + summary) | 0/0 | Not started | - |
```

WITH:
```
| 5. Reporter (Excel + summary) | 6/6 | Complete (6 plans shipped Wave 0..5; reports/YYYY-WNN.xlsx archived per D-510 + D-512; D-514 7-key stats namespace; D-507 status-gate REUSES D-411; D-405 KPI verbatim citation; D-515 size-guard flag-only with Phase 6 cascade) | 2026-05-MM |
```

(Replace `MM` with current date — same as STATE.md Task 2.)

**Critical invariants:**
- Preserve the rest of the Phase 5 section (Goal, Depends on, Requirements, Success Criteria) — only modify `**Plans**: TBD` line and add the bullet list below it.
- The `[x]` checkbox state for each plan reflects post-execution state — at the moment Plan 05-06 RUNS, plans 05-01..05-05 are already complete; 05-06 ships THIS edit + commits. Self-referential `- [x] 05-06-PLAN.md` is intentional.
- Progress table date: use the date Plan 05-06 commits. The executor fills `MM` at commit time via `Bash(date +%Y-%m-%d)`.
- Coverage Validation table at the bottom (L195-205) is correct as-is — no change needed (REPORT-01..06 row already shows 6 requirements mapped to Phase 5).
- Phase Dependencies diagram (L156-170) is unchanged — Phase 5 → Phase 6 → Phase 7 ordering preserved.
  </action>
  <verify>
    <automated>grep -c "05-01-PLAN.md\|05-02-PLAN.md\|05-03-PLAN.md\|05-04-PLAN.md\|05-05-PLAN.md\|05-06-PLAN.md" .planning/ROADMAP.md &amp;&amp; grep -c "5. Reporter.*6/6.*Complete" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "05-01-PLAN.md" .planning/ROADMAP.md` returns ≥1
    - `grep -c "05-02-PLAN.md\|05-03-PLAN.md\|05-04-PLAN.md\|05-05-PLAN.md\|05-06-PLAN.md" .planning/ROADMAP.md` returns ≥5 (all 6 plan refs)
    - `grep -c "5. Reporter.*6/6\|6/6.*Complete" .planning/ROADMAP.md` returns ≥1
    - `grep -c "Not started" .planning/ROADMAP.md` returns ≤2 (Phase 6 + Phase 7 still pending)
    - Phase 5 section in ROADMAP.md no longer contains the string `Plans**: TBD`
    - Pre-existing Phase 1-4 progress rows untouched
  </acceptance_criteria>
  <done>ROADMAP.md Phase 5 plans block populated with 6 checkmarked plan entries; Progress table updated to 6/6 Complete with date; UI hint preserved; rest of file unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation cascade | All edits are markdown changes in `.planning/`; no code/test/config files touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-doc-drift | Tampering (information integrity) | REQUIREMENTS.md / STATE.md / ROADMAP.md | accept | Doc cascade is the explicit mitigation — keeps decision record synced with shipped code. Reviewer (git PR) is the control. |
| T-05-stale-traceability | Information Disclosure | REQUIREMENTS.md Traceability table | mitigate | Plan 05-06 updates the table from Pending → Done with plan-ID + decision-ID references. Future audit can trace REPORT-XX → 05-XX-PLAN → 05-XX-SUMMARY → commit hash. |
| T-05-missed-cascade-to-phase6 | Information Disclosure | STATE.md Accumulated Decisions | mitigate | The 3 cascade rows (D-514 / D-515 / D-405) are NON-NEGOTIABLE per CONTEXT Action Items L278-283; explicit grep canaries in acceptance criteria verify their presence. Phase 6 planner reading STATE.md sees the D-515 cascade without traversing 05-CONTEXT.md. |
</threat_model>

<verification>
After all 3 tasks complete:

```bash
# REQUIREMENTS — 6 checked + amendment language
grep -c "^- \[x\] \*\*REPORT-" .planning/REQUIREMENTS.md  # expect 6
grep -c "brand-overlap CRAWL-02" .planning/REQUIREMENTS.md  # ≥1 (D-502 amendment)
grep -c "Plan 05-0" .planning/REQUIREMENTS.md  # ≥6 (closure annotations)

# STATE — 3 cascade rows present
grep -c "D-514\|D-515" .planning/STATE.md  # ≥2
grep -c "Phase 6 DELIVER-03" .planning/STATE.md  # ≥1
grep -c "05-01\|05-02\|05-03\|05-04\|05-05\|05-06" .planning/STATE.md  # ≥6 (metrics rows)

# ROADMAP — Phase 5 plans + progress
grep -c "05-01-PLAN.md\|05-06-PLAN.md" .planning/ROADMAP.md  # ≥2
grep -c "Plans\*\*: TBD" .planning/ROADMAP.md  # 0 (replaced)
grep -c "5. Reporter.*6/6\|6/6.*Complete" .planning/ROADMAP.md  # ≥1

# Sanity: no source code edits in this plan
git diff --name-only HEAD -- src/ tests/  # expect empty
```
</verification>

<success_criteria>
- REQUIREMENTS.md: all 6 REPORT-XX flipped to [x] with closure annotations; REPORT-01 amended per D-502; Traceability table rows updated to Done; bottom metadata appended
- STATE.md: 3 cascade rows (D-514 / D-515 / D-405) appended to Accumulated Key Decisions with Phase 6 cascade hints; Plan Execution Metrics for 05-01..05-06
- ROADMAP.md: Phase 5 Plans block has 6 entries (all checkmarked); Progress table reads "6/6 Complete" with completion date
- Zero source code changes (`git diff --stat HEAD -- src/ tests/ pyproject.toml` returns empty)
- All pre-existing Phase 1-4 content (decisions, traceability, plan lists) untouched
- Phase 6 planner can read STATE.md and inherit D-514/D-515 invariants without traversing 05-CONTEXT.md
</success_criteria>

<output>
After completion, create `.planning/phases/05-reporter-excel-summary/05-06-SUMMARY.md` per `~/.claude/get-shit-done/templates/summary.md`. Capture: 6 REQUIREMENTS.md REPORT-XX closures + 1 amendment, 3 STATE.md cascade rows + Plan Execution Metrics, ROADMAP.md plan list + progress update, git diff stats (3 files modified, 0 src/tests touched), commit-ready summary.
</output>
